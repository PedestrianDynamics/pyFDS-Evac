<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>pyFDS-Evac – Documentation</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/</link><description>Recent content in Documentation on pyFDS-Evac</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><atom:link href="https://pedestriandynamics.org/pyFDS-Evac/docs/index.xml" rel="self" type="application/rss+xml"/><item><title>ASET, RSET and the egress timeline</title><link>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/aset-rset/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/aset-rset/</guid><description>
&lt;p>Performance-based fire-safety design compares two times. The available
safe escape time (ASET) is the time until conditions in a space, or on the
route out of it, become untenable. The required safe escape time (RSET) is
the time occupants need to reach a place of safety. A design is acceptable
when ASET exceeds RSET by an adequate margin (ISO/TR 16738:2009, §5.6).&lt;/p>
&lt;h2>The equations as published&lt;span class="hx-absolute -hx-mt-20" id="the-equations-as-published">&lt;/span>
&lt;a href="#the-equations-as-published" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>ISO/TR 16738:2009 writes the margin of safety and the escape time as&lt;/p>
$$
t_{\mathrm{marg}} = t_{\mathrm{ASET}} - t_{\mathrm{RSET}} \qquad \text{(ISO/TR 16738, Eq. 1)}
$$$$
t_{\mathrm{RSET}} = t_{\mathrm{det}} + t_{\mathrm{warn}} + \left(t_{\mathrm{pre}} + t_{\mathrm{trav}}\right) \qquad \text{(Eq. 2)}
$$&lt;p>All times are in seconds or minutes. \(t_{\mathrm{det}}\) is the time from
ignition to detection, by a system or by the first occupant to notice fire
cues. \(t_{\mathrm{warn}}\) runs from detection to a general alarm or
warning. \(t_{\mathrm{pre}}\) is the pre-travel activity time, also called
pre-movement or pre-evacuation time, and \(t_{\mathrm{trav}}\) is the travel
time. The standard notes that the evacuation time \(t_{\mathrm{evac}}\)
consists of the last two terms only, and it splits \(t_{\mathrm{pre}}\) into
a recognition time and a response time. The Society of Fire Protection
Engineers (SFPE) Handbook uses the same terms
(Gwynne and Boyce 2016, Ch. 64).&lt;/p>
&lt;h2>What the timeline rests on&lt;span class="hx-absolute -hx-mt-20" id="what-the-timeline-rests-on">&lt;/span>
&lt;a href="#what-the-timeline-rests-on" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>ASET comes from the fire: the time-concentration curves of heat, toxic gases
and smoke at the occupants&amp;rsquo; positions, compared with tenability limits. The
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/asphyxiant-fed/">fractional effective dose (FED)&lt;/a>
, &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/irritants/">irritant&lt;/a>
, &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/heat/">heat&lt;/a>
and
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/">visibility&lt;/a>
pages give those limits. ISO 13571:2012 (§5.1)
states that its time to compromised tenability may reasonably be equated
to ASET when escape to a place of refuge is the outcome considered.&lt;/p>
&lt;p>RSET comes from people. ISO/TR 16738 (§5.4) reports that the pre-travel
activity phase can often be the longest part of the total escape time. Each
occupant has their own \(t_{\mathrm{pre}}\) and \(t_{\mathrm{trav}}\), so
the standard treats them as distributions, and warns that within an
enclosure the two terms cannot be added directly because the distributions
interact (§7). See &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/pre-movement/">Pre-movement time&lt;/a>
.&lt;/p>
&lt;h2>Known limits&lt;span class="hx-absolute -hx-mt-20" id="known-limits">&lt;/span>
&lt;a href="#known-limits" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The definitions differ in where the clock starts. ISO/TR 16738 Eq. 2 and
SFPE Ch. 64 count RSET from ignition, since they include detection. SFPE
Ch. 56 (Bukowski and Tubbs 2016) defines RSET from the notification of
occupants and ASET from notification to the onset of untenable conditions,
while also stating that evacuation times consist of detection, notification,
pre-movement and movement times. When two analyses are compared, check which
origin each uses. The terms also vary: &amp;ldquo;escape&amp;rdquo; in ISO/TR 16738, &amp;ldquo;egress&amp;rdquo; or
&amp;ldquo;evacuation&amp;rdquo; in the SFPE Handbook, for the same quantities.&lt;/p>
&lt;p>ASET is not one number. It varies with position and with the tenability
criterion chosen, and the criterion itself is set for a fraction of the
population (see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/incapacitation-thresholds/">Incapacitation thresholds&lt;/a>
).&lt;/p>
&lt;h2>Sources&lt;span class="hx-absolute -hx-mt-20" id="sources">&lt;/span>
&lt;a href="#sources" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>ISO (2009). &lt;em>ISO/TR 16738:2009 Fire-safety engineering — Technical
information on methods for evaluating behaviour and movement of people&lt;/em>.
ISO, Geneva. &lt;a href="https://www.iso.org/standard/42887.html" target="_blank" rel="noopener">iso.org/standard/42887&lt;/a>
.
Read from the public preview (§1–7).&lt;/li>
&lt;li>ISO (2012). &lt;em>ISO 13571:2012 Life-threatening components of fire —
Guidelines for the estimation of time to compromised tenability in
fires&lt;/em>. ISO, Geneva.
&lt;a href="https://www.iso.org/standard/56172.html" target="_blank" rel="noopener">iso.org/standard/56172&lt;/a>
.&lt;/li>
&lt;li>Bukowski, R. W., &amp;amp; Tubbs, J. S. (2016). &lt;em>Egress concepts and design
approaches&lt;/em>. SFPE Handbook of Fire Protection Engineering, 5th ed.,
Ch. 56, 2012–2046.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_56" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_56&lt;/a>
&lt;/li>
&lt;li>Gwynne, S. M. V., &amp;amp; Boyce, K. E. (2016). &lt;em>Engineering data&lt;/em>. SFPE
Handbook of Fire Protection Engineering, 5th ed., Ch. 64, 2429–2551.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_64" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_64&lt;/a>
&lt;/li>
&lt;/ul>
&lt;p>How pyFDS-Evac uses this: see the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/howto-rset-ensemble/">RSET ensemble how-to&lt;/a>
.&lt;/p></description></item><item><title>Quickstart</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/quickstart/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/quickstart/</guid><description>
&lt;p>&lt;strong>No FDS output is needed.&lt;/strong> This page runs a scenario tracked in the
repository with a uniform, prescribed smoke field.&lt;/p>
&lt;h2>Goal&lt;span class="hx-absolute -hx-mt-20" id="goal">&lt;/span>
&lt;a href="#goal" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Run one evacuation twice, once in clear air and once in smoke, and see how much
the smoke slows the agent. Then find the run manifest, which records how the
run was produced.&lt;/p>
&lt;h2>Prerequisites&lt;span class="hx-absolute -hx-mt-20" id="prerequisites">&lt;/span>
&lt;a href="#prerequisites" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>A clone of the repository and its environment (&lt;code>uv sync&lt;/code>).&lt;/li>
&lt;li>Run every command from the repository root. The scenario paths are relative
to it.&lt;/li>
&lt;li>Runtime: about 3 s.&lt;/li>
&lt;/ul>
&lt;p>The full script is &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/examples/quickstart.py" target="_blank" rel="noopener">&lt;code>examples/quickstart.py&lt;/code>&lt;/a>
.
Run it with:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python examples/quickstart.py&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The scenario is &lt;code>assets/ISO-table21&lt;/code>: a corridor 2 m wide and 100 m long, with
one agent at one end walking at 1.25 m/s towards the exit at the other end. It
is the geometry of ISO 20414 Test 18.&lt;/p>
&lt;h2>Steps&lt;span class="hx-absolute -hx-mt-20" id="steps">&lt;/span>
&lt;a href="#steps" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>1. Import the public API&lt;span class="hx-absolute -hx-mt-20" id="1-import-the-public-api">&lt;/span>
&lt;a href="#1-import-the-public-api" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Everything is imported from &lt;code>pyfds_evac&lt;/code>.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac &lt;span style="color:#f92672">import&lt;/span> (
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> ConstantExtinctionField,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> SmokeSpeedConfig,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> SmokeSpeedModel,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> load_scenario,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> run_scenario,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>K_PER_M &lt;span style="color:#f92672">=&lt;/span> &lt;span style="color:#ae81ff">3.0&lt;/span> &lt;span style="color:#75715e"># extinction coefficient K [1/m]; visibility S = 3/K = 1 m&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;em>K&lt;/em> is the extinction coefficient in 1/m. With a visibility factor &lt;em>C&lt;/em> = 3
(a reflective sign), the visibility is &lt;em>S&lt;/em> = &lt;em>C&lt;/em>/&lt;em>K&lt;/em> = 1 m.&lt;/p>
&lt;h3>2. Run in clear air&lt;span class="hx-absolute -hx-mt-20" id="2-run-in-clear-air">&lt;/span>
&lt;a href="#2-run-in-clear-air" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>scenario &lt;span style="color:#f92672">=&lt;/span> load_scenario(&lt;span style="color:#e6db74">&amp;#34;assets/ISO-table21&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>clear &lt;span style="color:#f92672">=&lt;/span> run_scenario(scenario, seed&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">420&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;clear air: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>clear&lt;span style="color:#f92672">.&lt;/span>evacuation_time&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.2f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s&amp;#34;&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Expected output:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>Evacuated 1/1 sim=78.8s wall=0m00s done
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>clear air: 78.77 s&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;code>run_scenario&lt;/code> prints the &lt;code>Evacuated …&lt;/code> progress line itself. The last line is
the evacuation time in seconds.&lt;/p>
&lt;h3>3. Run in smoke&lt;span class="hx-absolute -hx-mt-20" id="3-run-in-smoke">&lt;/span>
&lt;a href="#3-run-in-smoke" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;code>ConstantExtinctionField&lt;/code> returns the same &lt;em>K&lt;/em> at every point and time.
&lt;code>SmokeSpeedConfig()&lt;/code> uses the default speed law (&lt;code>&amp;quot;lund&amp;quot;&lt;/code>), which scales the
walking speed by &lt;code>1 + beta * K / alpha&lt;/code>, clamped to &lt;code>[min_speed_factor, 1.0]&lt;/code>,
with the defaults listed on the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/#parameters">smoke-speed model&lt;/a>
page.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>smoke &lt;span style="color:#f92672">=&lt;/span> SmokeSpeedModel(ConstantExtinctionField(K_PER_M), SmokeSpeedConfig())
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>smoky &lt;span style="color:#f92672">=&lt;/span> run_scenario(scenario, seed&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">420&lt;/span>, smoke_speed_model&lt;span style="color:#f92672">=&lt;/span>smoke)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;K = &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>K_PER_M&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> 1/m: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>smoky&lt;span style="color:#f92672">.&lt;/span>evacuation_time&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.2f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s&amp;#34;&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Expected output:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>Evacuated 1/1 sim=103.8s wall=0m00s done
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>K = 3.0 1/m: 103.77 s&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h3>4. Check the speed factor and find the manifest&lt;span class="hx-absolute -hx-mt-20" id="4-check-the-speed-factor-and-find-the-manifest">&lt;/span>
&lt;a href="#4-check-the-speed-factor-and-find-the-manifest" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>factor &lt;span style="color:#f92672">=&lt;/span> smoky&lt;span style="color:#f92672">.&lt;/span>smoke_history[&lt;span style="color:#f92672">-&lt;/span>&lt;span style="color:#ae81ff">1&lt;/span>][&lt;span style="color:#e6db74">&amp;#34;speed_factor&amp;#34;&lt;/span>]
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;speed factor in smoke: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>factor&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.4f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;time ratio smoke/clear: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>smoky&lt;span style="color:#f92672">.&lt;/span>evacuation_time &lt;span style="color:#f92672">/&lt;/span> clear&lt;span style="color:#f92672">.&lt;/span>evacuation_time&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.4f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;manifest: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>smoky&lt;span style="color:#f92672">.&lt;/span>manifest_file&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Expected output (the manifest path is a new temporary file on every run):&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>speed factor in smoke: 0.7578
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>time ratio smoke/clear: 1.3174
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>manifest: /tmp/tmpxbcef0vs.manifest.json&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;code>run_scenario&lt;/code> writes the trajectory to a temporary SQLite file and the
manifest next to it, as &lt;code>&amp;lt;trajectory stem&amp;gt;.manifest.json&lt;/code>. The manifest records
the versions of pyFDS-Evac, JuPedSim, fdsreader and fdsvismap, the &lt;code>uv.lock&lt;/code>
hash, the seed, the scenario path, and the FDS directory and version (both
&lt;code>null&lt;/code> here, because no FDS output was read).&lt;/p>
&lt;h3>5. Clean up&lt;span class="hx-absolute -hx-mt-20" id="5-clean-up">&lt;/span>
&lt;a href="#5-clean-up" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>clear&lt;span style="color:#f92672">.&lt;/span>cleanup()
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>smoky&lt;span style="color:#f92672">.&lt;/span>cleanup()&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;code>cleanup()&lt;/code> deletes the temporary trajectory file and its manifest. Copy them
first if you want to keep them.&lt;/p>
&lt;h2>Checkpoint&lt;span class="hx-absolute -hx-mt-20" id="checkpoint">&lt;/span>
&lt;a href="#checkpoint" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>Both runs print &lt;code>Evacuated 1/1&lt;/code>. Nobody is left in the corridor.&lt;/li>
&lt;li>The smoke run is slower than the clear run.&lt;/li>
&lt;li>The speed factor is &lt;code>1 + (-0.057 × 3.0) / 0.706 = 0.7578&lt;/code>, as the code
computes it.&lt;/li>
&lt;li>The time ratio (1.3174) is close to &lt;code>1 / 0.7578 = 1.3196&lt;/code>, but not equal to
it. The evacuation time comes from the full JuPedSim run, not from distance
divided by speed.&lt;/li>
&lt;/ul>
&lt;p>If the smoke run is not slower, check that you passed &lt;code>smoke_speed_model=&lt;/code>.
Without it, &lt;code>run_scenario&lt;/code> models no smoke.&lt;/p>
&lt;h2>Next steps&lt;span class="hx-absolute -hx-mt-20" id="next-steps">&lt;/span>
&lt;a href="#next-steps" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/fds-case-requirements/">What your FDS case must provide&lt;/a>
, before you point
the tool at your own FDS output.&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/walkthrough/">Real-FDS walkthrough&lt;/a>
: the same steps with extinction and
gas read from FDS slices, and how to spot a run that succeeds but is wrong.&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/howto-rset-ensemble/">How do I get RSET with its spread from an ensemble of seeds?&lt;/a>
&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/smoke-speed-model/">Smoke-speed model&lt;/a>
for the speed laws and their
parameters.&lt;/li>
&lt;/ul>
&lt;p>pyFDS-Evac is research software, provided without warranty.&lt;/p></description></item><item><title>Smoke-speed model</title><link>https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/</guid><description>
&lt;p>Based on: &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/walking-speed/">Walking speed in smoke&lt;/a>
and &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/extinction/">Extinction coefficient&lt;/a>
.&lt;/p>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
. The Python
API (extinction sources, &lt;code>SmokeSpeedModel&lt;/code>, conversion helpers) is in
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/smoke-speed-model/">docs/smoke-speed-model.md&lt;/a>
.&lt;/p>
&lt;h2>Coded form&lt;span class="hx-absolute -hx-mt-20" id="coded-form">&lt;/span>
&lt;a href="#coded-form" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The model turns the extinction coefficient &lt;em>K&lt;/em> [1/m] at the agent&amp;rsquo;s position
into a speed factor &lt;em>f&lt;/em> [-], selected by &lt;code>SmokeSpeedConfig.speed_law&lt;/code>
(&lt;code>pyfds_evac/core/smoke_speed.py&lt;/code>). A non-finite or negative &lt;em>K&lt;/em> is read as 0.&lt;/p>
&lt;ul>
&lt;li>
&lt;p>&lt;strong>&lt;code>&amp;quot;lund&amp;quot;&lt;/code> (default)&lt;/strong>, &lt;code>speed_factor_from_extinction&lt;/code>:&lt;/p>
$$
f(K) = \min\!\left(1,\ \max\!\left(f_{\min},\ 1 + \frac{\beta}{\alpha}K\right)\right).
$$&lt;/li>
&lt;li>
&lt;p>&lt;strong>&lt;code>&amp;quot;fridolf&amp;quot;&lt;/code>&lt;/strong>, &lt;code>speed_factor_from_extinction_fridolf&lt;/code>: with
\(V = C/K\) [m],&lt;/p>
$$
f(K) = \frac{V}{V + 2~\mathrm{m}}, \qquad f(0) = 1,
$$&lt;p>with no floor. Its attribution to Fridolf et al. (2019) is unverified, and
the paper&amp;rsquo;s own law differs; see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/walking-speed/">Walking speed in smoke&lt;/a>
and
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/146" target="_blank" rel="noopener">#146&lt;/a>
.&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h2>Parameters&lt;span class="hx-absolute -hx-mt-20" id="parameters">&lt;/span>
&lt;a href="#parameters" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Fields of &lt;code>SmokeSpeedConfig&lt;/code>, with the defaults in the code. &lt;code>run.py&lt;/code> and the
web GUI set only the last two (&lt;code>--smoke-update-interval&lt;/code>,
&lt;code>--smoke-slice-height&lt;/code>); the speed law and its coefficients need a
&lt;code>SmokeSpeedConfig&lt;/code> built in Python (see the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#the-parameter-split">parameter split&lt;/a>
).&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Field&lt;/th>
&lt;th>Default&lt;/th>
&lt;th>Unit&lt;/th>
&lt;th>Meaning&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>speed_law&lt;/code>&lt;/td>
&lt;td>&lt;code>&amp;quot;lund&amp;quot;&lt;/code>&lt;/td>
&lt;td>-&lt;/td>
&lt;td>&lt;code>&amp;quot;lund&amp;quot;&lt;/code> or &lt;code>&amp;quot;fridolf&amp;quot;&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>alpha&lt;/code>&lt;/td>
&lt;td>&lt;code>0.706&lt;/code>&lt;/td>
&lt;td>m/s&lt;/td>
&lt;td>\(\alpha\), &lt;code>lund&lt;/code> only&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>beta&lt;/code>&lt;/td>
&lt;td>&lt;code>-0.057&lt;/code>&lt;/td>
&lt;td>m²/s&lt;/td>
&lt;td>\(\beta\), &lt;code>lund&lt;/code> only&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>min_speed_factor&lt;/code>&lt;/td>
&lt;td>&lt;code>0.1&lt;/code>&lt;/td>
&lt;td>-&lt;/td>
&lt;td>\(f_{\min}\), &lt;code>lund&lt;/code> only&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>visibility_factor_c&lt;/code>&lt;/td>
&lt;td>&lt;code>3.0&lt;/code>&lt;/td>
&lt;td>-&lt;/td>
&lt;td>&lt;em>C&lt;/em>, &lt;code>fridolf&lt;/code> only&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>update_interval_s&lt;/code>&lt;/td>
&lt;td>&lt;code>1.0&lt;/code>&lt;/td>
&lt;td>s&lt;/td>
&lt;td>Time between samples of &lt;em>K&lt;/em> for each agent&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>slice_height_m&lt;/code>&lt;/td>
&lt;td>&lt;code>2.0&lt;/code>&lt;/td>
&lt;td>m&lt;/td>
&lt;td>Height of the FDS slice that is read&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>The routing block has its own copy of &lt;code>alpha&lt;/code>, &lt;code>beta&lt;/code> and &lt;code>min_speed_factor&lt;/code>,
used only to price routes; see the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/routing/#parameters">routing model&lt;/a>
.&lt;/p>
&lt;h2>Where it acts in the time step&lt;span class="hx-absolute -hx-mt-20" id="where-it-acts-in-the-time-step">&lt;/span>
&lt;a href="#where-it-acts-in-the-time-step" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Every &lt;code>update_interval_s&lt;/code> (&lt;code>--smoke-update-interval&lt;/code> in &lt;code>run.py&lt;/code>),
&lt;code>run_scenario&lt;/code> samples &lt;em>K&lt;/em> at each agent&amp;rsquo;s position and stores &lt;em>f&lt;/em> as the
agent&amp;rsquo;s smoke factor. The agent&amp;rsquo;s desired speed is then
\(v_0 \cdot f \cdot g(\mathrm{FIC})\) (&lt;code>direct_steering_runtime.py:186&lt;/code>–&lt;code>190&lt;/code>),
where \(g\) is the irritant factor of the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/">FED model&lt;/a>
.&lt;/p>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/concepts/speed_laws.png" alt="Speed factor v/v0 against extinction coefficient K for the Frantzich–Nilsson law and for the fridolf option V/(V&amp;#43;2) with C = 3 and C = 8" loading="lazy" />&lt;/p>
&lt;p>&lt;em>Speed factor \(v/v_0\) [-] against extinction coefficient K [1/m]. Solid dark
blue: Frantzich–Nilsson with the default constants, floor 0.1 reached at
K = 11.1 m⁻¹. The &lt;code>fridolf&lt;/code> option, \(V/(V+2)\) with \(V = C/K\): red dashed
for C = 3, orange dash-dotted for C = 8. The arrow marks the largest gap
between Frantzich–Nilsson and C = 3.
Script: &lt;code>scripts/figures/speed_laws.py&lt;/code>.&lt;/em>&lt;/p>
&lt;p>Background: the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/">Concepts&lt;/a>
page
and the talk &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/visibility-seminar-2026/" target="_blank" rel="noopener">&lt;em>A Modular Workflow for Visibility-Aware Evacuation Modelling&lt;/em>&lt;/a>
(&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/pyFDS-Evac_visibility_seminar_2026.pdf" target="_blank" rel="noopener">PDF&lt;/a>
).&lt;/p>
&lt;p>For real FDS output, &lt;code>fdsreader&lt;/code> provides the local extinction field
via &lt;code>SliceFieldSampler&lt;/code>. For verification cases such as ISO 20414:2020 Test 18 (Table 21),
the runner can also apply a constant extinction coefficient directly.&lt;/p>
&lt;h2>FDS data access&lt;span class="hx-absolute -hx-mt-20" id="fds-data-access">&lt;/span>
&lt;a href="#fds-data-access" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>All FDS slice data is read through a single library:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>&lt;code>fdsreader&lt;/code>&lt;/strong> — reads raw FDS slice quantities with nearest-neighbor
spatial and temporal lookup via &lt;code>SliceFieldSampler&lt;/code>
(&lt;code>pyfds_evac/core/fds_sampling.py&lt;/code>)&lt;/li>
&lt;li>Used by both the smoke-speed model (extinction &lt;code>K [1/m]&lt;/code>) and the FED
model (CO, CO2, O2, and optional irritant gases)&lt;/li>
&lt;li>When a scenario needs both extinction and FED fields from the same FDS
case, pass a shared &lt;code>fdsreader.Simulation&lt;/code> instance to avoid parsing
the directory twice (see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/fds-sampling/">FDS sampling API&lt;/a>
)&lt;/li>
&lt;/ul>
&lt;p>Run the ISO 20414 Test 18 (Table 21) corridor with a constant extinction coefficient:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run run.py &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --scenario assets/ISO-table21 &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --constant-extinction 1.0 &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --smoke-update-interval 0.1 &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-smoke-history /tmp/iso-table21-smoke-history.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --cleanup&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Run the smoke-speed model against FDS results read through &lt;code>fdsreader&lt;/code>. The
repository ships the deck, not its output — the slices are 4.2 MB and the full
run 54 MB — so run FDS once first:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>mkdir -p /tmp/iso21 &lt;span style="color:#f92672">&amp;amp;&amp;amp;&lt;/span> cd /tmp/iso21 &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> &lt;span style="color:#f92672">&amp;amp;&amp;amp;&lt;/span> fds /path/to/assets/ISO-table21/ISO-table21.fds &lt;span style="color:#f92672">&amp;amp;&amp;amp;&lt;/span> cd - &lt;span style="color:#75715e"># ~8 min&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>uv run run.py &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --scenario assets/ISO-table21 &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --fds-dir /tmp/iso21 &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --smoke-update-interval 0.1 &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-smoke-history /tmp/iso-table21-fds-smoke-history.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --cleanup&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Inspect the FDS quantities available through &lt;code>fdsreader&lt;/code>:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run run.py --inspect-fds --fds-dir /tmp/iso21 --scenario assets/ISO-table21&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>For a case where the coupling is exercised without running FDS yourself, see
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/assets/iso_table22_coupled/README.md" target="_blank" rel="noopener">&lt;code>assets/iso_table22_coupled&lt;/code>&lt;/a>
: its output
is committed (136 kB) and a test reads it on every CI run.&lt;/p>
&lt;p>Plot smoke-speed history for a single agent:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python scripts/plot_smoke_history.py &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --input /tmp/iso-table21-smoke-history.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output /tmp/iso-table21-smoke-history.png &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --agent-id &lt;span style="color:#ae81ff">1&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Plot aggregate smoke-speed history:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python scripts/plot_smoke_history.py &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --input /tmp/iso-table21-smoke-history.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output /tmp/iso-table21-smoke-history-aggregate.png&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Generate a stable ISO 20414 Test 18 (Table 21) sweep artifact under &lt;code>artifacts/&lt;/code>:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python scripts/generate_iso_table21_sweep.py&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Figure: &lt;img src="https://pedestriandynamics.org/pyFDS-Evac/artifacts/iso-table21-sweep.png" alt="ISO 20414 Test 18 (Table 21) sweep" loading="lazy" />&lt;/p>
&lt;p>Generate the FDS+Evac smoke-density vs speed verification plot:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python scripts/generate_smoke_density_speed_plot.py&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Figure: &lt;img src="https://pedestriandynamics.org/pyFDS-Evac/artifacts/smoke-density-vs-speed.png" alt="soot_density vs speed" loading="lazy" />&lt;/p>
&lt;h2>Deviations from the literature&lt;span class="hx-absolute -hx-mt-20" id="deviations-from-the-literature">&lt;/span>
&lt;a href="#deviations-from-the-literature" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The published laws are on &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/walking-speed/">Walking speed in smoke&lt;/a>
.
The code departs from them as follows.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Fractional, not absolute.&lt;/strong> The linear law is applied as a factor of each
agent&amp;rsquo;s own \(v_0\) (&lt;code>smoke_speed.py:227&lt;/code>), the FDS+Evac normalisation of an
absolute regression. It divides by the intercept \(\alpha\), an
extrapolation to &lt;em>K&lt;/em> = 0, not a measured free walking speed.&lt;/li>
&lt;li>&lt;strong>Floor.&lt;/strong> \(f_{\min}\) is FDS+Evac&amp;rsquo;s convention, not a measured minimum;
Ronchi et al. (2013) put the minimum speed in both the Jin and the
Frantzich–Nilsson data at about 0.3–0.4 m/s.&lt;/li>
&lt;li>&lt;strong>Range and spread.&lt;/strong> The law is evaluated at every &lt;em>K&lt;/em>, including below the
tunnel data, and uses only the mean coefficients, not their standard
deviations.&lt;/li>
&lt;li>&lt;strong>The &lt;code>fridolf&lt;/code> option.&lt;/strong> \(V/(V+2)\) (&lt;code>smoke_speed.py:261&lt;/code>) is not the law
of Fridolf et al. (2019), whose own law differs (see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/walking-speed/">Walking speed in smoke&lt;/a>
); the attribution
is unverified (&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/146" target="_blank" rel="noopener">#146&lt;/a>
). Fridolf et al. fitted with &lt;em>A&lt;/em> = 2 for reflecting
signs, while the code uses &lt;em>C&lt;/em> = 3 by default.&lt;/li>
&lt;li>&lt;strong>Irritancy counted twice.&lt;/strong> Frantzich and Nilsson&amp;rsquo;s smoke contained acetic
acid, so \(f(K)\) already includes irritant slowing, and multiplying by
\(g(\mathrm{FIC})\) partly counts irritancy twice. SFPE Eq. 63.14 adds the
two losses instead
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/153" target="_blank" rel="noopener">#153&lt;/a>
,
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/147" target="_blank" rel="noopener">#147&lt;/a>
).&lt;/li>
&lt;li>&lt;strong>Combination with irritants.&lt;/strong> \(g(\mathrm{FIC})\) multiplies \(f\); see the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#deviations-from-the-literature">FED page&lt;/a>
.&lt;/li>
&lt;/ul></description></item><item><title>Concepts</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/</guid><description>
&lt;p>ISO 13943:2023 defines human behaviour in fire as &amp;ldquo;actions performed in the
event of a fire as a result of a behavioural or decision-making process&amp;rdquo;. The
talk
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/visibility-seminar-2026/" target="_blank" rel="noopener">&lt;em>A Modular Workflow for Visibility-Aware Evacuation Modelling&lt;/em>&lt;/a>
(Visibility Seminar 2026, &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/pyFDS-Evac_visibility_seminar_2026.pdf" target="_blank" rel="noopener">PDF&lt;/a>
)
splits the behaviour that smoke disturbs into three questions. &lt;strong>Speed&lt;/strong> asks
how fast an occupant can walk. &lt;strong>Route choice&lt;/strong> asks which exit the occupant
heads for. &lt;strong>Wayfinding&lt;/strong> asks which exits the occupant knows about at all.
pyFDS-Evac answers each question from the output of a finished Fire Dynamics
Simulator (FDS) run and hands the answers to JuPedSim, which moves the agents.&lt;/p>
&lt;p>This page gives the intuition behind each answer and the rules you need to
read a result. The published laws the answers rest on, in their authors'
notation, are on the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/">Fundamentals&lt;/a>
pages. How the
code implements them, with its defaults and its departures from the
literature, is on the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/">Models&lt;/a>
pages.&lt;/p>
&lt;p>pyFDS-Evac is research software, provided without warranty. It is not intended
for regulatory or design use. See &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/limitations/">Limitations&lt;/a>
.&lt;/p>
&lt;h2>One-way coupling and the data flow&lt;span class="hx-absolute -hx-mt-20" id="one-way-coupling-and-the-data-flow">&lt;/span>
&lt;a href="#one-way-coupling-and-the-data-flow" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>FDS runs once, before any agent moves, and pyFDS-Evac only reads the slice
files it stored. This is one-way coupling: a crowd that blocks a door does not
change the smoke that reaches it. In return, one fire can serve any number of
egress runs, and a prescribed field can replace FDS entirely, as in the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/quickstart/">Quickstart&lt;/a>
.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>FDS output (.smv + slice files)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> │
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> ├─ fdsreader ─ SliceFieldSampler ─┬─ ExtinctionField K [1/m]
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> │ nearest-neighbour lookup ├─ FdsFedField CO, CO₂, O₂, irritants
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> │ of (t, x, y) at one height └─ FdsHeatField gas temperature [°C]
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> │
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> └─ fdsvismap ─ VisibilityModel which cells can read which sign, per time
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> │
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>Scenario (load_scenario) ──────────┤
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> ▼
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> run_scenario() ── JuPedSim moves the agents
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> │
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> ▼
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> trajectory (SQLite) + optional per-agent histories (smoke, FED, route,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> route cost, cognitive map) + &amp;lt;trajectory&amp;gt;.manifest.json&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The sampler takes the nearest stored value on a slice. Extinction and
temperature come from the horizontal slice nearest a set height, the same for
every agent; for the gases, see the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#what-is-not-modelled">FED model&lt;/a>
. The manifest records the package versions, the
seed, the scenario and the FDS version, so a result can be traced to the code
and the fire that produced it. The &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/walkthrough/">walkthrough&lt;/a>
runs this
chain on a tracked case, and &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/fds-case-requirements/">Your FDS case&lt;/a>
lists the slices the deck must write.&lt;/p>
&lt;h2>Speed&lt;span class="hx-absolute -hx-mt-20" id="speed">&lt;/span>
&lt;a href="#speed" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>From extinction to walking speed&lt;span class="hx-absolute -hx-mt-20" id="from-extinction-to-walking-speed">&lt;/span>
&lt;a href="#from-extinction-to-walking-speed" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Smoke enters the speed model through the extinction coefficient &lt;em>K&lt;/em> [1/m],
read from the FDS &lt;code>SOOT EXTINCTION COEFFICIENT&lt;/code> slice at the agent&amp;rsquo;s position.
Each agent keeps its own unimpeded speed \(v_0\) and walks at a fraction of it,&lt;/p>
$$
v = v_0 \, f(K),
$$&lt;p>where the speed factor \(f\) falls from 1 in clear air towards a floor in
dense smoke. The default is the linear Frantzich–Nilsson law in the fractional
form and with the constants of FDS+Evac, so a difference between the two tools
downstream of speed cannot come from the speed law. The alternative is the
&lt;code>fridolf&lt;/code> option, \(V/(V+2)\) with the sighting distance \(V = C/K\). Its
attribution to Fridolf et al. (2019) is unverified, and the paper&amp;rsquo;s own law
differs (&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/146" target="_blank" rel="noopener">#146&lt;/a>
). It is steeper in light smoke, has no floor, and is
available only from Python (see &lt;a href="#the-parameter-split">the parameter split&lt;/a>
). The published laws are on
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/walking-speed/">Walking speed in smoke&lt;/a>
; the coded forms are
on the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/">smoke-speed model&lt;/a>
page.&lt;/p>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/concepts/speed_laws.png" alt="Speed factor v/v0 against extinction coefficient K for the Frantzich–Nilsson law and for the fridolf option V/(V&amp;#43;2) with C = 3 and C = 8" loading="lazy" />&lt;/p>
&lt;p>&lt;em>Figure 1. Speed factor \(v/v_0\) [-] against K [1/m]. Solid dark blue:
Frantzich–Nilsson at the defaults, with its floor. The &lt;code>fridolf&lt;/code> option,
\(V/(V+2)\) with \(V = C/K\): red dashed for C = 3, orange dash-dotted for
C = 8. The arrow marks the largest gap between Frantzich–Nilsson and C = 3. Script: &lt;code>scripts/figures/speed_laws.py&lt;/code>.&lt;/em>&lt;/p>
&lt;h3>Tenability: a brake and a stop&lt;span class="hx-absolute -hx-mt-20" id="tenability-a-brake-and-a-stop">&lt;/span>
&lt;a href="#tenability-a-brake-and-a-stop" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Some effects of smoke depend only on what the agent meets now; others
accumulate into a dose. pyFDS-Evac keeps them apart: speed follows what the
agent meets at the moment, and a dose acts only as a switch:&lt;/p>
$$
v_i = v_{0,i}\; f(K)\; g(\mathrm{FIC})\;
\mathbb{1}\!\left[\mathrm{FED}_i &lt; D_i\right]\;
\mathbb{1}\!\left[\mathrm{FED}_{\mathrm{heat},i} &lt; D_{\mathrm{heat},i}\right].
$$&lt;p>The brake is \(f(K)\,g(\mathrm{FIC})\). The fractional irritant concentration
(FIC) sums the irritant gases, each over a reference concentration, and \(g\)
lowers the speed further as it rises, down to a floor. FIC is a concentration,
not a dose, so \(g\) follows the gas where the agent stands. One quirk: when
FIC drops to exactly zero the factor is not recomputed, so an agent that walks
out of a plume into clean air keeps its last slowdown
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/142" target="_blank" rel="noopener">#142&lt;/a>
).&lt;/p>
&lt;p>The stop is the fractional effective dose (FED). The toxic-gas dose follows
the Purser sum of the FDS+Evac guide, which adds irritants into the dose,
unlike ISO 13571. Convective heat from an FDS &lt;code>TEMPERATURE&lt;/code> slice accumulates
in a second dose; without that slice there is no heat dose, and the log says
so. The two doses are never added. Each agent draws its own threshold for each
dose, so a population does not stop all at once. When either dose crosses its
threshold, the agent&amp;rsquo;s speed is set to zero and it stays in place as an
obstacle. &lt;code>--disable-tenability&lt;/code> turns off the slowdown and both stops, while
the doses are still logged; &lt;code>--incapacitation-mode deterministic&lt;/code> stops every
agent at FED = 1, as FDS+Evac does.&lt;/p>
&lt;p>Two consequences matter when you read a result. An FED below the threshold
leaves the walking speed unchanged, however close it comes. And the heat dose
only incapacitates: it neither slows agents nor affects route choice, and
radiant heat is not modelled. The published dose laws are on
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/asphyxiant-fed/">Asphyxiant fractional effective dose&lt;/a>
,
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/irritants/">Irritant gases&lt;/a>
, &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/heat/">Heat&lt;/a>
and &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/incapacitation-thresholds/">Incapacitation thresholds&lt;/a>
.
The coded forms are on the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/">FED model&lt;/a>
page.&lt;/p>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/concepts/tenability_speed_curves.png" alt="Three panels: f(K) against K, g(FIC) against FIC, and their product as a heat map over K and FIC" loading="lazy" />&lt;/p>
&lt;p>&lt;em>Figure 2. The brake: (a) f(K), (b) g(FIC) and (c) their product, with
contours of equal speed factor. FED does not appear on
these axes; it only sets the speed to zero at the agent&amp;rsquo;s threshold.
Script: &lt;code>scripts/generate_tenability_curves.py&lt;/code>.&lt;/em>&lt;/p>
&lt;h2>Route choice&lt;span class="hx-absolute -hx-mt-20" id="route-choice">&lt;/span>
&lt;a href="#route-choice" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>Routes live on a stage graph&lt;span class="hx-absolute -hx-mt-20" id="routes-live-on-a-stage-graph">&lt;/span>
&lt;a href="#routes-live-on-a-stage-graph" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Routes are paths on a directed graph whose nodes are the spawn areas,
checkpoints and exits. Each edge carries a polyline that JuPedSim computes
along the walkable area, so it follows the corridors, not a straight line
through walls. Smoke is sampled at short steps along it, and the mean of the
samples, \(\bar K_{uv}\), is a discrete Beer–Lambert mean (see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/extinction/">Extinction coefficient&lt;/a>
) taken along a walked
path instead of a line of sight.&lt;/p>
&lt;p>A route is measured from where the agent stands. Its mean extinction
\(\bar K_k\) is the length-weighted mean over its edges, and \(L_k\) is the
distance still to walk. Each agent re-decides at a fixed interval, and a
decision is not remembered: every re-decision starts again from the current
field.&lt;/p>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/concepts/stage_graph.png" alt="Plan view of a room with an internal wall; a dashed straight line from agent to exit crosses the wall, a solid walked polyline goes around it with sample points coloured by extinction" loading="lazy" />&lt;/p>
&lt;p>&lt;em>Figure 3. Schematic with a toy extinction field, not a simulation. The
straight line (dashed) passes through a wall; the walked polyline (solid) goes
around it, its samples coloured by K [1/m] at the time the agent would reach
them. \(d_W\) is the walkable distance to the first node.
Script: &lt;code>scripts/figures/stage_graph.py&lt;/code>.&lt;/em>&lt;/p>
&lt;h3>The exposure gate&lt;span class="hx-absolute -hx-mt-20" id="the-exposure-gate">&lt;/span>
&lt;a href="#the-exposure-gate" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Under the default &lt;code>&amp;quot;gate&amp;quot;&lt;/code> cost model, one quantity carries all of the smoke
reasoning: the optical depth along the walk, \(\tau_k = \bar K_k L_k\), the
column of smoke the agent would pass through.&lt;/p>
&lt;p>First, a shortest-path search finds one path to each exit, with every edge
weighted by its own optical depth: the least-smoky path, which in clear air is
the shortest. Second, each candidate is re-evaluated with anticipation, each
edge sampled at the time the agent would reach it. By default the router sees
the whole future of the fire, so the result is an upper bound on how well an
occupant could route. Third, the gate refuses a route whose \(\tau\) exceeds a
budget \(\tau_{\max}\), or whose projected dose at arrival exceeds a fixed
limit. The
survivors are ranked by \(\tau\), and travel time breaks ties, so in clear
air the model reduces to the nearest exit.&lt;/p>
&lt;p>Because \(\tau\) contains the distance, a long clean route can beat a short
smoky one, but a slightly cleaner route cannot divert an agent for free. A
rival exit is held to a stricter budget than the current one, and the current
exit gets a discount in the ranking. If every route is refused, the agent
still moves and takes the least smoky one. Irritants and heat do not enter
route choice: FIC and the gate respond to the same smoke, so routing on both
would count it twice.&lt;/p>
&lt;p>The budget is inherited from the door rule of FDS+Evac, which rests on Jin&amp;rsquo;s
visibility law (see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/">Visibility through smoke&lt;/a>
).
Here it bounds smoke integrated along a walked path, which measures exposure,
not sight. It is cited, not calibrated, and FDS+Evac ranks on smoke only as a
last resort, so the gate departs from FDS+Evac rather than reproducing it. The
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/routing/">routing page&lt;/a>
gives the defaults, the derivation of the
budget, the &lt;code>&amp;quot;additive&amp;quot;&lt;/code> alternative and a measured case of oscillation.&lt;/p>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/concepts/exposure_gate.png" alt="Top: plan view with three routes from one agent to exits A, B and C around a smoke plume. Bottom: bar chart of optical depth per route against the budgets 4.8 and 6" loading="lazy" />&lt;/p>
&lt;p>&lt;em>Figure 4. Schematic with a toy plume, not a simulation. Top: three candidate
routes, their samples shaded by K [1/m]. Bottom: their optical depth \(\tau\) against the
budget for the current exit (solid) and the stricter one for a rival
(dashed). Route C is refused; route B ranks first although it is the longest
walk. Script: &lt;code>scripts/figures/exposure_gate.py&lt;/code>.&lt;/em>&lt;/p>
&lt;h2>Wayfinding&lt;span class="hx-absolute -hx-mt-20" id="wayfinding">&lt;/span>
&lt;a href="#wayfinding" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>A sign is more than a position&lt;span class="hx-absolute -hx-mt-20" id="a-sign-is-more-than-a-position">&lt;/span>
&lt;a href="#a-sign-is-more-than-a-position" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>What an agent knows about the building depends on the signs it can read.
Each exit, checkpoint and waypoint carries a sign with a position, a bearing
\(\alpha_s\) that gives the side from which it can be read, and a visibility
constant &lt;em>C&lt;/em>, a property of the sign (see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/">Visibility through smoke&lt;/a>
). A node without an
authored sign receives a reflective sign at its centroid, readable from every
direction.&lt;/p>
&lt;p>&lt;a href="https://github.com/FireDynamics/fdsvismap" target="_blank" rel="noopener">fdsvismap&lt;/a>
computes legibility
from the FDS extinction field when &lt;code>run.py&lt;/code> is given &lt;code>--fds-dir&lt;/code>, and from
clear air otherwise. A sign is legible from a floor cell when its visibility
along the line of sight, reduced by the viewing angle, reaches the distance to
it; walls block the line. The map is built once per run, and the run only asks
whether a node&amp;rsquo;s sign is legible from the agent&amp;rsquo;s cell at the current time.&lt;/p>
&lt;p>Legibility decides what an agent &lt;strong>knows&lt;/strong>, not whether a route is allowed. An
exit whose sign the agent has never read is absent from its graph. It is not
present and refused.&lt;/p>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/concepts/sign_bearing.png" alt="Two 30 m corridors, each with an exit at both ends; left, the near sign faces the agents and all 40 go to the near exit; right, the near sign faces away and all 40 walk to the far exit" loading="lazy" />&lt;/p>
&lt;p>&lt;em>Figure 5. Asset &lt;code>assets/exit_visibility_alpha&lt;/code>: a corridor in clear air with
an exit at each end and 40 agents with familiarity 0. Only the bearing of the
near exit&amp;rsquo;s sign differs. Facing the agents, all 40 took the near exit; facing
away, all 40 walked to the far one, and egress took 26.0 s instead of 18.2 s.
Outcomes as recorded in the asset README.
Script: &lt;code>scripts/figures/sign_bearing.py&lt;/code>.&lt;/em>&lt;/p>
&lt;h3>Per-agent cognitive maps&lt;span class="hx-absolute -hx-mt-20" id="per-agent-cognitive-maps">&lt;/span>
&lt;a href="#per-agent-cognitive-maps" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Each agent carries a cognitive map: the graph of stages it knows, not a
psychological construct. Routing runs only on this subgraph. The &lt;code>familiarity&lt;/code>
key of a distribution group sets how it starts. With &lt;code>&amp;quot;full&amp;quot;&lt;/code>, the default,
the agent knows the whole graph, as trained staff would. With &lt;code>&amp;quot;discovery&amp;quot;&lt;/code>,
it knows its spawn node and the neighbours whose signs are legible from the
spawn area. A number &lt;em>p&lt;/em> between 0 and 1 sits between the two: each exit
enters the map at spawn with probability &lt;em>p&lt;/em>, so a population can be a
gradient rather than two groups. An &lt;code>entrance&lt;/code> key names an exit the agent
always knows, such as the door it came in by. Setting familiarity
separately for each exit is not supported
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/136" target="_blank" rel="noopener">#136&lt;/a>
). The
literature on why familiarity matters is summarised on
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/exit-choice/">Exit choice and familiarity&lt;/a>
.&lt;/p>
&lt;p>The map grows at spawn, on arrival at a node and at each re-decision, by the
neighbours legible from where the agent stands. A learned corridor is known in
both directions, so an agent can always retrace its steps out of a dead end.
If no exit is known, the agent heads for the nearest known node it has not
visited; when none is left, it patrols the nodes it knows, because a new
position can bring a new sign into view.&lt;/p>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/concepts/cognitive_map.png" alt="Four panels of a T-shaped corridor showing which nodes and edges an agent knows: at spawn, at the junction, with smoke hiding exit B, and for a fully familiar agent" loading="lazy" />&lt;/p>
&lt;p>&lt;em>Figure 6. Schematic on a T-shaped corridor. Filled nodes and solid edges are
in the agent&amp;rsquo;s map. (1) At spawn a discovery agent knows the junction, whose
sign is legible. (2) At the junction both exits enter the map. (3) Smoke fills
the right arm; exit B stays known, and the router picks exit A. (4) A fully
familiar agent knows the whole graph at t = 0.
Script: &lt;code>scripts/figures/cognitive_map.py&lt;/code>.&lt;/em>&lt;/p>
&lt;h3>The map remembers&lt;span class="hx-absolute -hx-mt-20" id="the-map-remembers">&lt;/span>
&lt;a href="#the-map-remembers" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Nothing leaves a cognitive map. An exit learned while its sign was legible
stays known and routable after smoke or distance hides the sign; with a
visibility query alone, the agent would forget it as soon as it lost sight of
it.&lt;/p>
&lt;p>The map limits topology, not knowledge of the smoke. A discovery agent ranks
the exits it knows by \(\tau\) over the whole remaining route, including legs
it has never walked and, with anticipation, times that have not yet happened.
Its route choice is a best case over its known subgraph, not a model of what
it can perceive
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/125" target="_blank" rel="noopener">#125&lt;/a>
). The
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/visibility/">visibility page&lt;/a>
lists the command-line settings for
sight gating and what each one means.&lt;/p>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/concepts/map_memory.png" alt="Six copies of a corridor with an agent probe at y = 4, 10, 14, 20, 26 and 30 m; the side exit is unknown below the legibility band, known inside it, and still known above it" loading="lazy" />&lt;/p>
&lt;p>&lt;em>Figure 7. Asset &lt;code>assets/cognitive_map_memory&lt;/code>: a corridor whose side-exit
sign is legible only inside the shaded band. A discovery agent probed at six
positions: above the band the sign is no longer legible, but the exit is still
in the map and still chosen. Outcomes as recorded in the asset README and
pinned by tests. Script: &lt;code>scripts/figures/map_memory.py&lt;/code>.&lt;/em>&lt;/p>
&lt;h2>The parameter split&lt;span class="hx-absolute -hx-mt-20" id="the-parameter-split">&lt;/span>
&lt;a href="#the-parameter-split" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Some parameters with the same name live in two places, and they do different
things. The walking-speed parameters (&lt;code>speed_law&lt;/code>, &lt;code>alpha&lt;/code>, &lt;code>beta&lt;/code>,
&lt;code>min_speed_factor&lt;/code> and &lt;code>visibility_factor_c&lt;/code>) are fields of &lt;code>SmokeSpeedConfig&lt;/code>.
&lt;code>run.py&lt;/code> and the web GUI build that object with its defaults, so every run they
start uses the Frantzich–Nilsson law with the FDS+Evac constants. Another law
or other coefficients require building the model in Python and passing it to
&lt;code>run_scenario()&lt;/code>.&lt;/p>
&lt;p>The &lt;code>routing&lt;/code> block of the scenario JSON accepts &lt;code>alpha&lt;/code>, &lt;code>beta&lt;/code> and
&lt;code>min_speed_factor&lt;/code> with the same defaults. They only set the speed factor the
router uses to estimate travel time when it prices a route. Setting
&lt;code>routing.alpha&lt;/code> changes which route an agent prefers, but not how fast it walks
in smoke. The same holds for speed itself: the router prices routes at its own
speed, &lt;code>routing.base_speed_m_per_s&lt;/code>, not at the agent&amp;rsquo;s \(v_0\). The router
always uses the linear law, even when agents walk with the &lt;code>fridolf&lt;/code> option. The
defaults are on the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/">smoke-speed&lt;/a>
and
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/routing/">routing&lt;/a>
pages; see also
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/limitations/">Limitations&lt;/a>
.&lt;/p>
&lt;h2>Notation&lt;span class="hx-absolute -hx-mt-20" id="notation">&lt;/span>
&lt;a href="#notation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This table is the shared notation for the Concepts, Fundamentals and Models
pages. Defaults are on the Models pages.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Symbol&lt;/th>
&lt;th>Meaning&lt;/th>
&lt;th>Unit&lt;/th>
&lt;th>Code name&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;em>K&lt;/em>&lt;/td>
&lt;td>Extinction coefficient at a point (FDS &lt;code>SOOT EXTINCTION COEFFICIENT&lt;/code>)&lt;/td>
&lt;td>1/m&lt;/td>
&lt;td>&lt;code>extinction_per_m&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>\(\bar K\)&lt;/td>
&lt;td>Mean extinction along an edge or a route&lt;/td>
&lt;td>1/m&lt;/td>
&lt;td>&lt;code>k_avg&lt;/code>, &lt;code>k_ave_route&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;em>S&lt;/em> = &lt;em>C&lt;/em>/&lt;em>K&lt;/em>&lt;/td>
&lt;td>Sighting distance (Jin); &lt;em>V&lt;/em> in the code&amp;rsquo;s &lt;code>fridolf&lt;/code> option&lt;/td>
&lt;td>m&lt;/td>
&lt;td>—&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;em>C&lt;/em>&lt;/td>
&lt;td>Visibility constant of a sign (Jin); reflective or light-emitting&lt;/td>
&lt;td>-&lt;/td>
&lt;td>&lt;code>visibility_factor_c&lt;/code>, sign &lt;code>c&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>\(v_0\)&lt;/td>
&lt;td>Unimpeded desired speed of an agent&lt;/td>
&lt;td>m/s&lt;/td>
&lt;td>&lt;code>v0&lt;/code> (distribution group)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;em>f&lt;/em>(&lt;em>K&lt;/em>)&lt;/td>
&lt;td>Smoke speed factor&lt;/td>
&lt;td>-&lt;/td>
&lt;td>&lt;code>speed_factor&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>\(\alpha\), \(\beta\)&lt;/td>
&lt;td>Frantzich–Nilsson coefficients&lt;/td>
&lt;td>m/s, m²/s&lt;/td>
&lt;td>&lt;code>alpha&lt;/code>, &lt;code>beta&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>\(f_{\min}\)&lt;/td>
&lt;td>Floor of the smoke speed factor&lt;/td>
&lt;td>-&lt;/td>
&lt;td>&lt;code>min_speed_factor&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>FIC&lt;/td>
&lt;td>Fractional irritant concentration (Purser; related to, but not the same as, ISO 13571 FEC)&lt;/td>
&lt;td>-&lt;/td>
&lt;td>&lt;code>fic&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;em>g&lt;/em>(FIC)&lt;/td>
&lt;td>Irritant speed factor&lt;/td>
&lt;td>-&lt;/td>
&lt;td>&lt;code>fic_min_factor&lt;/code>, &lt;code>fic_alpha&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>FED&lt;/td>
&lt;td>Fractional effective dose, toxic gases&lt;/td>
&lt;td>-&lt;/td>
&lt;td>&lt;code>fed&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>\(\mathrm{FED}_{\mathrm{heat}}\)&lt;/td>
&lt;td>Fractional effective dose, convective heat&lt;/td>
&lt;td>-&lt;/td>
&lt;td>heat FED&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>\(D_i\), \(D_{\mathrm{heat},i}\)&lt;/td>
&lt;td>Incapacitation threshold of agent &lt;em>i&lt;/em>, log-normal&lt;/td>
&lt;td>-&lt;/td>
&lt;td>&lt;code>fed_threshold&lt;/code>, &lt;code>heat_fed_threshold&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;em>T&lt;/em>&lt;/td>
&lt;td>Gas temperature&lt;/td>
&lt;td>°C&lt;/td>
&lt;td>&lt;code>TEMPERATURE&lt;/code> slice&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>\(L_k\)&lt;/td>
&lt;td>Walkable distance still to go on route &lt;em>k&lt;/em>&lt;/td>
&lt;td>m&lt;/td>
&lt;td>&lt;code>effective_length&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>\(\tau_k = \bar K_k L_k\)&lt;/td>
&lt;td>Optical depth along route &lt;em>k&lt;/em>&lt;/td>
&lt;td>-&lt;/td>
&lt;td>&lt;code>tau_route&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>\(\tau_{\max}\)&lt;/td>
&lt;td>Refusal budget for \(\tau\)&lt;/td>
&lt;td>-&lt;/td>
&lt;td>&lt;code>tau_max&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>\(\Delta s\)&lt;/td>
&lt;td>Maximum spacing of smoke samples along a polyline&lt;/td>
&lt;td>m&lt;/td>
&lt;td>&lt;code>sampling_step_m&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>\(\alpha_s\)&lt;/td>
&lt;td>Bearing of a sign, degrees clockwise from north&lt;/td>
&lt;td>°&lt;/td>
&lt;td>sign &lt;code>alpha&lt;/code>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>The same code name &lt;code>alpha&lt;/code> is used for the Frantzich–Nilsson coefficient, for
the irritant slope &lt;code>fic_alpha&lt;/code> and for the sign bearing; the table keeps them
apart. In FDS+Evac, \(\tau\) is the relaxation time of the movement model. On
these pages \(\tau\) is always the dimensionless optical depth. &amp;ldquo;Gate&amp;rdquo; is used
in two senses only: the exposure gate (the route-cost gate) that refuses a
route, and sight gating, which decides whether a sign is legible and so what
enters the cognitive map. The dose does not gate anything; it stops the agent.&lt;/p>
&lt;h2>Further reading&lt;span class="hx-absolute -hx-mt-20" id="further-reading">&lt;/span>
&lt;a href="#further-reading" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>The talk: &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/visibility-seminar-2026/" target="_blank" rel="noopener">slides&lt;/a>
and &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/pyFDS-Evac_visibility_seminar_2026.pdf" target="_blank" rel="noopener">PDF&lt;/a>
, Visibility
Seminar 2026, University of Wuppertal, 25 September 2026.&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/">Fundamentals&lt;/a>
: the published laws, with their
sources.&lt;/li>
&lt;li>Model pages: &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/">smoke speed&lt;/a>
, &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/">FED&lt;/a>
,
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/routing/">routing&lt;/a>
, &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/visibility/">visibility and cognitive maps&lt;/a>
,
and &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/verification/">verification&lt;/a>
.&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/limitations/">Limitations&lt;/a>
: what is not modelled, and which
parameters are library-level.&lt;/li>
&lt;/ul></description></item><item><title>Extinction coefficient</title><link>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/extinction/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/extinction/</guid><description>
&lt;p>The extinction coefficient &lt;em>K&lt;/em> [1/m] measures how strongly smoke attenuates
light per metre of path. It is the quantity from which visibility, walking
speed in smoke and most smoke tenability limits are computed.&lt;/p>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
; here &lt;em>L&lt;/em> is
the length of a light path, not the route length \(L_k\) of the table.&lt;/p>
&lt;h2>The equations as published&lt;span class="hx-absolute -hx-mt-20" id="the-equations-as-published">&lt;/span>
&lt;a href="#the-equations-as-published" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Light of intensity \(I_0\) that crosses a path of length &lt;em>L&lt;/em> [m] through
smoke leaves with intensity &lt;em>I&lt;/em> given by the Beer–Lambert law, written in the
Fire Dynamics Simulator (FDS) User Guide (McGrattan et al., FDS 6.10.1,
§22.10.5) as&lt;/p>
$$
\frac{I}{I_0} = e^{-KL} \qquad \text{(FDS UG Eq. 22.21)}
$$&lt;p>and &lt;em>K&lt;/em> is the product of a mass-specific extinction coefficient
\(K_m\) [m²/kg] and the mass concentration of smoke particulate
\(\rho Y_S\) [kg/m³]:&lt;/p>
$$
K = K_m\, \rho Y_S \qquad \text{(Eq. 22.22)}
$$&lt;p>Along a path on which &lt;em>K&lt;/em> varies, the exponent becomes the integral of &lt;em>K&lt;/em>
along the path, which FDS evaluates as a sum over cells for its beam detector
(Eq. 18.5). That dimensionless integral is the optical depth. The optical
density per metre &lt;em>D&lt;/em> [1/m] uses base-10 logarithms instead of natural ones:&lt;/p>
$$
D \equiv -\frac{1}{L}\log_{10}\frac{I}{I_0} = K \log_{10} e \approx K/2.3 \qquad \text{(Eq. 22.24)}
$$&lt;p>The distinction matters when reading the literature. Tenability limits are
often quoted as OD/m (optical density per metre, &lt;em>D&lt;/em>), and Purser and
McAllister (2016), in the Society of Fire Protection Engineers (SFPE)
Handbook, give both forms: for example OD/m = 0.2 corresponds to an
extinction coefficient of about 0.5 1/m (Ch. 63, Table 63.5 and the text
beside it).&lt;/p>
&lt;h2>The data behind \(K_m\)&lt;span class="hx-absolute -hx-mt-20" id="the-data-behind">&lt;/span>
&lt;a href="#the-data-behind" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>FDS uses \(K_m\) = 8700 m²/kg by default (&lt;code>MASS_EXTINCTION_COEFFICIENT&lt;/code>).
The User Guide describes it as a value suggested for flaming combustion of
wood and plastics, and gives 8700 ± 1100 m²/kg at a wavelength of 633 nm for
most flaming fuels (FDS User Guide §22.10.5, footnote 7), citing
Mulholland and Croarkin (2000).&lt;/p>
&lt;h2>Known limits&lt;span class="hx-absolute -hx-mt-20" id="known-limits">&lt;/span>
&lt;a href="#known-limits" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;em>K&lt;/em> depends on wavelength, and the 8700 m²/kg figure is for red light at
633 nm. It is a property of flame-generated soot, not of every fire effluent.
In a fire model &lt;em>K&lt;/em> also inherits every uncertainty of the predicted soot
yield and soot transport. FDS writes the local &lt;em>K&lt;/em> as the &lt;code>EXTINCTION COEFFICIENT&lt;/code> output quantity (§22.10.5); the unrelated quantity
&lt;code>EXTINCTION&lt;/code> is a combustion-suppression flag (§22.10.29).&lt;/p>
&lt;h2>Sources&lt;span class="hx-absolute -hx-mt-20" id="sources">&lt;/span>
&lt;a href="#sources" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>McGrattan, K., Hostikka, S., Floyd, J., McDermott, R., Vanella, M.,
Mueller, E., &amp;amp; Paul, C. (2025). &lt;em>Fire Dynamics Simulator User&amp;rsquo;s
Guide&lt;/em>. NIST Special Publication 1019, 6th ed., revision
FDS-6.10.1-0-g12efa16, §18.3.6 and §22.10.5.
&lt;a href="https://github.com/firemodels/fds/releases/tag/FDS-6.10.1" target="_blank" rel="noopener">github.com/firemodels/fds/releases/tag/FDS-6.10.1&lt;/a>
&lt;/li>
&lt;li>Mulholland, G. W., &amp;amp; Croarkin, C. (2000). &lt;em>Specific extinction
coefficient of flame generated smoke&lt;/em>. Fire and Materials, 24(5),
227–230.
&lt;a href="https://doi.org/10.1002/1099-1018%28200009/10%2924:5%3C227::AID-FAM742%3E3.0.CO;2-9" target="_blank" rel="noopener">doi:10.1002/1099-1018(200009/10)24:5&amp;lt;227::AID-FAM742&amp;gt;3.0.CO;2-9&lt;/a>
(value quoted from the FDS User Guide).&lt;/li>
&lt;li>Purser, D. A., &amp;amp; McAllister, J. L. (2016). &lt;em>Assessment of hazards to
occupants from smoke, toxic gases, and heat&lt;/em>. SFPE Handbook of Fire
Protection Engineering, 5th ed., Ch. 63, 2308–2428.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_63" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_63&lt;/a>
&lt;/li>
&lt;/ul>
&lt;p>How pyFDS-Evac uses this: see the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/">smoke-speed model&lt;/a>
and &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/routing/">route rerouting&lt;/a>
.&lt;/p></description></item><item><title>Fractional effective dose</title><link>https://pedestriandynamics.org/pyFDS-Evac/models/fed/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/models/fed/</guid><description>
&lt;p>Based on: &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/asphyxiant-fed/">Asphyxiant fractional effective dose&lt;/a>
, &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/irritants/">Irritant gases&lt;/a>
, &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/heat/">Heat&lt;/a>
and &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/incapacitation-thresholds/">Incapacitation thresholds&lt;/a>
.&lt;/p>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
. The model
codes the Purser sum in the form of the FDS+Evac guide (Korhonen 2021, §3.4),
which adds irritants into the dose; ISO 13571 keeps them in a separate
concentration endpoint. The published equations are on the Fundamentals pages
linked above.&lt;/p>
&lt;p>Background: the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/">Concepts&lt;/a>
page
and the talk &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/visibility-seminar-2026/" target="_blank" rel="noopener">&lt;em>A Modular Workflow for Visibility-Aware Evacuation Modelling&lt;/em>&lt;/a>
(&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/pyFDS-Evac_visibility_seminar_2026.pdf" target="_blank" rel="noopener">PDF&lt;/a>
).&lt;/p>
&lt;h2>Coded form&lt;span class="hx-absolute -hx-mt-20" id="coded-form">&lt;/span>
&lt;a href="#coded-form" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;code>FedComponents.total_rate_per_min&lt;/code> (&lt;code>pyfds_evac/core/fed.py:334&lt;/code>) returns the
gas FED rate [1/min]&lt;/p>
$$
\dot{\mathrm{FED}} = \bigl(\dot{\mathrm{FED}}_{\mathrm{CO}} + \dot{\mathrm{FED}}_{\mathrm{CN}} + \dot{\mathrm{FED}}_{\mathrm{NO_x}} + \dot{\mathrm{FLD}}_{\mathrm{irr}}\bigr) \times \mathrm{HV}_{\mathrm{CO_2}} + \dot{\mathrm{FED}}_{\mathrm{O_2}},
$$&lt;p>and &lt;code>DefaultFedModel&lt;/code> integrates it over time, with &lt;em>t&lt;/em> in minutes. Each term
is one function in &lt;code>fed.py&lt;/code>:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Term&lt;/th>
&lt;th>Function&lt;/th>
&lt;th>Coded rate [1/min]&lt;/th>
&lt;th>Input&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>CO&lt;/td>
&lt;td>&lt;code>_co_fed_rate_per_minute&lt;/code>&lt;/td>
&lt;td>\(2.764 \times 10^{-5}\, C_{\mathrm{CO}}^{1.036}\)&lt;/td>
&lt;td>CO (ppm)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>CN&lt;/td>
&lt;td>&lt;code>_cn_fed_rate_per_minute&lt;/code>&lt;/td>
&lt;td>\(\max\bigl(0,\ \exp(C_{\mathrm{CN}}/43)/220 - 0.0045\bigr)\), \(C_{\mathrm{CN}} = \max(0,\ C_{\mathrm{HCN}} - C_{\mathrm{NO_2}})\)&lt;/td>
&lt;td>HCN, NO2 (ppm)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>NOₓ&lt;/td>
&lt;td>&lt;code>_nox_fed_rate_per_minute&lt;/code>&lt;/td>
&lt;td>\((C_{\mathrm{NO}} + C_{\mathrm{NO_2}})/1500\)&lt;/td>
&lt;td>NO, NO2 (ppm)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Irritants&lt;/td>
&lt;td>&lt;code>_irritant_fld_rate_per_minute&lt;/code>&lt;/td>
&lt;td>\(\sum_i C_i / F_{\mathrm{FLD},i}\)&lt;/td>
&lt;td>seven irritants (ppm)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>HV_CO2&lt;/td>
&lt;td>&lt;code>_hyperventilation_factor&lt;/code>&lt;/td>
&lt;td>\(\exp(0.1903\, C_{\mathrm{CO_2}} + 2.0004)/7.1\) (a factor, not a rate)&lt;/td>
&lt;td>CO2 (vol %)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>O2&lt;/td>
&lt;td>&lt;code>_o2_hypoxia_rate_per_minute&lt;/code>&lt;/td>
&lt;td>\(1/\exp\bigl(8.13 - 0.54\,(20.9 - C_{\mathrm{O_2}})\bigr)\); 0 at or above 19.5 %&lt;/td>
&lt;td>O2 (vol %)&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>The lethal Ct doses \(F_{\mathrm{FLD},i}\) [ppm·min] are constants in
&lt;code>_irritant_fld_rate_per_minute&lt;/code>, taken from Table 2 of the FDS+Evac guide:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Species&lt;/th>
&lt;th>HCl&lt;/th>
&lt;th>HBr&lt;/th>
&lt;th>HF&lt;/th>
&lt;th>SO2&lt;/th>
&lt;th>NO2&lt;/th>
&lt;th>acrolein&lt;/th>
&lt;th>formaldehyde&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>F_FLD&lt;/td>
&lt;td>114000&lt;/td>
&lt;td>114000&lt;/td>
&lt;td>87000&lt;/td>
&lt;td>12000&lt;/td>
&lt;td>1900&lt;/td>
&lt;td>4500&lt;/td>
&lt;td>22500&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>Gas species are read from FDS slice outputs via &lt;code>fdsreader&lt;/code>. CO, CO2 and O2
are required. The optional species (HCN, NO, NO2, HCl, HBr, HF, SO2, acrolein,
formaldehyde), the guide&amp;rsquo;s optional terms, are loaded when available; a
missing species reads 0 and contributes nothing. With only the three required
species the sum reduces to the FDS+Evac default,
\(\dot{\mathrm{FED}}_{\mathrm{CO}}\,\mathrm{HV}_{\mathrm{CO_2}} + \dot{\mathrm{FED}}_{\mathrm{O_2}}\).&lt;/p>
&lt;h2>Convective heat&lt;span class="hx-absolute -hx-mt-20" id="convective-heat">&lt;/span>
&lt;a href="#convective-heat" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>When the case has a &lt;code>TEMPERATURE&lt;/code> slice, a separate heat dose accumulates at
&lt;code>_heat_fed_rate_per_minute&lt;/code> (&lt;code>fed.py:207&lt;/code>),&lt;/p>
$$
\dot{\mathrm{FED}}_{\mathrm{heat}} = T^{3.4} / (5 \times 10^{7}) \quad [1/\mathrm{min}],
$$&lt;p>with &lt;em>T&lt;/em> in °C. It is a running total of its own, never added to the gas FED.
The basis is on &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/heat/">Heat&lt;/a>
.&lt;/p>
&lt;h2>Tenability: irritant slowdown and incapacitation&lt;span class="hx-absolute -hx-mt-20" id="tenability-irritant-slowdown-and-incapacitation">&lt;/span>
&lt;a href="#tenability-irritant-slowdown-and-incapacitation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;code>TenabilityConfig&lt;/code> (&lt;code>fed.py:216&lt;/code>) adds two rules. The slowdown and the gas
stop need the gas FED model; the heat stop needs only the heat FED model
(&lt;code>run_config.py:246&lt;/code>–&lt;code>256&lt;/code>).&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Irritant slowdown.&lt;/strong> &lt;code>default_fic&lt;/code> sums \(C_i / F_{\mathrm{FIC},i}\) over the
same seven irritants (constants in &lt;code>_FIC_COEFFS_PPM&lt;/code>, not integrated over
time). At each FED update where FIC &amp;gt; 0, the agent&amp;rsquo;s irritant factor is set
to \(g = \max(\texttt{fic\_min\_factor},\ 1 - \texttt{fic\_alpha}\cdot\mathrm{FIC})\)
(&lt;code>scenario.py:2076&lt;/code>–&lt;code>2081&lt;/code>) and multiplies the smoke factor. When FIC is
exactly 0 the last factor stays in force
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/142" target="_blank" rel="noopener">#142&lt;/a>
).&lt;/li>
&lt;li>&lt;strong>Incapacitation.&lt;/strong> Once the cumulative gas FED or heat FED reaches the
agent&amp;rsquo;s threshold for that dose, its desired speed is set to zero and it
remains as a static obstacle. An agent incapacitated during pre-movement is
released again when its pre-movement time ends
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/145" target="_blank" rel="noopener">#145&lt;/a>
). In
&lt;code>probabilistic&lt;/code> mode each agent draws each threshold once, from the run&amp;rsquo;s
seed, as &lt;code>_sample_threshold&lt;/code> does:
\(D_i = \texttt{fed\_threshold} \cdot \exp(\sigma Z)\), \(Z \sim N(0, 1)\).
In &lt;code>deterministic&lt;/code> mode every agent uses the threshold itself.&lt;/li>
&lt;/ul>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Field&lt;/th>
&lt;th>Default&lt;/th>
&lt;th>CLI flag&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>fic_alpha&lt;/code>&lt;/td>
&lt;td>&lt;code>0.7&lt;/code>&lt;/td>
&lt;td>&lt;code>--fic-alpha&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>fic_min_factor&lt;/code>&lt;/td>
&lt;td>&lt;code>0.3&lt;/code>&lt;/td>
&lt;td>&lt;code>--fic-min-factor&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>fed_threshold&lt;/code>&lt;/td>
&lt;td>&lt;code>1.0&lt;/code>&lt;/td>
&lt;td>&lt;code>--fed-threshold&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>incapacitation_mode&lt;/code>&lt;/td>
&lt;td>&lt;code>&amp;quot;probabilistic&amp;quot;&lt;/code>&lt;/td>
&lt;td>&lt;code>--incapacitation-mode&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>susceptibility_sigma&lt;/code>&lt;/td>
&lt;td>&lt;code>0.94&lt;/code>&lt;/td>
&lt;td>&lt;code>--susceptibility-sigma&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>heat_fed_threshold&lt;/code>&lt;/td>
&lt;td>&lt;code>1.0&lt;/code>&lt;/td>
&lt;td>&lt;code>--heat-fed-threshold&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>heat_incapacitation_mode&lt;/code>&lt;/td>
&lt;td>&lt;code>&amp;quot;probabilistic&amp;quot;&lt;/code>&lt;/td>
&lt;td>&lt;code>--heat-incapacitation-mode&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>heat_susceptibility_sigma&lt;/code>&lt;/td>
&lt;td>&lt;code>0.94&lt;/code>&lt;/td>
&lt;td>&lt;code>--heat-susceptibility-sigma&lt;/code>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>The gas and heat FED are updated every &lt;code>DefaultFedConfig.update_interval_s&lt;/code>,
which &lt;code>run.py&lt;/code> sets from &lt;code>--smoke-update-interval&lt;/code>. &lt;code>--disable-tenability&lt;/code>
turns off the slowdown and both stops; the doses are still computed. The FED
history CSV (&lt;code>--output-fed-history&lt;/code>) carries the columns &lt;code>fic&lt;/code>,
&lt;code>fic_speed_factor&lt;/code> and &lt;code>incapacitated&lt;/code>.&lt;/p>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/concepts/tenability_speed_curves.png" alt="Three panels: f(K) against K, g(FIC) against FIC, and their product as a heat map over K and FIC" loading="lazy" />&lt;/p>
&lt;p>&lt;em>(a) Frantzich–Nilsson factor f(K) [-] against K [1/m], floor 0.1.
(b) Irritant factor g(FIC) [-] against FIC [-], floor 0.3. (c) The product
f(K)·g(FIC) [-], with contours at 0.25, 0.5 and 0.75; where both floors apply
it is 0.1 × 0.3 = 0.03. FED does not appear on these axes; it only sets the speed to
zero at the agent&amp;rsquo;s threshold. Script: &lt;code>scripts/generate_tenability_curves.py&lt;/code>.&lt;/em>&lt;/p>
&lt;h2>FDS input pitfalls&lt;span class="hx-absolute -hx-mt-20" id="fds-input-pitfalls">&lt;/span>
&lt;a href="#fds-input-pitfalls" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;strong>Conflicting &lt;code>&amp;amp;INIT&lt;/code> records silently zero out prescribed gas
concentrations.&lt;/strong> FDS resets the &lt;em>entire&lt;/em> domain&amp;rsquo;s species composition on
each &lt;code>&amp;amp;INIT&lt;/code> record that has no &lt;code>XB&lt;/code> bounding box, so a second &lt;code>&amp;amp;INIT&lt;/code>
(e.g. one that only sets soot) overwrites an earlier one (e.g. one that
sets CO/CO2/O2), leaving those species at 0 with no warning. All species
prescribed via &lt;code>&amp;amp;INIT&lt;/code> in a test deck must go in a single record. This is
an FDS input-authoring pitfall, not a pyFDS-Evac bug; its symptom is
near-zero toxic gas readings.&lt;/li>
&lt;li>&lt;strong>&lt;code>EXTINCTION&lt;/code> is not the smoke extinction coefficient.&lt;/strong> It is an
unrelated FDS quantity (see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/extinction/">Extinction coefficient&lt;/a>
).
&lt;code>load_slice_sampler&lt;/code> requires &lt;code>SOOT EXTINCTION COEFFICIENT&lt;/code> and raises
&lt;code>IndexError&lt;/code> when it is absent (&lt;code>pyfds_evac/core/fds_sampling.py&lt;/code>).&lt;/li>
&lt;/ul>
&lt;h2>Verification&lt;span class="hx-absolute -hx-mt-20" id="verification">&lt;/span>
&lt;a href="#verification" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>Equation-level constant-exposure checks for all coded terms are covered in
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/tests/test_fed.py" target="_blank" rel="noopener">tests/test_fed.py&lt;/a>
&lt;/li>
&lt;li>An ISO 20414:2020 Test 19 (Table 22) stationary benchmark is covered with &lt;code>assets/ISO-table22&lt;/code>,
comparing the runtime &lt;code>FED=1&lt;/code> crossing time against the analytical reference&lt;/li>
&lt;/ul>
&lt;p>Generate the ISO 20414 Test 19 (Table 22) stationary FED verification figure:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python scripts/generate_iso_table22_stationary_plot.py&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Figure: &lt;img src="https://pedestriandynamics.org/pyFDS-Evac/artifacts/iso-table22-stationary-fed.png" alt="ISO 20414 Test 19 (Table 22) stationary FED verification" loading="lazy" />&lt;/p>
&lt;h2>What is not modelled&lt;span class="hx-absolute -hx-mt-20" id="what-is-not-modelled">&lt;/span>
&lt;a href="#what-is-not-modelled" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>Radiant heat. Only convective heat from the gas temperature is modelled.&lt;/li>
&lt;li>Effects of heat on route choice or walking speed: the heat FED only
incapacitates.&lt;/li>
&lt;li>FED activity level: the CO term is fixed at light work; rest and heavy
work are not supported
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/135" target="_blank" rel="noopener">#135&lt;/a>
).&lt;/li>
&lt;li>&lt;strong>Height-relative FED and smoke sampling&lt;/strong>: extinction and temperature are
sampled from the horizontal FDS slice closest to &lt;code>--smoke-slice-height&lt;/code>
(default 2.0 m); each gas is read from the first slice of its quantity in the
deck, whatever its height. All agents share these slices regardless of
their individual heights. Pathfinder samples at 90 % of each occupant&amp;rsquo;s height,
which is more accurate for scenarios with mixed-height populations (children,
wheelchair users). A per-agent sampling height would require either multiple
slice outputs at different elevations or 3-D slice data, and is a known
approximation of the current model.&lt;/li>
&lt;/ul>
&lt;h2>Usage&lt;span class="hx-absolute -hx-mt-20" id="usage">&lt;/span>
&lt;a href="#usage" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>See &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/usage/">docs/usage.md&lt;/a>
for the full catalogue of
&lt;code>run.py&lt;/code> flags (scenario, FDS coupling, FED, rerouting, tenability)
and the post-processing scripts. Note: if an agent
sample lies outside the FDS domain the implementation falls back to
ambient conditions.&lt;/p>
&lt;h2>Deviations from the literature&lt;span class="hx-absolute -hx-mt-20" id="deviations-from-the-literature">&lt;/span>
&lt;a href="#deviations-from-the-literature" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The published forms are on &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/asphyxiant-fed/">Asphyxiant FED&lt;/a>
and &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/irritants/">Irritant gases&lt;/a>
. The gas sum has the Purser /
FDS+Evac guide structure (&lt;code>fed.py:334&lt;/code>), not the ISO 13571 one. Within it, the
code follows the guide, which cites the 3rd edition of the SFPE Handbook,
rather than the 5th edition
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/149" target="_blank" rel="noopener">#149&lt;/a>
):&lt;/p>
&lt;ul>
&lt;li>&lt;strong>HCN.&lt;/strong> The guide&amp;rsquo;s exponential term (&lt;code>fed.py:106&lt;/code>, &lt;code>:109&lt;/code>), not the
5th-edition power law (Eq. 63.24), and \(C_{\mathrm{CN}}\) ignores nitriles
and NO (Eq. 63.26).&lt;/li>
&lt;li>&lt;strong>CO₂.&lt;/strong> Eq. 63.34 (&lt;code>fed.py:63&lt;/code>), not its simplification Eq. 63.35 used in
Eq. 63.38. The 70 L/min limit on \(V_E\times VCO_2\) is not applied, and
the CO₂ asphyxiant endpoint \(F_{I_{CO_2}}\) is not computed.&lt;/li>
&lt;li>&lt;strong>CO.&lt;/strong> Fixed at light work (&lt;code>fed.py:54&lt;/code>), Eq. 63.18 at its default
\(V_E\) and &lt;em>D&lt;/em>.&lt;/li>
&lt;li>&lt;strong>O₂.&lt;/strong> The rate is zero at or above 19.5 % O₂ (&lt;code>fed.py:66&lt;/code>, &lt;code>:92&lt;/code>), a
guard that neither Purser nor the guide has; it stops a tiny ambient rate
from accumulating over long runs or outside the FDS domain, as Pathfinder
does. The guide&amp;rsquo;s Eq. 18 carries a factor 60 in the denominator while
stating that &lt;em>t&lt;/em> is in minutes; the code follows Handbook Eq. 63.50 without
it.&lt;/li>
&lt;/ul>
&lt;p>The irritant slowdown \(g\) (&lt;code>fed.py:241&lt;/code>–&lt;code>242&lt;/code>) is multiplied with the smoke
factor (&lt;code>direct_steering_runtime.py:186&lt;/code>–&lt;code>190&lt;/code>). Its constants were not found
in the Handbook, the FDS+Evac guide or &lt;code>evac.f90&lt;/code>
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/147" target="_blank" rel="noopener">#147&lt;/a>
). The
Handbook uses a different curve and adds the smoke and irritant losses instead
of multiplying them (Eq. 63.14). Because the Frantzich–Nilsson smoke contained
acetic acid, \(f(K)\) already includes irritant slowing, so multiplying it by
\(g\) partly counts irritancy twice
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/153" target="_blank" rel="noopener">#153&lt;/a>
).&lt;/p>
&lt;p>Heat uses the convective Eq. 63.44 only (&lt;code>fed.py:207&lt;/code>), not the mid-humidity
design form Eq. 63.45, and no radiant term. The log-normal σ of both
thresholds (&lt;code>fed.py:252&lt;/code>, &lt;code>:260&lt;/code>) is, for the gas dose, a compromise between
two bin edges of NIST TN 1797: it puts 10 % of agents below FED 0.3 and 88 %
below 3 (see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/incapacitation-thresholds/">Incapacitation thresholds&lt;/a>
and &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/148" target="_blank" rel="noopener">#148&lt;/a>
). The
heat dose reuses the same σ without a data basis.&lt;/p></description></item><item><title>Coming from FDS+Evac</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/coming-from-fds-evac/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/coming-from-fds-evac/</guid><description>
&lt;p>This page is for engineers who have an FDS+Evac input file and want to run the
same case in pyFDS-Evac. It maps each FDS+Evac input to its place here, and it
lists what has no equivalent. Read &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/limitations/">Limitations&lt;/a>
before you
rely on a result.&lt;/p>
&lt;p>pyFDS-Evac is research software, provided without warranty. It is not intended
for regulatory or design use.&lt;/p>
&lt;h2>What carries over and what changes&lt;span class="hx-absolute -hx-mt-20" id="what-carries-over-and-what-changes">&lt;/span>
&lt;a href="#what-carries-over-and-what-changes" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;strong>The fire part of your deck carries over.&lt;/strong> Remove the evacuation namelists
and the evacuation meshes, keep the fire meshes, obstructions, &lt;code>&amp;amp;REAC&lt;/code> and
&lt;code>&amp;amp;SURF&lt;/code> lines, and add the slices pyFDS-Evac reads
(&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/fds-case-requirements/">What your FDS case must provide&lt;/a>
). Then run FDS
as usual.&lt;/p>
&lt;p>&lt;strong>The occupants move in a different program.&lt;/strong> FDS+Evac (Korhonen 2021)
computes movement inside the FDS executable, with its own social-force model
on 2-D evacuation meshes. In pyFDS-Evac, &lt;a href="https://www.jupedsim.org" target="_blank" rel="noopener">JuPedSim&lt;/a>
moves the agents on a continuous walkable polygon. The operational model is
chosen in the scenario JSON (&lt;code>model_type&lt;/code>, default &lt;code>CollisionFreeSpeedModel&lt;/code>).&lt;/p>
&lt;p>&lt;strong>Smoke is read, not computed alongside.&lt;/strong> pyFDS-Evac never runs FDS. It reads
the slice files of a finished FDS run and samples them at the agents'
positions. The coupling is one-way: the fire acts on the occupants, and
nothing the occupants do (opening a door, for example) acts on the fire.&lt;/p>
&lt;p>A case therefore has three parts: the FDS output directory, a scenario JSON,
and a walkable geometry as WKT (well-known text, a plain-text polygon format). &lt;code>uv run python run.py --scenario &amp;lt;json|dir|zip&amp;gt; --fds-dir &amp;lt;fds output&amp;gt;&lt;/code> combines them.&lt;/p>
&lt;h2>Where each FDS+Evac input goes&lt;span class="hx-absolute -hx-mt-20" id="where-each-fdsevac-input-goes">&lt;/span>
&lt;a href="#where-each-fdsevac-input-goes" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Section numbers refer to the FDS+Evac Technical Reference and User&amp;rsquo;s Guide
(Korhonen 2021). &amp;ldquo;JSON&amp;rdquo; means the scenario JSON. Its stage layout
(&lt;code>distributions&lt;/code>, &lt;code>exits&lt;/code>, &lt;code>checkpoints&lt;/code>, &lt;code>journeys&lt;/code>) is the one the JuPedSim
web editor writes, and &lt;code>load_scenario&lt;/code> converts the editor&amp;rsquo;s &lt;code>journeys_v2&lt;/code>
routes on load. The pyFDS-Evac web GUI (graphical user interface, &lt;code>app.py&lt;/code>)
runs uploaded scenarios. It does not edit geometry or stages.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>FDS+Evac input&lt;/th>
&lt;th>What it did&lt;/th>
&lt;th>In pyFDS-Evac&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>Evacuation meshes (&lt;code>&amp;amp;MESH EVACUATION=.TRUE.&lt;/code>, §8.1)&lt;/td>
&lt;td>2-D grids for movement, separate from the fire meshes&lt;/td>
&lt;td>Replaced by one walkable polygon (WKT). &lt;code>scripts/generate_walkable_from_fds.py&lt;/code> derives it from the &lt;code>&amp;amp;OBST&lt;/code> lines of a deck (see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/usage/">Usage&lt;/a>
).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>EVAC_Z_OFFSET&lt;/code> (§8.1)&lt;/td>
&lt;td>Height at which smoke and FED are read&lt;/td>
&lt;td>&lt;code>--smoke-slice-height&lt;/code> [m], default 2.0. It selects among the slices in the output, so the deck must contain an &lt;code>&amp;amp;SLCF PBZ=&lt;/code> at that height.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>&amp;amp;EVAC&lt;/code> (§8.8)&lt;/td>
&lt;td>Places a group of agents in a rectangle&lt;/td>
&lt;td>A &lt;code>distributions&lt;/code> entry in the JSON: a polygon plus &lt;code>parameters&lt;/code> (&lt;code>number&lt;/code>, &lt;code>v0&lt;/code>, &lt;code>radius&lt;/code>, pre-movement, familiarity).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>&amp;amp;PERS&lt;/code> (§8.7)&lt;/td>
&lt;td>Agent type: body size, speed, pre-movement, force constants&lt;/td>
&lt;td>Per distribution in the JSON: &lt;code>v0&lt;/code> [m/s], &lt;code>radius&lt;/code> [m] and the pre-movement keys below. There are no named agent types and no three-circle body; an agent is a circle.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>&amp;amp;EVHO&lt;/code> (§8.9)&lt;/td>
&lt;td>Area where no agents are placed&lt;/td>
&lt;td>Not supported. Draw the distribution polygon so that it excludes the area.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>&amp;amp;EXIT&lt;/code> (§8.10)&lt;/td>
&lt;td>Line that removes agents&lt;/td>
&lt;td>An &lt;code>exits&lt;/code> entry in the JSON (a polygon). Optional &lt;code>enable_throughput_throttling&lt;/code> and &lt;code>max_throughput&lt;/code> cap the flow, and an optional &lt;code>sign&lt;/code> feeds visibility.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>&amp;amp;DOOR&lt;/code> (§8.12)&lt;/td>
&lt;td>Moves agents to another part of the calculation&lt;/td>
&lt;td>No door object. A doorway is a gap in the walkable polygon; an intermediate target is a &lt;code>checkpoints&lt;/code> entry in a journey.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>&amp;amp;ENTR&lt;/code> (§8.11)&lt;/td>
&lt;td>Adds agents at a constant rate&lt;/td>
&lt;td>Flow spawning on a distribution: &lt;code>use_flow_spawning&lt;/code>, &lt;code>flow_start_time&lt;/code>, &lt;code>flow_end_time&lt;/code> [s].&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>&amp;amp;CORR&lt;/code> (§8.13)&lt;/td>
&lt;td>One-way corridor or stair between floors&lt;/td>
&lt;td>Not supported. pyFDS-Evac is single-floor.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>&amp;amp;STRS&lt;/code> (§8.15)&lt;/td>
&lt;td>Whole staircase with its own mesh&lt;/td>
&lt;td>Not supported.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>&amp;amp;EVSS&lt;/code> (§8.14)&lt;/td>
&lt;td>Incline with speed factors &lt;code>FAC_V0_UP/DOWN/HORI&lt;/code>&lt;/td>
&lt;td>Not supported as an incline. A &lt;code>zones&lt;/code> or &lt;code>checkpoints&lt;/code> entry with a &lt;code>speed_factor&lt;/code> (0 to 3) scales the speed of agents inside a polygon, with no direction dependence.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>DET_*&lt;/code> and &lt;code>PRE_*&lt;/code> (§8.7, §8.8)&lt;/td>
&lt;td>Detection time plus reaction time, each from a distribution&lt;/td>
&lt;td>One pre-movement delay per agent: &lt;code>use_premovement&lt;/code>, &lt;code>premovement_distribution&lt;/code>, &lt;code>premovement_param_a&lt;/code>, &lt;code>premovement_param_b&lt;/code>, &lt;code>premovement_seed&lt;/code>. There is no separate detection phase. See the table below.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>TDET_SMOKE_DENS&lt;/code> (§8.7)&lt;/td>
&lt;td>Smoke at the agent&amp;rsquo;s position triggers detection&lt;/td>
&lt;td>Not supported. The pre-movement delay does not depend on smoke.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>VELOCITY_DIST&lt;/code> and speed ranges (§8.7)&lt;/td>
&lt;td>Unimpeded walking speed distribution&lt;/td>
&lt;td>&lt;code>v0&lt;/code> [m/s], default 1.2, with &lt;code>v0_distribution&lt;/code> = &lt;code>&amp;quot;constant&amp;quot;&lt;/code> or &lt;code>&amp;quot;gaussian&amp;quot;&lt;/code> and &lt;code>v0_std&lt;/code>. These are the keys the run reads from the JSON. &lt;code>desired_speed&lt;/code>, &lt;code>desired_speed_distribution&lt;/code> and &lt;code>desired_speed_std&lt;/code> are aliases accepted only by &lt;code>Scenario.set_agent_params()&lt;/code> in Python, which writes the &lt;code>v0&lt;/code> keys; in the JSON they are ignored. Gaussian draws are clipped to 0.1–5.0 m/s. There is no uniform speed distribution.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Smoke-speed reduction (Eq. 11) and &lt;code>SMOKE_MIN_SPEED&lt;/code>&lt;/td>
&lt;td>Linear speed reduction with extinction, floored at &lt;code>SMOKE_MIN_SPEED&lt;/code> × &lt;em>v&lt;/em>0&lt;/td>
&lt;td>The default is the same law, Frantzich–Nilsson (the &lt;code>lund&lt;/code> option; the linear FDS+Evac law). Its coefficients (&lt;code>alpha&lt;/code>, &lt;code>beta&lt;/code>, &lt;code>min_speed_factor&lt;/code>) are library-level; see &lt;a href="#what-needs-python">What needs Python&lt;/a>
.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>FED, fractional effective dose (§3.4)&lt;/td>
&lt;td>Purser FED from CO, CO2, O2 (and optional gases), incapacitation at FED ≥ 1&lt;/td>
&lt;td>Computed when the output has CO, CO2 and O2 slices; HCN, NOx and irritant slices are added when present. A separate convective heat dose is computed from a &lt;code>TEMPERATURE&lt;/code> slice. By default each agent draws its own threshold (log-normal, median 1; spread on the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#tenability-irritant-slowdown-and-incapacitation">FED model&lt;/a>
page), so about half stop below FED = 1. Use &lt;code>--incapacitation-mode deterministic&lt;/code> (and &lt;code>--heat-incapacitation-mode deterministic&lt;/code>) to stop every agent at FED = 1 as FDS+Evac does. Thresholds are set with &lt;code>--fed-threshold&lt;/code> and &lt;code>--heat-fed-threshold&lt;/code>.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>FED activity level&lt;/td>
&lt;td>Rest, light work or heavy work&lt;/td>
&lt;td>Not supported. The CO term is fixed. See &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/135" target="_blank" rel="noopener">issue #135&lt;/a>
.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>KNOWN_DOOR_NAMES&lt;/code>, &lt;code>KNOWN_DOOR_PROBS&lt;/code> (§8.8)&lt;/td>
&lt;td>Which exits an agent knows, with a probability per exit&lt;/td>
&lt;td>&lt;code>familiarity&lt;/code> on a distribution: &lt;code>&amp;quot;full&amp;quot;&lt;/code>, &lt;code>&amp;quot;discovery&amp;quot;&lt;/code>, or one probability in [0, 1] applied to every exit. &lt;code>entrance&lt;/code> names one exit the agents always know. A probability per exit is not supported; see &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/136" target="_blank" rel="noopener">issue #136&lt;/a>
.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Door selection with smoke (&lt;code>FED_DOOR_CRIT&lt;/code>)&lt;/td>
&lt;td>Ranks doors as smoke-free by FED or visibility&lt;/td>
&lt;td>Route choice is a different model, configured in the JSON &lt;code>routing&lt;/code> block and switched on by default (&lt;code>--enable-rerouting&lt;/code>). See &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/routing/">Smoke-aware routing&lt;/a>
.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>TAU&lt;/code> (&lt;code>TAU_MEAN&lt;/code> etc., §8.7)&lt;/td>
&lt;td>Relaxation time of the social-force model&lt;/td>
&lt;td>No equivalent. Movement parameters belong to the JuPedSim model named in &lt;code>model_type&lt;/code>.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Pre-movement parameters&lt;span class="hx-absolute -hx-mt-20" id="pre-movement-parameters">&lt;/span>
&lt;a href="#pre-movement-parameters" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>The two parameters mean different things for each distribution, so FDS+Evac
values cannot be copied across unchanged. The delay is in seconds.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>&lt;code>premovement_distribution&lt;/code>&lt;/th>
&lt;th>&lt;code>premovement_param_a&lt;/code>&lt;/th>
&lt;th>&lt;code>premovement_param_b&lt;/code>&lt;/th>
&lt;th>Preset (&lt;em>a&lt;/em>, &lt;em>b&lt;/em>)&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>gamma&lt;/code> (default)&lt;/td>
&lt;td>shape&lt;/td>
&lt;td>scale [s]&lt;/td>
&lt;td>1.291, 103.901&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>lognormal&lt;/code>&lt;/td>
&lt;td>mean of ln(&lt;em>t&lt;/em>)&lt;/td>
&lt;td>standard deviation of ln(&lt;em>t&lt;/em>)&lt;/td>
&lt;td>4.586, 0.967&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>weibull&lt;/code>&lt;/td>
&lt;td>scale [s]&lt;/td>
&lt;td>shape&lt;/td>
&lt;td>139.285, 1.195&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>uniform&lt;/code>&lt;/td>
&lt;td>lower bound [s]&lt;/td>
&lt;td>upper bound [s]&lt;/td>
&lt;td>0.0, 60.0&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>The presets are illustrative, not from a cited dataset
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/144" target="_blank" rel="noopener">#144&lt;/a>
). Set both
parameters from data for your occupancy.&lt;/p>
&lt;p>The presets are used when &lt;code>use_premovement&lt;/code> is &lt;code>true&lt;/code> and &lt;code>premovement_param_a&lt;/code>
or &lt;code>premovement_param_b&lt;/code> is missing; both parameters must be given for either
to take effect. With &lt;code>use_premovement&lt;/code> false, which is the default, agents
start moving at &lt;em>t&lt;/em> = 0. When &lt;code>premovement_seed&lt;/code> is null, the draws are seeded
from the run seed, so they repeat under a fixed &lt;code>--seed&lt;/code>.&lt;/p>
&lt;p>While an agent waits, its walking speed is not reduced by the smoke around
it. It starts at its full clear-air speed when it is released. Its toxic dose
does accumulate while it waits.&lt;/p>
&lt;h2>What is lost&lt;span class="hx-absolute -hx-mt-20" id="what-is-lost">&lt;/span>
&lt;a href="#what-is-lost" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Some FDS+Evac features have no counterpart. The social-force movement model
with its three-circle body, and its dedicated counterflow algorithm, are
replaced by the JuPedSim model you choose. Floors, stairs, &lt;code>&amp;amp;EVHO&lt;/code> holes,
smoke-triggered detection, per-exit known-door probabilities and the FED
activity level are not supported. Each is described on the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/limitations/">Limitations&lt;/a>
page.&lt;/p>
&lt;h2>What needs Python&lt;span class="hx-absolute -hx-mt-20" id="what-needs-python">&lt;/span>
&lt;a href="#what-needs-python" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Some parameters cannot be set from the CLI or the scenario JSON. The
smoke-speed parameters are the main case. &lt;code>speed_law&lt;/code> (&lt;code>&amp;quot;lund&amp;quot;&lt;/code> or
&lt;code>&amp;quot;fridolf&amp;quot;&lt;/code>), &lt;code>alpha&lt;/code>, &lt;code>beta&lt;/code>, &lt;code>min_speed_factor&lt;/code> and &lt;code>visibility_factor_c&lt;/code>
are fields of &lt;code>SmokeSpeedConfig&lt;/code>, and &lt;code>run.py&lt;/code> and the web GUI build that
object with its defaults. A run started from either one uses the
Frantzich–Nilsson law with the defaults listed on the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/#parameters">smoke-speed model&lt;/a>
page.&lt;/p>
&lt;p>To change them, build a &lt;code>SmokeSpeedModel&lt;/code> yourself and pass it to
&lt;code>run_scenario()&lt;/code> from Python.&lt;/p>
&lt;p>The JSON &lt;code>routing&lt;/code> block accepts keys with the same names (&lt;code>alpha&lt;/code>, &lt;code>beta&lt;/code>,
&lt;code>min_speed_factor&lt;/code>). They set the speed factor used to estimate travel time
when a route is priced. Changing them changes what routes cost, not how fast
agents walk.&lt;/p>
&lt;h2>Before you convert&lt;span class="hx-absolute -hx-mt-20" id="before-you-convert">&lt;/span>
&lt;a href="#before-you-convert" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;input disabled="" type="checkbox"> Read &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/fds-case-requirements/">What your FDS case must provide&lt;/a>
and add
the slices it lists: extinction coefficient, CO, CO2, O2 and
&lt;code>TEMPERATURE&lt;/code>, with the &lt;code>&amp;amp;REAC&lt;/code> yields that produce the gases.&lt;/li>
&lt;li>&lt;input disabled="" type="checkbox"> Put the slices at the height you will pass to &lt;code>--smoke-slice-height&lt;/code>.&lt;/li>
&lt;li>&lt;input disabled="" type="checkbox"> Check that the building fits on one floor. Stairs and several floors
are not supported. &lt;code>generate_walkable_from_fds.py&lt;/code> treats stair treads
and risers as floor, so a staircase in the deck becomes flat floor
walked at full speed.&lt;/li>
&lt;li>&lt;input disabled="" type="checkbox"> Check the walkable polygon it produces (&lt;code>--plot&lt;/code>, &lt;code>--report&lt;/code>). The
script decides what blocks from the CAD layer name in the comment after
each &lt;code>&amp;amp;OBST&lt;/code>, so a deck without such comments needs checking by hand.&lt;/li>
&lt;li>&lt;input disabled="" type="checkbox"> Translate pre-movement into one delay per agent (table above). Detection
and reaction are no longer separate.&lt;/li>
&lt;li>&lt;input disabled="" type="checkbox"> Decide what each group knows (&lt;code>familiarity&lt;/code>, &lt;code>entrance&lt;/code>).&lt;/li>
&lt;li>&lt;input disabled="" type="checkbox"> Run FDS, then &lt;code>uv run python run.py --scenario &amp;lt;json&amp;gt; --fds-dir &amp;lt;dir&amp;gt; --inspect-fds&lt;/code>
to list the quantities pyFDS-Evac finds.&lt;/li>
&lt;li>&lt;input disabled="" type="checkbox"> Read the warnings of the first run. A missing gas slice switches FED off
and the run still finishes, reporting FED = 0.&lt;/li>
&lt;li>&lt;input disabled="" type="checkbox"> Read &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/limitations/">Limitations&lt;/a>
and the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/verification/" target="_blank" rel="noopener">verification status&lt;/a>
.&lt;/li>
&lt;/ul>
&lt;h2>Words that changed meaning&lt;span class="hx-absolute -hx-mt-20" id="words-that-changed-meaning">&lt;/span>
&lt;a href="#words-that-changed-meaning" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;blockquote>
&lt;p>&lt;strong>Terminology.&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>&lt;strong>&lt;code>tau&lt;/code>&lt;/strong> is an &lt;em>optical depth&lt;/em> here: the mean extinction coefficient
along a route times its length, &lt;em>τ&lt;/em> = &lt;em>K&lt;/em>ave · &lt;em>L&lt;/em> (dimensionless).
It is not the relaxation time &lt;code>TAU&lt;/code> of FDS+Evac. The routing key
&lt;code>tau_max&lt;/code> is an optical depth (default on the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/routing/#parameters">routing model&lt;/a>
page).&lt;/li>
&lt;li>&lt;strong>Stage&lt;/strong> is a JuPedSim target: a distribution, a checkpoint or an exit.
Journeys are sequences of stages.&lt;/li>
&lt;li>&lt;strong>Gate&lt;/strong> has two meanings, and only two. The route-cost gate
(&lt;code>routing.cost_model = &amp;quot;gate&amp;quot;&lt;/code>, the default) refuses routes whose
optical depth is too high. Sight gating decides whether an agent can
read a sign, and so which exits enter its cognitive map. The dose is not
a gate: when it reaches the agent&amp;rsquo;s threshold, the agent stops.&lt;/li>
&lt;li>&lt;strong>FIC&lt;/strong>, Purser&amp;rsquo;s fractional irritant concentration, which slows agents,
is related to, but not the same as, the FEC (fractional effective
concentration) of ISO 13571, which uses different denominators.&lt;/li>
&lt;li>&lt;strong>Cognitive map&lt;/strong> is the graph of stages an agent knows. It is not meant
in the psychological sense.&lt;/li>
&lt;/ul>&lt;/blockquote>
&lt;h2>References&lt;span class="hx-absolute -hx-mt-20" id="references">&lt;/span>
&lt;a href="#references" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Korhonen, T. (2021). &lt;em>Fire Dynamics Simulator with Evacuation: FDS+Evac.
Technical Reference and User&amp;rsquo;s Guide&lt;/em> (FDS 6.7.6, Evac 2.6.0). VTT Technical
Research Centre of Finland.&lt;/p></description></item><item><title>Dynamic route rerouting</title><link>https://pedestriandynamics.org/pyFDS-Evac/models/routing/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/models/routing/</guid><description>
&lt;p>Based on: &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/extinction/">Extinction coefficient&lt;/a>
, &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/">Visibility through smoke&lt;/a>
and &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/exit-choice/">Exit choice and familiarity&lt;/a>
.&lt;/p>
&lt;p>See &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/routing/">docs/routing.md&lt;/a>
for the full routing model,
cost formulas, and API reference, and
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/docs/routing-and-signs-notes.md" target="_blank" rel="noopener">docs/routing-and-signs-notes.md&lt;/a>
for
working notes on exit choice and where the exit-choice research papers
disagree with each other.&lt;/p>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
.&lt;/p>
&lt;p>Background: the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/">Concepts&lt;/a>
page and the talk &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/visibility-seminar-2026/" target="_blank" rel="noopener">&lt;em>A Modular Workflow for Visibility-Aware Evacuation Modelling&lt;/em>&lt;/a>
(&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/pyFDS-Evac_visibility_seminar_2026.pdf" target="_blank" rel="noopener">PDF&lt;/a>
).&lt;/p>
&lt;h2>How smoke enters route choice&lt;span class="hx-absolute -hx-mt-20" id="how-smoke-enters-route-choice">&lt;/span>
&lt;a href="#how-smoke-enters-route-choice" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Two models, selected per deck with &lt;code>routing.cost_model&lt;/code>.&lt;/p>
&lt;p>&lt;strong>&lt;code>&amp;quot;gate&amp;quot;&lt;/code> (default).&lt;/strong> One quantity does the whole of the smoke reasoning: the
route&amp;rsquo;s &lt;strong>optical depth&lt;/strong>&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>tau = K_ave * L&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>the soot column the agent walks through, with &lt;code>K_ave&lt;/code> the mean extinction along
the route polyline and &lt;code>L&lt;/code> the distance still to walk. On this page &lt;code>tau&lt;/code> is
always this dimensionless optical depth, not the relaxation time τ of
FDS+Evac&amp;rsquo;s movement model. A route is refused when
&lt;code>tau&lt;/code> exceeds &lt;code>tau_max&lt;/code> (see &lt;a href="#parameters">Parameters&lt;/a>
), Dijkstra weights every edge by its own
&lt;code>tau&lt;/code>, and &lt;code>tau&lt;/code> orders the routes that survive, with travel time breaking ties.
Path choice and exit choice are therefore one objective. In clear air every
&lt;code>tau&lt;/code> is zero, nothing is refused, and the model reduces to nearest-exit.&lt;/p>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/concepts/exposure_gate.png" alt="Top: plan view with three routes from one agent to exits A, B and C around a smoke plume. Bottom: bar chart of optical depth per route against the budgets 4.8 and 6" loading="lazy" />&lt;/p>
&lt;p>&lt;em>Schematic with a prescribed toy plume, not a simulation. Top: three candidate
routes, sampled along each walk, the samples shaded by extinction K [1/m]. Bottom:
optical depth &lt;code>tau&lt;/code> [-] per route against &lt;code>tau_max&lt;/code> = 6 (current exit and
initial choice) and 0.8 &lt;code>tau_max&lt;/code> = 4.8 (any other exit). Route C is refused;
route B ranks first although it is the longest walk.
Script: &lt;code>scripts/figures/exposure_gate.py&lt;/code>.&lt;/em>&lt;/p>
&lt;p>&lt;code>tau&lt;/code> is an &lt;strong>exposure&lt;/strong> statement, not a sighting distance; where its budget
comes from is under &lt;a href="#deviations-from-the-literature">Deviations from the literature&lt;/a>
.&lt;/p>
&lt;p>Refusals are &lt;strong>not remembered&lt;/strong>. The criterion is relative to the distance still
to walk, so it relaxes on approach: smoke that refuses a door at 40 m accepts it
at 2 m. When every route is refused the agent still has to move, so it takes the
one with least smoke to walk through and holds it unless a rival&amp;rsquo;s worst stretch
is clearly milder (&lt;code>fallback_switch_margin&lt;/code>). Churn is held down by the
exit-switch anchor, by a stricter budget for a rival exit
(&lt;code>tau_return_margin&lt;/code>), and by a discount on the current exit&amp;rsquo;s &lt;code>tau&lt;/code> in the
sort (&lt;code>current_exit_discount&lt;/code>, FDS+Evac&amp;rsquo;s &lt;code>FAC_DOOR_OLD2&lt;/code>).&lt;/p>
&lt;p>&lt;strong>Measured, with the limitation stated.&lt;/strong> Ranking on &lt;code>tau&lt;/code> sends 39 of
&lt;code>world100&lt;/code>&amp;rsquo;s agents to the far clean exit, with 9 switches and no agent
returning to an exit it abandoned &amp;ndash; the outcome the model exists to produce.
On &lt;code>l_corridor&lt;/code> agents oscillate: 34 returns to abandoned exits across 14
agents and 55 switches, with a far-exit share of about 18. The cause is &lt;strong>not&lt;/strong>
a currency mismatch between the &lt;code>tau&lt;/code> ordering and the anchor&amp;rsquo;s time
fallthrough: 29 of the 34 returns go to a route cleaner by more than the
deadband, and 31 fall in &lt;code>t = 40-60 s&lt;/code> where the two routes&amp;rsquo; &lt;code>tau&lt;/code> genuinely
cross over. The model is following a field that reverses, and no constant damps
that &amp;ndash; what is missing is commitment
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/124" target="_blank" rel="noopener">#124&lt;/a>
).
See &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/#known-limitations">docs/route-cost-gate.md&lt;/a>
.&lt;/p>
&lt;p>&lt;strong>This is a departure from FDS+Evac, not a reproduction of it.&lt;/strong> The threshold
&lt;code>tau &amp;gt; 6&lt;/code> is borrowed with a citation; the &lt;em>place&lt;/em> it is used is not. In the
reference&amp;rsquo;s first three tiers the rank is a time or distance norm and smoke is
only a boolean admission test (&lt;code>evac.f90:16265, :16354, :16401&lt;/code>), so smoke can
move a door between tiers but cannot reorder candidates. It ranks on smoke only
in the tier-4 last resort, over known-or-visible doors, on a bee line, with
permanent strike-out. Here &lt;code>tau&lt;/code> is the ordering everywhere, with no memory.
On &lt;code>l_corridor&lt;/code> that diverts 18 of 100 agents where the reference criterion
would send essentially everyone to the near exit &amp;ndash; a prediction reasoned from
&lt;code>evac.f90&lt;/code>, not a measured run of it. A &lt;code>L/d&lt;/code> geometric bias (1.41 near against
1.27 far on that deck) tilts the criterion about 11 % further in the same
direction. Both are written up in
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/#the-diversion-is-a-departure-from-fdsevac-not-a-reproduction-of-it">docs/route-cost-gate.md&lt;/a>
.&lt;/p>
&lt;p>An optional &lt;strong>clean-exit tier&lt;/strong> (&lt;code>clean_extinction_threshold&lt;/code>, &lt;strong>off by
default&lt;/strong>) prefers exits below an absolute smoke criterion outright, however
far. It is FDS+Evac&amp;rsquo;s primary door rule, and measured on both reference decks it
did not redirect anyone while costing monotonicity &amp;ndash; see
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/docs/gate-model-review-notes.md" target="_blank" rel="noopener">docs/gate-model-review-notes.md&lt;/a>
.&lt;/p>
&lt;p>Each segment is priced at the time the agent would &lt;em>arrive&lt;/em> there (&lt;code>anticipate&lt;/code>,
&lt;code>foresight_horizon_s&lt;/code>), using unimpeded speed.&lt;/p>
&lt;p>&lt;strong>&lt;code>&amp;quot;additive&amp;quot;&lt;/code>.&lt;/strong> The original model: smoke is a toll per metre walked,
&lt;code>effective_length * (1 + w_smoke * k_ave) + w_fed * fed_max&lt;/code>. Both terms scale
with route length, so a long clean detour pays for its length twice and can
never win &amp;ndash; which is why the gate exists. Pin it with
&lt;code>{&amp;quot;cost_model&amp;quot;: &amp;quot;additive&amp;quot;, &amp;quot;anticipate&amp;quot;: false}&lt;/code>; &lt;code>anticipate&lt;/code> is independent
of the model, so the pin needs both.&lt;/p>
&lt;p>&lt;strong>FIC does not route&lt;/strong> under either model. It drives the Purser slowdown and
incapacitation only. FIC and the optical-depth gate are driven by the same
smoke, so routing on both would double-count.&lt;/p>
&lt;p>&lt;strong>What this model does not do.&lt;/strong> It is not hazard avoidance. On the fires
measured here the dose veto never comes close to firing &amp;ndash; the largest projected
FED over a whole &lt;code>l_corridor&lt;/code> run is 0.0016 against a threshold of 1.0 &amp;ndash; and
&lt;code>impassable_extinction_threshold&lt;/code> cannot fire under the gate at all, because it
is reached only from a rejection reason containing &lt;code>&amp;quot;visible&amp;quot;&lt;/code> and the gate&amp;rsquo;s
only reason string starts &lt;code>tau&lt;/code>. So no smoke rejection bypasses the exit-switch
anchor at any density. What the gate does is exposure-gated wayfinding.&lt;/p>
&lt;p>The model reference is &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/">docs/route-cost-gate.md&lt;/a>
;
provenance and the open questions are in
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/docs/gate-model-review-notes.md" target="_blank" rel="noopener">docs/gate-model-review-notes.md&lt;/a>
.
&lt;code>assets/l_corridor&lt;/code> is the deck the model is judged on &amp;ndash; a near exit behind the
fire and a clean 58 m way round &amp;ndash; and its results are in the sciebo case folder.&lt;/p>
&lt;h2>Components&lt;span class="hx-absolute -hx-mt-20" id="components">&lt;/span>
&lt;a href="#components" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;strong>StageGraph&lt;/strong>: Dijkstra-based shortest-path routing on a graph of
stages (distributions, checkpoints, exits)&lt;/li>
&lt;li>&lt;strong>Route cost evaluation&lt;/strong>: Samples extinction (K) along candidate paths
to compute smoke exposure. When a &lt;code>fed_model&lt;/code> is provided, projected FED
vetoes routes under both models and enters the ranking only under
&lt;code>&amp;quot;additive&amp;quot;&lt;/code>&lt;/li>
&lt;li>&lt;strong>Dynamic rerouting&lt;/strong>: Agents recompute routes at configurable intervals,
selecting lower-exposure paths when available&lt;/li>
&lt;li>&lt;strong>Cognitive-map history&lt;/strong>: &lt;code>run_scenario(collect_cognitive_map_history=True)&lt;/code>
records what each agent knows, every time it changes — see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/testing-familiarity/">docs/testing-familiarity.md&lt;/a>
and
&lt;code>scripts/animate_cognitive_map.py&lt;/code>&lt;/li>
&lt;li>&lt;strong>Discovery-world generator&lt;/strong>: &lt;code>scripts/generate_discovery_world.py&lt;/code> produces
random test decks (open room, convex obstacles, signed checkpoints, exits
optionally hidden from the spawn) for exercising discovery routing; the same
decks drive the invariant tests in &lt;code>tests/test_generated_worlds.py&lt;/code>&lt;/li>
&lt;li>&lt;strong>Congestion-aware routing&lt;/strong>: Optional exit-congestion term (&lt;code>w_queue&lt;/code>), &lt;strong>off
by default&lt;/strong> — it scales with a global agent count, so no constant suits every
scenario. &lt;code>assets/station_fahy&lt;/code> opts in at 0.024, calibrated against Fahy
Table 2 — see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/routing/#why-it-is-opt-in-and-what-0024-means">docs/routing.md&lt;/a>
and &lt;code>scripts/sweep_queue_weight.py&lt;/code>&lt;/li>
&lt;li>&lt;strong>Throughput throttling&lt;/strong>: Optional exit flux limiting via
&lt;code>enable_throughput_throttling&lt;/code> and &lt;code>max_throughput&lt;/code> in scenario config&lt;/li>
&lt;/ul>
&lt;h2>Parameters&lt;span class="hx-absolute -hx-mt-20" id="parameters">&lt;/span>
&lt;a href="#parameters" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Defaults in the code, as a scenario&amp;rsquo;s &lt;code>routing&lt;/code> block reads them
(&lt;code>RouteCostConfig.from_routing_params&lt;/code>, &lt;code>pyfds_evac/core/route_graph.py&lt;/code>). The
full key table, with which keys act under which cost model, is in
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/#configuration">docs/route-cost-gate.md&lt;/a>
.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>&lt;code>routing&lt;/code> key&lt;/th>
&lt;th>Default&lt;/th>
&lt;th>Meaning&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>cost_model&lt;/code>&lt;/td>
&lt;td>&lt;code>&amp;quot;gate&amp;quot;&lt;/code>&lt;/td>
&lt;td>&lt;code>&amp;quot;gate&amp;quot;&lt;/code> or &lt;code>&amp;quot;additive&amp;quot;&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>tau_max&lt;/code>&lt;/td>
&lt;td>&lt;code>6.0&lt;/code>&lt;/td>
&lt;td>Budget \(\tau_{\max}\) on the optical depth of a route&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>tau_return_margin&lt;/code>&lt;/td>
&lt;td>&lt;code>0.8&lt;/code>&lt;/td>
&lt;td>A rival exit must come in under &lt;code>tau_max&lt;/code> times this&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>current_exit_discount&lt;/code>&lt;/td>
&lt;td>&lt;code>0.9&lt;/code>&lt;/td>
&lt;td>Factor on the current exit&amp;rsquo;s &lt;code>tau&lt;/code> in the sort&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>tau_deadband&lt;/code>&lt;/td>
&lt;td>&lt;code>0.1&lt;/code>&lt;/td>
&lt;td>Anchor deadband, as a fraction of &lt;code>tau_max&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>fed_rejection_threshold&lt;/code>&lt;/td>
&lt;td>&lt;code>1.0&lt;/code>&lt;/td>
&lt;td>Projected FED above which a route is refused&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>anticipate&lt;/code>&lt;/td>
&lt;td>&lt;code>true&lt;/code>&lt;/td>
&lt;td>Price each segment at the agent&amp;rsquo;s arrival time&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>sampling_step_m&lt;/code>&lt;/td>
&lt;td>&lt;code>2.0&lt;/code>&lt;/td>
&lt;td>\(\Delta s\), spacing of smoke samples along a polyline [m]&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>base_speed_m_per_s&lt;/code>&lt;/td>
&lt;td>&lt;code>1.3&lt;/code>&lt;/td>
&lt;td>Router&amp;rsquo;s clear-air speed [m/s], not the agent&amp;rsquo;s \(v_0\)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>alpha&lt;/code>&lt;/td>
&lt;td>&lt;code>0.706&lt;/code>&lt;/td>
&lt;td>Router&amp;rsquo;s copy of \(\alpha\) [m/s], for travel time only&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>beta&lt;/code>&lt;/td>
&lt;td>&lt;code>-0.057&lt;/code>&lt;/td>
&lt;td>Router&amp;rsquo;s copy of \(\beta\) [m²/s], for travel time only&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>min_speed_factor&lt;/code>&lt;/td>
&lt;td>&lt;code>0.1&lt;/code>&lt;/td>
&lt;td>Router&amp;rsquo;s copy of \(f_{\min}\), for travel time only&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>The router always uses the linear speed law, whatever &lt;code>SmokeSpeedConfig&lt;/code>
the agents walk with. Each agent re-decides every
&lt;code>RerouteConfig.reevaluation_interval_s&lt;/code>:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Where the run starts&lt;/th>
&lt;th>Re-decision interval&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>RerouteConfig()&lt;/code> built in Python&lt;/td>
&lt;td>&lt;code>10.0&lt;/code> s&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>run.py --reroute-interval&lt;/code>&lt;/td>
&lt;td>&lt;code>1.0&lt;/code> s&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h2>Usage&lt;span class="hx-absolute -hx-mt-20" id="usage">&lt;/span>
&lt;a href="#usage" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>See &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/usage/">docs/usage.md&lt;/a>
for the full rerouting CLI
(&lt;code>--enable-rerouting&lt;/code>, &lt;code>--reroute-interval&lt;/code>, &lt;code>--output-route-history&lt;/code>,
&lt;code>--output-route-cost-history&lt;/code>, &lt;code>--vis-cache&lt;/code>) and the plotting scripts
that consume the generated route-cost CSVs.&lt;/p>
&lt;h2>Deviations from the literature&lt;span class="hx-absolute -hx-mt-20" id="deviations-from-the-literature">&lt;/span>
&lt;a href="#deviations-from-the-literature" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The published laws are on &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/">Visibility through smoke&lt;/a>
and &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/exit-choice/">Exit choice and familiarity&lt;/a>
.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Where the budget comes from.&lt;/strong> FDS+Evac&amp;rsquo;s tier-4 door rule requires the
visibility of a reflective sign at the door, \(3/\bar K\), to be at least half
the distance &lt;em>d&lt;/em> to the door, which rearranges to \(\bar K d \le 6\)
(&lt;code>evac.f90:16458&lt;/code>, &lt;code>:16463&lt;/code>). That is the default &lt;code>tau_max&lt;/code>
(&lt;code>route_graph.py:683&lt;/code>). Jin&amp;rsquo;s law describes a straight line of sight to a
sign in uniform smoke; here the same number bounds the integral of &lt;em>K&lt;/em> along
a walked polyline, which measures exposure, not sight. The two agree only
on a straight corridor, and the budget has not been calibrated against a
smoke-exposure or FED limit. The line-level comparison with &lt;code>evac.f90&lt;/code> is in
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/#where-the-6-comes-from">docs/route-cost-gate.md&lt;/a>
.&lt;/li>
&lt;li>&lt;strong>Exit choice.&lt;/strong> Routes are ranked on optical depth and travel time.
Familiarity and social influence, which the exit-choice literature finds
significant, enter only through the cognitive map, not through the route
cost, and herding is not modelled.&lt;/li>
&lt;/ul></description></item><item><title>Visibility through smoke</title><link>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/</guid><description>
&lt;p>Visibility &lt;em>S&lt;/em> [m] is the greatest distance at which an object, typically an
exit sign, can still be seen through smoke. Its standard engineering form is
due to Tadahisa Jin, who measured the distance at which signs vanished in
smoke-filled chambers.&lt;/p>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
. The
equations below keep the SFPE Handbook&amp;rsquo;s own notation: &lt;em>V&lt;/em> for the
visibility &lt;em>S&lt;/em>, \(C_s\) for the extinction coefficient &lt;em>K&lt;/em> and &lt;em>k&lt;/em> for the
constant &lt;em>C&lt;/em>.&lt;/p>
&lt;h2>The equation as published&lt;span class="hx-absolute -hx-mt-20" id="the-equation-as-published">&lt;/span>
&lt;a href="#the-equation-as-published" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Jin found that, in the range of visibilities from about 5 to 15 m, the
product of the visibility at the obscuration threshold and the smoke density
was almost constant (Jin 1978, as reported by Yamada and Akizuki 2016,
Society of Fire Protection Engineers (SFPE) Handbook Ch. 61, Eq. 61.4 and
Fig. 61.8). Ch. 61
writes it with \(C_s\) [1/m] for the extinction coefficient:&lt;/p>
$$
V = k\,\frac{1}{C_s} \qquad \text{(Eq. 61.4)}
$$&lt;p>with the constant in the range&lt;/p>
$$
V = \frac{5}{C_s} \text{ to } \frac{10}{C_s} \;\text{for a light-emitting sign},
\qquad
V = \frac{2}{C_s} \text{ to } \frac{4}{C_s} \;\text{for a reflecting sign}
\qquad \text{(Eqs. 61.5, 61.6)}
$$&lt;p>The Fire Dynamics Simulator (FDS) User Guide (§22.10.5, Eq. 22.23) writes the same law as
\(S = C/K\), with &lt;em>C&lt;/em> = 8 for a light-emitting sign and &lt;em>C&lt;/em> = 3 for a
light-reflecting sign, citing Mulholland (2002) in the 3rd edition of the
SFPE Handbook, and uses &lt;em>C&lt;/em> = 3 by default (&lt;code>VISIBILITY_FACTOR&lt;/code>). FDS
reports visibility up to a maximum of 30 m by default
(&lt;code>MAXIMUM_VISIBILITY&lt;/code>, §22.10.5).&lt;/p>
&lt;h2>What &lt;em>C&lt;/em> means&lt;span class="hx-absolute -hx-mt-20" id="what-c-means">&lt;/span>
&lt;a href="#what-c-means" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;em>C&lt;/em> is not a tuning constant. Jin derived the law from a contrast model
(Ch. 61, Eq. 61.3) in which the visibility of a sign depends on the sign&amp;rsquo;s
brightness, the contrast threshold of the observer, the illuminance and the
scattering properties of the smoke. For reflecting signs the constant depends
mainly on the sign&amp;rsquo;s reflectance and the illuminating light. &lt;em>C&lt;/em> is therefore
a property of the sign and its lighting, and should be chosen from the type
of sign, not adjusted to fit an outcome.&lt;/p>
&lt;h2>The data&lt;span class="hx-absolute -hx-mt-20" id="the-data">&lt;/span>
&lt;a href="#the-data" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Jin viewed signs in a smoke-filled chamber from outside through a window
(Ch. 61, Fig. 61.8). Later experiments in non-irritant white smoke without
background light found the constant tends to be larger than Jin&amp;rsquo;s values;
ordinary light-emitting exit signs were lost at about 10 m in smoke of
\(C_s\) = 1.0 1/m (Ch. 61, Fig. 61.9 and Table 61.1). Visibility in black (flaming) smoke was somewhat better than
in white smoke of the same density. For reading the words on a sign, the
constant product holds only in non-irritant smoke. In irritant smoke,
legibility falls sharply above a certain density, and above about
0.5 1/m subjects could keep their eyes open only briefly (Ch. 61,
Fig. 61.11, from the corridor experiments of Jin 1978 and Jin and Yamada
1985).&lt;/p>
&lt;h2>Known limits&lt;span class="hx-absolute -hx-mt-20" id="known-limits">&lt;/span>
&lt;a href="#known-limits" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The law describes a straight, unobstructed line of sight through uniform
smoke to one object. It does not describe how far a person can see around
corners, or how much smoke a person walks through. The constant was measured
over a limited visibility range (about 5–15 m for Eq. 61.4), and the range of
&lt;em>C&lt;/em> for a given sign type is a factor of two wide. The equation gives a
threshold for seeing a sign, not for recognising or understanding it.&lt;/p>
&lt;h2>Sources&lt;span class="hx-absolute -hx-mt-20" id="sources">&lt;/span>
&lt;a href="#sources" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>Jin, T. (1978). &lt;em>Visibility through fire smoke&lt;/em>. Journal of Fire &amp;amp;
Flammability, 9, 135–157. No DOI or public URL; read through Ch. 61
(its ref. 6).
&lt;a href="https://doi.org/10.1016/j.tust.2019.04.016" target="_blank" rel="noopener">Fridolf et al. (2019)&lt;/a>
give
9(2), 135–155, and the
&lt;a href="https://github.com/tkorhon1/FDS-Evac-Guide" target="_blank" rel="noopener">FDS+Evac guide&lt;/a>
gives
9, 135–155.&lt;/li>
&lt;li>Jin, T., &amp;amp; Yamada, T. (1985). &lt;em>Irritating effects of fire smoke on
visibility&lt;/em>. Fire Science and Technology, 5(1), 79–90.
&lt;a href="https://doi.org/10.3210/fst.5.79" target="_blank" rel="noopener">doi:10.3210/fst.5.79&lt;/a>
&lt;/li>
&lt;li>Yamada, T., &amp;amp; Akizuki, Y. (2016). &lt;em>Visibility and human behavior in fire
smoke&lt;/em>. SFPE Handbook of Fire Protection Engineering, 5th ed., Ch. 61,
2181–2206.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_61" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_61&lt;/a>
&lt;/li>
&lt;li>McGrattan, K., Hostikka, S., Floyd, J., McDermott, R., Vanella, M.,
Mueller, E., &amp;amp; Paul, C. (2025). &lt;em>Fire Dynamics Simulator User&amp;rsquo;s
Guide&lt;/em>. National Institute of Standards and Technology (NIST) Special
Publication 1019, 6th ed., revision FDS-6.10.1-0-g12efa16, §22.10.5.
&lt;a href="https://github.com/firemodels/fds/releases/tag/FDS-6.10.1" target="_blank" rel="noopener">github.com/firemodels/fds/releases/tag/FDS-6.10.1&lt;/a>
&lt;/li>
&lt;li>Mulholland, G. W. (2002). &lt;em>Smoke production and properties&lt;/em>. SFPE
Handbook of Fire Protection Engineering, 3rd ed. National Fire
Protection Association, Quincy, MA. No DOI or public URL; cited through
the FDS User Guide (its ref. 83).&lt;/li>
&lt;/ul>
&lt;p>How pyFDS-Evac uses this: see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/visibility/">visibility and cognitive maps&lt;/a>
.&lt;/p></description></item><item><title>Visibility-aware routing and cognitive maps</title><link>https://pedestriandynamics.org/pyFDS-Evac/models/visibility/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/models/visibility/</guid><description>
&lt;p>Based on: &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/">Visibility through smoke&lt;/a>
and &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/exit-choice/">Exit choice and familiarity&lt;/a>
.&lt;/p>
&lt;p>Implements &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/specs/008-visibility-aware-routing/SPEC.md" target="_blank" rel="noopener">Spec 008&lt;/a>
: sign
visibility gates what each agent comes to &lt;em>know&lt;/em>, and per-agent cognitive maps
carry that knowledge into routing. Sign visibility does not reject routes.&lt;/p>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
.&lt;/p>
&lt;p>Background: the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/">Concepts&lt;/a>
page and the talk &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/visibility-seminar-2026/" target="_blank" rel="noopener">&lt;em>A Modular Workflow for Visibility-Aware Evacuation Modelling&lt;/em>&lt;/a>
(&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/talks/pyFDS-Evac_visibility_seminar_2026.pdf" target="_blank" rel="noopener">PDF&lt;/a>
).&lt;/p>
&lt;h2>Sign visibility&lt;span class="hx-absolute -hx-mt-20" id="sign-visibility">&lt;/span>
&lt;a href="#sign-visibility" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Each exit and checkpoint can carry a &lt;code>&amp;quot;sign&amp;quot;&lt;/code> descriptor in the scenario
config:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-json" data-lang="json">&lt;span style="display:flex;">&lt;span>{
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;exits&amp;#34;&lt;/span>: {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;exit_A&amp;#34;&lt;/span>: {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;sign&amp;#34;&lt;/span>: {&lt;span style="color:#f92672">&amp;#34;x&amp;#34;&lt;/span>: &lt;span style="color:#ae81ff">0.5&lt;/span>, &lt;span style="color:#f92672">&amp;#34;y&amp;#34;&lt;/span>: &lt;span style="color:#ae81ff">11.5&lt;/span>, &lt;span style="color:#f92672">&amp;#34;alpha&amp;#34;&lt;/span>: &lt;span style="color:#ae81ff">90&lt;/span>, &lt;span style="color:#f92672">&amp;#34;c&amp;#34;&lt;/span>: &lt;span style="color:#ae81ff">3&lt;/span>}
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>}&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;code>alpha&lt;/code> is a compass bearing (degrees from north, clockwise): 90 = sign
visible from the east, 270 = from the west, 180 = from the south.&lt;/p>
&lt;p>Every exit, checkpoint and waypoint gets a sign: nodes without an authored
&lt;code>&amp;quot;sign&amp;quot;&lt;/code> get one synthesised at the node&amp;rsquo;s polygon centroid (&lt;code>c=3&lt;/code>,
&lt;code>alpha=None&lt;/code>, i.e. omni-directional). A node is never exempt from
visibility gating for lack of an authored sign.&lt;/p>
&lt;p>At each reevaluation tick the &lt;code>VisibilityModel&lt;/code> checks whether an agent can see
a node&amp;rsquo;s sign, using a cached
&lt;a href="https://github.com/FireDynamics/fdsvismap" target="_blank" rel="noopener">fdsvismap&lt;/a>
grid.&lt;/p>
&lt;p>&lt;strong>Sign legibility decides what an agent &lt;em>knows&lt;/em>, not whether a route is
allowed.&lt;/strong> A sign it can see admits that node to the agent&amp;rsquo;s cognitive map
(&lt;code>cognitive_map.expand_from_visibility&lt;/code>), and the map is what Dijkstra may route
over &amp;ndash; so an unknown exit is &lt;em>absent from the graph&lt;/em> rather than
present-and-vetoed. Route choice does not consult the visibility model at all,
so an agent can use an exit it has learned after the sign goes out of view.&lt;/p>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/concepts/sign_bearing.png" alt="Two 30 m corridors, each with an exit at both ends; left, the near sign faces the agents and all 40 go to the near exit; right, the near sign faces away and all 40 walk to the far exit" loading="lazy" />&lt;/p>
&lt;p>&lt;em>&lt;code>assets/exit_visibility_alpha&lt;/code>: 4 m × 30 m corridor in clear air, 40 agents with
familiarity 0. The two configurations differ only in the bearing of the near
exit&amp;rsquo;s sign. Facing the agents (0°), all 40 took the near exit; facing away
(180°), all 40 walked to the far exit. One run of each configuration, as
recorded in the asset README. Script: &lt;code>scripts/figures/sign_bearing.py&lt;/code>.&lt;/em>&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># Build or reuse the vismap cache; smoke hides signs from the cognitive map&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>uv run run.py &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --scenario assets/t_junction &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --fds-dir assets/t_junction &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --enable-rerouting &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --vis-cache assets/t_junction/vismap_cache.npz &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-route-cost-history route_costs.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --cleanup&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Under the default &lt;code>&amp;quot;gate&amp;quot;&lt;/code> route-cost model the only rejection reasons a route-cost CSV carries are &lt;code>tau ...&lt;/code>
(optical depth over budget), &lt;code>FED_max ...&lt;/code> (dose over threshold), and either of
those under a &lt;code>fallback:&lt;/code> prefix.&lt;/p>
&lt;h3>Which visibility setting am I running?&lt;span class="hx-absolute -hx-mt-20" id="which-visibility-setting-am-i-running">&lt;/span>
&lt;a href="#which-visibility-setting-am-i-running" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Sight gating decides how an agent comes to know a node, and the flags choose
between three different scenarios. Nothing here changes the physics; it changes
what the agent is allowed to perceive.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>invocation&lt;/th>
&lt;th>model&lt;/th>
&lt;th>the scenario it means&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;em>(no flags)&lt;/em>, deck has discovery agents&lt;/td>
&lt;td>clear air&lt;/td>
&lt;td>&lt;strong>no fire.&lt;/strong> Agents learn a node by seeing its sign: walls occlude, sign facing and contrast apply&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;em>(no flags)&lt;/em>, every agent &lt;code>familiarity = 1.0&lt;/code>&lt;/td>
&lt;td>none built&lt;/td>
&lt;td>agents hold the whole graph from t=0 and never consult it, so building one would cost time and change nothing&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--clear-air-visibility&lt;/code>&lt;/td>
&lt;td>clear air, forced&lt;/td>
&lt;td>as above, on a deck whose agents are fully familiar &amp;ndash; only useful for comparison runs&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--no-visibility&lt;/code>&lt;/td>
&lt;td>none&lt;/td>
&lt;td>&lt;strong>not a fire scenario.&lt;/strong> Sight gating is absent, so an agent learns every neighbour of each node it reaches, by contact rather than by sight&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--fds-dir DIR&lt;/code>&lt;/td>
&lt;td>none for sight&lt;/td>
&lt;td>smoke drives speed reduction and FED, but what an agent can &lt;em>see&lt;/em> is ungated&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--fds-dir DIR --vis-cache PATH&lt;/code>&lt;/td>
&lt;td>smoke&lt;/td>
&lt;td>&lt;strong>the coupled run.&lt;/strong> Sight gated by that fire&amp;rsquo;s extinction field: smoke hides signs, so the map stops growing&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--vis-cache PATH&lt;/code> (no &lt;code>--fds-dir&lt;/code>)&lt;/td>
&lt;td>clear air, cached&lt;/td>
&lt;td>same as the default, with the grid reused between runs&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>Rejected combinations:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>combination&lt;/th>
&lt;th>why&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>--clear-air-visibility --fds-dir&lt;/code>&lt;/td>
&lt;td>claims clear sight while a fire burns &amp;ndash; the one combination that silently produces a wrong answer&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--clear-air-visibility --no-visibility&lt;/code>&lt;/td>
&lt;td>contradictory&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--vis-cache&lt;/code> or &lt;code>--clear-air-visibility&lt;/code> with &lt;code>--no-enable-rerouting&lt;/code>&lt;/td>
&lt;td>a sight gate with nothing to act on&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;code>--vis-cell-size&lt;/code> sets the grid the scene is rasterised at (default 0.25 m). A
wall thinner than one cell stops occluding, so keep it below the thinnest wall
that must block sight.&lt;/p>
&lt;p>The three settings are genuinely different agents. Measured on
&lt;code>assets/world_100&lt;/code>, one agent, &lt;code>familiarity = 0&lt;/code>, same seed:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>setting&lt;/th>
&lt;th>evacuation&lt;/th>
&lt;th>route switches&lt;/th>
&lt;th>cognitive map&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>--no-visibility&lt;/code>&lt;/td>
&lt;td>58.5 s&lt;/td>
&lt;td>0&lt;/td>
&lt;td>learns each node&amp;rsquo;s neighbours on arrival&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>clear air (default)&lt;/td>
&lt;td>188.8 s&lt;/td>
&lt;td>31, mostly &lt;code>explore&lt;/code>&lt;/td>
&lt;td>grows 3 -&amp;gt; 28 nodes&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--fds-dir&lt;/code> + &lt;code>--vis-cache&lt;/code>&lt;/td>
&lt;td>did not evacuate (400 s cap)&lt;/td>
&lt;td>30, nearly all &lt;code>wander&lt;/code>&lt;/td>
&lt;td>stays at 3 nodes: no sign is legible, so no frontier appears&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Diagnostic scripts&lt;span class="hx-absolute -hx-mt-20" id="diagnostic-scripts">&lt;/span>
&lt;a href="#diagnostic-scripts" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># Coverage and ASET maps (sign placement validation)&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>uv run python scripts/demo_vismap_phase0.py
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># With fresh vismap recompute&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>uv run python scripts/demo_vismap_phase0.py --no-cache&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h2>Cognitive maps&lt;span class="hx-absolute -hx-mt-20" id="cognitive-maps">&lt;/span>
&lt;a href="#cognitive-maps" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Agents have a familiarity tier that controls how much of the building they
know at the start of the simulation:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Tier&lt;/th>
&lt;th>&lt;code>familiarity&lt;/code>&lt;/th>
&lt;th>Knowledge at spawn&lt;/th>
&lt;th>Expansion&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>Trained staff&lt;/td>
&lt;td>&lt;code>&amp;quot;full&amp;quot;&lt;/code>&lt;/td>
&lt;td>Complete stage graph&lt;/td>
&lt;td>—&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Visitors&lt;/td>
&lt;td>&lt;code>&amp;quot;discovery&amp;quot;&lt;/code>&lt;/td>
&lt;td>Spawn node + visible neighbors&lt;/td>
&lt;td>On arrival + at reevaluation&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;strong>The tier limits topology, not perception of smoke.&lt;/strong> A &lt;code>discovery&lt;/code> agent does
not know the building — it routes only over its cognitive subgraph and learns
nodes through the perception-limited &lt;code>VisibilityModel&lt;/code>. It &lt;em>does&lt;/em> know the smoke
field: to choose among the exits it knows, it integrates &lt;code>tau = K_ave * L&lt;/code> over
the whole remaining route (optical depth, dimensionless), including legs it has never visited, and with
&lt;code>anticipate = True&lt;/code> and &lt;code>foresight_horizon_s = inf&lt;/code> at times that have not
happened. So route choice is an &lt;strong>optimality bound over the agent&amp;rsquo;s known
subgraph&lt;/strong>, not a behavioural model — map growth, exploration order and wander
behaviour are unaffected, but nothing here licenses the claim that a discovery
agent&amp;rsquo;s &lt;em>route choice&lt;/em> is perception-limited. Open as
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/125" target="_blank" rel="noopener">#125&lt;/a>
.&lt;/p>
&lt;p>Set per distribution group in the scenario config:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-json" data-lang="json">&lt;span style="display:flex;">&lt;span>{
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;distributions&amp;#34;&lt;/span>: {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;visitors&amp;#34;&lt;/span>: {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;parameters&amp;#34;&lt;/span>: {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;familiarity&amp;#34;&lt;/span>: &lt;span style="color:#e6db74">&amp;#34;discovery&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>}&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>A deck with discovery agents gets a visibility model either way — from the FDS
extinction field when &lt;code>--fds-dir&lt;/code> is given, from clear air otherwise (see the
table above) — because sight is gated by geometry, sign facing and contrast
whether or not there is a fire. Running with no model at all is what
&lt;code>--no-visibility&lt;/code> asks for. Without a model the perception step adds nothing, so
a &lt;code>discovery&lt;/code> agent starts
out knowing only its spawn node, its &lt;code>entrance&lt;/code>, and whatever the familiarity
draw gave it. Graph adjacency is not used as a stand-in for line of sight:
with no &lt;code>transitions&lt;/code> declared the graph is auto-wired from every spawn area to
every unblocked stage, so treating a neighbour as seen would hand most agents
most of the building&amp;rsquo;s exits at t=0 and make &lt;code>familiarity&lt;/code> inert.&lt;/p>
&lt;p>The tier binds whether or not the scenario defines a journey, and it binds from
the &lt;strong>first step&lt;/strong>: the agent&amp;rsquo;s cognitive map is built at spawn and the exits it
knows are ranked by the same cost the reroute pass uses, so an agent
who knows only the front door walks to the front door however far it is.
&lt;code>tests/test_initial_exit_from_cognitive_map.py&lt;/code> pins the
contract with rerouting off, where the opening choice is the only choice.&lt;/p>
&lt;p>Each agent is rooted at its spawn area, and routing ranks every exit reachable
from there. Rooting it at the assigned exit instead would collapse the ranking
to that one exit at zero cost;
&lt;code>tests/test_no_journey_routing_origin.py&lt;/code> pins the contract.&lt;/p>
&lt;p>Default when the key is absent: &lt;code>&amp;quot;full&amp;quot;&lt;/code> (backward compatible).&lt;/p>
&lt;p>&lt;strong>Discovery expansion rules:&lt;/strong>&lt;/p>
&lt;ol>
&lt;li>&lt;strong>At spawn&lt;/strong> — agent learns its spawn node, its &lt;code>entrance&lt;/code> if one is set,
each exit drawn as known by a scalar &lt;code>familiarity&lt;/code>, and any adjacent node
whose sign is currently visible from the spawn centroid. It then picks its
first exit by ranking that map, not by geometry.&lt;/li>
&lt;li>&lt;strong>On arrival&lt;/strong> — when an agent physically reaches a node, the immediate
neighbours whose sign is visible from where it stands are added (all of
them when no visibility model is supplied).&lt;/li>
&lt;li>&lt;strong>At reevaluation&lt;/strong> — adjacent nodes whose sign is visible from the
agent&amp;rsquo;s current position are added.&lt;/li>
&lt;/ol>
&lt;p>Learning an edge also learns its reverse when the graph has one: knowledge
of a corridor is bidirectional, so an agent can always retrace its steps
out of a dead end whose far side shows it nothing new.&lt;/p>
&lt;p>Routing (Dijkstra) runs over the agent&amp;rsquo;s known sub-graph only. If no exit
is reachable in the cognitive map, the agent heads toward the nearest
known-but-unexplored node instead (a doorway it knows exists but hasn&amp;rsquo;t
been through) — expanding its knowledge on arrival and re-evaluating from
there, until an exit becomes known. Reroute events for this show up in
&lt;code>route_history&lt;/code> with &lt;code>reason=&amp;quot;explore&amp;quot;&lt;/code>. When every known node has been
visited and still no exit is reachable, the agent wanders: it patrols the
nodes it knows in a deterministic rotation (&lt;code>reason=&amp;quot;wander&amp;quot;&lt;/code>), because
sign legibility depends on position — a leg walked between two known nodes
can make a sign readable that never was from either node, restarting
discovery. Once an exit is known, the agent
also reroutes onto a cheaper &lt;em>path&lt;/em> to that same exit as its knowledge
grows, not only when a different exit becomes preferable
(&lt;code>reason=&amp;quot;better_path&amp;quot;&lt;/code>).&lt;/p>
&lt;h3>Visualising cognitive map evolution&lt;span class="hx-absolute -hx-mt-20" id="visualising-cognitive-map-evolution">&lt;/span>
&lt;a href="#visualising-cognitive-map-evolution" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># 4-panel figure: spawn → junction → reroute → full baseline&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>uv run python scripts/demo_cognitive_map_vis.py
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># Without cached vismap (all neighbours assumed visible at spawn)&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>uv run python scripts/demo_cognitive_map_vis.py --no-cache&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Figure: &lt;img src="https://pedestriandynamics.org/pyFDS-Evac/t_junction/cognitive_map_evolution.png" alt="cognitive map evolution" loading="lazy" />&lt;/p>
&lt;h2>Verification: familiarity comparison&lt;span class="hx-absolute -hx-mt-20" id="verification-familiarity-comparison">&lt;/span>
&lt;a href="#verification-familiarity-comparison" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Two scenario configs differ only in familiarity tier:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Config&lt;/th>
&lt;th>Tier&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>assets/t_junction/config_full.json&lt;/code>&lt;/td>
&lt;td>&lt;code>familiarity=full&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>assets/t_junction/config_discovery.json&lt;/code>&lt;/td>
&lt;td>&lt;code>familiarity=discovery&lt;/code>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>Run both back-to-back and produce a 3-panel comparison (exit split,
rejection timeline, evacuation time):&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python scripts/run_familiarity_comparison.py &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --fds-dir assets/t_junction &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --vis-cache assets/t_junction/vismap_cache.npz&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Outputs: &lt;code>results/familiarity_comparison/{full,discovery}_route_costs.csv&lt;/code>,
&lt;code>results/familiarity_comparison/comparison.png&lt;/code>.&lt;/p>
&lt;h2>Deviations from the literature&lt;span class="hx-absolute -hx-mt-20" id="deviations-from-the-literature">&lt;/span>
&lt;a href="#deviations-from-the-literature" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The published law is on &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/">Visibility through smoke&lt;/a>
.
There &lt;em>C&lt;/em> is a property of the sign, but every node without an authored sign
receives a synthetic reflective sign, readable from every direction, at its
centroid (&lt;code>pyfds_evac/core/visibility.py:58&lt;/code>).
Legibility uses the mean extinction along the line of sight and a
view-angle correction (Börger et al. 2024), neither of which is part of Jin&amp;rsquo;s
experiments in uniform smoke. fdsvismap&amp;rsquo;s 30 m cap on visibility, the FDS
default, is raised to the domain diagonal before the map is computed
(&lt;code>visibility.py:103&lt;/code>).&lt;/p></description></item><item><title>Walking speed in smoke</title><link>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/walking-speed/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/walking-speed/</guid><description>
&lt;p>Smoke slows people down because they see less, and irritant smoke slows them
further because it hurts their eyes and airways. Evacuation models take this
from a small number of experiments that relate walking speed &lt;em>v&lt;/em> [m/s] to the
extinction coefficient &lt;em>K&lt;/em> [1/m] (see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/extinction/">Extinction coefficient&lt;/a>
).
The data sets were recorded under different conditions and are not
interchangeable (Ronchi et al. 2013).&lt;/p>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
. Each
section below keeps its source&amp;rsquo;s own notation: Korhonen&amp;rsquo;s \(K_s\) and
\(v_i^0\) for &lt;em>K&lt;/em> and \(v_0\); Purser&amp;rsquo;s \(\alpha_k\), which is the
extinction coefficient &lt;em>K&lt;/em>, not the coefficient \(\alpha\); and Fridolf et
al.&amp;rsquo;s &lt;em>w&lt;/em> for walking speed, &lt;em>x&lt;/em> for visibility (&lt;em>S&lt;/em> in the table) and &lt;em>A&lt;/em>
for the visibility constant (&lt;em>C&lt;/em>).&lt;/p>
&lt;h2>Jin: irritant and non-irritant smoke&lt;span class="hx-absolute -hx-mt-20" id="jin-irritant-and-non-irritant-smoke">&lt;/span>
&lt;a href="#jin-irritant-and-non-irritant-smoke" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Jin&amp;rsquo;s subjects walked along a 20 m corridor filled with irritant white smoke
(burning wood cribs) or less irritant black smoke (burning kerosene)
(Yamada and Akizuki 2016, Society of Fire Protection Engineers (SFPE)
Handbook Ch. 61, Fig. 61.22, citing Jin 1978 and Jin and Yamada 1985; see
also Jin 1997). In irritant smoke the speed fell from about 1.0 to 0.3 m/s
as &lt;em>K&lt;/em> rose from 0.1 to 0.5 1/m, and subjects walked zigzag or along the
wall because they could not keep their eyes open. In non-irritant smoke it
fell from about 1.0 to 0.5 m/s over &lt;em>K&lt;/em> = 0.2 to 1.0 1/m (Ronchi et al.
2013, p. 414 and Fig. 1). At high densities subjects moved at about
0.3 m/s, as if in darkness (Purser and McAllister 2016, Ch. 63,
pp. 2340–2341).&lt;/p>
&lt;h2>Frantzich and Nilsson: a linear regression in dense smoke&lt;span class="hx-absolute -hx-mt-20" id="frantzich-and-nilsson-a-linear-regression-in-dense-smoke">&lt;/span>
&lt;a href="#frantzich-and-nilsson-a-linear-regression-in-dense-smoke" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Frantzich and Nilsson (2003, Lund report 3126) sent 46 young volunteers one
at a time through a 36.75 m tunnel filled with artificial smoke made
irritant with acetic acid, at &lt;em>K&lt;/em> ≈ 2–7 1/m (§3.2). For the 32 runs with
the lighting on, a linear regression (report Eq. 3, Table D2, model 1) gave
the &lt;strong>absolute&lt;/strong> speed&lt;/p>
$$
v = \alpha + \beta K, \qquad \alpha = 0.706~\mathrm{m/s}\;(\text{s.d. } 0.069),\quad
\beta = -0.057~\mathrm{m^2/s}\;(\text{s.d. } 0.015),
$$&lt;p>with \(R^2\) = 0.342.&lt;/p>
&lt;details class="last-of-type:hx-mb-0 hx-rounded-lg hx-bg-neutral-50 dark:hx-bg-neutral-800 hx-p-2 hx-mt-4 hx-group" >
&lt;summary class="hx-flex hx-items-center hx-cursor-pointer hx-select-none hx-list-none hx-p-1 hx-rounded hx-transition-colors hover:hx-bg-gray-100 dark:hover:hx-bg-neutral-800 before:hx-mr-1 before:hx-inline-block before:hx-transition-transform before:hx-content-[''] dark:before:hx-invert rtl:before:hx-rotate-180 group-open:before:hx-rotate-90">
&lt;strong class="hx-text-lg">More on the Frantzich–Nilsson data&lt;/strong>
&lt;/summary>
&lt;div class="hx-p-2 hx-overflow-hidden">
The participants were 30 men and 16 women, mean age about 22, mostly
students; the tunnel was 5 m wide. Their comparison with Jin gives the
smoke range as about 2 to 8 1/m. With the lighting off (12 observations)
the slope was not significantly different from zero. A second model with
the share of the route walked along the wall fitted better (adjusted
\(R^2\) 0.416 against 0.320): walking along the wall raised the speed. The
95 % prediction interval at &lt;em>K&lt;/em> = 4 1/m was about 0.2 to 0.7 m/s, and the
authors state that a randomly chosen person in dense smoke (&lt;em>K&lt;/em> = 8 1/m)
could walk at anything between 0 and 0.6 m/s. The participants were young
and fit, so the authors expect a real population to do worse.
&lt;/div>
&lt;/details>
&lt;h2>The fractional form is FDS+Evac&amp;rsquo;s normalisation&lt;span class="hx-absolute -hx-mt-20" id="the-fractional-form-is-fdsevacs-normalisation">&lt;/span>
&lt;a href="#the-fractional-form-is-fdsevacs-normalisation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>FDS+Evac, the evacuation module of the Fire Dynamics Simulator (FDS), does
not use the regression as an absolute speed. It scales each agent&amp;rsquo;s
unimpeded speed \(v_i^0\) by the same factor (Korhonen 2021, Eq. 11):&lt;/p>
$$
v_i^0(K_s) = \max\!\left(v^0_{i,\min},\; v_i^0\left(1 + \frac{\beta}{\alpha}K_s\right)\right),
\qquad v^0_{i,\min} = 0.1\,v_i^0 \text{ by default.}
$$&lt;p>The factor divides the regression by its intercept, 0.706 m/s, an
extrapolation to &lt;em>K&lt;/em> = 0 from data at &lt;em>K&lt;/em> ≈ 2–8 1/m, not a measured free
walking speed. Ronchi et al. (2013) call the two readings fractional and
absolute reductions, and show that the same model can give different
evacuation times depending on the data set and reading chosen.&lt;/p>
&lt;h2>Purser: a logarithmic fit for irritant smoke&lt;span class="hx-absolute -hx-mt-20" id="purser-a-logarithmic-fit-for-irritant-smoke">&lt;/span>
&lt;a href="#purser-a-logarithmic-fit-for-irritant-smoke" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>For able-bodied occupants in moderately irritant smoke, Purser and
McAllister (2016, Ch. 63) give an average speed, with a population
standard deviation of 0.157 m/s (p. 2414),&lt;/p>
$$
W_{\mathrm{smoke}}~[\mathrm{m/s}] = -0.1364\,\ln \alpha_k + 0.6423 \qquad \text{(Eq. 63.10)}
$$&lt;p>where \(\alpha_k\) [1/m] is the extinction coefficient.&lt;/p>
&lt;h2>Fridolf et al. 2019: speed as a function of visibility&lt;span class="hx-absolute -hx-mt-20" id="fridolf-et-al-2019-speed-as-a-function-of-visibility">&lt;/span>
&lt;a href="#fridolf-et-al-2019-speed-as-a-function-of-visibility" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Fridolf, Ronchi, Nilsson and Frantzich (2019) reviewed smoke-filled tunnel
and corridor experiments from seven countries and express walking speed
&lt;em>w&lt;/em> [m/s] as a function of visibility &lt;em>x&lt;/em> [m], which lets data recorded
under different lighting be compared. They take \(x = A/K_s\) with &lt;em>A&lt;/em> = 2
for light-reflecting and 8 for light-emitting items (their Eq. 1); note
that the reflecting value differs from the conventional &lt;em>C&lt;/em> = 3 of Jin and
FDS (see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/">Visibility through smoke&lt;/a>
). Fitting
the data at 0–3 m visibility gives&lt;/p>
$$
w = 0.34\,x + 0.31 \qquad \text{(Fridolf et al. 2019, Eq. 2)}
$$&lt;p>with \(R^2\) = 0.54. For design, each person&amp;rsquo;s clear-condition speed
\(w_{\text{smoke free}}\) is reduced by 0.34 m/s per metre of visibility
below 3 m, down to 0.2 m/s:&lt;/p>
$$
w = \min\!\left(w_{\text{smoke free}},\; \max\!\left(0.2,\; w_{\text{smoke free}} - 0.34\,(3 - x)\right)\right) \qquad \text{(Eq. 7)}
$$&lt;details class="last-of-type:hx-mb-0 hx-rounded-lg hx-bg-neutral-50 dark:hx-bg-neutral-800 hx-p-2 hx-mt-4 hx-group" >
&lt;summary class="hx-flex hx-items-center hx-cursor-pointer hx-select-none hx-list-none hx-p-1 hx-rounded hx-transition-colors hover:hx-bg-gray-100 dark:hover:hx-bg-neutral-800 before:hx-mr-1 before:hx-inline-block before:hx-transition-transform before:hx-content-[''] dark:before:hx-invert rtl:before:hx-rotate-180 group-open:before:hx-rotate-90">
&lt;strong class="hx-text-lg">Fridolf et al.&amp;rsquo;s three design methods&lt;/strong>
&lt;/summary>
&lt;div class="hx-p-2 hx-overflow-hidden">
The methods differ in \(w_{\text{smoke free}}\): 1 m/s for everyone
(method 1, Eq. 3); 1.35, 1.10 or 0.85 m/s for medium, slow and very slow
walkers (method 2, Eqs. 4–6); or a value drawn for each person from a
normal distribution with mean 1.35 m/s and standard deviation 0.25 m/s,
truncated at 0.85 and 1.85 m/s (method 3, Eq. 7). The authors describe the
reduction as absolute, because every person loses the same speed, and
fractional, because it starts from each person&amp;rsquo;s own clear-condition speed
(§3.4).
&lt;/div>
&lt;/details>
&lt;h2>Known limits&lt;span class="hx-absolute -hx-mt-20" id="known-limits">&lt;/span>
&lt;a href="#known-limits" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>All of these relations come from short walks by healthy, mostly young adults
who knew they were in an experiment. None covers a real fire&amp;rsquo;s heat or toxic
gases. The Jin and Frantzich–Nilsson data do not overlap in &lt;em>K&lt;/em>, differ in
irritancy and lighting, and should not be combined as if they were one data
set. Using a relation outside its measured &lt;em>K&lt;/em> range is an extrapolation.&lt;/p>
&lt;h2>Sources&lt;span class="hx-absolute -hx-mt-20" id="sources">&lt;/span>
&lt;a href="#sources" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>Jin, T. (1978). &lt;em>Visibility through fire smoke&lt;/em>. Journal of Fire &amp;amp;
Flammability, 9, 135–157. No DOI or public URL; pages 135–155 in
Fridolf et al. (2019).&lt;/li>
&lt;li>Jin, T., &amp;amp; Yamada, T. (1985). &lt;em>Irritating effects of fire smoke on
visibility&lt;/em>. Fire Science and Technology, 5(1), 79–90.
&lt;a href="https://doi.org/10.3210/fst.5.79" target="_blank" rel="noopener">doi:10.3210/fst.5.79&lt;/a>
&lt;/li>
&lt;li>Jin, T. (1997). &lt;em>Studies on human behavior and tenability in fire
smoke&lt;/em>. Fire Safety Science, 5, 3–21.
&lt;a href="https://doi.org/10.3801/iafss.fss.5-3" target="_blank" rel="noopener">doi:10.3801/iafss.fss.5-3&lt;/a>
&lt;/li>
&lt;li>Frantzich, H., &amp;amp; Nilsson, D. (2003). &lt;em>Utrymning genom tät rök: beteende
och förflyttning&lt;/em> [Evacuation in dense smoke: behaviour and movement].
Report 3126, LUTVDG/TVBB&amp;ndash;3126&amp;ndash;SE, Department of Fire Safety
Engineering, Lund University.
&lt;a href="https://lup.lub.lu.se/search/publication/2b23c428-7809-46e1-87b0-22bf247f6386" target="_blank" rel="noopener">lup.lub.lu.se (open access)&lt;/a>
&lt;/li>
&lt;li>Ronchi, E., Gwynne, S. M. V., Purser, D. A., &amp;amp; Colonna, P. (2013).
&lt;em>Representation of the impact of smoke on agent walking speeds in
evacuation models&lt;/em>. Fire Technology, 49(2), 411–431.
&lt;a href="https://doi.org/10.1007/s10694-012-0280-y" target="_blank" rel="noopener">doi:10.1007/s10694-012-0280-y&lt;/a>
&lt;/li>
&lt;li>Fridolf, K., Ronchi, E., Nilsson, D., &amp;amp; Frantzich, H. (2019). &lt;em>The
representation of evacuation movement in smoke-filled underground
transportation systems&lt;/em>. Tunnelling and Underground Space Technology,
90, 28–41.
&lt;a href="https://doi.org/10.1016/j.tust.2019.04.016" target="_blank" rel="noopener">doi:10.1016/j.tust.2019.04.016&lt;/a>
&lt;/li>
&lt;li>Purser, D. A., &amp;amp; McAllister, J. L. (2016). &lt;em>Assessment of hazards to
occupants from smoke, toxic gases, and heat&lt;/em>. SFPE Handbook of Fire
Protection Engineering, 5th ed., Ch. 63, 2308–2428.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_63" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_63&lt;/a>
&lt;/li>
&lt;li>Yamada, T., &amp;amp; Akizuki, Y. (2016). &lt;em>Visibility and human behavior in fire
smoke&lt;/em>. SFPE Handbook of Fire Protection Engineering, 5th ed., Ch. 61,
2181–2206.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_61" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_61&lt;/a>
&lt;/li>
&lt;li>Korhonen, T. (2021). &lt;em>Fire Dynamics Simulator with Evacuation: FDS+Evac.
Technical Reference and User&amp;rsquo;s Guide&lt;/em> (FDS 6.7.6, Evac 2.6.0 draft). VTT
Technical Research Centre of Finland.
&lt;a href="https://github.com/tkorhon1/FDS-Evac-Guide" target="_blank" rel="noopener">github.com/tkorhon1/FDS-Evac-Guide&lt;/a>
.
Secondary source for the fractional form.&lt;/li>
&lt;/ul>
&lt;p>How pyFDS-Evac uses this: see the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/">smoke-speed model&lt;/a>
.&lt;/p></description></item><item><title>What your FDS case must provide</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/fds-case-requirements/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/fds-case-requirements/</guid><description>
&lt;p>Read this before pointing &lt;code>--fds-dir&lt;/code> at a case for the first time.&lt;/p>
&lt;p>&lt;strong>pyFDS-Evac never runs FDS.&lt;/strong> It reads the output of a finished FDS run.
A deck written for some other purpose usually will not work as-is: it has to
be told to dump the specific slices this tool samples.&lt;/p>
&lt;h2>The slices&lt;span class="hx-absolute -hx-mt-20" id="the-slices">&lt;/span>
&lt;a href="#the-slices" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>&amp;amp;SLCF PBZ=2.0, QUANTITY=&amp;#39;EXTINCTION COEFFICIENT&amp;#39; /
&amp;amp;SLCF PBZ=2.0, QUANTITY=&amp;#39;VOLUME FRACTION&amp;#39;, SPEC_ID=&amp;#39;CARBON MONOXIDE&amp;#39; /
&amp;amp;SLCF PBZ=2.0, QUANTITY=&amp;#39;VOLUME FRACTION&amp;#39;, SPEC_ID=&amp;#39;CARBON DIOXIDE&amp;#39; /
&amp;amp;SLCF PBZ=2.0, QUANTITY=&amp;#39;VOLUME FRACTION&amp;#39;, SPEC_ID=&amp;#39;OXYGEN&amp;#39; /
&amp;amp;SLCF PBZ=2.0, QUANTITY=&amp;#39;TEMPERATURE&amp;#39; /
&amp;amp;DUMP DT_SLCF=1.0 /&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Nothing here is dumped by default. FDS writes only what the deck asks for.&lt;/p>
&lt;p>For the default species (soot), FDS records the extinction-coefficient
slice internally as &lt;code>SOOT EXTINCTION COEFFICIENT&lt;/code>, and that is the exact
quantity name &lt;code>load_slice_sampler&lt;/code> looks up
(&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/fds-sampling/">fds-sampling.md&lt;/a>
).&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Slice&lt;/th>
&lt;th>Feeds&lt;/th>
&lt;th>If absent&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>Extinction coefficient&lt;/td>
&lt;td>smoke-speed, visibility gating, GUI smoke layer&lt;/td>
&lt;td>&lt;code>IndexError&lt;/code> when &lt;code>--fds-dir&lt;/code> is given without &lt;code>--constant-extinction&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>CO &lt;strong>and&lt;/strong> CO2 &lt;strong>and&lt;/strong> O2&lt;/td>
&lt;td>FED toxic dose&lt;/td>
&lt;td>&lt;strong>FED is switched off and the run continues&lt;/strong> (see below)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>TEMPERATURE&lt;/td>
&lt;td>heat FED (SFPE Handbook Eq. 63.44)&lt;/td>
&lt;td>&lt;strong>heat FED is switched off and the run continues&lt;/strong> (see below)&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>It is all three gases or none of them; there is no partial FED. TEMPERATURE
is independent of that gate — it needs neither CO/CO2/O2 nor any &lt;code>&amp;amp;REAC&lt;/code>
yield to work (see below), so a case can have heat FED without toxic FED
or vice versa.&lt;/p>
&lt;p>&lt;code>EXTINCTION&lt;/code> and &lt;code>EXTINCTION COEFFICIENT&lt;/code> are two unrelated gas-phase output
quantities (FDS User Guide Table 22.4) — don&amp;rsquo;t confuse them. &lt;code>EXTINCTION&lt;/code>
(Sec. 22.10.29) is a combustion-suppression flag with no units: 0 if the
extinction routine did not prevent combustion, 1 if it did, -1 if there was
no fuel or oxidizer. &lt;code>EXTINCTION COEFFICIENT&lt;/code> (Sec. 22.10.5) is the light
extinction coefficient K [1/m] that the smoke-speed model actually needs.
If your deck has &lt;code>QUANTITY='EXTINCTION'&lt;/code> where you meant the extinction
coefficient, fix it to &lt;code>QUANTITY='EXTINCTION COEFFICIENT'&lt;/code> and rerun FDS.&lt;/p>
&lt;h2>Declaring a slice is not enough&lt;span class="hx-absolute -hx-mt-20" id="declaring-a-slice-is-not-enough">&lt;/span>
&lt;a href="#declaring-a-slice-is-not-enough" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The species also has to exist in the run. Under simple chemistry, &lt;code>OXYGEN&lt;/code>,
&lt;code>NITROGEN&lt;/code>, &lt;code>WATER VAPOR&lt;/code> and &lt;code>CARBON DIOXIDE&lt;/code> are always present, but
&lt;code>CARBON MONOXIDE&lt;/code> only exists if &lt;code>CO_YIELD&lt;/code> is set on &lt;code>&amp;amp;REAC&lt;/code>, and &lt;code>SOOT&lt;/code> only
if &lt;code>SOOT_YIELD&lt;/code> is set. A plain &lt;code>&amp;amp;REAC FUEL='PROPANE' /&lt;/code> cannot produce FED no
matter what &lt;code>&amp;amp;SLCF&lt;/code> lines you add. Fix it at the source:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>&amp;amp;REAC FUEL=&amp;#39;CABLE_FUEL&amp;#39;, C=3, H=5, O=0, N=1,
SOOT_YIELD=0.172, CO_YIELD=0.063, HCN_YIELD=0.006,
HEAT_OF_COMBUSTION=16400 /&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>That is the &lt;code>t_junction&lt;/code> reaction. Yields are fuel properties: take them from
your material&amp;rsquo;s data rather than copying these.&lt;/p>
&lt;p>&lt;code>TEMPERATURE&lt;/code> is the one exception: it is a core solved gas-phase variable in
every FDS run, not a species yield, so it needs no &lt;code>&amp;amp;REAC&lt;/code> setup at all — the
&lt;code>&amp;amp;SLCF&lt;/code> line above is sufficient on its own.&lt;/p>
&lt;h2>Two silent failure modes&lt;span class="hx-absolute -hx-mt-20" id="two-silent-failure-modes">&lt;/span>
&lt;a href="#two-silent-failure-modes" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;strong>FED disabled.&lt;/strong> If CO, CO2 or O2 is missing, pyFDS-Evac carries on with
smoke-speed only and every FED column reads zero. A zero-dose result looks
exactly like a never-computed one. A warning is logged when this happens, and
it is the only signal you get:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>FED is disabled for &amp;lt;dir&amp;gt;: it has no CO slice, and all three of CO, CO2 and
O2 are needed. ...&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;strong>Heat FED disabled.&lt;/strong> The same applies to the independent heat FED track: if
there is no &lt;code>TEMPERATURE&lt;/code> slice, heat FED is silently off, and every heat FED
column reads zero:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>Heat FED is disabled for &amp;lt;dir&amp;gt;: it has no TEMPERATURE slice. ...&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;strong>Wrong slice height.&lt;/strong> &lt;code>--smoke-slice-height&lt;/code> (default 2.0 m) is a
preference, not a filter: a case with one slice of a quantity uses it whatever
its height, and a mismatch never fails the run. A case you inherit often
carries a single slice at whatever height its author chose, so you can end up
sampling floor-level CO for standing agents. A warning fires past 0.5 m:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>Requested a &amp;#39;SOOT EXTINCTION COEFFICIENT&amp;#39; slice at z=0.50 m but the nearest
available in &amp;lt;dir&amp;gt; is at z=2.00 m ...&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Do not ignore either warning. Nothing else will tell you.&lt;/p>
&lt;h2>Failure modes&lt;span class="hx-absolute -hx-mt-20" id="failure-modes">&lt;/span>
&lt;a href="#failure-modes" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Symptom&lt;/th>
&lt;th>Cause&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>IndexError: No slice with quantity 'SOOT EXTINCTION COEFFICIENT' found in &amp;lt;dir&amp;gt;&lt;/code>&lt;/td>
&lt;td>The deck never declared &lt;code>&amp;amp;SLCF QUANTITY='EXTINCTION COEFFICIENT'&lt;/code>, or the species was never tracked.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>IndexError: No slice with quantity '...' found in &amp;lt;dir&amp;gt;&lt;/code>&lt;/td>
&lt;td>The deck never declared that &lt;code>&amp;amp;SLCF&lt;/code>, or the species was never tracked.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Warning: FED is disabled for &lt;code>&amp;lt;dir&amp;gt;&lt;/code>&lt;/td>
&lt;td>CO, CO2 or O2 is missing. Check &lt;code>CO_YIELD&lt;/code> on &lt;code>&amp;amp;REAC&lt;/code>.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Warning: Heat FED is disabled for &lt;code>&amp;lt;dir&amp;gt;&lt;/code>&lt;/td>
&lt;td>No &lt;code>TEMPERATURE&lt;/code> slice. Add &lt;code>&amp;amp;SLCF QUANTITY='TEMPERATURE'&lt;/code> — no &lt;code>&amp;amp;REAC&lt;/code> change needed.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>FED is zero everywhere and nobody is incapacitated&lt;/td>
&lt;td>Either genuinely survivable, or FED never ran. Check for the warning above before concluding the former.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>ValueError: Point (x, y) is outside the sampled FDS slice domain&lt;/code>&lt;/td>
&lt;td>Walkable area extends past the slice extent.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Warning: requested slice at z=A, nearest is z=B&lt;/td>
&lt;td>Your case has no slice near the height you asked for.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table></description></item><item><title>Asphyxiant fractional effective dose</title><link>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/asphyxiant-fed/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/asphyxiant-fed/</guid><description>
&lt;p>The fractional effective dose (FED) [-] is the fraction of an
incapacitating dose received, summed over short time steps; incapacitation
of a person of average susceptibility is predicted when it reaches 1. The
asphyxiants in fires are carbon monoxide (CO), hydrogen cyanide (HCN), low
oxygen (O₂) and, indirectly, carbon dioxide (CO₂), which speeds up
breathing and so the uptake of the others.&lt;/p>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
. The
equations below keep the sources&amp;rsquo; own notation (\(F_I\) terms,
concentrations in brackets such as [CO], ventilation \(V_E\)).&lt;/p>
&lt;h2>The ISO 13571 principle&lt;span class="hx-absolute -hx-mt-20" id="the-iso-13571-principle">&lt;/span>
&lt;a href="#the-iso-13571-principle" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>ISO 13571:2012 (§6.1.1, Eq. 1) sums over gases &lt;em>i&lt;/em> and time steps
\(\Delta t\) [min] the ratio \(C_i\,\Delta t/(C\cdot t)_i\) of the average
concentration \(C_i\) [µL/L] to the dose \((C\cdot t)_i\) that compromises
tenability, or equivalently \(\Delta t / t_i\) (Eq. 1a). Its gas-specific
terms are not reproduced here. Unlike Purser, it expresses the CO term as a
Ct dose of 35 000 ppm·min (Purser and McAllister 2016, Note 2 to
Eq. 63.18) and does &amp;ldquo;not incorporate the rarefaction of oxygen&amp;rdquo;, while
treating CO₂ as a hyperventilation factor (ISO/TR 13571-2:2016, §6.2).&lt;/p>
&lt;h2>Purser&amp;rsquo;s simplified equation&lt;span class="hx-absolute -hx-mt-20" id="pursers-simplified-equation">&lt;/span>
&lt;a href="#pursers-simplified-equation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Purser and McAllister (2016, Society of Fire Protection Engineers (SFPE)
Handbook Ch. 63) give, for loss of consciousness of an adult doing light
work such as walking to escape,&lt;/p>
$$
F_{IN} = \left(F_{I_{CO}} + F_{I_{CN}} + F_{I_{NO_x}} + FLD_{irr}\right)\times VCO_2 + F_{I_O}
\quad \text{or}\quad F_{I_{CO_2}} \qquad \text{(Eq. 63.38)}
$$&lt;p>with the terms, &lt;em>t&lt;/em> in minutes and concentrations in ppm or % by volume:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>CO&lt;/strong> (Stewart equation, Eq. 63.18):
\(F_{I_{CO}} = 3.317\times10^{-5}\,[\mathrm{CO}]^{1.036}\,V_E\,t/D\), with
\(V_E\) = 25 L/min (light work) and &lt;em>D&lt;/em> = 30 % carboxyhaemoglobin (COHb)
by default. With these defaults the coefficient is
\(3.317\times10^{-5}\times 25/30 = 2.764\times10^{-5}\) per minute, the
constant used by FDS+Evac, the evacuation module of the Fire Dynamics
Simulator (Korhonen 2021, Eq. 13).&lt;/li>
&lt;li>&lt;strong>HCN&lt;/strong> (Eq. 63.24): \(F_{I_{CN}} = [\mathrm{CN}]^{2.36}\,t/(1.2\times10^{6})\),
where [CN] may be corrected for other nitriles and for the protective effect
of NO and NO₂ as \([\mathrm{CN}] = [\mathrm{HCN}] + [\text{organic nitriles}] - [\mathrm{NO}+\mathrm{NO_2}]\)
(Eq. 63.26).&lt;/li>
&lt;li>&lt;strong>NOₓ&lt;/strong>: \(F_{I_{NO_x}} = [\mathrm{NO_x}]\,t/1500\) (definitions under Eq. 63.38).&lt;/li>
&lt;li>&lt;strong>Irritants&lt;/strong>: \(FLD_{irr}\), the fractional lethal dose of irritants
(Eq. 63.15; see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/irritants/">Irritant gases&lt;/a>
), included because
irritants impair lung function and so add some hypoxia.&lt;/li>
&lt;li>&lt;strong>CO₂ hyperventilation&lt;/strong> (Eq. 63.35): \(VCO_2 = \exp([\mathrm{CO_2}]/5)\).
Purser recommends a limiting value of 70 L/min for \(V_E\times VCO_2\)
(Ch. 63, p. 2416).&lt;/li>
&lt;li>&lt;strong>Low-oxygen hypoxia&lt;/strong> (Eq. 63.50):
\(F_{I_O} = t/\exp\left[8.13 - 0.54\,(20.9 - [\%\mathrm{O_2}])\right]\),
added outside the CO₂ multiplier because CO₂ improves oxygen uptake.&lt;/li>
&lt;li>&lt;strong>CO₂ as an asphyxiant&lt;/strong>, \(F_{I_{CO_2}}\), from
\(t_{I_{CO_2}} = \exp(6.1623 - 0.5189\,\%\mathrm{CO_2})\) (Eqs. 63.36–63.37),
used as an alternative endpoint and normally negligible.&lt;/li>
&lt;/ul>
&lt;h2>Published forms differ by edition&lt;span class="hx-absolute -hx-mt-20" id="published-forms-differ-by-edition">&lt;/span>
&lt;a href="#published-forms-differ-by-edition" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The FDS+Evac guide follows the 3rd edition of the Handbook and uses a
different CO₂ factor and HCN term from Eq. 63.38, so a result quoted as
&amp;ldquo;Purser FED&amp;rdquo; depends on the edition (Korhonen 2021, Eqs. 12–19).&lt;/p>
&lt;details class="last-of-type:hx-mb-0 hx-rounded-lg hx-bg-neutral-50 dark:hx-bg-neutral-800 hx-p-2 hx-mt-4 hx-group" >
&lt;summary class="hx-flex hx-items-center hx-cursor-pointer hx-select-none hx-list-none hx-p-1 hx-rounded hx-transition-colors hover:hx-bg-gray-100 dark:hover:hx-bg-neutral-800 before:hx-mr-1 before:hx-inline-block before:hx-transition-transform before:hx-content-[''] dark:before:hx-invert rtl:before:hx-rotate-180 group-open:before:hx-rotate-90">
&lt;strong class="hx-text-lg">The CO₂ factor and HCN term by edition (Eqs. 63.31–63.35)&lt;/strong>
&lt;/summary>
&lt;div class="hx-p-2 hx-overflow-hidden">
&lt;p>A regression of minute volume on CO₂, fitted to three published human data
sets (Eq. 63.31, Fig. 63.26), gives the factor
\(\exp(0.2496\,\%\mathrm{CO_2} + 1.9086)/6.8\) (Eq. 63.32), simplified to
\(\exp([\mathrm{CO_2}]/4)\) (Eq. 63.33). Because that overstates uptake,
Ch. 63 modifies it after the Coburn–Forster–Kane equation to&lt;/p>
$$
VCO_2 = \frac{\exp(0.1903\,\%\mathrm{CO_2} + 2.0004)}{7.1} \qquad \text{(Eq. 63.34)}
$$&lt;p>and simplifies this to \(\exp([\mathrm{CO_2}]/5)\) (Eq. 63.35), the form
used in Eq. 63.38. The FDS+Evac guide uses Eq. 63.34 and the older HCN
term \(\left(\exp(C_{CN}/43)/220 - 0.0045\right)\) with
\(C_{CN} = C_{HCN} - C_{NO_2}\), citing Purser in the 3rd edition
(Korhonen 2021, Eq. 14; Purser 2002); the 5th edition gives the power form
instead (Eq. 63.24).&lt;/p>
&lt;/div>
&lt;/details>
&lt;details class="last-of-type:hx-mb-0 hx-rounded-lg hx-bg-neutral-50 dark:hx-bg-neutral-800 hx-p-2 hx-mt-4 hx-group" >
&lt;summary class="hx-flex hx-items-center hx-cursor-pointer hx-select-none hx-list-none hx-p-1 hx-rounded hx-transition-colors hover:hx-bg-gray-100 dark:hover:hx-bg-neutral-800 before:hx-mr-1 before:hx-inline-block before:hx-transition-transform before:hx-content-[''] dark:before:hx-invert rtl:before:hx-rotate-180 group-open:before:hx-rotate-90">
&lt;strong class="hx-text-lg">The data behind the terms&lt;/strong>
&lt;/summary>
&lt;div class="hx-p-2 hx-overflow-hidden">
The CO term comes from the Stewart equation, obtained from young adult male
volunteers; Purser notes that it somewhat underestimates uptake in children
and that the Coburn–Forster–Kane equation is preferable near equilibrium
(Ch. 63, Notes 1–2 to Eq. 63.18, pp. 2416–2417). The HCN term is fitted to
exposures of non-human primates and the hypoxia term to human data
(p. 2417), and the CO₂ curve to three human data sets (Fig. 63.26). The
default is light work; \(V_E\) = 8.5 L/min at rest and 50 L/min for heavy
work (table beside Eq. 63.39, p. 2416).
&lt;/div>
&lt;/details>
&lt;h2>Known limits&lt;span class="hx-absolute -hx-mt-20" id="known-limits">&lt;/span>
&lt;a href="#known-limits" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The equations predict incapacitation of an average person; protecting more
susceptible people needs a threshold below 1 (see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/incapacitation-thresholds/">Incapacitation thresholds&lt;/a>
).
ISO 13571 (§5.8) finds very little reliable information on exposures
shorter than 1 min or longer than 1 h, and its Introduction states that,
for ethical reasons, much of the method cannot be validated with humans,
although the CO database is extensive and well validated.&lt;/p>
&lt;h2>Sources&lt;span class="hx-absolute -hx-mt-20" id="sources">&lt;/span>
&lt;a href="#sources" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>Purser, D. A., &amp;amp; McAllister, J. L. (2016). &lt;em>Assessment of hazards to
occupants from smoke, toxic gases, and heat&lt;/em>. SFPE Handbook of Fire
Protection Engineering, 5th ed., Ch. 63, 2308–2428.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_63" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_63&lt;/a>
&lt;/li>
&lt;li>ISO (2012). &lt;em>ISO 13571:2012 Life-threatening components of fire —
Guidelines for the estimation of time to compromised tenability in
fires&lt;/em>. ISO, Geneva.
&lt;a href="https://www.iso.org/standard/56172.html" target="_blank" rel="noopener">iso.org/standard/56172&lt;/a>
. Read
from the public preview (§1–6.1.1).&lt;/li>
&lt;li>ISO (2016). &lt;em>ISO/TR 13571-2:2016 Life-threatening components of fire —
Part 2: Methodology and examples of tenability assessment&lt;/em>. ISO, Geneva.
&lt;a href="https://www.iso.org/standard/65996.html" target="_blank" rel="noopener">iso.org/standard/65996&lt;/a>
. Read
from the public preview.&lt;/li>
&lt;li>Korhonen, T. (2021). &lt;em>Fire Dynamics Simulator with Evacuation: FDS+Evac.
Technical Reference and User&amp;rsquo;s Guide&lt;/em> (FDS 6.7.6, Evac 2.6.0 draft),
§3.4. VTT Technical Research Centre of Finland.
&lt;a href="https://github.com/tkorhon1/FDS-Evac-Guide" target="_blank" rel="noopener">github.com/tkorhon1/FDS-Evac-Guide&lt;/a>
.
Secondary source.&lt;/li>
&lt;li>Purser, D. A. (2002). &lt;em>Toxicity assessment of combustion products&lt;/em>. SFPE
Handbook of Fire Protection Engineering, 3rd ed., 2-83–2-171. National
Fire Protection Association, Quincy, MA. No DOI or public URL; cited
through Korhonen (2021, ref. 29), which dates it 2003.&lt;/li>
&lt;/ul>
&lt;p>How pyFDS-Evac uses this: see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/">Fractional effective dose&lt;/a>
.&lt;/p></description></item><item><title>Real-FDS walkthrough</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/walkthrough/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/walkthrough/</guid><description>
&lt;p>&lt;strong>This page reads FDS output, but you do not need to run FDS.&lt;/strong> The output of
two small FDS cases is tracked in the repository, and the page uses it as is.&lt;/p>
&lt;h2>Goal&lt;span class="hx-absolute -hx-mt-20" id="goal">&lt;/span>
&lt;a href="#goal" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Go from FDS output to an evacuation result:&lt;/p>
&lt;ol>
&lt;li>list what an FDS case contains;&lt;/li>
&lt;li>slow an agent with the extinction coefficient read from an FDS slice;&lt;/li>
&lt;li>accumulate a toxic dose (FED, fractional effective dose) from the CO, CO2
and O2 slices;&lt;/li>
&lt;li>see a run that finishes normally but has no FED in it, and learn how to
detect it.&lt;/li>
&lt;/ol>
&lt;h2>Prerequisites&lt;span class="hx-absolute -hx-mt-20" id="prerequisites">&lt;/span>
&lt;a href="#prerequisites" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>A clone of the repository and its environment (&lt;code>uv sync&lt;/code>). fdsreader, which
reads the FDS output, is a core dependency.&lt;/li>
&lt;li>Run every command from the repository root. The paths are relative to it.&lt;/li>
&lt;li>Runtime: about 15 s.&lt;/li>
&lt;li>Read &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/fds-case-requirements/">What your FDS case must provide&lt;/a>
. This page
shows two of the failure modes listed there.&lt;/li>
&lt;/ul>
&lt;p>The full script is &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/examples/walkthrough.py" target="_blank" rel="noopener">&lt;code>examples/walkthrough.py&lt;/code>&lt;/a>
.
Run it with:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python examples/walkthrough.py&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>It uses two tracked cases, because neither has everything this page needs:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>FDS case&lt;/th>
&lt;th>Slices&lt;/th>
&lt;th>Scenario&lt;/th>
&lt;th>Used for&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>assets/iso_table21_coupled/fds&lt;/code>&lt;/td>
&lt;td>extinction coefficient&lt;/td>
&lt;td>1 agent walks a 100 m corridor through &lt;em>K&lt;/em> ≈ 1 1/m&lt;/td>
&lt;td>smoke speed, exit time&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>assets/iso_table22_coupled/fds/a&lt;/code>&lt;/td>
&lt;td>CO, CO2, O2, extinction coefficient&lt;/td>
&lt;td>1 occupant held still for 1150 s in a 10 m × 10 m room&lt;/td>
&lt;td>FED history&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>The occupant in the second case never leaves: its pre-movement time is drawn
above 1.2 × 10⁷ s, as ISO 20414 Test 19 asks. So that case gives a FED history
but no exit time.&lt;/p>
&lt;h2>Steps&lt;span class="hx-absolute -hx-mt-20" id="steps">&lt;/span>
&lt;a href="#steps" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The script starts with these imports and paths. Everything from pyFDS-Evac is
imported from &lt;code>pyfds_evac&lt;/code>.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">import&lt;/span> json
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">import&lt;/span> pathlib
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">import&lt;/span> shutil
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">import&lt;/span> tempfile
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> types &lt;span style="color:#f92672">import&lt;/span> SimpleNamespace
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac &lt;span style="color:#f92672">import&lt;/span> (
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> DefaultFedConfig,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> DefaultFedModel,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> ExtinctionField,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> FdsFedField,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> SmokeSpeedConfig,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> SmokeSpeedModel,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> TenabilityConfig,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> build_run_kwargs,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> load_scenario,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> run_scenario,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>SMOKE_DIR &lt;span style="color:#f92672">=&lt;/span> &lt;span style="color:#e6db74">&amp;#34;assets/iso_table21_coupled/fds&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>GAS_DIR &lt;span style="color:#f92672">=&lt;/span> &lt;span style="color:#e6db74">&amp;#34;assets/iso_table22_coupled/fds/a&amp;#34;&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h3>1. List what the FDS case contains&lt;span class="hx-absolute -hx-mt-20" id="1-list-what-the-fds-case-contains">&lt;/span>
&lt;a href="#1-list-what-the-fds-case-contains" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Before any run, list the slices. The command-line runner does this with
&lt;code>--inspect-fds&lt;/code>:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python run.py --scenario assets/iso_table22_coupled/config_a.json &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --fds-dir assets/iso_table22_coupled/fds/a --inspect-fds&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Expected output:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>Initialization started.
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>{
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &amp;#34;data_3d&amp;#34;: [],
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &amp;#34;devices&amp;#34;: [],
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &amp;#34;slices&amp;#34;: [
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &amp;#34;CARBON DIOXIDE VOLUME FRACTION&amp;#34;,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &amp;#34;CARBON MONOXIDE VOLUME FRACTION&amp;#34;,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &amp;#34;OXYGEN VOLUME FRACTION&amp;#34;,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &amp;#34;SOOT EXTINCTION COEFFICIENT&amp;#34;
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> ],
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &amp;#34;smoke_3d&amp;#34;: []
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>}&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The same command on &lt;code>assets/iso_table21_coupled/fds&lt;/code> lists only
&lt;code>&amp;quot;SOOT EXTINCTION COEFFICIENT&amp;quot;&lt;/code>. That case can drive the smoke-speed model, but
not FED.&lt;/p>
&lt;p>FED needs all three of &lt;code>CARBON MONOXIDE VOLUME FRACTION&lt;/code>,
&lt;code>CARBON DIOXIDE VOLUME FRACTION&lt;/code> and &lt;code>OXYGEN VOLUME FRACTION&lt;/code>. Heat FED needs
&lt;code>TEMPERATURE&lt;/code>. Neither tracked case has a &lt;code>TEMPERATURE&lt;/code> slice.&lt;/p>
&lt;p>fdsreader may also log &lt;code>Module csv: [Errno 2] No such file or directory: …_hrr.csv&lt;/code>
and &lt;code>…_steps.csv&lt;/code>, followed by &amp;ldquo;The error can be safely ignored if not requiring
the csv module&amp;rdquo;. The tracked cases carry no CSV output. These messages come from
fdsreader and do not affect the run.&lt;/p>
&lt;h3>2. Smoke: extinction from FDS slows the walk&lt;span class="hx-absolute -hx-mt-20" id="2-smoke-extinction-from-fds-slows-the-walk">&lt;/span>
&lt;a href="#2-smoke-extinction-from-fds-slows-the-walk" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>corridor &lt;span style="color:#f92672">=&lt;/span> load_scenario(&lt;span style="color:#e6db74">&amp;#34;assets/iso_table21_coupled&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>clear &lt;span style="color:#f92672">=&lt;/span> run_scenario(corridor, seed&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">420&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>extinction &lt;span style="color:#f92672">=&lt;/span> ExtinctionField&lt;span style="color:#f92672">.&lt;/span>from_fds(SMOKE_DIR)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>smoke &lt;span style="color:#f92672">=&lt;/span> SmokeSpeedModel(extinction, SmokeSpeedConfig())
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>smoky &lt;span style="color:#f92672">=&lt;/span> run_scenario(corridor, seed&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">420&lt;/span>, smoke_speed_model&lt;span style="color:#f92672">=&lt;/span>smoke)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>row &lt;span style="color:#f92672">=&lt;/span> smoky&lt;span style="color:#f92672">.&lt;/span>smoke_history[&lt;span style="color:#f92672">-&lt;/span>&lt;span style="color:#ae81ff">1&lt;/span>]
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;K sampled: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>row[&lt;span style="color:#e6db74">&amp;#39;extinction_per_m&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.5f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> 1/m&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;speed factor: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>row[&lt;span style="color:#e6db74">&amp;#39;speed_factor&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.6f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;exit time: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>clear&lt;span style="color:#f92672">.&lt;/span>evacuation_time&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.2f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s clear, &amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>&lt;span style="color:#e6db74">{&lt;/span>smoky&lt;span style="color:#f92672">.&lt;/span>evacuation_time&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.2f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s in smoke&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>manifest &lt;span style="color:#f92672">=&lt;/span> json&lt;span style="color:#f92672">.&lt;/span>loads(pathlib&lt;span style="color:#f92672">.&lt;/span>Path(smoky&lt;span style="color:#f92672">.&lt;/span>manifest_file)&lt;span style="color:#f92672">.&lt;/span>read_text())
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;manifest: fds_dir=&lt;/span>&lt;span style="color:#e6db74">{&lt;/span>manifest[&lt;span style="color:#e6db74">&amp;#39;fds_dir&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34; fds_version=&lt;/span>&lt;span style="color:#e6db74">{&lt;/span>manifest[&lt;span style="color:#e6db74">&amp;#39;fds_version&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Expected output:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>Evacuated 1/1 sim=78.1s wall=0m00s done
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>Evacuated 1/1 sim=85.0s wall=0m00s done
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>K sampled: 0.99545 1/m
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>speed factor: 0.919631
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>exit time: 78.14 s clear, 84.97 s in smoke
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>manifest: fds_dir=/…/fds-evac/assets/iso_table21_coupled/fds
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> fds_version=FDS-6.10.1-0-g12efa16-release&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The manifest stores &lt;code>fds_dir&lt;/code> as an absolute path; it is shortened here.&lt;/p>
&lt;p>&lt;code>ExtinctionField.from_fds&lt;/code> reads the &lt;code>SOOT EXTINCTION COEFFICIENT&lt;/code> slice
nearest to &lt;code>slice_height_m&lt;/code> (default 2.0 m). The deck prescribes a soot density
that gives &lt;em>K&lt;/em> = 1.0 1/m. The slice returns 0.99545 1/m, and the default speed
law turns it into a speed factor of 0.919631. The ratio of the exit times,
84.97 / 78.14 = 1.0874, matches 1 / 0.919631 = 1.0874 to four decimals.&lt;/p>
&lt;p>&lt;code>ExtinctionField.from_fds&lt;/code> remembers the directory it read. The run manifest
records that directory and reads the FDS version from the &lt;code>FDSVERSION&lt;/code> line of
its &lt;code>.smv&lt;/code> file. The manifest also records the package versions, the seed, the
scenario path, the &lt;code>uv.lock&lt;/code> hash, and the git commit (&lt;code>git_commit&lt;/code>) and
whether the working tree had uncommitted changes (&lt;code>git_dirty&lt;/code>). With &lt;code>run.py --output-sqlite PATH&lt;/code>, the manifest is copied next to the trajectory as
&lt;code>&amp;lt;stem&amp;gt;.manifest.json&lt;/code>.&lt;/p>
&lt;h3>3. Gas: FED from the CO, CO2 and O2 slices&lt;span class="hx-absolute -hx-mt-20" id="3-gas-fed-from-the-co-co2-and-o2-slices">&lt;/span>
&lt;a href="#3-gas-fed-from-the-co-co2-and-o2-slices" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>room &lt;span style="color:#f92672">=&lt;/span> load_scenario(&lt;span style="color:#e6db74">&amp;#34;assets/iso_table22_coupled/config_a.json&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>fed &lt;span style="color:#f92672">=&lt;/span> DefaultFedModel(FdsFedField&lt;span style="color:#f92672">.&lt;/span>from_fds(GAS_DIR), DefaultFedConfig())
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>tenability &lt;span style="color:#f92672">=&lt;/span> TenabilityConfig(incapacitation_mode&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;deterministic&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>exposed &lt;span style="color:#f92672">=&lt;/span> run_scenario(room, seed&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">420&lt;/span>, fed_model&lt;span style="color:#f92672">=&lt;/span>fed, tenability_config&lt;span style="color:#f92672">=&lt;/span>tenability)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">for&lt;/span> row &lt;span style="color:#f92672">in&lt;/span> exposed&lt;span style="color:#f92672">.&lt;/span>fed_history[::&lt;span style="color:#ae81ff">200&lt;/span>]:
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> print(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;t=&lt;/span>&lt;span style="color:#e6db74">{&lt;/span>row[&lt;span style="color:#e6db74">&amp;#39;time_s&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">6.0f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s CO=&lt;/span>&lt;span style="color:#e6db74">{&lt;/span>row[&lt;span style="color:#e6db74">&amp;#39;co_percent&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.3f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> % &amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;FED=&lt;/span>&lt;span style="color:#e6db74">{&lt;/span>row[&lt;span style="color:#e6db74">&amp;#39;fed_cumulative&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.3f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> )
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>crossing &lt;span style="color:#f92672">=&lt;/span> next(r &lt;span style="color:#66d9ef">for&lt;/span> r &lt;span style="color:#f92672">in&lt;/span> exposed&lt;span style="color:#f92672">.&lt;/span>fed_history &lt;span style="color:#66d9ef">if&lt;/span> r[&lt;span style="color:#e6db74">&amp;#34;fed_cumulative&amp;#34;&lt;/span>] &lt;span style="color:#f92672">&amp;gt;=&lt;/span> &lt;span style="color:#ae81ff">1.0&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;FED &amp;gt;= 1 at t = &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>crossing[&lt;span style="color:#e6db74">&amp;#39;time_s&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.0f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s; &amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;incapacitated: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>exposed&lt;span style="color:#f92672">.&lt;/span>fed_history[&lt;span style="color:#f92672">-&lt;/span>&lt;span style="color:#ae81ff">1&lt;/span>][&lt;span style="color:#e6db74">&amp;#39;incapacitated&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;FED max: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>exposed&lt;span style="color:#f92672">.&lt;/span>metrics[&lt;span style="color:#e6db74">&amp;#39;fed_max&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.3f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Expected output:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>Evacuated 0/1 sim=1150.0s wall=0m05s done
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>t= 0 s CO=0.100 % FED=0.000
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>t= 200 s CO=0.100 % FED=0.204
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>t= 400 s CO=0.100 % FED=0.407
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>t= 600 s CO=0.100 % FED=0.611
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>t= 800 s CO=0.100 % FED=0.815
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>t= 1000 s CO=0.100 % FED=1.019
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>FED &amp;gt;= 1 at t = 982 s; incapacitated: True
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>FED max: 1.170&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The deck fills the room with 0.10 % CO, 2.0 % CO2 and 15.0 % O2. The hand
calculation for this mixture reaches FED = 1 at 981.7 s
(&lt;code>assets/iso_table22_coupled/build_geometry.py&lt;/code>), and the run crosses it at
982 s. &lt;code>fed_history&lt;/code> has one row per agent per update interval (1 s by
default).&lt;/p>
&lt;p>&lt;code>TenabilityConfig()&lt;/code> defaults to &lt;code>incapacitation_mode=&amp;quot;probabilistic&amp;quot;&lt;/code>, which
draws a log-normal threshold for each agent with median &lt;code>fed_threshold = 1.0&lt;/code>.
This page uses &lt;code>&amp;quot;deterministic&amp;quot;&lt;/code>, so the threshold is exactly 1.0, as in
FDS+Evac. The command-line equivalent is &lt;code>--incapacitation-mode deterministic&lt;/code>.&lt;/p>
&lt;p>The incapacitated occupant stays in the simulation, so the run continues to
&lt;code>max_simulation_time&lt;/code> and &lt;code>exposed.evacuation_time&lt;/code> reports 1150 s. It is not
an exit time (&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/141" target="_blank" rel="noopener">#141&lt;/a>
).&lt;/p>
&lt;p>&lt;code>FdsFedField.from_fds&lt;/code> takes the first slice of each gas and does not match
the slice height. &lt;code>ExtinctionField.from_fds&lt;/code> and &lt;code>FdsHeatField.from_fds&lt;/code> do.&lt;/p>
&lt;h3>4. Checkpoint: a run that succeeds but is wrong&lt;span class="hx-absolute -hx-mt-20" id="4-checkpoint-a-run-that-succeeds-but-is-wrong">&lt;/span>
&lt;a href="#4-checkpoint-a-run-that-succeeds-but-is-wrong" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>This step copies case &lt;code>a&lt;/code> to a temporary directory and deletes its CO slice:
the &lt;code>.sf&lt;/code> and &lt;code>.sf.bnd&lt;/code> files, and the five-line &lt;code>SLCF&lt;/code> entry in the &lt;code>.smv&lt;/code>
file that points to them. The copy looks like FDS output from a deck that
never asked for a CO slice.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>broken &lt;span style="color:#f92672">=&lt;/span> pathlib&lt;span style="color:#f92672">.&lt;/span>Path(tempfile&lt;span style="color:#f92672">.&lt;/span>mkdtemp()) &lt;span style="color:#f92672">/&lt;/span> &lt;span style="color:#e6db74">&amp;#34;a&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>shutil&lt;span style="color:#f92672">.&lt;/span>copytree(GAS_DIR, broken, ignore&lt;span style="color:#f92672">=&lt;/span>shutil&lt;span style="color:#f92672">.&lt;/span>ignore_patterns(&lt;span style="color:#e6db74">&amp;#34;*.pickle&amp;#34;&lt;/span>))
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">for&lt;/span> path &lt;span style="color:#f92672">in&lt;/span> broken&lt;span style="color:#f92672">.&lt;/span>glob(&lt;span style="color:#e6db74">&amp;#34;iso_table22_a_1_1.sf*&amp;#34;&lt;/span>): &lt;span style="color:#75715e"># the CO slice&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> path&lt;span style="color:#f92672">.&lt;/span>unlink()
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>smv &lt;span style="color:#f92672">=&lt;/span> broken &lt;span style="color:#f92672">/&lt;/span> &lt;span style="color:#e6db74">&amp;#34;iso_table22_a.smv&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>lines &lt;span style="color:#f92672">=&lt;/span> smv&lt;span style="color:#f92672">.&lt;/span>read_text()&lt;span style="color:#f92672">.&lt;/span>splitlines(keepends&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#66d9ef">True&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>start &lt;span style="color:#f92672">=&lt;/span> next(i &lt;span style="color:#66d9ef">for&lt;/span> i, line &lt;span style="color:#f92672">in&lt;/span> enumerate(lines) &lt;span style="color:#66d9ef">if&lt;/span> &lt;span style="color:#e6db74">&amp;#34;iso_table22_a_1_1.sf&amp;#34;&lt;/span> &lt;span style="color:#f92672">in&lt;/span> line) &lt;span style="color:#f92672">-&lt;/span> &lt;span style="color:#ae81ff">1&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>smv&lt;span style="color:#f92672">.&lt;/span>write_text(&lt;span style="color:#e6db74">&amp;#34;&amp;#34;&lt;/span>&lt;span style="color:#f92672">.&lt;/span>join(lines[:start] &lt;span style="color:#f92672">+&lt;/span> lines[start &lt;span style="color:#f92672">+&lt;/span> &lt;span style="color:#ae81ff">5&lt;/span> :]))&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The &lt;code>*.pickle&lt;/code> file is a cache that fdsreader writes into the case directory.
It is skipped so that the copy is read fresh.&lt;/p>
&lt;p>&lt;strong>If you build the FED field yourself, the missing slice fails loudly:&lt;/strong>&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">try&lt;/span>:
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> FdsFedField&lt;span style="color:#f92672">.&lt;/span>from_fds(str(broken))
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">except&lt;/span> &lt;span style="color:#a6e22e">IndexError&lt;/span> &lt;span style="color:#66d9ef">as&lt;/span> error:
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;FdsFedField.from_fds: IndexError: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>error&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>FdsFedField.from_fds: IndexError: tuple index out of range&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The message does not name the missing slice.&lt;/p>
&lt;p>&lt;strong>If you go through &lt;code>build_run_kwargs&lt;/code>, the run continues without FED.&lt;/strong>
&lt;code>build_run_kwargs&lt;/code> is what &lt;code>run.py&lt;/code> and the web GUI call. It turns their
options into &lt;code>run_scenario&lt;/code> keywords:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>opts &lt;span style="color:#f92672">=&lt;/span> SimpleNamespace(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> seed&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">420&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> fds_dir&lt;span style="color:#f92672">=&lt;/span>str(broken),
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> constant_extinction&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#66d9ef">None&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> smoke_update_interval&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">1.0&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> smoke_slice_height&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">2.0&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> disable_tenability&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#66d9ef">False&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> fed_threshold&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">1.0&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> fic_alpha&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">0.7&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> fic_min_factor&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">0.3&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> incapacitation_mode&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;deterministic&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> enable_rerouting&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#66d9ef">False&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> reroute_interval&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">1.0&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> vis_cache&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#66d9ef">None&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>kwargs &lt;span style="color:#f92672">=&lt;/span> build_run_kwargs(room, opts)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>silent &lt;span style="color:#f92672">=&lt;/span> run_scenario(room, &lt;span style="color:#f92672">**&lt;/span>kwargs)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;finished at &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>silent&lt;span style="color:#f92672">.&lt;/span>evacuation_time&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.0f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s, &amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>&lt;span style="color:#e6db74">{&lt;/span>silent&lt;span style="color:#f92672">.&lt;/span>agents_remaining&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> agent still in the room&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>Evacuated 0/1 sim=1150.0s wall=0m05s done
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>finished at 1150 s, 1 agent still in the room&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The run finishes at the same time as in step 3, with no error. But no dose was
computed. In step 3, the same occupant in the same gas was incapacitated at
982 s.&lt;/p>
&lt;h3>5. Detect it&lt;span class="hx-absolute -hx-mt-20" id="5-detect-it">&lt;/span>
&lt;a href="#5-detect-it" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>Read the warnings.&lt;/strong> &lt;code>build_run_kwargs&lt;/code> logs this to stderr (the temporary
path differs):&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>WARNING:pyfds_evac.core.run_config:FED is disabled for /tmp/tmp3i0w6sml/a: it has no CO slice, and all three of CO, CO2 and O2 are needed. Results will report zero dose and no incapacitation. FDS only writes these species when the &amp;amp;REAC line asks for them (CO needs CO_YIELD); see docs/fds-case-requirements.md.
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>WARNING:pyfds_evac.core.run_config:Heat FED is disabled for /tmp/tmp3i0w6sml/a: it has no TEMPERATURE slice. Results will report zero heat dose and no thermal incapacitation. Add `&amp;amp;SLCF QUANTITY=&amp;#39;TEMPERATURE&amp;#39;` to the FDS deck; see docs/fds-case-requirements.md.&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;code>build_run_kwargs&lt;/code> logs the &lt;code>Heat FED is disabled&lt;/code> warning for every case
without a &lt;code>TEMPERATURE&lt;/code> slice, including the intact case &lt;code>a&lt;/code>. (Step 3 builds
its models by hand, so it logs neither warning.) The fdsreader &lt;code>csv&lt;/code>
messages from step 1 appear in the same stream, so search for &lt;code>FED is disabled&lt;/code>
rather than for &amp;ldquo;warning&amp;rdquo;.&lt;/p>
&lt;p>&lt;strong>Check before the run&lt;/strong> that &lt;code>build_run_kwargs&lt;/code> returned a FED model, and
&lt;strong>check after the run&lt;/strong> that the metrics hold &lt;code>fed_max&lt;/code>:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;before: fed_model = &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>kwargs[&lt;span style="color:#e6db74">&amp;#39;fed_model&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;after: fed_history = &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>silent&lt;span style="color:#f92672">.&lt;/span>fed_history&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">, &amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&amp;#39;fed_max&amp;#39; in metrics = &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>&lt;span style="color:#e6db74">&amp;#39;fed_max&amp;#39;&lt;/span> &lt;span style="color:#f92672">in&lt;/span> silent&lt;span style="color:#f92672">.&lt;/span>metrics&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>before: fed_model = None
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>after: fed_history = None, &amp;#39;fed_max&amp;#39; in metrics = False&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Here FED and heat FED are both off, so the result carries no FED at all:
&lt;code>fed_history&lt;/code> is &lt;code>None&lt;/code> and &lt;code>metrics&lt;/code> has no &lt;code>fed_max&lt;/code> key. It does not report
FED = 0 (&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/137" target="_blank" rel="noopener">#137&lt;/a>
). With &lt;code>run.py&lt;/code>, &lt;code>--output-fed-history&lt;/code> writes no file in this case.&lt;/p>
&lt;p>&lt;code>fed_history&lt;/code> alone is not a reliable check. &lt;code>run_scenario&lt;/code> fills it whenever
either FED track runs. A case with a &lt;code>TEMPERATURE&lt;/code> slice but no CO slice runs
heat FED only: &lt;code>fed_history&lt;/code> then exists, every &lt;code>fed_cumulative&lt;/code> in it is 0.0,
and &lt;code>metrics&lt;/code> has &lt;code>heat_fed_max&lt;/code> but no &lt;code>fed_max&lt;/code>. That zero column looks
exactly like a case where nobody received a dose. &lt;code>metrics&lt;/code> holds &lt;code>fed_max&lt;/code> only when the gas FED
model ran, so check &lt;code>fed_max&lt;/code>, and &lt;code>kwargs[&amp;quot;fed_model&amp;quot;]&lt;/code> before the run.&lt;/p>
&lt;p>A third check is the inventory from step 1: if any of CO, CO2 and O2 is
missing from &lt;code>&amp;quot;slices&amp;quot;&lt;/code>, FED will not run.&lt;/p>
&lt;p>The script ends by calling &lt;code>cleanup()&lt;/code> on each result and deleting the
temporary copy.&lt;/p>
&lt;h2>Next steps&lt;span class="hx-absolute -hx-mt-20" id="next-steps">&lt;/span>
&lt;a href="#next-steps" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/fds-case-requirements/">What your FDS case must provide&lt;/a>
: the slices to
add to your deck and the full list of failure modes.&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/howto-rset-ensemble/">How do I get RSET with its spread from an ensemble of seeds?&lt;/a>
&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/fds-sampling/">FDS sampling&lt;/a>
: how slices are selected and sampled.&lt;/li>
&lt;/ul>
&lt;p>pyFDS-Evac is research software, provided without warranty.&lt;/p></description></item><item><title>Verification suite</title><link>https://pedestriandynamics.org/pyFDS-Evac/models/verification/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/models/verification/</guid><description>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
.&lt;/p>
&lt;p>A two-layer verification suite checks each sub-model against a hand-computable
reference:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Tier A&lt;/strong> pins each model &lt;em>function&lt;/em> (FED, smoke-speed, cognitive map,
pre-movement) to a closed form, to machine precision.&lt;/li>
&lt;li>&lt;strong>Behavioural&lt;/strong> scenarios drive the coupling inside &lt;code>run_scenario&lt;/code> with
injected synthetic fields (no FDS run): a corridor for FED lethality (S1) and
smoke-speed slowdown (S2), and a T-junction for dynamic rerouting (S4). Each
uses a control / treatment / null-field design and asserts both exact wiring
(from the per-agent history logs) and aggregate behaviour.&lt;/li>
&lt;/ul>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run pytest tests/verification -m &lt;span style="color:#e6db74">&amp;#34;not slow&amp;#34;&lt;/span> &lt;span style="color:#75715e"># fast suite&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>uv run pytest tests/verification &lt;span style="color:#75715e"># incl. ensemble checks&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>See &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/tests/verification/README.md" target="_blank" rel="noopener">tests/verification/README.md&lt;/a>
for the full
catalogue and &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/specs/012-model-verification/SPEC.md" target="_blank" rel="noopener">specs/012-model-verification/SPEC.md&lt;/a>
for the design. The suite already surfaced two engine findings: trajectory-level
nondeterminism (only aggregate outcomes reproduce under a fixed seed) and a
rerouting bug under by-number placement
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/21" target="_blank" rel="noopener">issue #21&lt;/a>
).&lt;/p>
&lt;h2>Status against ISO 20414&lt;span class="hx-absolute -hx-mt-20" id="status-against-iso-20414">&lt;/span>
&lt;a href="#status-against-iso-20414" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>ISO 20414:2020 (&lt;em>Fire safety engineering — Verification and validation
protocol for building fire evacuation models&lt;/em>) defines 21 verification tests
(Tests 1–21) and 9 validation tests (Tests 22–30). The table below gives the
status of each verification test for pyFDS-Evac. FED means fractional
effective dose, and &lt;em>K&lt;/em> [1/m] is the extinction coefficient. Everything on this page is
&lt;strong>verification&lt;/strong>: it checks that the code reproduces the correlation or hand
calculation it implements. None of it is &lt;strong>validation&lt;/strong> against observed
evacuations, and none of ISO 20414&amp;rsquo;s validation tests (22–30) has been run.&lt;/p>
&lt;h3>Tests 18 and 19: the fire–people interaction tests&lt;span class="hx-absolute -hx-mt-20" id="tests-18-and-19-the-firepeople-interaction-tests">&lt;/span>
&lt;a href="#tests-18-and-19-the-firepeople-interaction-tests" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>These two tests cover the part of the model that pyFDS-Evac adds to JuPedSim,
so they were run first. Each is implemented twice: once with the hazard
supplied directly, as the standard allows, and once with the hazard read from
committed FDS output, so that the path from slice file to agent is checked as
well. Each condition is one run with a single occupant, so no spread over
seeds is reported.&lt;/p>
&lt;p>&lt;strong>Test 18, reduced visibility vs walking speed.&lt;/strong> The occupant walks the ISO
corridor (2 m × 100 m, 1 m exit) once in clear air, which is the control, and
once under a constant extinction coefficient &lt;em>K&lt;/em>. The reference is the model&amp;rsquo;s
own correlation, as the standard requires: the ratio of the two egress times
must equal 1/&lt;em>f&lt;/em>(&lt;em>K&lt;/em>), where &lt;em>f&lt;/em> is the speed factor of the Frantzich–Nilsson
law (the &lt;code>lund&lt;/code> option; the linear FDS+Evac law). The ratio must agree within 8 %, which absorbs the finite spawn box, the
exit width and the 0.1 s update interval, and every recorded speed factor must
equal &lt;em>f&lt;/em>(&lt;em>K&lt;/em>) exactly. With &lt;em>K&lt;/em> supplied as a constant field
(&lt;code>assets/ISO-table21&lt;/code>, &lt;code>tests/test_smoke_speed.py&lt;/code>), the test runs at
&lt;em>K&lt;/em> = 0.5, 1.0, 3.0, 7.5 and 10 1/m, and at unimpeded speeds of 1.0, 0.75, 0.5
and 0.25 m/s at &lt;em>K&lt;/em> = 1.0 1/m. With &lt;em>K&lt;/em> read from FDS
(&lt;code>assets/iso_table21_coupled&lt;/code>, &lt;code>tests/test_iso_table21_coupled.py&lt;/code>), the deck
prescribes the soot density for &lt;em>K&lt;/em> = 1.0 1/m. The test requires the slice to
return that value within 1 % and to be non-zero. The second check is a null
control, because a missing slice reads as clear air. Only the default
Frantzich–Nilsson law is run in the ISO layout. The &lt;code>fridolf&lt;/code> option (&lt;code>speed_law=&amp;quot;fridolf&amp;quot;&lt;/code>), which
is reachable only from Python, is checked by the S2 corridor scenario of the
suite above, not by this test.&lt;/p>
&lt;p>&lt;strong>Test 19, occupant incapacitation by fire or smoke.&lt;/strong> The occupant stands at
the centre of a 10 m × 10 m room. The time at which the simulated dose reaches
FED = 1 is compared with a hand calculation of the same dose
(&lt;code>time_to_fed_threshold_s()&lt;/code>). With the gas supplied directly
(&lt;code>assets/ISO-table22&lt;/code>, &lt;code>tests/test_fed.py&lt;/code>), the test holds the occupant by
setting its speed to zero, one mixture is run, and the crossing must fall
within one time step of the hand calculation. With the gas
read from FDS (&lt;code>assets/iso_table22_coupled&lt;/code>,
&lt;code>tests/test_iso_table22_coupled.py&lt;/code>), the occupant is held by a
pre-evacuation time above 10⁷ s, the method the standard prescribes, and four
cases are run. They take their
concentrations from Fig. 8 of the FDS+Evac guide (Korhonen 2021) and separate
the CO, CO2 and O2 terms. The crossing must fall within two intervals of the
FED history. The test also checks that each species arrives at its prescribed
concentration and that the occupant does not move. The standard asks for the
test to be repeated for each hazardous condition the sub-model has. That is
done for CO, CO2 and O2 only. The convective heat dose is verified against its
closed form by the S6 scenario and the Tier A tests, but not in the Test 19
layout. The optional gases (HCN, NOx and the irritants) are not run in this
layout.&lt;/p>
&lt;h3>All verification tests&lt;span class="hx-absolute -hx-mt-20" id="all-verification-tests">&lt;/span>
&lt;a href="#all-verification-tests" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&amp;ldquo;Not yet run&amp;rdquo; means that the component belongs to pyFDS-Evac, or is
configured through it, but no test reproduces the ISO setup. &amp;ldquo;Inherited&amp;rdquo; means
that the component is JuPedSim&amp;rsquo;s movement model, which pyFDS-Evac does not
change; pyFDS-Evac has not rerun it. &amp;ldquo;Out of scope&amp;rdquo; means that pyFDS-Evac does
not model the component.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Test&lt;/th>
&lt;th>Title&lt;/th>
&lt;th>Status&lt;/th>
&lt;th>Reason&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>18&lt;/td>
&lt;td>Reduced visibility vs walking speed&lt;/td>
&lt;td>Run, passes&lt;/td>
&lt;td>See above. Frantzich–Nilsson law only.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>19&lt;/td>
&lt;td>Occupant incapacitation by fire/smoke&lt;/td>
&lt;td>Run for CO, CO2, O2&lt;/td>
&lt;td>See above. Heat and optional gases not run in this layout.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>1&lt;/td>
&lt;td>Pre-evacuation time assignment&lt;/td>
&lt;td>Partly covered&lt;/td>
&lt;td>The four distributions are checked against closed-form moments (&lt;code>test_premovement_verif.py&lt;/code>); the ISO room and per-agent start times are not run.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>2&lt;/td>
&lt;td>Walking speed in a corridor&lt;/td>
&lt;td>Inherited&lt;/td>
&lt;td>Movement is JuPedSim&amp;rsquo;s.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>3&lt;/td>
&lt;td>Walking speed on stairs&lt;/td>
&lt;td>Out of scope&lt;/td>
&lt;td>No stairs; the walkable area is one floor.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>4&lt;/td>
&lt;td>Movement around a corner&lt;/td>
&lt;td>Inherited&lt;/td>
&lt;td>Movement is JuPedSim&amp;rsquo;s.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>5&lt;/td>
&lt;td>Assigned demographics&lt;/td>
&lt;td>Not yet run&lt;/td>
&lt;td>Speed distributions (&lt;code>v0_distribution&lt;/code>) are set in pyFDS-Evac; their assignment has not been tested in the ISO setup.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>6&lt;/td>
&lt;td>Horizontal counter-flows&lt;/td>
&lt;td>Inherited&lt;/td>
&lt;td>Movement is JuPedSim&amp;rsquo;s.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>7&lt;/td>
&lt;td>Overtaking people with movement disabilities&lt;/td>
&lt;td>Inherited&lt;/td>
&lt;td>Movement is JuPedSim&amp;rsquo;s; slower agents are set with &lt;code>v0&lt;/code>.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>8&lt;/td>
&lt;td>Exit route allocation&lt;/td>
&lt;td>Not yet run&lt;/td>
&lt;td>Exits and journeys are set in the scenario JSON, but rerouting is on by default (&lt;code>--enable-rerouting&lt;/code>), so an allocated route may not be kept; no test reproduces the ISO layout.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>9&lt;/td>
&lt;td>Dynamic availability of exit&lt;/td>
&lt;td>Not supported&lt;/td>
&lt;td>An exit cannot be closed at a set time. Availability changes only when routing refuses a route for its smoke or dose.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>10&lt;/td>
&lt;td>Congestion in front of a flight of stairs&lt;/td>
&lt;td>Out of scope&lt;/td>
&lt;td>No stairs.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>11&lt;/td>
&lt;td>Maximum flow rates at an opening/exit&lt;/td>
&lt;td>Not yet run&lt;/td>
&lt;td>Optional throughput throttling on exits exists (&lt;code>enable_throughput_throttling&lt;/code>, &lt;code>max_throughput&lt;/code>); it has not been tested in the ISO setup.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>12&lt;/td>
&lt;td>Stair flow rates&lt;/td>
&lt;td>Out of scope&lt;/td>
&lt;td>No stairs.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>13&lt;/td>
&lt;td>Flow rate, density and walking speed in a corridor&lt;/td>
&lt;td>Inherited&lt;/td>
&lt;td>Movement is JuPedSim&amp;rsquo;s.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>14&lt;/td>
&lt;td>Group behaviour&lt;/td>
&lt;td>Out of scope&lt;/td>
&lt;td>Groups are not modelled.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>15&lt;/td>
&lt;td>Social influence on exit choice&lt;/td>
&lt;td>Out of scope&lt;/td>
&lt;td>Herding is not modelled (&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/78" target="_blank" rel="noopener">issue #78&lt;/a>
).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>16&lt;/td>
&lt;td>Affiliation to familiar exits&lt;/td>
&lt;td>Not yet run&lt;/td>
&lt;td>Familiarity and the &lt;code>entrance&lt;/code> key exist; whether they meet the test&amp;rsquo;s criterion has not been checked.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>17&lt;/td>
&lt;td>Route choice based on geometric layout&lt;/td>
&lt;td>Out of scope&lt;/td>
&lt;td>The ISO layout has two floors.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>20&lt;/td>
&lt;td>Lift usage&lt;/td>
&lt;td>Out of scope&lt;/td>
&lt;td>Lifts are not modelled.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>21&lt;/td>
&lt;td>Escalator usage&lt;/td>
&lt;td>Out of scope&lt;/td>
&lt;td>Escalators are not modelled.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Movement is inherited from JuPedSim&lt;span class="hx-absolute -hx-mt-20" id="movement-is-inherited-from-jupedsim">&lt;/span>
&lt;a href="#movement-is-inherited-from-jupedsim" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>pyFDS-Evac does not implement locomotion. Walking, collision avoidance,
counter-flow and flow through openings are computed by the JuPedSim model
named in the scenario (&lt;code>model_type&lt;/code>, default &lt;code>CollisionFreeSpeedModel&lt;/code>).
pyFDS-Evac sets each agent&amp;rsquo;s desired speed and target, and it can cap the
flow through an exit, but locomotion itself is JuPedSim&amp;rsquo;s. The movement tests are
therefore inherited, and pyFDS-Evac has not rerun them. JuPedSim reports its
own status against ISO 20414, RiMEA, NIST TN 1822 and IMO MSC.1/Circ.1533 on
its &lt;a href="https://pedestriandynamics.org/jupedsim-web-community/" target="_blank" rel="noopener">V&amp;amp;V standards coverage dashboard&lt;/a>
,
including the tests it marks as expected failures. That status applies to the
models and versions it names, which may differ from the model and version a
pyFDS-Evac scenario uses.&lt;/p></description></item><item><title>Irritant gases</title><link>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/irritants/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/irritants/</guid><description>
&lt;p>Irritant gases such as hydrogen chloride (HCl), hydrogen bromide (HBr),
hydrogen fluoride (HF), sulphur dioxide (SO₂), nitrogen dioxide (NO₂),
acrolein and formaldehyde act in two ways. Sensory irritation of the eyes and
upper airways is felt immediately and depends on the concentration: it
impairs escape at once. Damage to the deep lung depends on the inhaled dose
and develops over hours. The literature treats the two with different
quantities, and the standards and the handbook combine them differently.&lt;/p>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
. The
equations below keep the sources&amp;rsquo; own notation (FEC, \(F_{\mathrm{FIC}}\),
\(F_{\mathrm{FLD}}\)).&lt;/p>
&lt;h2>ISO 13571: a separate concentration endpoint&lt;span class="hx-absolute -hx-mt-20" id="iso-13571-a-separate-concentration-endpoint">&lt;/span>
&lt;a href="#iso-13571-a-separate-concentration-endpoint" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>ISO 13571:2012 treats asphyxiants and irritants separately, because they are
&amp;ldquo;physiologically unrelated and mechanistically independent&amp;rdquo; (§4.2.1). For
irritants it considers only eye and upper-respiratory-tract sensory
irritation. It computes a fractional effective concentration (FEC) [-] for
each irritant at each time increment, from the concentration and not from
the dose, and compromised tenability is predicted when the sum of the FECs
exceeds a threshold (§4.2.3). The FEC is a separate endpoint: it is not added
to the asphyxiant fractional effective dose (FED). The same threshold value must be used for FED and FEC
in a given estimation (§5.4). Pulmonary irritation is excluded because its
serious effects appear hours to days after exposure (§4.2.1).&lt;/p>
&lt;h2>Purser: a concentration term and a dose term&lt;span class="hx-absolute -hx-mt-20" id="purser-a-concentration-term-and-a-dose-term">&lt;/span>
&lt;a href="#purser-a-concentration-term-and-a-dose-term" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Purser and McAllister (2016, Society of Fire Protection Engineers (SFPE)
Handbook Ch. 63) define the fractional irritant
concentration (FIC) [-] as a sum over irritants, each term being the current
concentration divided by the concentration predicted to cause a chosen
endpoint:&lt;/p>
$$
\mathrm{FIC} = \mathrm{FIC_{HCl}} + \mathrm{FIC_{HBr}} + \mathrm{FIC_{HF}} + \mathrm{FIC_{SO_2}} + \mathrm{FIC_{NO_2}} + \mathrm{FIC_{CH_2CHO}} + \mathrm{FIC_{HCHO}} + \sum \mathrm{FIC}_x
\qquad \text{(Eq. 63.11)}
$$&lt;p>The denominators are tabulated for two endpoints, escape impairment and
incapacitation (Table 63.6, SFPE columns). Purser states that a factor of
0.3 on the FEC for escape impairment should allow nearly all exposed people
to escape (Ch. 63, p. 2414). Like the ISO FEC, the FIC is a concentration criterion and is
not integrated over time. The same table lists the corresponding ISO 13571
values, which differ for several gases: for HCl, for example, the SFPE
incapacitation concentration is 900 ppm and the ISO value 1000 ppm
(Table 63.6).&lt;/p>
&lt;p>For the effect of irritants on walking, Ch. 63 gives a fractional walking
speed that falls to zero at FIC = 1 (Eq. 63.13, a Gaussian-type curve with
&lt;em>b&lt;/em> = 160), and combines it with the smoke-visibility effect by adding the
two losses, \(F_{wv} = 1 - (1 - F_{wv\,smoke}) - (1 - F_{wv\,irr})\) (Eq. 63.14).&lt;/p>
&lt;p>The lung effects are a dose. The fractional lethal dose of irritants
\(FLD_{irr}\) [-] sums, for each irritant, the Ct exposure dose [ppm·min]
divided by the dose predicted to be lethal to half the population (Eq. 63.15,
Table 63.7, e.g. 114 000 ppm·min for HCl). Purser notes that lung irritation
has relatively little effect on escape capability and &amp;ldquo;can be omitted from an
escape calculation&amp;rdquo; (p. 2415), but in the simplified asphyxiant equation he includes
\(FLD_{irr}\) inside the sum that is multiplied by the CO₂ factor, because
irritants impair lung function and add some hypoxia (Eq. 63.38; see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/asphyxiant-fed/">Asphyxiant FED&lt;/a>
).&lt;/p>
&lt;h2>The Purser / FDS+Evac guide form&lt;span class="hx-absolute -hx-mt-20" id="the-purser--fdsevac-guide-form">&lt;/span>
&lt;a href="#the-purser--fdsevac-guide-form" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The guide to FDS+Evac, the evacuation module of the Fire Dynamics Simulator
(Korhonen 2021, Eqs. 12 and 17, Table 2), follows this
structure: its total dose&lt;/p>
$$
\mathrm{FED_{tot}} = \left(\mathrm{FED_{CO}} + \mathrm{FED_{CN}} + \mathrm{FED_{NO_x}} + \mathrm{FLD_{irr}}\right)\times \mathrm{HV_{CO_2}} + \mathrm{FED_{O_2}}
$$&lt;p>adds the irritant lethal dose into the incapacitation sum, and its Table 2
lists both the lethal doses \(F_{FLD}\) and the incapacitating concentrations
\(F_{FIC}\). The values in that table agree with the SFPE columns of Tables
63.6 and 63.7. This sum is the Purser / FDS+Evac guide form. It is not the
ISO 13571 form, which keeps irritants out of the FED altogether.&lt;/p>
&lt;h2>Known limits&lt;span class="hx-absolute -hx-mt-20" id="known-limits">&lt;/span>
&lt;a href="#known-limits" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Human measurements at incapacitating concentrations cannot be made for
ethical reasons, and most of the experimental work behind the irritant
values used animals, mostly rodents (Ch. 63, p. 2343). The endpoint concentrations
differ between sources by up to an order of magnitude; for SO₂, Table 63.6
gives 24 ppm for SFPE escape impairment and 150 ppm for ISO incapacitation.
Individual sensitivity is wide: for HCl, Purser proposes 200 ppm as the
escape-impairment concentration for the average person, but notes that some
people may be impaired at 60 ppm and others may escape through about
400–600 ppm (p. 2343). The concentrations of acrolein and formaldehyde are
rarely known in a fire, and Ch. 63 suggests a smoke-density surrogate when
they are not (pp. 2344–2345).&lt;/p>
&lt;h2>Sources&lt;span class="hx-absolute -hx-mt-20" id="sources">&lt;/span>
&lt;a href="#sources" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>ISO (2012). &lt;em>ISO 13571:2012 Life-threatening components of fire —
Guidelines for the estimation of time to compromised tenability in
fires&lt;/em>, §4.2 and §5.4. ISO, Geneva.
&lt;a href="https://www.iso.org/standard/56172.html" target="_blank" rel="noopener">iso.org/standard/56172&lt;/a>
. Read
from the public preview.&lt;/li>
&lt;li>Purser, D. A., &amp;amp; McAllister, J. L. (2016). &lt;em>Assessment of hazards to
occupants from smoke, toxic gases, and heat&lt;/em>. SFPE Handbook of Fire
Protection Engineering, 5th ed., Ch. 63, 2308–2428. Eqs. 63.11,
63.13–63.15, 63.38, Tables 63.6–63.7.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_63" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_63&lt;/a>
&lt;/li>
&lt;li>Korhonen, T. (2021). &lt;em>Fire Dynamics Simulator with Evacuation: FDS+Evac.
Technical Reference and User&amp;rsquo;s Guide&lt;/em> (FDS 6.7.6, Evac 2.6.0 draft),
§3.4. VTT Technical Research Centre of Finland.
&lt;a href="https://github.com/tkorhon1/FDS-Evac-Guide" target="_blank" rel="noopener">github.com/tkorhon1/FDS-Evac-Guide&lt;/a>
.
Secondary source.&lt;/li>
&lt;/ul>
&lt;p>How pyFDS-Evac uses this: see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/">Fractional effective dose&lt;/a>
.&lt;/p></description></item><item><title>Usage: running simulations and producing plots</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/usage/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/usage/</guid><description>
&lt;p>This page catalogues every user-facing script in the repository: how to run
an evacuation simulation, what artefacts it writes, and which plotting
script consumes each artefact. All commands assume the project venv
(&lt;code>uv run ...&lt;/code>).&lt;/p>
&lt;h2>Running a simulation — &lt;code>run.py&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="running-a-simulation--runpy">&lt;/span>
&lt;a href="#running-a-simulation--runpy" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;code>run.py&lt;/code> is the single entry point for an evacuation run. It loads a
JSON-first JuPedSim scenario, optionally couples it to an FDS case for
smoke-speed / FED / visibility, and writes CSV/SQLite artefacts for
post-processing.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>uv run python run.py --scenario &amp;lt;scenario.json|.zip|dir&amp;gt; [options]&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h3>Scenario selection and export&lt;span class="hx-absolute -hx-mt-20" id="scenario-selection-and-export">&lt;/span>
&lt;a href="#scenario-selection-and-export" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Flag&lt;/th>
&lt;th>Purpose&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>--scenario PATH&lt;/code>&lt;/td>
&lt;td>Scenario JSON, ZIP, or directory (required).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--seed N&lt;/code>&lt;/td>
&lt;td>Override scenario seed.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--print-summary&lt;/code>&lt;/td>
&lt;td>Print the loaded scenario summary before running.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--output-sqlite PATH&lt;/code>&lt;/td>
&lt;td>Copy the JuPedSim trajectory SQLite here. When FED is computed, also writes an optional &lt;code>agent_scalars(frame, id, fed, heat_fed, speed)&lt;/code> side table (base JuPedSim schema untouched) so &lt;a href="https://github.com/PedestrianDynamics/fds-viewer" target="_blank" rel="noopener">fds-viewer&lt;/a>
can colour agents by FED or speed.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--cleanup&lt;/code>&lt;/td>
&lt;td>Delete the temp SQLite after the run.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--export-app-bundle DIR&lt;/code>&lt;/td>
&lt;td>Write &lt;code>config.json&lt;/code> and &lt;code>geometry.wkt&lt;/code> for the app.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--export-only&lt;/code>&lt;/td>
&lt;td>Export the bundle without running the simulation.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>FDS coupling (smoke / FED / visibility)&lt;span class="hx-absolute -hx-mt-20" id="fds-coupling-smoke--fed--visibility">&lt;/span>
&lt;a href="#fds-coupling-smoke--fed--visibility" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Your FDS case has to dump specific slices before any of this works. See
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/fds-case-requirements/">fds-case-requirements.md&lt;/a>
for the deck lines to add
and two failure modes (FED silently disabled, wrong slice height) that stay
silent unless you check the warning log.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Flag&lt;/th>
&lt;th>Purpose&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>--fds-dir DIR&lt;/code>&lt;/td>
&lt;td>FDS result directory driving smoke-speed and FED.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--constant-extinction K&lt;/code>&lt;/td>
&lt;td>Use a constant &lt;code>K&lt;/code> [1/m] instead of FDS.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--smoke-update-interval S&lt;/code>&lt;/td>
&lt;td>Seconds between smoke-speed refreshes.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--smoke-slice-height M&lt;/code>&lt;/td>
&lt;td>FDS slice height (m) for extinction sampling.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--output-smoke-history CSV&lt;/code>&lt;/td>
&lt;td>Write &lt;code>(t, agent, K, v, factor)&lt;/code> CSV.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--output-fed-history CSV&lt;/code>&lt;/td>
&lt;td>Write per-agent per-sample FED+species CSV.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--inspect-fds&lt;/code>&lt;/td>
&lt;td>Inspect FDS quantities (like &lt;code>scripts/inspect_fds.py&lt;/code>) and exit.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Dynamic rerouting (smoke-aware route choice)&lt;span class="hx-absolute -hx-mt-20" id="dynamic-rerouting-smoke-aware-route-choice">&lt;/span>
&lt;a href="#dynamic-rerouting-smoke-aware-route-choice" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Flag&lt;/th>
&lt;th>Purpose&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>--enable-rerouting&lt;/code> / &lt;code>--no-enable-rerouting&lt;/code>&lt;/td>
&lt;td>Let agents re-evaluate exits during the run (on by default).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--reroute-interval S&lt;/code>&lt;/td>
&lt;td>Seconds between per-agent reevaluations (default 1).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--output-route-history CSV&lt;/code>&lt;/td>
&lt;td>Write route switches.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--output-route-cost-history CSV&lt;/code>&lt;/td>
&lt;td>Ranked route cost snapshots.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--vis-cache NPZ&lt;/code>&lt;/td>
&lt;td>Path to a vismap &lt;code>.npz&lt;/code> cache — written if missing, loaded if present. Requires rerouting to be enabled (the run aborts otherwise). With &lt;code>--fds-dir&lt;/code> it caches the smoke-aware vismap; without one, the clear-air grid.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--clear-air-visibility&lt;/code>&lt;/td>
&lt;td>Force sight gating on a deck with no fire. Conflicts with &lt;code>--fds-dir&lt;/code> (a deck with a fire has smoke to decide sight) and with &lt;code>--no-visibility&lt;/code>.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--no-visibility&lt;/code>&lt;/td>
&lt;td>Turn sight gating off entirely; agents then learn each node&amp;rsquo;s neighbours by contact. Not a fire scenario.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--vis-cell-size M&lt;/code>&lt;/td>
&lt;td>Resolution of the clear-air visibility grid (default 0.25 m). Keep it below the thinnest wall that must block sight.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;strong>The default route-choice model does not trigger the visibility model.&lt;/strong> The
default &lt;code>&amp;quot;gate&amp;quot;&lt;/code> route-cost model reads no vismap: its smoke criterion is the
optical depth along the route polyline, and sign legibility only decides what
enters an agent&amp;rsquo;s cognitive map. A deck whose agents all start fully familiar therefore builds
no visibility model at all unless you pass &lt;code>--vis-cache&lt;/code> or
&lt;code>--clear-air-visibility&lt;/code>. A deck with discovery agents builds one either way,
because they need it to learn the graph. See
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/">route-cost-gate.md&lt;/a>
.&lt;/p>
&lt;h3>Tenability (FIC slowdown + FED incapacitation)&lt;span class="hx-absolute -hx-mt-20" id="tenability-fic-slowdown--fed-incapacitation">&lt;/span>
&lt;a href="#tenability-fic-slowdown--fed-incapacitation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>A FED model is instantiated automatically when &lt;code>--fds-dir&lt;/code> points at
an FDS case that exposes the FED species (CO, CO₂, O₂ at
minimum — HCN, NO/NO₂, and irritants are used if present). When that
happens, the Purser FIC slowdown and the FED incapacitation rule
are on by default. Without &lt;code>--fds-dir&lt;/code> (or with a case missing the
required species) no FED is computed and these flags have no effect;
a case with a &lt;code>TEMPERATURE&lt;/code> slice still gets heat incapacitation.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Flag&lt;/th>
&lt;th>Purpose&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>--disable-tenability&lt;/code>&lt;/td>
&lt;td>Turn both rules off.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--fic-alpha F&lt;/code>&lt;/td>
&lt;td>Slope of &lt;code>v/v₀ = max(μ, 1 − α·FIC)&lt;/code> (default 0.7).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--fic-min-factor F&lt;/code>&lt;/td>
&lt;td>Floor &lt;code>μ&lt;/code> (default 0.3).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--fed-threshold F&lt;/code>&lt;/td>
&lt;td>Median FED at which agents are incapacitated (default 1.0); each agent draws its own threshold unless &lt;code>--incapacitation-mode deterministic&lt;/code>.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Agent visualisation&lt;span class="hx-absolute -hx-mt-20" id="agent-visualisation">&lt;/span>
&lt;a href="#agent-visualisation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Agent 3-D visualisation is handled by
&lt;a href="https://github.com/PedestrianDynamics/fds-viewer" target="_blank" rel="noopener">fds-viewer&lt;/a>
, which
loads the JuPedSim trajectory SQLite written by &lt;code>--output-sqlite&lt;/code> (and
its optional &lt;code>agent_scalars&lt;/code> table for FED/speed colouring).&lt;/p>
&lt;h3>Typical invocations&lt;span class="hx-absolute -hx-mt-20" id="typical-invocations">&lt;/span>
&lt;a href="#typical-invocations" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># Bare JuPedSim run, no smoke coupling&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>uv run python run.py --scenario assets/t_junction/config.json
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># Smoke-coupled run with all diagnostic outputs&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>uv run python run.py --scenario assets/t_junction/config.json &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --fds-dir assets/t_junction &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-sqlite demo3.sqlite &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-smoke-history smoke.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-fed-history fed.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-route-history routes.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-route-cost-history route_costs.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --enable-rerouting &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --vis-cache assets/t_junction/vismap_cache.npz&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h2>Inspecting an FDS case&lt;span class="hx-absolute -hx-mt-20" id="inspecting-an-fds-case">&lt;/span>
&lt;a href="#inspecting-an-fds-case" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;code>scripts/inspect_fds.py&lt;/code> summarises what quantities FDS wrote and whether
they are within tenability-relevant ranges.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python scripts/inspect_fds.py assets/t_junction --plot --height 2.0&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h2>Probing FED without running a simulation&lt;span class="hx-absolute -hx-mt-20" id="probing-fed-without-running-a-simulation">&lt;/span>
&lt;a href="#probing-fed-without-running-a-simulation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;code>scripts/probe_fed.py&lt;/code> integrates FED at fixed &lt;code>(x, y)&lt;/code> points directly
from the FDS output. Useful as a sanity check (&amp;ldquo;if an agent stood still
here, would it be incapacitated?&amp;rdquo;).&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python scripts/probe_fed.py --fds-dir assets/t_junction &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --point 17,12 --point 20,12 &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output probe.csv --plot probe.png&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h2>Plotting&lt;span class="hx-absolute -hx-mt-20" id="plotting">&lt;/span>
&lt;a href="#plotting" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>All plotting scripts are pure post-processors: they consume one of the
CSV/SQLite artefacts produced by &lt;code>run.py&lt;/code> and write PNGs.&lt;/p>
&lt;h3>Artefact → plotting-script map&lt;span class="hx-absolute -hx-mt-20" id="artefact--plotting-script-map">&lt;/span>
&lt;a href="#artefact--plotting-script-map" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Artefact (run.py flag that writes it)&lt;/th>
&lt;th>Plotting scripts&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>FED history (&lt;code>--output-fed-history fed.csv&lt;/code>)&lt;/td>
&lt;td>&lt;code>plot_fed_history.py&lt;/code>, &lt;code>plot_trajectories_by_speed.py&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Smoke history (&lt;code>--output-smoke-history smoke.csv&lt;/code>)&lt;/td>
&lt;td>&lt;code>plot_smoke_history.py&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Route cost history (&lt;code>--output-route-cost-history route_costs.csv&lt;/code>)&lt;/td>
&lt;td>&lt;code>plot_route_costs.py&lt;/code>, &lt;code>plot_exit_choice.py&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>JuPedSim SQLite (&lt;code>--output-sqlite demo3.sqlite&lt;/code>)&lt;/td>
&lt;td>&lt;code>plot_trajectories.py&lt;/code>, &lt;code>plot_trajectories_by_speed.py&lt;/code> (backdrop)&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>FED curves — &lt;code>plot_fed_history.py&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="fed-curves--plot_fed_historypy">&lt;/span>
&lt;a href="#fed-curves--plot_fed_historypy" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>uv run python scripts/plot_fed_history.py fed.csv [options]&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Flag&lt;/th>
&lt;th>Mode&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;em>(default)&lt;/em>&lt;/td>
&lt;td>Spaghetti plot: one cumulative-FED line per agent.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--show-rate&lt;/code>&lt;/td>
&lt;td>Add a second panel with the FED rate.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--stack AGENT_ID&lt;/code>&lt;/td>
&lt;td>Per-species stacked FED breakdown for one agent.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--stack-all DIR&lt;/code>&lt;/td>
&lt;td>One stacked plot per agent, written as &lt;code>DIR/fed_agent_NNNN.png&lt;/code>.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--speed-vs-fed&lt;/code>&lt;/td>
&lt;td>Scatter of desired speed vs cumulative FED, coloured by time.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--speed-and-fed AGENT_ID&lt;/code>&lt;/td>
&lt;td>Dual-axis time series (speed + FED) for one agent.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--threshold F&lt;/code>&lt;/td>
&lt;td>Threshold line (default 1.0).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--title STR&lt;/code>&lt;/td>
&lt;td>Override title.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--output PNG&lt;/code>&lt;/td>
&lt;td>Write PNG instead of showing.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Trajectories coloured by speed — &lt;code>plot_trajectories_by_speed.py&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="trajectories-coloured-by-speed--plot_trajectories_by_speedpy">&lt;/span>
&lt;a href="#trajectories-coloured-by-speed--plot_trajectories_by_speedpy" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>uv run python scripts/plot_trajectories_by_speed.py fed.csv \
--sqlite demo3.sqlite --output trajs.png&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Per-segment RdBu colouring (red = slow, blue = fast) from the extended
FED CSV. The walkable area is drawn as backdrop via pedpy.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Flag&lt;/th>
&lt;th>Purpose&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>--sqlite PATH&lt;/code>&lt;/td>
&lt;td>JuPedSim SQLite; backdrop via &lt;code>pedpy.load_walkable_area_from_jupedsim_sqlite&lt;/code>.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--agents 7,8,43&lt;/code>&lt;/td>
&lt;td>Comma-separated agent ids (default: all).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--vmax F&lt;/code>&lt;/td>
&lt;td>Upper bound for the colormap (default: data max).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--linewidth F&lt;/code>&lt;/td>
&lt;td>Polyline width (default 0.5).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--alpha F&lt;/code>&lt;/td>
&lt;td>Polyline transparency (default 0.4).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--title STR&lt;/code> / &lt;code>--output PNG&lt;/code>&lt;/td>
&lt;td>As usual.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Smoke history — &lt;code>plot_smoke_history.py&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="smoke-history--plot_smoke_historypy">&lt;/span>
&lt;a href="#smoke-history--plot_smoke_historypy" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>uv run python scripts/plot_smoke_history.py --input smoke.csv --output smoke.png
uv run python scripts/plot_smoke_history.py --input smoke.csv --output smoke_43.png --agent-id 43&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Flag&lt;/th>
&lt;th>Purpose&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>--input CSV&lt;/code> (required)&lt;/td>
&lt;td>Smoke history CSV.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--output PNG&lt;/code> (required)&lt;/td>
&lt;td>Output path.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--agent-id N&lt;/code>&lt;/td>
&lt;td>Single-agent plot instead of the aggregate.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Trajectories coloured by exit — &lt;code>plot_trajectories.py&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="trajectories-coloured-by-exit--plot_trajectoriespy">&lt;/span>
&lt;a href="#trajectories-coloured-by-exit--plot_trajectoriespy" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Reads &lt;code>trajectory_data&lt;/code> from the run&amp;rsquo;s SQLite: what agents &lt;em>did&lt;/em>, as opposed to
what &lt;code>rank_routes&lt;/code> says they would do.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>uv run python scripts/plot_trajectories.py &amp;lt;traj.sqlite&amp;gt; \
--config &amp;lt;config.json&amp;gt; -o out.png [--title &amp;#34;...&amp;#34;] \
[--route-history routes.csv] [--geometry geometry.wkt] [--reach 1.5]&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Flag&lt;/th>
&lt;th>Effect&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>--config&lt;/code> (required)&lt;/td>
&lt;td>Exit polygons, and the colour key.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>-o/--out&lt;/code> (required)&lt;/td>
&lt;td>Output PNG.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--route-history&lt;/code>&lt;/td>
&lt;td>&lt;code>run.py --output-route-history&lt;/code> CSV. Colours each path by the exit targeted &lt;strong>at that moment&lt;/strong> and marks every switch with a dot. Without it paths are coloured by the exit finally reached, which hides mid-run decisions entirely.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--geometry&lt;/code>&lt;/td>
&lt;td>Walkable-area WKT; defaults to &lt;code>geometry.wkt&lt;/code> beside the config.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--reach&lt;/code>&lt;/td>
&lt;td>Metres from an exit polygon that count as having reached it (default 1.5). Agents that finish elsewhere are drawn grey and counted separately.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Route cost curves — &lt;code>plot_route_costs.py&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="route-cost-curves--plot_route_costspy">&lt;/span>
&lt;a href="#route-cost-curves--plot_route_costspy" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Mean composite cost per exit over time.&lt;/p>
&lt;p>&lt;strong>Under the default &lt;code>&amp;quot;gate&amp;quot;&lt;/code> cost model this plot is not what ranks exits.&lt;/strong>
The script reads the &lt;code>composite_cost&lt;/code> column, which the gate computes and
reports but does not order on — it orders on &lt;code>tau_route&lt;/code>, the route&amp;rsquo;s optical
depth, with &lt;code>rank_cost&lt;/code> (travel time plus weighted queue time) as tie-break. Read the curves as a smoke-exposure diagnostic, and
take exit choice from &lt;code>plot_exit_choice.py&lt;/code>, the &lt;code>tau_route&lt;/code> column or the
&lt;code>rejection_reason&lt;/code> column instead.&lt;/p>
&lt;p>The CSV also carries the gate&amp;rsquo;s own diagnostics — &lt;code>tau_route&lt;/code>, &lt;code>rank_cost&lt;/code>,
&lt;code>k_max_route&lt;/code>, &lt;code>k_leg_max&lt;/code>, &lt;code>clean&lt;/code> and &lt;code>feasible&lt;/code> — which is what
makes a gate decision auditable: &lt;code>feasible&lt;/code> says whether the route was
available, &lt;code>tau_route&lt;/code> is the optical depth that both refused it and ordered
it, and &lt;code>rank_cost&lt;/code> breaks ties between routes of equal &lt;code>tau_route&lt;/code>. &lt;code>k_leg_max&lt;/code> is the smokiest leg of the route and
&lt;code>clean&lt;/code> whether that puts the exit in the clean-exit tier; &lt;code>clean&lt;/code> is &lt;code>False&lt;/code>
throughout unless the deck sets &lt;code>clean_extinction_threshold&lt;/code>.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>uv run python scripts/plot_route_costs.py route_costs.csv [routes.csv]&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h3>Exit choice distribution — &lt;code>plot_exit_choice.py&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="exit-choice-distribution--plot_exit_choicepy">&lt;/span>
&lt;a href="#exit-choice-distribution--plot_exit_choicepy" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Time series of how many agents target each exit at each evaluation tick,
plus a histogram of total agent-ticks per exit. Saves
&lt;code>exit_choice_plot.png&lt;/code>.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>uv run python scripts/plot_exit_choice.py route_costs.csv [routes.csv [config.json]]&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h3>Quick interactive replay — &lt;code>vis.py&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="quick-interactive-replay--vispy">&lt;/span>
&lt;a href="#quick-interactive-replay--vispy" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>A one-line JuPedSim viewer: opens an interactive animation of a trajectory
SQLite instead of writing a file. Handy for a fast look without picking plot
options.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>uv run python scripts/vis.py demo3.sqlite&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h2>Cognitive-map movies and diagnostics&lt;span class="hx-absolute -hx-mt-20" id="cognitive-map-movies-and-diagnostics">&lt;/span>
&lt;a href="#cognitive-map-movies-and-diagnostics" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>One agent&amp;rsquo;s walk, animated — &lt;code>animate_cognitive_map.py&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="one-agents-walk-animated--animate_cognitive_mappy">&lt;/span>
&lt;a href="#one-agents-walk-animated--animate_cognitive_mappy" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Renders an MP4 (requires &lt;code>ffmpeg&lt;/code> on PATH for MP4 output) of a single
agent walking a deck, with its known/unknown exits and checkpoints
colour-coded, sign facing arrows, and amber trail segments where the agent
is wandering (no known route). Runs its own simulation internally with
&lt;code>collect_cognitive_map_history=True&lt;/code> — it does not consume a &lt;code>run.py&lt;/code>
artefact.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>uv run python scripts/animate_cognitive_map.py --scenario BUNDLE_DIR \
-o cognitive_map.mp4 [--familiarity 0.0] [--agent ID] [--fps 12]&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Flag&lt;/th>
&lt;th>Purpose&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>--scenario&lt;/code> (required)&lt;/td>
&lt;td>Bundle directory with &lt;code>config.json&lt;/code> + &lt;code>geometry.wkt&lt;/code> (e.g. from &lt;code>run.py --export-app-bundle&lt;/code>).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--familiarity F&lt;/code>&lt;/td>
&lt;td>Starting familiarity scalar for the spawn distributions (default 0.0).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--agent ID&lt;/code>&lt;/td>
&lt;td>Agent to follow (default: lowest id in the run).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--seed N&lt;/code>&lt;/td>
&lt;td>Run seed (default 420); needed to reproduce a specific movie.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--fps N&lt;/code>&lt;/td>
&lt;td>Movie frame rate (default 12).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--cell-size M&lt;/code>&lt;/td>
&lt;td>Visibility grid resolution in metres (default 0.5).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--work DIR&lt;/code>&lt;/td>
&lt;td>Working directory for the deck variant and run SQLite (default &lt;code>results/cognitive_map_movie&lt;/code>).&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h2>Deriving inputs from an FDS deck&lt;span class="hx-absolute -hx-mt-20" id="deriving-inputs-from-an-fds-deck">&lt;/span>
&lt;a href="#deriving-inputs-from-an-fds-deck" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>Walkable area from FDS obstructions — &lt;code>generate_walkable_from_fds.py&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="walkable-area-from-fds-obstructions--generate_walkable_from_fdspy">&lt;/span>
&lt;a href="#walkable-area-from-fds-obstructions--generate_walkable_from_fdspy" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Subtracts an FDS deck&amp;rsquo;s blocking &lt;code>&amp;amp;OBST&lt;/code> records from its mesh footprint and
writes the interior as WKT for JuPedSim. See the script&amp;rsquo;s module docstring
for the &lt;code>XB&lt;/code> axis-pairing pitfall, zero-thickness obstructions, and how the
CAD layer name decides what blocks.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>uv run python scripts/generate_walkable_from_fds.py DECK.fds -o out.wkt \
[--z-band 0.1 1.8] [--min-hole 0.25] [--plot out.png] [--report]&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Flag&lt;/th>
&lt;th>Purpose&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>deck&lt;/code> (positional)&lt;/td>
&lt;td>FDS input file.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>-o/--out&lt;/code> (required)&lt;/td>
&lt;td>Output WKT path.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--z-band LO HI&lt;/code>&lt;/td>
&lt;td>Height band an upright occupant occupies (default 0.1 1.8).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--half-cell M&lt;/code>&lt;/td>
&lt;td>Half the grid spacing; widens zero-thickness obstructions (default 0.05).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--min-hole M2&lt;/code>&lt;/td>
&lt;td>Interior rings smaller than this are grid noise and get filled (default 0.25).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--plot PNG&lt;/code>&lt;/td>
&lt;td>Also render the walkable polygon.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--report&lt;/code>&lt;/td>
&lt;td>Print the per-layer blocked footprint and blocking verdict.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h2>Paper figures&lt;span class="hx-absolute -hx-mt-20" id="paper-figures">&lt;/span>
&lt;a href="#paper-figures" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>These are generators for figures in &lt;code>../pyFDS-Evac-paper/&lt;/code>. They don&amp;rsquo;t
need a simulation run.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Script&lt;/th>
&lt;th>Figure&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>generate_tenability_curves.py&lt;/code>&lt;/td>
&lt;td>3-panel Frantzich + FIC + combined heatmap (&lt;code>--output PATH&lt;/code>).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>generate_fed_guide_plot.py&lt;/code>&lt;/td>
&lt;td>FED guide reference curves.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>generate_iso_table21_sweep.py&lt;/code> / &lt;code>generate_iso_table22_stationary_plot.py&lt;/code>&lt;/td>
&lt;td>ISO 20414:2020 Test 18 (Table 21) sweep over extinction and walking speed / Test 19 (Table 22) stationary FED check.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>generate_routing_diagram.py&lt;/code>&lt;/td>
&lt;td>Routing / cognitive-map diagram.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>generate_smoke_density_speed_plot.py&lt;/code>&lt;/td>
&lt;td>Smoke-speed reference curve.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>generate_exit_visibility_map.py&lt;/code>&lt;/td>
&lt;td>Which exit a &lt;code>discovery&lt;/code> agent would take, gridded by position, for the two &lt;code>assets/exit_visibility_alpha&lt;/code> configs (&lt;code>-o OUT.png&lt;/code>).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>generate_cognitive_map_states.py&lt;/code>&lt;/td>
&lt;td>Known/legible/remembered exit states probed along &lt;code>assets/cognitive_map_memory&lt;/code>&amp;rsquo;s corridor (&lt;code>-o OUT.png&lt;/code>).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>generate_smoke_weight_sweep.py&lt;/code>&lt;/td>
&lt;td>How &lt;code>w_smoke&lt;/code> reprices the T-junction&amp;rsquo;s two routes, uniform vs. asymmetric smoke (&lt;code>-o OUT.png&lt;/code>).&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h2>Calibration sweeps&lt;span class="hx-absolute -hx-mt-20" id="calibration-sweeps">&lt;/span>
&lt;a href="#calibration-sweeps" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>Route-cost queue weight — &lt;code>sweep_queue_weight.py&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="route-cost-queue-weight--sweep_queue_weightpy">&lt;/span>
&lt;a href="#route-cost-queue-weight--sweep_queue_weightpy" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Sweeps &lt;code>RouteCostConfig&lt;/code>&amp;rsquo;s queue weight against Fahy Table 2&amp;rsquo;s front-door
share. Writes one scenario bundle per &lt;code>(w_queue, seed)&lt;/code>, runs each with
&lt;code>run.py&lt;/code>, and scores it against &lt;code>assets/station_fahy/validate.py&lt;/code>&amp;rsquo;s
&lt;code>observed_matrix&lt;/code>. Unlike the paper-figure scripts above, this drives real
simulation runs (in parallel processes), so it takes minutes, not seconds.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>uv run python scripts/sweep_queue_weight.py \
[--weights 0.0 0.03 0.1 ...] [--seeds 420 421 422] \
[--jobs 4] [--out results/queue_weight_sweep] [--reuse-existing]&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Flag&lt;/th>
&lt;th>Purpose&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>--weights F [F ...]&lt;/code>&lt;/td>
&lt;td>&lt;code>w_queue&lt;/code> values to sweep (default: the grid in &lt;code>docs/routing.md&lt;/code>).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--seeds N [N ...]&lt;/code>&lt;/td>
&lt;td>Seeds per weight (default &lt;code>420 421 422&lt;/code>).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--jobs N&lt;/code>&lt;/td>
&lt;td>Parallel worker processes (default 4).&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--out DIR&lt;/code>&lt;/td>
&lt;td>Output directory for &lt;code>sweep.csv&lt;/code>, &lt;code>summary.csv&lt;/code>, &lt;code>sweep.png&lt;/code>.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>--reuse-existing&lt;/code>&lt;/td>
&lt;td>Score an existing &lt;code>run.sqlite&lt;/code> instead of rerunning it, if its deck/seed digest still matches.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h2>One-shot driver — &lt;code>scripts/run_and_plot.sh&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="one-shot-driver--scriptsrun_and_plotsh">&lt;/span>
&lt;a href="#one-shot-driver--scriptsrun_and_plotsh" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Runs one simulation and produces the full plot set into a results
directory.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>./scripts/run_and_plot.sh assets/t_junction/config.json assets/t_junction results/demo&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Arguments: &lt;code>&amp;lt;scenario&amp;gt; &amp;lt;fds-dir&amp;gt; &amp;lt;results-dir&amp;gt;&lt;/code>. The script calls
&lt;code>run.py&lt;/code> with all diagnostic outputs enabled and then invokes each
plotting script against the resulting CSVs / SQLite.&lt;/p></description></item><item><title>Heat</title><link>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/heat/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/heat/</guid><description>
&lt;p>Heat can incapacitate in three ways: heat stroke (hyperthermia), skin pain
followed by burns, and burns to the respiratory tract (Purser and McAllister
2016, Society of Fire Protection Engineers (SFPE) Handbook Ch. 63). The first is driven mainly by hot air around the body
(convective heat), the second mainly by thermal radiation onto exposed skin.
The two are described by &lt;strong>separate equations with different endpoints&lt;/strong>,
and neither should be read as the other.&lt;/p>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
; &lt;em>T&lt;/em> is the
gas temperature [°C]. The equations below keep the SFPE Handbook&amp;rsquo;s notation
(\(t_I\), &lt;em>q&lt;/em>, &lt;em>r&lt;/em>).&lt;/p>
&lt;h2>Convective heat: time to incapacitation&lt;span class="hx-absolute -hx-mt-20" id="convective-heat-time-to-incapacitation">&lt;/span>
&lt;a href="#convective-heat-time-to-incapacitation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>For exposures of up to 2 h to convected heat from air containing less than
10 % water vapour by volume, the time to incapacitation
\(t_{I\,\mathrm{conv}}\) [min] at air temperature &lt;em>T&lt;/em> [°C] is&lt;/p>
$$
t_{I\,\mathrm{conv}} = 5\times10^{7}\,T^{-3.4} \qquad \text{(Eq. 63.44)}
$$&lt;p>derived from the tolerance data in Fig. 63.28. Purser notes that the
expression follows the worst-case (100 % humidity) line and deviates from
Blockley&amp;rsquo;s curve at the ends: it is somewhat non-conservative at high
temperatures and somewhat over-conservative at low ones. For design, Ch. 63
proposes a better fit for mid-humidity conditions,&lt;/p>
$$
t_{\mathrm{tol}} = 2\times10^{31}\,T^{-16.963} + 4\times10^{8}\,T^{-3.7561} \qquad \text{(Eq. 63.45)}
$$&lt;p>with further expressions for serious injury (Eq. 63.46) and for fatal
exposure (Eq. 63.47). Thermal tolerance data for unprotected skin suggest a
limit of about 120 °C for convected heat, above which considerable pain
occurs quickly (Ch. 63, p. 2383 and Table 63.20). Burns to the respiratory
tract do not occur from air with less than 10 % water vapour in the absence
of burns to the facial skin, but saturated air above only 60 °C can cause
them (p. 2382).&lt;/p>
&lt;h2>Radiant heat: pain and burns&lt;span class="hx-absolute -hx-mt-20" id="radiant-heat-pain-and-burns">&lt;/span>
&lt;a href="#radiant-heat-pain-and-burns" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The tenability limit for radiant heat on skin is about
&lt;strong>2.5 kW/m²&lt;/strong>, below which exposure can be tolerated for at least several
minutes; at and above it, pain is followed by burns within seconds
(Ch. 63, p. 2382 and Table 63.20). Below
this flux the radiant contribution is neglected. Above it, the time
\(t_{I\,\mathrm{rad}}\) [min] to a given endpoint at radiant flux
&lt;em>q&lt;/em> [kW/m²] is&lt;/p>
$$
t_{I\,\mathrm{rad}} = \frac{r}{q^{1.33}} \qquad \text{(Eq. 63.43)}
$$&lt;p>where &lt;em>r&lt;/em> [(kW/m²)^4/3·min] is the dose for the endpoint: about 1.33–1.67 for
severe skin pain, 4.0–12.2 for second-degree burns and 16.7 for third-degree
burns. Purser proposes 1.33 as a tolerance threshold and 10 as a threshold
for incapacitation and serious injury (p. 2382). For occupants passing
under a hot smoke layer, 2.5 kW/m² corresponds approximately to a layer
temperature of 200 °C (p. 2382).&lt;/p>
&lt;h2>Combining the two&lt;span class="hx-absolute -hx-mt-20" id="combining-the-two">&lt;/span>
&lt;a href="#combining-the-two" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Ch. 63 describes two ways to account for both. One sums the fractional
effective doses (FED) over time,&lt;/p>
$$
\mathrm{FED} = \int_{t_1}^{t_2}\left(\frac{1}{t_{I\,\mathrm{rad}}} + \frac{1}{t_{I\,\mathrm{conv}}}\right)\mathrm{d}t \qquad \text{(Eq. 63.48)}
$$&lt;p>valid while the temperature is stable or increasing. The other computes the
total heat flux to the skin from radiant and convective components (Eq. 63.49)
and applies Eq. 63.43 to it. ISO 13571:2012 (§4.4) likewise assesses heat
and radiant energy with an FED model analogous to the gas model. Note (not
from the sources): a summed dose is interpretable only when both terms are
taken for the same endpoint, for example incapacitation (&lt;em>r&lt;/em> = 10) with
Eq. 63.44; the radiant pain dose (&lt;em>r&lt;/em> = 1.33) is a different endpoint.&lt;/p>
&lt;h2>Known limits&lt;span class="hx-absolute -hx-mt-20" id="known-limits">&lt;/span>
&lt;a href="#known-limits" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Some cross-references in the text of Ch. 63 do not match the printed
equation labels: the text calls the radiant equation &amp;ldquo;Equation 63.41&amp;rdquo; and the
summed dose &amp;ldquo;Equation 63.46&amp;rdquo;. The labels above are those printed beside each
equation.
The convective data concern hyperthermia in air of low humidity, and the
radiant data concern bare skin: clothing changes both. None of these
equations describe the effect of heat on walking speed or on route choice.&lt;/p>
&lt;h2>Sources&lt;span class="hx-absolute -hx-mt-20" id="sources">&lt;/span>
&lt;a href="#sources" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>Purser, D. A., &amp;amp; McAllister, J. L. (2016). &lt;em>Assessment of hazards to
occupants from smoke, toxic gases, and heat&lt;/em>. SFPE Handbook of Fire
Protection Engineering, 5th ed., Ch. 63, 2308–2428. Eqs. 63.43–63.49,
Fig. 63.28 and Table 63.20.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_63" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_63&lt;/a>
&lt;/li>
&lt;li>ISO (2012). &lt;em>ISO 13571:2012 Life-threatening components of fire —
Guidelines for the estimation of time to compromised tenability in
fires&lt;/em>, §4.4. ISO, Geneva.
&lt;a href="https://www.iso.org/standard/56172.html" target="_blank" rel="noopener">iso.org/standard/56172&lt;/a>
.&lt;/li>
&lt;/ul>
&lt;p>How pyFDS-Evac uses this: see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#convective-heat">Fractional effective dose&lt;/a>
.&lt;/p></description></item><item><title>Incapacitation thresholds</title><link>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/incapacitation-thresholds/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/incapacitation-thresholds/</guid><description>
&lt;p>A fractional effective dose (FED) of 1 is, by definition, the dose at which
a person of median susceptibility is predicted to be incapacitated. Half of a
population is more susceptible. Design therefore uses a lower threshold, most
often 0.3. This page gives the published rationale for that value and the
limits of its population basis.&lt;/p>
&lt;p>Symbols follow the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/concepts/#notation">notation table&lt;/a>
.&lt;/p>
&lt;h2>The ISO 13571 rationale&lt;span class="hx-absolute -hx-mt-20" id="the-iso-13571-rationale">&lt;/span>
&lt;a href="#the-iso-13571-rationale" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>ISO 13571:2012 (§4.1, §5.4) assumes a priori that occupant responses follow a
log-normal distribution about a median. The standard states that, in the
absence of actual data, the log-normal is the most defensible choice. FED and
fractional effective concentration (FEC) values of 1.0 correspond by
definition to the median, and users &amp;ldquo;shall use reduced FED and/or FEC
threshold criteria&amp;rdquo; for more conservative objectives (§5.4). The earlier
edition, ISO 13571:2007, gave 0.3 as an example threshold for most general
occupancies, noted that &amp;ldquo;the distribution of human responses to fire gases is
not known&amp;rdquo;, and stated that at 0.3 about 11.4 % of the population would still
be susceptible, adding that no threshold is statistically safe for every
occupant (§5, Note). ISO/TR 13571-2:2016 repeats the 11.4 % figure for 0.3,
citing ISO 13571:2012, A.5.2.&lt;/p>
&lt;h2>What the handbook says&lt;span class="hx-absolute -hx-mt-20" id="what-the-handbook-says">&lt;/span>
&lt;a href="#what-the-handbook-says" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Purser and McAllister (2016, Society of Fire Protection Engineers (SFPE)
Handbook Ch. 63) state that the endpoints of their
equations represent the median of the distribution, and that approximately
11.3 % of the population is likely to be susceptible below an FED of 0.3,
citing ISO 13571 (p. 2334; repeated on p. 2415). In the same paragraph they
write that approximately 90 % of the population is susceptible below an FED
of 1.3. For a log-normal with
median 1, these two statements are not consistent: 11.3 % below 0.3 implies a
log-scale standard deviation near 1.0, whereas 90 % below 1.3 implies about
0.2. The source does not give the distribution parameters that would resolve
this. Ch. 63 also notes that, because gas concentrations rise quickly in most
flaming fires, variations in individual susceptibility have relatively minor
effects on predicted times to incapacitation (p. 2334).&lt;/p>
&lt;h2>NIST Technical Note 1797&lt;span class="hx-absolute -hx-mt-20" id="nist-technical-note-1797">&lt;/span>
&lt;a href="#nist-technical-note-1797" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The National Institute of Standards and Technology (NIST) Technical Note (TN)
1797 is often cited for population bands.&lt;/p>
&lt;p>Averill et al. (2013), a report on high-rise fireground field experiments,
bins FED into four ranges with estimated population fractions incapacitated:
up to 11 % below 0.3, 11–50 % between 0.3 and 1.0, 50–89 % between 1.0 and
3.0, and more than 89 % above 3.0 (Table 8). The report cites ISO 13571
(2007) as the basis of FED. It contains no population data of its own, it
states that &amp;ldquo;the detailed probabilistic relationship between FED and the
percentage of people incapacitated is unknown&amp;rdquo;, and its uncertainty appendix
cites an uncertainty of as much as ±20 % to 35 % (Appendix F). Its FED
included CO, CO₂ and O₂ only.&lt;/p>
&lt;h2>What the numbers support&lt;span class="hx-absolute -hx-mt-20" id="what-the-numbers-support">&lt;/span>
&lt;a href="#what-the-numbers-support" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>A log-normal with median 1 cannot match both TN 1797 bins exactly: 11 % at
0.3 implies a log-scale standard deviation of about 0.98, and 89 % at 3.0
implies about 0.90. Any single value is a compromise between two bin edges
that themselves rest on an assumed distribution, not on measured human
incapacitation. The 0.3 threshold and the 11 % figure are best read as a
design convention derived from the log-normal assumption. For reference
(arithmetic, not a statement of the sources): a log-normal with median 1 and
log-scale standard deviation 1.0 puts 11.4 % below 0.3, ISO&amp;rsquo;s figure; the
preview does not state which standard deviation ISO used.&lt;/p>
&lt;h2>Sources&lt;span class="hx-absolute -hx-mt-20" id="sources">&lt;/span>
&lt;a href="#sources" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>ISO (2012). &lt;em>ISO 13571:2012 Life-threatening components of fire —
Guidelines for the estimation of time to compromised tenability in
fires&lt;/em>, §4.1 and §5.4. ISO, Geneva.
&lt;a href="https://www.iso.org/standard/56172.html" target="_blank" rel="noopener">iso.org/standard/56172&lt;/a>
. Read
from the public preview; Annex A.5.2 was not available.&lt;/li>
&lt;li>ISO (2007). &lt;em>ISO 13571:2007 Life-threatening components of fire —
Guidelines for the estimation of time available for escape using fire
data&lt;/em>, §5. ISO, Geneva. Withdrawn.
&lt;a href="https://www.iso.org/standard/42967.html" target="_blank" rel="noopener">iso.org/standard/42967&lt;/a>
. Read
from the public preview.&lt;/li>
&lt;li>ISO (2016). &lt;em>ISO/TR 13571-2:2016 Life-threatening components of fire —
Part 2: Methodology and examples of tenability assessment&lt;/em>. ISO, Geneva.
&lt;a href="https://www.iso.org/standard/65996.html" target="_blank" rel="noopener">iso.org/standard/65996&lt;/a>
. Read
from the public preview.&lt;/li>
&lt;li>Purser, D. A., &amp;amp; McAllister, J. L. (2016). &lt;em>Assessment of hazards to
occupants from smoke, toxic gases, and heat&lt;/em>. SFPE Handbook of Fire
Protection Engineering, 5th ed., Ch. 63, 2308–2428.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_63" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_63&lt;/a>
&lt;/li>
&lt;li>Averill, J. D., Moore-Merrell, L., Ranellone, R. T., Jr., Weinschenk, C.,
Taylor, N., Goldstein, R., Santos, R., Wissoker, D., &amp;amp; Notarianni, K. A.
(2013). &lt;em>Report on high-rise fireground field experiments&lt;/em> (K. M.
Butler, Ed.). NIST Technical Note 1797.
&lt;a href="https://doi.org/10.6028/NIST.TN.1797" target="_blank" rel="noopener">doi:10.6028/NIST.TN.1797&lt;/a>
&lt;/li>
&lt;/ul>
&lt;p>How pyFDS-Evac uses this: see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#tenability-irritant-slowdown-and-incapacitation">Fractional effective dose&lt;/a>
.&lt;/p></description></item><item><title>Limitations</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/limitations/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/limitations/</guid><description>
&lt;h2>Status&lt;span class="hx-absolute -hx-mt-20" id="status">&lt;/span>
&lt;a href="#status" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>pyFDS-Evac is research software, provided without warranty. It is not intended
for regulatory or design use. It is maintained by one researcher at
Forschungszentrum Jülich (IAS-7) and has no release policy yet, so behaviour
and defaults can change between commits. A result from pyFDS-Evac is a
research result. It is not an assessment of a building.&lt;/p>
&lt;p>The evidence that exists is listed on the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/verification/" target="_blank" rel="noopener">verification page&lt;/a>
.
That evidence is verification (the code does what its equations say), not
validation (the equations describe how people behave).&lt;/p>
&lt;h2>Some parameters are library-level&lt;span class="hx-absolute -hx-mt-20" id="some-parameters-are-library-level">&lt;/span>
&lt;a href="#some-parameters-are-library-level" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Not every model parameter is reachable from the command line or the scenario
JSON. The smoke-speed parameters (&lt;code>speed_law&lt;/code>, &lt;code>alpha&lt;/code>, &lt;code>beta&lt;/code>,
&lt;code>min_speed_factor&lt;/code> and &lt;code>visibility_factor_c&lt;/code>) are fields of
&lt;code>SmokeSpeedConfig&lt;/code>. &lt;code>run.py&lt;/code> and the web GUI build that object with its
defaults, so every run they start uses the Frantzich–Nilsson law (the &lt;code>lund&lt;/code>
option; the linear FDS+Evac law) with the defaults listed on the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/#parameters">smoke-speed model&lt;/a>
page. Another law or
other coefficients require building the model in Python and passing it to
&lt;code>run_scenario()&lt;/code>.&lt;/p>
&lt;p>The JSON &lt;code>routing&lt;/code> block has keys with the same names. They parameterise the
speed factor used to estimate travel time when a route is priced, not the
walking speed. Setting &lt;code>routing.alpha&lt;/code> therefore changes which route an agent
prefers but leaves its speed in smoke unchanged. The same split applies to
speed itself: &lt;code>routing.base_speed_m_per_s&lt;/code> (1.3 m/s) prices routes, while an
agent walks at its own &lt;code>v0&lt;/code> (1.2 m/s by default).&lt;/p>
&lt;h2>Incapacitation is probabilistic by default&lt;span class="hx-absolute -hx-mt-20" id="incapacitation-is-probabilistic-by-default">&lt;/span>
&lt;a href="#incapacitation-is-probabilistic-by-default" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>FED below means fractional effective dose. By default
(&lt;code>--incapacitation-mode probabilistic&lt;/code>), each agent draws its own
incapacitation threshold from a log-normal distribution with median
&lt;code>--fed-threshold&lt;/code> and log-scale spread &lt;code>--susceptibility-sigma&lt;/code> (defaults on
the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#tenability-irritant-slowdown-and-incapacitation">FED model&lt;/a>
page).
About half of the agents therefore stop below FED = 1, and about 10 % stop
below FED = 0.3. The default spread is fitted to the incapacitation fractions
of NIST TN 1797
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/148" target="_blank" rel="noopener">#148&lt;/a>
). The
heat dose uses the same mechanism, and its spread is reused from the gas value
without a data basis of its own. FDS+Evac stops
every agent at FED = 1. For results comparable with FDS+Evac, run with
&lt;code>--incapacitation-mode deterministic&lt;/code> (and &lt;code>--heat-incapacitation-mode deterministic&lt;/code> for the heat dose).&lt;/p>
&lt;h2>Evacuation time when anyone is incapacitated&lt;span class="hx-absolute -hx-mt-20" id="evacuation-time-when-anyone-is-incapacitated">&lt;/span>
&lt;a href="#evacuation-time-when-anyone-is-incapacitated" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>An incapacitated agent stays in the simulation as a stationary obstacle. The
run ends early only when no agent is left, so a run in which any agent is
incapacitated continues until &lt;code>max_simulation_time&lt;/code> (300 s by default). Its
reported &lt;code>evacuation_time&lt;/code> is then that time limit, not the time the last
mobile agent left. &lt;code>success&lt;/code> is also true at the time limit even when agents
remain (&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/139" target="_blank" rel="noopener">issue #139&lt;/a>
).
Read &lt;code>agents_remaining&lt;/code>, and take the time each agent left from the
trajectory file, instead of relying on these two values
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/141" target="_blank" rel="noopener">issue #141&lt;/a>
).&lt;/p>
&lt;h2>Incapacitation during pre-movement is undone&lt;span class="hx-absolute -hx-mt-20" id="incapacitation-during-pre-movement-is-undone">&lt;/span>
&lt;a href="#incapacitation-during-pre-movement-is-undone" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>An agent that is incapacitated while it is still waiting out its
pre-movement time starts walking when that time ends
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/145" target="_blank" rel="noopener">issue #145&lt;/a>
).
Scenarios that combine pre-movement with FED therefore under-report
incapacitations and over-report evacuees. Until this is fixed, check the
per-agent FED history for agents that crossed their threshold and still
reached an exit.&lt;/p>
&lt;h2>What is not modelled&lt;span class="hx-absolute -hx-mt-20" id="what-is-not-modelled">&lt;/span>
&lt;a href="#what-is-not-modelled" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;strong>Radiant heat.&lt;/strong> The heat dose is the convective term of Purser and
McAllister (SFPE Handbook, 5th ed., Eq. 63.44), computed from the gas
temperature of an FDS &lt;code>TEMPERATURE&lt;/code> slice. Radiant heat flux is not read and
does not contribute to any dose. An agent near a flame or under a hot layer
that radiates strongly is therefore treated as less exposed than it is.&lt;/p>
&lt;p>&lt;strong>Heat does not affect route choice or walking speed.&lt;/strong> The heat dose is
tracked per agent, separately from the toxic dose, and an agent is
incapacitated when either dose reaches its threshold. Before that point, heat
has no effect. Route choice is given the toxic dose only, so an agent can
choose a route that will incapacitate it thermally. Walking speed is reduced
by extinction and by irritant gases, but not by temperature, so an agent walks
at full speed through a hot layer until the heat dose is reached.&lt;/p>
&lt;p>&lt;strong>Multi-floor buildings and stairs.&lt;/strong> The walkable area is a single 2-D
polygon, and hazards are sampled from slices at one height
(&lt;code>--smoke-slice-height&lt;/code>). There is no stair model, no floor-to-floor
connection and no speed reduction on inclines. &lt;code>generate_walkable_from_fds.py&lt;/code>
treats stair treads and risers as floor, so a staircase in an FDS deck becomes
flat floor that agents cross at full speed. A &lt;code>zones&lt;/code> entry with a
&lt;code>speed_factor&lt;/code> can slow agents inside a polygon, but it does not represent
direction of travel on a stair.&lt;/p>
&lt;p>&lt;strong>FED activity level.&lt;/strong> The CO term of the toxic dose uses one fixed
coefficient, the FDS+Evac default for light work (Korhonen 2021, Eq. 13; see
the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#coded-form">FED model&lt;/a>
). Rest and heavy work cannot be selected, although
breathing rate changes the CO dose. Not supported; see
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/135" target="_blank" rel="noopener">issue #135&lt;/a>
.&lt;/p>
&lt;p>&lt;strong>Per-exit familiarity.&lt;/strong> Familiarity is set per spawn distribution, as
&lt;code>&amp;quot;full&amp;quot;&lt;/code>, &lt;code>&amp;quot;discovery&amp;quot;&lt;/code>, or one probability applied to every exit. The
&lt;code>entrance&lt;/code> key adds one exit that the whole group knows. A separate
probability for each exit, as FDS+Evac allows with &lt;code>KNOWN_DOOR_PROBS&lt;/code>, is not
supported; see
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/136" target="_blank" rel="noopener">issue #136&lt;/a>
.&lt;/p>
&lt;p>&lt;strong>Herding and social influence.&lt;/strong> Each agent chooses its route from its own
cognitive map and the hazard along each route. When &lt;code>routing.w_queue&lt;/code> is
above zero (the default is 0.0), expected queueing at an exit also enters the
cost, so a crowded exit becomes less attractive. Agents do not follow others,
and an exit never becomes more attractive because others use it. See
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/78" target="_blank" rel="noopener">issue #78&lt;/a>
.&lt;/p>
&lt;p>&lt;strong>Perception-limited route choice.&lt;/strong> A discovery agent knows only the exits
whose signs it has read, but it prices the routes to them with the smoke
sampled along the whole route, including stretches it has never seen
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/125" target="_blank" rel="noopener">issue #125&lt;/a>
).
By default (&lt;code>routing.anticipate&lt;/code> = true, &lt;code>routing.foresight_horizon_s&lt;/code>
unbounded), each stretch is also priced with the smoke that the finished FDS
run holds for the time the agent would arrive there. Route choice is therefore
a best case with perfect foresight, not a model of what an occupant can see or
predict.&lt;/p>
&lt;p>&lt;strong>Recovery from irritants.&lt;/strong> The irritant slowdown is recomputed only while
the sampled fractional irritant concentration (FIC) is positive. When it
returns to exactly zero, the last slowdown stays in force, so an agent that
leaves an irritant plume into clean air does not return to full speed
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/142" target="_blank" rel="noopener">issue #142&lt;/a>
).&lt;/p>
&lt;p>&lt;strong>Sourced pre-movement defaults.&lt;/strong> The preset parameters of the four
pre-movement distributions are illustrative, not from a cited dataset. Set
&lt;code>premovement_param_a&lt;/code> and &lt;code>premovement_param_b&lt;/code> from data for your occupancy
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/144" target="_blank" rel="noopener">issue #144&lt;/a>
).&lt;/p>
&lt;p>&lt;strong>Smoke-triggered detection.&lt;/strong> Pre-movement is one delay per agent, drawn
from a distribution. Smoke reaching the agent does not end the delay early,
and there is no separate detection phase.&lt;/p>
&lt;p>&lt;strong>Feedback from occupants to the fire.&lt;/strong> The coupling is one-way. The FDS run
is finished before the evacuation starts, so occupants cannot open doors or
otherwise change the fire. The time resolution of the hazard is that of the
slice output (&lt;code>&amp;amp;DUMP DT_SLCF&lt;/code>).&lt;/p>
&lt;h2>Reproducibility&lt;span class="hx-absolute -hx-mt-20" id="reproducibility">&lt;/span>
&lt;a href="#reproducibility" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>A fixed seed does not give identical trajectories. The verification suite
found that individual trajectories differ between runs with the same seed,
while aggregate outcomes (counts and fractions, such as the number of agents
incapacitated or rerouted) reproduce. Report aggregate outcomes over several seeds, with their spread,
and do not compare single trajectories between runs. See the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/verification/" target="_blank" rel="noopener">verification page&lt;/a>
.&lt;/p>
&lt;h2>References&lt;span class="hx-absolute -hx-mt-20" id="references">&lt;/span>
&lt;a href="#references" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Korhonen, T. (2021). &lt;em>Fire Dynamics Simulator with Evacuation: FDS+Evac.
Technical Reference and User&amp;rsquo;s Guide&lt;/em>. VTT Technical Research Centre of
Finland.&lt;/p>
&lt;p>Purser, D. A., and McAllister, J. L. (2016). Assessment of hazards to
occupants from smoke, toxic gases, and heat. In &lt;em>SFPE Handbook of Fire
Protection Engineering&lt;/em>, 5th ed., Chapter 63. Springer.&lt;/p></description></item><item><title>Pre-movement time</title><link>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/pre-movement/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/pre-movement/</guid><description>
&lt;p>Pre-movement time, also called pre-travel activity time or pre-evacuation
time, \(t_{\mathrm{pre}}\) [s], is the interval between the alarm or first
cue and the first deliberate movement towards safety. It is a term of the
required safe escape time (RSET; see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/aset-rset/">ASET and RSET&lt;/a>
).&lt;/p>
&lt;h2>Definition as published&lt;span class="hx-absolute -hx-mt-20" id="definition-as-published">&lt;/span>
&lt;a href="#definition-as-published" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The Society of Fire Protection Engineers (SFPE) Handbook defines \(t_{\mathrm{pre}}\) as the interval between the
time at which a general alarm or warning is given and the time at which the
first deliberate evacuation movement is made, and splits it into a
recognition time, from perceiving the alarm to interpreting it as an
emergency, and a response time, from recognition to the first move
(Gwynne and Boyce 2016, Ch. 64). ISO/TR 16738:2009 (§5.7) uses the same two
elements and distinguishes the pre-travel time of the first occupants from
the distribution of pre-travel times over the whole group. There is no
equation for \(t_{\mathrm{pre}}\): it is taken from observed distributions.&lt;/p>
&lt;h2>Why it is long&lt;span class="hx-absolute -hx-mt-20" id="why-it-is-long">&lt;/span>
&lt;a href="#why-it-is-long" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>ISO/TR 16738 (§5.4) reports that the pre-travel activity phase can often be
the longest part of the total escape time. Response time includes activities
such as fighting the fire, warning others, gathering family members,
dressing, collecting belongings and calling the fire service (Ch. 64).
Kuligowski (2016, Ch. 58) explains these delays with the Protective Action
Decision Model, in which environmental cues such as the sight of smoke and
social cues such as warnings interrupt normal activity only if they are
perceived as a threat; the occupant then seeks more information, protects
people or property, or resumes normal activity, depending on the perceived
threat and on whether protective action seems feasible.&lt;/p>
&lt;h2>The data&lt;span class="hx-absolute -hx-mt-20" id="the-data">&lt;/span>
&lt;a href="#the-data" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Lovreglio, Kuligowski, Gwynne and Boyce (2019) assembled pre-evacuation times
from 9 fire incidents and 103 evacuation drills, covering 13 591 evacuees in
16 countries, grouped by occupancy type and clustered to identify
sub-groups. They fitted gamma, log-normal, log-logistic and Weibull
distributions, all two-parameter, positive and right-skewed, for use as
model inputs (their Eqs. 2–5). The database extends the tables of Gwynne and Boyce (2016,
Ch. 64). A corrigendum was published in 2019. ISO/TR 16738 (Annex E) gives
guidance and default pre-travel times from published data.&lt;/p>
&lt;h2>Known limits&lt;span class="hx-absolute -hx-mt-20" id="known-limits">&lt;/span>
&lt;a href="#known-limits" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Drills make up 103 of the 112 data sets, so the database describes drills
far more than fires. The authors could not definitively
establish the factors that generated the clusters, and advise users to pick
the occupancy and cluster closest to their case and to consult the original
sources. Pre-movement and travel times within an enclosure interact, so the
two distributions cannot simply be added (ISO/TR 16738, §7).&lt;/p>
&lt;h2>Sources&lt;span class="hx-absolute -hx-mt-20" id="sources">&lt;/span>
&lt;a href="#sources" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>ISO (2009). &lt;em>ISO/TR 16738:2009 Fire-safety engineering — Technical
information on methods for evaluating behaviour and movement of people&lt;/em>,
§5.4, §5.7 and §7. ISO, Geneva.
&lt;a href="https://www.iso.org/standard/42887.html" target="_blank" rel="noopener">iso.org/standard/42887&lt;/a>
. Read
from the public preview.&lt;/li>
&lt;li>Gwynne, S. M. V., &amp;amp; Boyce, K. E. (2016). &lt;em>Engineering data&lt;/em>. SFPE
Handbook of Fire Protection Engineering, 5th ed., Ch. 64, 2429–2551.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_64" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_64&lt;/a>
&lt;/li>
&lt;li>Kuligowski, E. D. (2016). &lt;em>Human behavior in fire&lt;/em>. SFPE Handbook of
Fire Protection Engineering, 5th ed., Ch. 58, 2070–2114.
&lt;a href="https://doi.org/10.1007/978-1-4939-2565-0_58" target="_blank" rel="noopener">doi:10.1007/978-1-4939-2565-0_58&lt;/a>
&lt;/li>
&lt;li>Lovreglio, R., Kuligowski, E., Gwynne, S., &amp;amp; Boyce, K. (2019). &lt;em>A
pre-evacuation database for use in egress simulations&lt;/em>. Fire Safety
Journal, 105, 107–128.
&lt;a href="https://doi.org/10.1016/j.firesaf.2018.12.009" target="_blank" rel="noopener">doi:10.1016/j.firesaf.2018.12.009&lt;/a>
.
Corrigendum: Fire Safety Journal, 108, 102829.
&lt;a href="https://doi.org/10.1016/j.firesaf.2019.102829" target="_blank" rel="noopener">doi:10.1016/j.firesaf.2019.102829&lt;/a>
&lt;/li>
&lt;li>Lovreglio, R., Ronchi, E., &amp;amp; Nilsson, D. (2015). &lt;em>A model of the
decision-making process during pre-evacuation&lt;/em>. Fire Safety Journal, 78,
168–179.
&lt;a href="https://doi.org/10.1016/j.firesaf.2015.07.001" target="_blank" rel="noopener">doi:10.1016/j.firesaf.2015.07.001&lt;/a>
(further reading; not summarised here).&lt;/li>
&lt;/ul>
&lt;p>How pyFDS-Evac uses this: see the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/howto-rset-ensemble/">RSET ensemble how-to&lt;/a>
.&lt;/p></description></item><item><title>Smoke-speed model</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/smoke-speed-model/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/smoke-speed-model/</guid><description>
&lt;blockquote>
&lt;p>Part of &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/README.md" target="_blank" rel="noopener">pyFDS-Evac&lt;/a>
.&lt;/p>&lt;/blockquote>
&lt;p>The pyFDS-Evac smoke-speed model reduces agent walking speed based on
local smoke conditions. It takes the extinction coefficient K [1/m] as
its primary input and applies one of two speed-reduction laws, selected
via &lt;code>SmokeSpeedConfig.speed_law&lt;/code>.&lt;/p>
&lt;h2>Speed-reduction laws&lt;span class="hx-absolute -hx-mt-20" id="speed-reduction-laws">&lt;/span>
&lt;a href="#speed-reduction-laws" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;code>speed_law=&amp;quot;lund&amp;quot;&lt;/code> (default) applies the linear Frantzich–Nilsson law in the
FDS+Evac fractional form, clamped to &lt;code>[min_speed_factor, 1.0]&lt;/code>;
&lt;code>speed_law=&amp;quot;fridolf&amp;quot;&lt;/code> applies &lt;code>V / (V + 2)&lt;/code> through the sighting distance
&lt;code>V = C / K&lt;/code>, with no floor. Its attribution to Fridolf et al. (2019) is
unverified: the paper&amp;rsquo;s own law differs
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/146" target="_blank" rel="noopener">#146&lt;/a>
). In both cases the
agent walks at &lt;code>v0 * speed_factor(K)&lt;/code>, where &lt;code>v0&lt;/code> is its clear-air speed.&lt;/p>
&lt;p>The coded equations, the &lt;code>SmokeSpeedConfig&lt;/code> defaults and the departures from
the literature are on the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/">smoke-speed model&lt;/a>
page;
the published laws are on
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/walking-speed/">Walking speed in smoke&lt;/a>
.&lt;/p>
&lt;h2>Extinction sources&lt;span class="hx-absolute -hx-mt-20" id="extinction-sources">&lt;/span>
&lt;a href="#extinction-sources" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The model accepts any object that implements the &lt;code>ExtinctionSampler&lt;/code>
protocol (a &lt;code>sample_extinction(time_s, x, y) -&amp;gt; float&lt;/code> method). Two
built-in implementations are available:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>&lt;code>ExtinctionField&lt;/code>&lt;/strong> &amp;ndash; reads the &lt;code>SOOT EXTINCTION COEFFICIENT&lt;/code>
quantity from FDS slice data via &lt;code>fdsreader&lt;/code>. Use this for real FDS
output.&lt;/li>
&lt;li>&lt;strong>&lt;code>ConstantExtinctionField&lt;/code>&lt;/strong> &amp;ndash; returns a fixed K value everywhere.
Use this for deterministic verification cases such as ISO 20414
Table 21.&lt;/li>
&lt;/ul>
&lt;h3>Loading from FDS data&lt;span class="hx-absolute -hx-mt-20" id="loading-from-fds-data">&lt;/span>
&lt;a href="#loading-from-fds-data" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>To load extinction data from an FDS case directory:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.smoke_speed &lt;span style="color:#f92672">import&lt;/span> ExtinctionField
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>field &lt;span style="color:#f92672">=&lt;/span> ExtinctionField&lt;span style="color:#f92672">.&lt;/span>from_fds(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">&amp;#34;path/to/fds_case&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> slice_height_m&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">2.0&lt;/span>, &lt;span style="color:#75715e"># select the horizontal slice closest to 2 m&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>If a queried point falls outside the FDS domain, &lt;code>sample_extinction&lt;/code>
returns &lt;code>0.0&lt;/code> (clear air) and logs a warning on the first occurrence.&lt;/p>
&lt;h3>Using a constant field&lt;span class="hx-absolute -hx-mt-20" id="using-a-constant-field">&lt;/span>
&lt;a href="#using-a-constant-field" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>To use a uniform extinction value for verification:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.smoke_speed &lt;span style="color:#f92672">import&lt;/span> ConstantExtinctionField
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>field &lt;span style="color:#f92672">=&lt;/span> ConstantExtinctionField(extinction_per_m&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">1.0&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h2>Configuration&lt;span class="hx-absolute -hx-mt-20" id="configuration">&lt;/span>
&lt;a href="#configuration" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;code>SmokeSpeedConfig&lt;/code> bundles the model coefficients with runtime
settings:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.smoke_speed &lt;span style="color:#f92672">import&lt;/span> SmokeSpeedConfig
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>config &lt;span style="color:#f92672">=&lt;/span> SmokeSpeedConfig(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> fds_dir&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;path/to/fds_case&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> update_interval_s&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">1.0&lt;/span>, &lt;span style="color:#75715e"># how often agents resample extinction&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> slice_height_m&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">2.0&lt;/span>, &lt;span style="color:#75715e"># FDS slice height&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> speed_law&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;lund&amp;#34;&lt;/span>, &lt;span style="color:#75715e"># or &amp;#34;fridolf&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The law coefficients (&lt;code>alpha&lt;/code>, &lt;code>beta&lt;/code>, &lt;code>min_speed_factor&lt;/code>,
&lt;code>visibility_factor_c&lt;/code>) are further fields; their defaults are listed on the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/#parameters">smoke-speed model&lt;/a>
page.&lt;/p>
&lt;p>The &lt;code>update_interval_s&lt;/code> field controls how frequently each agent
queries the extinction field during the simulation loop. A value of
&lt;code>1.0&lt;/code> means one sample per agent per second of simulated time.&lt;/p>
&lt;h2>Putting it together&lt;span class="hx-absolute -hx-mt-20" id="putting-it-together">&lt;/span>
&lt;a href="#putting-it-together" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>To create a full smoke-speed model and query it:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.smoke_speed &lt;span style="color:#f92672">import&lt;/span> (
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> ExtinctionField,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> SmokeSpeedConfig,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> SmokeSpeedModel,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>field &lt;span style="color:#f92672">=&lt;/span> ExtinctionField&lt;span style="color:#f92672">.&lt;/span>from_fds(config&lt;span style="color:#f92672">.&lt;/span>fds_dir)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>model &lt;span style="color:#f92672">=&lt;/span> SmokeSpeedModel(field, config)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># Query at a specific point and time&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>extinction_K, speed_factor &lt;span style="color:#f92672">=&lt;/span> model&lt;span style="color:#f92672">.&lt;/span>sample(time_s&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">30.0&lt;/span>, x&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">5.0&lt;/span>, y&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">3.0&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># Or get just the factor&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>factor &lt;span style="color:#f92672">=&lt;/span> model&lt;span style="color:#f92672">.&lt;/span>speed_factor(time_s&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">30.0&lt;/span>, x&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">5.0&lt;/span>, y&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">3.0&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h2>Conversion utilities&lt;span class="hx-absolute -hx-mt-20" id="conversion-utilities">&lt;/span>
&lt;a href="#conversion-utilities" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Two helper functions support the soot-density-based workflow from the
original FDS+Evac guide:&lt;/p>
&lt;ul>
&lt;li>&lt;code>extinction_from_soot_density(soot_density_mg_per_m3)&lt;/code> &amp;ndash; converts
soot density to extinction using &lt;code>K = K_m * rho_s * 1e-6&lt;/code>, where &lt;code>K_m&lt;/code>
(&lt;code>mass_extinction_coefficient_m2_per_kg&lt;/code>, default 8700 m²/kg) is the FDS
default mass-specific extinction coefficient (see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/extinction/">Extinction coefficient&lt;/a>
).&lt;/li>
&lt;li>&lt;code>speed_from_soot_density(base_speed, soot_density_mg_per_m3)&lt;/code> &amp;ndash;
computes the reduced walking speed directly from soot density.&lt;/li>
&lt;/ul>
&lt;h2>References&lt;span class="hx-absolute -hx-mt-20" id="references">&lt;/span>
&lt;a href="#references" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/">Smoke-speed model&lt;/a>
: coded form, defaults and
deviations from the literature.&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/walking-speed/">Walking speed in smoke&lt;/a>
and
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/extinction/">Extinction coefficient&lt;/a>
: the published laws
and their sources.&lt;/li>
&lt;li>&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/materials/evac.f90" target="_blank" rel="noopener">evac.f90&lt;/a>
&amp;ndash; Original FDS+Evac Fortran
source for cross-referencing implementation details.&lt;/li>
&lt;/ul></description></item><item><title>Exit choice and familiarity</title><link>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/exit-choice/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/fundamentals/exit-choice/</guid><description>
&lt;p>Which exit an occupant heads for is not decided by distance alone. The studies
below find that occupants tend to leave the way they know, are influenced by
what other people do, and respond to smoke, lighting and crowding at the
exits. There is no single published law for exit choice; there are
observations and calibrated statistical models.&lt;/p>
&lt;h2>Movement to the familiar&lt;span class="hx-absolute -hx-mt-20" id="movement-to-the-familiar">&lt;/span>
&lt;a href="#movement-to-the-familiar" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Sime (1985) studied the direction of escape in a fire in a large room with an
entrance and an emergency exit in opposite corners. He contrasted the
&amp;ldquo;affiliative&amp;rdquo; model, in which people under threat of entrapment move towards
familiar persons and places, with design assumptions that the physical
availability and proximity of an exit determine its use. Proximity mattered,
but so did affiliation: staff generally left by the fire exit, whereas members
of the public who were separated from their group moved towards, and left by,
the entrance. Sime argued that place affiliation is not addressed
sufficiently in escape-route design.&lt;/p>
&lt;p>Kinateder, Comunale and Warren (2018) tested this in an ambulatory virtual
museum. Participants entered through one door and, when an alarm sounded,
were significantly more likely to leave through that familiar door than
through a second exit. The effect grew when virtual neighbours also left by
the familiar door, shrank when they left by the other door, and the social
influence was stronger with two neighbours than with one.&lt;/p>
&lt;h2>Discrete-choice models&lt;span class="hx-absolute -hx-mt-20" id="discrete-choice-models">&lt;/span>
&lt;a href="#discrete-choice-models" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Lovreglio, Borri, dell&amp;rsquo;Olio and Ibeas (2014) introduced a random-utility
discrete-choice model for exit choice in emergency evacuations. Haghani and
Sarvi (2017, p. 241) describe it as an internet-based stated-choice
experiment with binary choices in simple visualised geometries, which
investigates the effect of individuals&amp;rsquo; demographic characteristics on exit
choice. Lovreglio,
Fonzone and dell&amp;rsquo;Olio (2016) calibrated a mixed logit model on an online
stated-preference survey with non-immersive virtual reality and 1503
participants. Smoke, emergency lighting, exit distance, the number of
evacuees near the exits and near the decision-maker, and the flow of evacuees
through the exits all affected local exit choice significantly, with a high
degree of behavioural uncertainty.&lt;/p>
&lt;p>Haghani and Sarvi (2017) compared stated choices (4958 observations from
face-to-face interviews in three public places) with revealed choices (3015
exit choices extracted from video of evacuation trials in which participants
competed in a real crowd). The four data sets gave fairly similar patterns of
parameter estimates, and the stated-choice models predicted choices
reasonably similar to the revealed-choice model, despite significant
differences in parameter scale. Their review of empirical methods in crowd
research (Haghani and Sarvi 2018) surveys more than 160 studies.&lt;/p>
&lt;h2>Known limits&lt;span class="hx-absolute -hx-mt-20" id="known-limits">&lt;/span>
&lt;a href="#known-limits" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Apart from incident studies such as Sime&amp;rsquo;s, the evidence above comes from
hypothetical choices, virtual reality and evacuation trials, none of which
carries the threat of a real fire. Whether parameters calibrated in one
geometry and population transfer to another is an open question; Haghani and
Sarvi (2017) set out to test exactly this context-dependence. The guide to FDS+Evac, the evacuation module of the Fire Dynamics
Simulator (FDS),
notes, citing the socio-psychological literature, that familiarity of
exit routes is an essential factor in evacuees&amp;rsquo; decisions and that emergency
exits are rarely used in many real evacuations because they are unfamiliar
(Korhonen 2021, §3.5).&lt;/p>
&lt;h2>Sources&lt;span class="hx-absolute -hx-mt-20" id="sources">&lt;/span>
&lt;a href="#sources" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>Sime, J. D. (1985). &lt;em>Movement toward the familiar: person and place
affiliation in a fire entrapment setting&lt;/em>. Environment and Behavior,
17(6), 697–724.
&lt;a href="https://doi.org/10.1177/0013916585176003" target="_blank" rel="noopener">doi:10.1177/0013916585176003&lt;/a>
&lt;/li>
&lt;li>Kinateder, M., Comunale, B., &amp;amp; Warren, W. H. (2018). &lt;em>Exit choice in an
emergency evacuation scenario is influenced by exit familiarity and
neighbor behavior&lt;/em>. Safety Science, 106, 170–175.
&lt;a href="https://doi.org/10.1016/j.ssci.2018.03.015" target="_blank" rel="noopener">doi:10.1016/j.ssci.2018.03.015&lt;/a>
&lt;/li>
&lt;li>Lovreglio, R., Borri, D., dell&amp;rsquo;Olio, L., &amp;amp; Ibeas, A. (2014). &lt;em>A discrete
choice model based on random utilities for exit choice in emergency
evacuations&lt;/em>. Safety Science, 62, 418–426.
&lt;a href="https://doi.org/10.1016/j.ssci.2013.10.004" target="_blank" rel="noopener">doi:10.1016/j.ssci.2013.10.004&lt;/a>
&lt;/li>
&lt;li>Lovreglio, R., Fonzone, A., &amp;amp; dell&amp;rsquo;Olio, L. (2016). &lt;em>A mixed logit model
for predicting exit choice during building evacuations&lt;/em>. Transportation
Research Part A: Policy and Practice, 92, 59–75.
&lt;a href="https://doi.org/10.1016/j.tra.2016.06.018" target="_blank" rel="noopener">doi:10.1016/j.tra.2016.06.018&lt;/a>
&lt;/li>
&lt;li>Haghani, M., &amp;amp; Sarvi, M. (2017). &lt;em>Stated and revealed exit choices of
pedestrian crowd evacuees&lt;/em>. Transportation Research Part B:
Methodological, 95, 238–259.
&lt;a href="https://doi.org/10.1016/j.trb.2016.10.019" target="_blank" rel="noopener">doi:10.1016/j.trb.2016.10.019&lt;/a>
&lt;/li>
&lt;li>Haghani, M., &amp;amp; Sarvi, M. (2018). &lt;em>Crowd behaviour and motion: empirical
methods&lt;/em>. Transportation Research Part B: Methodological, 107, 253–294.
&lt;a href="https://doi.org/10.1016/j.trb.2017.06.017" target="_blank" rel="noopener">doi:10.1016/j.trb.2017.06.017&lt;/a>
&lt;/li>
&lt;li>Korhonen, T. (2021). &lt;em>Fire Dynamics Simulator with Evacuation: FDS+Evac.
Technical Reference and User&amp;rsquo;s Guide&lt;/em> (FDS 6.7.6, Evac 2.6.0 draft),
§3.5. VTT Technical Research Centre of Finland.
&lt;a href="https://github.com/tkorhon1/FDS-Evac-Guide" target="_blank" rel="noopener">github.com/tkorhon1/FDS-Evac-Guide&lt;/a>
.
Secondary source.&lt;/li>
&lt;/ul>
&lt;p>How pyFDS-Evac uses this: see &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/routing/">route rerouting&lt;/a>
and
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/visibility/">visibility and cognitive maps&lt;/a>
.&lt;/p></description></item><item><title>Smoke-aware routing</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/routing/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/routing/</guid><description>
&lt;blockquote>
&lt;p>Part of &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/README.md" target="_blank" rel="noopener">pyFDS-Evac&lt;/a>
.&lt;/p>&lt;/blockquote>
&lt;p>The pyFDS-Evac routing system implements dynamic, smoke-aware path
planning. Agents evaluate candidate routes against the current hazard
fields and periodically reroute as conditions change. Costs are
recomputed at each reevaluation tick, so the chosen path adapts as
conditions evolve.&lt;/p>
&lt;blockquote>
&lt;p>&lt;strong>Which model is running.&lt;/strong> Two cost models exist, selected per deck
with &lt;code>routing.cost_model&lt;/code>. The &lt;strong>default is &lt;code>&amp;quot;gate&amp;quot;&lt;/code>&lt;/strong>: the route&amp;rsquo;s
optical depth &lt;code>K_ave * L&lt;/code> decides which exits stay available and
orders the survivors, with travel time as tie-break. The historical
&lt;code>&amp;quot;additive&amp;quot;&lt;/code> model, in which smoke is a toll per metre, is still
available. This page covers the machinery both share; the
gate itself is documented in
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/">route-cost-gate.md&lt;/a>
.&lt;/p>&lt;/blockquote>
&lt;blockquote>
&lt;p>&lt;strong>Note:&lt;/strong> The cost model supports both smoke and FED (toxic gas)
terms. FED-based route cost is active when a &lt;code>fed_model&lt;/code> is
provided to &lt;code>run_scenario&lt;/code>; otherwise &lt;code>fed_rate_sampler&lt;/code> is &lt;code>None&lt;/code>
and only smoke influences route ranking.&lt;/p>&lt;/blockquote>
&lt;h2>Stage graph&lt;span class="hx-absolute -hx-mt-20" id="stage-graph">&lt;/span>
&lt;a href="#stage-graph" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Routes are evaluated on a &lt;code>StageGraph&lt;/code> &amp;ndash; a directed weighted graph
where nodes represent stages (distributions, checkpoints, exits).
The graph is built once at simulation start from the scenario
configuration.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.route_graph &lt;span style="color:#f92672">import&lt;/span> StageGraph
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>graph &lt;span style="color:#f92672">=&lt;/span> StageGraph&lt;span style="color:#f92672">.&lt;/span>from_scenario(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> direct_steering_info&lt;span style="color:#f92672">=&lt;/span>stage_info, &lt;span style="color:#75715e"># stage_id -&amp;gt; {polygon, stage_type}&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> transitions&lt;span style="color:#f92672">=&lt;/span>transitions, &lt;span style="color:#75715e"># [{from, to}, ...]&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> distributions&lt;span style="color:#f92672">=&lt;/span>distributions, &lt;span style="color:#75715e"># optional spawn areas&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> walkable_polygon&lt;span style="color:#f92672">=&lt;/span>walkable_polygon, &lt;span style="color:#75715e"># optional Shapely Polygon&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h3>Graph construction without a journey&lt;span class="hx-absolute -hx-mt-20" id="graph-construction-without-a-journey">&lt;/span>
&lt;a href="#graph-construction-without-a-journey" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>When a scenario defines no &lt;code>transitions&lt;/code>, the stage graph wires itself by
stage type: spawn areas and crossings reach every crossing and every exit,
exits are terminal, and nothing points back at a spawn area. Crossings
therefore participate in cost-driven routing without a hand-authored journey.
In clear air the direct spawn-to-exit edge is cheapest, so agents take the
nearest exit and crossings sit inert; smoke can make a route through a
crossing cheaper.&lt;/p>
&lt;p>Explicit &lt;code>transitions&lt;/code> remain authoritative and skip this path entirely.&lt;/p>
&lt;h3>Edge geometry&lt;span class="hx-absolute -hx-mt-20" id="edge-geometry">&lt;/span>
&lt;a href="#edge-geometry" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Edges carry a polyline that follows the corridor geometry computed
by JuPedSim&amp;rsquo;s &lt;code>RoutingEngine&lt;/code> at graph construction time. Smoke
and FED are sampled along this polyline, not along a straight
centroid-to-centroid ray. Edge weight is the polyline arc length.&lt;/p>
&lt;p>When no walkable polygon is provided (e.g. in unit tests), edges
fall back to a straight centroid-to-centroid ray.&lt;/p>
&lt;h3>Shortest-path queries&lt;span class="hx-absolute -hx-mt-20" id="shortest-path-queries">&lt;/span>
&lt;a href="#shortest-path-queries" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>The graph provides Dijkstra-based shortest-path queries to find
reachable exits. When dynamic weights are provided, Dijkstra uses
smoke/FED-adjusted costs instead of static arc lengths:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># All reachable exits with costs and paths&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>paths &lt;span style="color:#f92672">=&lt;/span> graph&lt;span style="color:#f92672">.&lt;/span>shortest_paths_to_exits(source&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;dist_1&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># Nearest exit only&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>result &lt;span style="color:#f92672">=&lt;/span> graph&lt;span style="color:#f92672">.&lt;/span>shortest_exit(source&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;dist_1&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">if&lt;/span> result:
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> exit_id, cost, path &lt;span style="color:#f92672">=&lt;/span> result&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h2>Route cost evaluation&lt;span class="hx-absolute -hx-mt-20" id="route-cost-evaluation">&lt;/span>
&lt;a href="#route-cost-evaluation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Each candidate route is scored by evaluating its segments (edges)
against current smoke conditions. The cost model combines path
length and smoke exposure (FED terms are supported but not currently
active — see note above).&lt;/p>
&lt;h3>Segment evaluation&lt;span class="hx-absolute -hx-mt-20" id="segment-evaluation">&lt;/span>
&lt;a href="#segment-evaluation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>For each segment (edge between two stages), the system performs
the following steps:&lt;/p>
&lt;ol>
&lt;li>Sample the extinction coefficient K along the edge polyline
and take its mean (see &lt;a href="#line-of-sight-extinction">Line-of-sight extinction&lt;/a>
).&lt;/li>
&lt;li>Compute the smoke-adjusted speed factor from the mean K with the
linear speed law, using the router&amp;rsquo;s own &lt;code>alpha&lt;/code>, &lt;code>beta&lt;/code> and
&lt;code>min_speed_factor&lt;/code> (see the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/">smoke-speed model&lt;/a>
).&lt;/li>
&lt;li>Estimate the travel time from the segment length and reduced
speed.&lt;/li>
&lt;li>Optionally, estimate the FED growth along the segment from the
FED rate at the polyline midpoint (by arc length) and the
estimated travel time. (Active only when a &lt;code>fed_model&lt;/code> is
provided; otherwise &lt;code>fed_rate_sampler&lt;/code> is &lt;code>None&lt;/code>.)&lt;/li>
&lt;/ol>
&lt;h3>Line-of-sight extinction&lt;span class="hx-absolute -hx-mt-20" id="line-of-sight-extinction">&lt;/span>
&lt;a href="#line-of-sight-extinction" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>The mean extinction along a segment is computed by sampling K at
uniform intervals along the edge polyline:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>sigma_bar = (1 / |P|) * sum(K_p)&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>where &lt;code>|P|&lt;/code> is the number of sample points and &lt;code>K_p&lt;/code> is the
extinction at each point. The sample spacing is controlled by
&lt;code>sampling_step_m&lt;/code>. This is the discrete form of the Beer-Lambert
path-integrated mean of
&lt;a href="https://doi.org/10.1016/j.firesaf.2024.104269" target="_blank" rel="noopener">Boerger et al. (2024)&lt;/a>
,
Eq. 8-9; the Beer-Lambert law itself is on
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/extinction/">Extinction coefficient&lt;/a>
.&lt;/p>
&lt;h3>Arrival-time pricing&lt;span class="hx-absolute -hx-mt-20" id="arrival-time-pricing">&lt;/span>
&lt;a href="#arrival-time-pricing" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>With &lt;code>anticipate&lt;/code> (default &lt;code>true&lt;/code>, and &lt;strong>independent of &lt;code>cost_model&lt;/code>&lt;/strong>),
each segment is priced at the time the agent would reach it rather than
the time it decides:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>arrival_time = now &amp;#43; min(walked_so_far / base_speed_m_per_s,
foresight_horizon_s)&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The unimpeded &lt;code>base_speed_m_per_s&lt;/code> is used, not the smoke-reduced speed.
&lt;code>foresight_horizon_s&lt;/code> defaults to infinity.&lt;/p>
&lt;h3>Composite cost&lt;span class="hx-absolute -hx-mt-20" id="composite-cost">&lt;/span>
&lt;a href="#composite-cost" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>The composite is the additive model&amp;rsquo;s ranking number. It is still
computed and reported under the gate, where it does not rank:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>composite = effective_length * (1 &amp;#43; w_smoke * K_ave) &amp;#43; w_fed * FED_max&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>where:&lt;/p>
&lt;ul>
&lt;li>&lt;code>effective_length&lt;/code> is the route length measured from the agent&amp;rsquo;s own
position when one is supplied, otherwise the node-to-node path length&lt;/li>
&lt;li>&lt;code>K_ave&lt;/code> is the length-weighted average extinction along the route&lt;/li>
&lt;li>&lt;code>FED_max&lt;/code> is the projected cumulative FED at route completion&lt;/li>
&lt;li>&lt;code>w_smoke&lt;/code> and &lt;code>w_fed&lt;/code> are configurable weights&lt;/li>
&lt;/ul>
&lt;p>Exposure on the part of the first segment the agent has already walked
is credited out of &lt;code>K_ave&lt;/code> and &lt;code>FED_max&lt;/code>, because it is already carried
in &lt;code>current_fed&lt;/code>.&lt;/p>
&lt;h3>What each model ranks on&lt;span class="hx-absolute -hx-mt-20" id="what-each-model-ranks-on">&lt;/span>
&lt;a href="#what-each-model-ranks-on" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Both models write a single &lt;code>rank_cost&lt;/code> field, which ordering, the
exit-switch anchor and the same-exit path test all read:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>model&lt;/th>
&lt;th>&lt;code>rank_cost&lt;/code>&lt;/th>
&lt;th>sort key&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>&amp;quot;gate&amp;quot;&lt;/code>&lt;/td>
&lt;td>&lt;code>travel_time_s + w_queue * queue_time_s&lt;/code>&lt;/td>
&lt;td>&lt;code>(rejected, tier, tau, rank_cost, hops)&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>&amp;quot;additive&amp;quot;&lt;/code>&lt;/td>
&lt;td>&lt;code>composite_cost&lt;/code>&lt;/td>
&lt;td>&lt;code>(rejected, tier, 0.0, rank_cost, hops)&lt;/code>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>Under the gate the route&amp;rsquo;s optical depth &lt;code>tau = K_ave * L&lt;/code> leads the sort and
&lt;code>rank_cost&lt;/code> breaks ties; under the additive model the &lt;code>tau&lt;/code> slot is a constant
and &lt;code>rank_cost&lt;/code> decides alone. The &lt;code>tau&lt;/code> in the key is scaled by
&lt;code>current_exit_discount&lt;/code> (0.9) for the exit the agent already heads for.&lt;/p>
&lt;p>&lt;code>rank_cost&lt;/code> is still what the exit-switch anchor and the same-exit path test
compare — a travel time under the gate. That the ordering and the anchor read
different quantities is a known defect; see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/#known-limitations">route-cost-gate.md&lt;/a>
.&lt;/p>
&lt;p>&lt;code>tier&lt;/code> is 0 for every route unless &lt;code>clean_extinction_threshold &amp;gt; 0&lt;/code> under the
gate, which is not the default; then a route whose smokiest leg is at or below
the threshold takes tier 0 and every other route takes tier 1. See
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/#the-clean-exit-tier-off-by-default">the clean-exit tier&lt;/a>
for
what it does and why it ships off.&lt;/p>
&lt;h3>Route rejection&lt;span class="hx-absolute -hx-mt-20" id="route-rejection">&lt;/span>
&lt;a href="#route-rejection" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>A route is rejected under any of these conditions:&lt;/p>
&lt;ul>
&lt;li>&lt;code>FED_max&lt;/code> exceeds &lt;code>fed_rejection_threshold&lt;/code> (default 1.0) —
evaluated per route inside &lt;code>evaluate_route&lt;/code>. The threshold is
asymmetric: a route to an exit that is &lt;em>not&lt;/em> the agent&amp;rsquo;s current one
must come in under &lt;code>fed_rejection_threshold * fed_return_margin&lt;/code>
(0.9), so an agent flees a deadly exit at once but only switches onto
a rival that is clearly safe.&lt;/li>
&lt;li>&lt;strong>Gate model only:&lt;/strong> the route&amp;rsquo;s optical depth &lt;code>tau = K_ave * L_eff&lt;/code> exceeds
&lt;code>tau_max&lt;/code> (default 6). This test is asymmetric too: for an exit that is not
the agent&amp;rsquo;s current one the budget is &lt;code>tau_max * tau_return_margin&lt;/code> (0.8), so
switching needs a cleaner route than staying. &lt;code>K_ave&lt;/code> is the length-weighted
mean over the route&amp;rsquo;s own polyline, and every exit is judged on it — see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/#optical-depth-what-it-measures-and-what-it-does-not">route-cost-gate.md&lt;/a>
.
The rejection reason reads &lt;code>tau 8.41 &amp;gt; 6.00 (K_ave 0.145 x 58.0 m)&lt;/code>.&lt;/li>
&lt;li>&lt;strong>Additive model only:&lt;/strong> &lt;strong>all&lt;/strong> of its segments have K ≥
&lt;code>visibility_extinction_threshold&lt;/code> &lt;strong>and&lt;/strong> at least one other route has at
least one visible segment — evaluated as a second pass in &lt;code>rank_routes&lt;/code> after
all routes are scored. This pass is skipped under the gate, where it was a
second, hysteresis-free smoke criterion on top of the optical-depth test.&lt;/li>
&lt;/ul>
&lt;p>The last condition means a smoky-but-short route is only rejected
when a cleaner alternative exists. If every route is fully obscured,
none are visibility-rejected.&lt;/p>
&lt;p>If all routes end up rejected, the least-bad one is un-rejected as a
fallback so the agent always has a path, and its reason is prefixed
&lt;code>fallback: &lt;/code>. Under the gate the least-bad route is the one with the
lowest undiscounted &lt;code>tau_route&lt;/code>, then the lowest &lt;code>rank_cost&lt;/code>, with
&lt;code>fallback_switch_margin&lt;/code> hysteresis on &lt;code>k_max_route&lt;/code>; under the additive
model it is the lowest-composite route.&lt;/p>
&lt;p>Rejections are never remembered. Each tick re-decides from the current
field, which is what lets the optical-depth criterion relax as an agent
closes on an exit — the distance in &lt;code>tau&lt;/code> is the distance that remains.&lt;/p>
&lt;h3>Configuration&lt;span class="hx-absolute -hx-mt-20" id="configuration">&lt;/span>
&lt;a href="#configuration" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;code>RouteCostConfig&lt;/code> controls all cost evaluation parameters. Most fields are
also readable from a scenario&amp;rsquo;s &lt;code>routing&lt;/code> block via
&lt;code>RouteCostConfig.from_routing_params&lt;/code>. The defaults are on the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/routing/#parameters">routing model&lt;/a>
page, and the full JSON key
table is in &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/#configuration">route-cost-gate.md&lt;/a>
.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.route_graph &lt;span style="color:#f92672">import&lt;/span> RouteCostConfig
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># Fields not passed keep their defaults (see the routing model page).&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>config &lt;span style="color:#f92672">=&lt;/span> RouteCostConfig(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> cost_model&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;gate&amp;#34;&lt;/span>, &lt;span style="color:#75715e"># &amp;#34;gate&amp;#34; (default) or &amp;#34;additive&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> w_queue&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">0.03&lt;/span>, &lt;span style="color:#75715e"># override: turn the congestion term on (default off)&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;code>alpha&lt;/code>, &lt;code>beta&lt;/code> and &lt;code>min_speed_factor&lt;/code> are the router&amp;rsquo;s copy of the linear
speed law, used only to estimate travel time; their defaults are on the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/routing/#parameters">routing model&lt;/a>
page.&lt;/p>
&lt;p>Two further fields exist on the dataclass but are &lt;strong>not&lt;/strong> readable from
the &lt;code>routing&lt;/code> block, so a scenario run always gets their defaults:
&lt;code>fed_return_margin&lt;/code> (0.9) and &lt;code>impassable_extinction_threshold&lt;/code> (3.0).&lt;/p>
&lt;p>&lt;code>w_smoke&lt;/code> and &lt;code>w_fed&lt;/code> &lt;strong>no longer reach route choice under the gate.&lt;/strong>
Since &lt;code>0d9bf79&lt;/code> a gate run weights each Dijkstra edge by its own optical
depth, &lt;code>k_avg * length&lt;/code> (plus a &lt;code>1e-6 * length&lt;/code> floor that keeps a
clear-air graph from collapsing to all-zero weights). The composite is
still computed and reported; it does not rank, and it no longer picks
the path either. Under &lt;code>&amp;quot;additive&amp;quot;&lt;/code> both weights are active as before.&lt;/p>
&lt;h3>Congestion-aware routing&lt;span class="hx-absolute -hx-mt-20" id="congestion-aware-routing">&lt;/span>
&lt;a href="#congestion-aware-routing" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>When &lt;code>w_queue &amp;gt; 0&lt;/code>, an exit-congestion term enters the ranking. Under
the additive model it is converted to distance and added to the
composite:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>queue_time = N_exit / capacity
queue_distance = base_speed_m_per_s * queue_time
composite = path_length * (1 &amp;#43; w_smoke * K_ave)
&amp;#43; w_fed * FED_max
&amp;#43; w_queue * queue_distance&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Under the gate the ranking number is a time, so the delay is added as a
time instead — &lt;code>rank_cost = travel_time_s + w_queue * queue_time_s&lt;/code>.
Opting into congestion-aware routing therefore works under both models,
but &lt;code>w_queue&lt;/code> is not the same quantity in the two: the additive form
carries the extra &lt;code>base_speed_m_per_s&lt;/code> factor.&lt;/p>
&lt;p>where:&lt;/p>
&lt;ul>
&lt;li>&lt;code>N_exit&lt;/code> is the number of agents currently targeting that exit&lt;/li>
&lt;li>&lt;code>capacity&lt;/code> is the exit&amp;rsquo;s &lt;code>capacity_agents_per_s&lt;/code> (default 1.3)&lt;/li>
&lt;li>&lt;code>base_speed_m_per_s&lt;/code> converts queueing delay (seconds) into
distance-equivalent cost (metres) so all terms share the same
unit space&lt;/li>
&lt;/ul>
&lt;p>The queue term is applied at route-level ranking (Phase 3) only,
not in Dijkstra edge weights, because it is a per-exit constant
that cannot change which path is selected to a given exit.&lt;/p>
&lt;p>&lt;strong>Congestion-aware routing is off by default&lt;/strong> (&lt;code>w_queue = 0&lt;/code>); set it in a
scenario&amp;rsquo;s &lt;code>routing&lt;/code> block to switch it on.&lt;/p>
&lt;h4>Why it is opt-in, and what 0.024 means&lt;span class="hx-absolute -hx-mt-20" id="why-it-is-opt-in-and-what-0024-means">&lt;/span>
&lt;a href="#why-it-is-opt-in-and-what-0024-means" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;p>The term is &lt;code>w_queue * v0 * N / c&lt;/code> with &lt;code>N&lt;/code> a &lt;strong>global&lt;/strong> tally of every agent
targeting the exit — where &lt;code>v0&lt;/code> is the &lt;code>base_speed_m_per_s&lt;/code> conversion constant
(not any agent&amp;rsquo;s desired speed) and &lt;code>c&lt;/code> is the exit&amp;rsquo;s &lt;code>capacity_agents_per_s&lt;/code>,
falling back to &lt;code>default_exit_capacity&lt;/code>. With both at their 1.3 defaults the
penalty is simply
&lt;code>w_queue * N&lt;/code> metres, so it grows without bound in the population while the
path-length differences it competes against are fixed by the geometry:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>N&lt;/th>
&lt;th>&lt;code>w_queue = 1.0&lt;/code>&lt;/th>
&lt;th>&lt;code>w_queue = 0.03&lt;/code>&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>25&lt;/td>
&lt;td>25 m&lt;/td>
&lt;td>0.8 m&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>50&lt;/td>
&lt;td>50 m&lt;/td>
&lt;td>1.5 m&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>333&lt;/td>
&lt;td>333 m&lt;/td>
&lt;td>10 m&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>No constant is right at more than one crowd size. 1.0 puts 333 m on a door at
Station scale; the 0.03 that fixes that is inert in an ordinary room. So the
library ships no congestion weight at all, and the calibrated value lives with
the deck it was calibrated on. Issue #89 tracks replacing the global &lt;code>N&lt;/code> with a
queue the agent can perceive, which would remove the scale dependence.&lt;/p>
&lt;p>&lt;strong>&lt;code>assets/station_fahy&lt;/code> currently ships &lt;code>w_queue = 0.024&lt;/code>, not 0.03.&lt;/strong> The 0.03
below was swept against a geometry whose doorways were narrower than the
building&amp;rsquo;s; when the doorways were opened to their clear width the same sweep
scored 0.03 at 50.2 % and 0.024 at 53.0 % (seeds 420–422) against Fahy&amp;rsquo;s 52.9 %.
The reasoning in this section is unchanged — only the fitted number moved.&lt;/p>
&lt;p>&lt;strong>Two further caveats on that number.&lt;/strong> The sweep was run under the additive
composite, where &lt;code>w_queue&lt;/code> multiplies a &lt;em>distance&lt;/em>
(&lt;code>w_queue * base_speed_m_per_s * queue_time&lt;/code>). Under the default gate model it
multiplies a &lt;em>time&lt;/em> (&lt;code>w_queue * queue_time_s&lt;/code>, against travel time), which is a
different quantity, and no deck pins &lt;code>cost_model&lt;/code> — so the Station deck now runs
under the gate with a weight fitted under the additive model. It has not been
re-swept there.&lt;/p>
&lt;h4>Provenance of the Station&amp;rsquo;s queue weight&lt;span class="hx-absolute -hx-mt-20" id="provenance-of-the-stations-queue-weight">&lt;/span>
&lt;a href="#provenance-of-the-stations-queue-weight" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;p>Calibrated, not conventional. The table below is the original sweep, which
selected 0.03; see the caveats above for why the deck now ships 0.024 and why
neither value has been re-fitted under the gate cost model.
&lt;code>scripts/sweep_queue_weight.py&lt;/code> sweeps the
weight on &lt;code>assets/station_fahy&lt;/code> with rerouting on, three seeds per
point, and scores each run with that asset&amp;rsquo;s own &lt;code>validate.py&lt;/code>. The
front-door share falls monotonically with &lt;code>w_queue&lt;/code>:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>&lt;code>w_queue&lt;/code>&lt;/th>
&lt;th>front-door share&lt;/th>
&lt;th>mean row deviation&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>0&lt;/td>
&lt;td>65.9 %&lt;/td>
&lt;td>18.2 %&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>0.03&lt;/strong>&lt;/td>
&lt;td>&lt;strong>53.3 %&lt;/strong>&lt;/td>
&lt;td>&lt;strong>17.9 %&lt;/strong>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>0.05&lt;/td>
&lt;td>48.4 %&lt;/td>
&lt;td>18.0 %&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>0.1&lt;/td>
&lt;td>44.9 %&lt;/td>
&lt;td>18.9 %&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>1.0&lt;/td>
&lt;td>22.9 %&lt;/td>
&lt;td>21.8 %&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>Fahy, Proulx &amp;amp; Flynn measured 52.9 % of door users at the front door,
so &lt;code>w_queue = 0.03&lt;/code> reproduces the Station&amp;rsquo;s aggregate exit split and
the previous default of &lt;code>1.0&lt;/code> did not. The mean per-row deviation is
also lower, but it is nearly flat across 0.02–0.05 (17.1–18.0 %) while
the front-door share swings 58.1 % → 48.4 %, so the row structure
confirms that low weights beat 1.0 without discriminating 0.03 within
that band. The value is fixed by the aggregate target alone.&lt;/p>
&lt;p>&lt;strong>This calibration holds at one crowd size.&lt;/strong> The term is
&lt;code>w_queue * v0 * N / c&lt;/code> (&lt;code>v0&lt;/code>, &lt;code>c&lt;/code> as above) with &lt;code>N&lt;/code> a global tally, so its
magnitude
relative to the path-length differences it competes with grows linearly
with the population — 0.03 was fitted at the Station&amp;rsquo;s 333 agents, and
a scenario an order of magnitude smaller wants a correspondingly larger
weight to express the same preference. That scale dependence is a
property of the global-&lt;code>N&lt;/code> form rather than of this number; a queue an
agent could actually perceive would not have it.&lt;/p>
&lt;p>Exit capacity can be configured per exit in the scenario config:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-json" data-lang="json">&lt;span style="display:flex;">&lt;span>{
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;exits&amp;#34;&lt;/span>: {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;exit_1&amp;#34;&lt;/span>: {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;capacity_agents_per_s&amp;#34;&lt;/span>: &lt;span style="color:#ae81ff">2.5&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>}&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>When not specified, the default from
&lt;code>RouteCostConfig.default_exit_capacity&lt;/code> (1.3 agents/s) is used.&lt;/p>
&lt;p>This approach is inspired by the game-theoretic exit selection
model of Ehtamo et al. (2010), where each agent minimises
estimated evacuation time (queueing + walking). The staggered
reevaluation schedule provides natural convergence to Nash
equilibrium without explicit iteration.&lt;/p>
&lt;h2>Dynamic rerouting&lt;span class="hx-absolute -hx-mt-20" id="dynamic-rerouting">&lt;/span>
&lt;a href="#dynamic-rerouting" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>During simulation, agents periodically reevaluate their routes and
switch to lower-cost exits when conditions change.&lt;/p>
&lt;h3>Reevaluation scheduling&lt;span class="hx-absolute -hx-mt-20" id="reevaluation-scheduling">&lt;/span>
&lt;a href="#reevaluation-scheduling" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Each agent has a personal time offset derived from its ID so that
not all agents reevaluate on the same timestep:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>offset = (agent_id % steps_per_interval) * dt_s&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>An agent fires its &lt;strong>first&lt;/strong> evaluation once &lt;code>current_time &amp;gt;= offset&lt;/code>,
then fires again every &lt;code>reevaluation_interval_s&lt;/code> thereafter. With a
10-second interval and 100 agents the load is spread uniformly across
the interval.&lt;/p>
&lt;h3>Rerouting decision flow&lt;span class="hx-absolute -hx-mt-20" id="rerouting-decision-flow">&lt;/span>
&lt;a href="#rerouting-decision-flow" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;code>evaluate_and_reroute&lt;/code> runs once per agent per reevaluation tick:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>1. Resolve source node
├─ use current_origin (stage the agent is coming from)
└─ fall back to current_target_stage
→ if source not in graph → skip (return None)
2. rank_routes(source, t, FED, K_field)
├─ evaluate all edges → dynamic costs from current smoke/FED
│ (gate: k_avg * length &amp;#43; 1e-6 * length, the edge&amp;#39;s own optical depth;
│ additive: w_smoke / w_fed weighted composite)
├─ Dijkstra with dynamic weights → one minimum-cost path per reachable exit
│ (only the single lowest-cost path to each exit under these weights
│ is evaluated; alternative paths to the same exit are not enumerated)
├─ evaluate_route on each path
│ ├─ FED rejection (asymmetric: x fed_return_margin for a non-current exit)
│ └─ gate only: optical-depth rejection, reason &amp;#34;tau ...&amp;#34;, every exit
│ tested (asymmetric: budget x tau_return_margin for a non-current
│ exit)
├─ visibility rejection pass (additive only)
│ └─ if ≥1 route has any visible segment:
│ mark routes where ALL segments are non-visible as rejected
├─ sort: non-rejected first, rejected last
│ └─ gate → (rejected, tier, tau x current_exit_discount, rank_cost, hops)
│ additive → (rejected, tier, 0.0, rank_cost, hops)
│ tier splits clean from smoky only when the gate runs with
│ clean_extinction_threshold &amp;gt; 0 (not the default)
└─ if all rejected → un-reject the least-bad route as fallback
├─ gate → by (tau_route, rank_cost), held by fallback_switch_margin
└─ additive → lowest composite
3. Pick best = ranked[0]
└─ if best is hard-rejected (not a fallback) → skip (return None)
4. Compare best.exit_id to agent&amp;#39;s current exit
├─ same exit → reroute only if the new path beats the path actually
│ being walked by more than 10 % on rank_cost (&amp;#34;better_path&amp;#34;),
│ else update the cached path silently and return None
└─ different exit → exit-switch anchor, then reroute_agent(wait_info, best.path)
├─ candidates are tried in rank order; a promotion the anchor would
│ refuse is skipped so it cannot hide the rest of the list
├─ anchor: adopt only if rank_cost &amp;lt; old_cost * exit_switch_anchor
│ ├─ bypassed when the old exit is FED-lethal or impassably smoky
│ └─ gate: bypassed when the rival is feasible, clearer in metres by
│ the anchor margin, AND either a whole band clearer or clean
│ while the current exit is not
├─ rewrite path_choices deterministically along new path
├─ retarget agent to first unvisited stage in new path
└─ return RouteSwitch record&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>When &lt;code>rank_routes&lt;/code> returns nothing at all — a discovery agent whose
known subgraph holds no exit — the agent is sent toward the nearest
unexplored frontier node instead of standing still (&lt;code>explore&lt;/code>), or
patrols known nodes when its knowledge is exhausted (&lt;code>wander&lt;/code>).&lt;/p>
&lt;h3>When a switch is triggered&lt;span class="hx-absolute -hx-mt-20" id="when-a-switch-is-triggered">&lt;/span>
&lt;a href="#when-a-switch-is-triggered" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>An exit switch is recorded when &lt;strong>all four&lt;/strong> conditions hold:&lt;/p>
&lt;ol>
&lt;li>The agent&amp;rsquo;s reevaluation tick fires (staggered offset + interval).&lt;/li>
&lt;li>&lt;code>rank_routes&lt;/code> finds a best route that is not hard-rejected.&lt;/li>
&lt;li>That best route leads to a &lt;strong>different exit&lt;/strong> than the current one.&lt;/li>
&lt;li>It clears the exit-switch anchor, or qualifies for one of the anchor
bypasses (old exit FED-lethal or impassably smoky; or, under the gate, a
feasible rival clearer in metres of sighting distance by the anchor margin
&lt;em>and&lt;/em> either a whole visibility band clearer or clean while the current exit
is not).&lt;/li>
&lt;/ol>
&lt;p>No switch is recorded when:&lt;/p>
&lt;ul>
&lt;li>The agent has not yet reached its offset time.&lt;/li>
&lt;li>The source node is missing from the graph (e.g., agent is in a stage not included in the routing graph).&lt;/li>
&lt;li>All routes are hard-rejected and none was un-rejected as a fallback.&lt;/li>
&lt;li>The anchor holds the agent on its current exit.&lt;/li>
&lt;li>The best route leads to the same exit — though the path to it may
still be rewritten, which is recorded as &lt;code>better_path&lt;/code>.&lt;/li>
&lt;/ul>
&lt;h3>Route switch reasons&lt;span class="hx-absolute -hx-mt-20" id="route-switch-reasons">&lt;/span>
&lt;a href="#route-switch-reasons" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Each &lt;code>RouteSwitch&lt;/code> record includes a &lt;code>reason&lt;/code> field:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Reason&lt;/th>
&lt;th>Condition&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>initial&lt;/code>&lt;/td>
&lt;td>Agent had no previous exit assignment&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>smoke_reroute&lt;/code>&lt;/td>
&lt;td>Best route is a different exit (lower &lt;code>rank_cost&lt;/code>)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>fallback&lt;/code>&lt;/td>
&lt;td>Best route was un-rejected as fallback (all routes rejected)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>better_path&lt;/code>&lt;/td>
&lt;td>Same exit, but a path more than 10 % cheaper on &lt;code>rank_cost&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>explore&lt;/code>&lt;/td>
&lt;td>No exit known yet; heading to the nearest unexplored frontier&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>wander&lt;/code>&lt;/td>
&lt;td>Knowledge exhausted; patrolling known nodes&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>Segment caching&lt;span class="hx-absolute -hx-mt-20" id="segment-caching">&lt;/span>
&lt;a href="#segment-caching" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Route evaluation supports an optional &lt;code>cached_segments&lt;/code> dictionary.
When provided, segment costs are cached by &lt;code>(source, target)&lt;/code> key and
reused across route evaluations within the same timestep. This avoids
redundant extinction sampling when multiple candidate routes share
segments. Under &lt;code>anticipate&lt;/code> the same edge on two routes is priced at
two different arrival times, so the key becomes
&lt;code>(source, target, round(arrival_time_s))&lt;/code>. The re-measured first leg of
a position-aware route is deliberately not cached: it belongs to one
agent&amp;rsquo;s position, and the cache is shared across agents in a pass.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>cache: dict[SegmentCacheKey, SegmentCost] &lt;span style="color:#f92672">=&lt;/span> {}
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>ranked &lt;span style="color:#f92672">=&lt;/span> rank_routes(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> graph, source, time_s, current_fed,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> extinction_sampler, fed_rate_sampler, config,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> cached_segments&lt;span style="color:#f92672">=&lt;/span>cache,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h2>Data structures&lt;span class="hx-absolute -hx-mt-20" id="data-structures">&lt;/span>
&lt;a href="#data-structures" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The routing module uses two main data structures for cost reporting.&lt;/p>
&lt;h3>&lt;code>SegmentCost&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="segmentcost">&lt;/span>
&lt;a href="#segmentcost" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Cost breakdown for one edge of a route:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Field&lt;/th>
&lt;th>Type&lt;/th>
&lt;th>Description&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>source&lt;/code>&lt;/td>
&lt;td>&lt;code>str&lt;/code>&lt;/td>
&lt;td>Source stage ID&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>target&lt;/code>&lt;/td>
&lt;td>&lt;code>str&lt;/code>&lt;/td>
&lt;td>Target stage ID&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>length_m&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Segment length (polyline arc length)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>k_avg&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Mean extinction along the segment&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>speed_factor&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Speed multiplier from smoke law&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>travel_time_s&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Estimated travel time&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>fed_growth&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Estimated FED increase&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>visible&lt;/code>&lt;/td>
&lt;td>&lt;code>bool&lt;/code>&lt;/td>
&lt;td>Whether &lt;code>k_avg&lt;/code> is below &lt;code>visibility_extinction_threshold&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>k_max&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Worst extinction sampled on the segment&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>arrival_time_s&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Time the segment was priced at (see anticipation)&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h3>&lt;code>RouteCost&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="routecost">&lt;/span>
&lt;a href="#routecost" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Full cost evaluation for one candidate route:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Field&lt;/th>
&lt;th>Type&lt;/th>
&lt;th>Description&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>exit_id&lt;/code>&lt;/td>
&lt;td>&lt;code>str&lt;/code>&lt;/td>
&lt;td>Target exit stage ID&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>path&lt;/code>&lt;/td>
&lt;td>&lt;code>list[str]&lt;/code>&lt;/td>
&lt;td>Stage IDs from source to exit&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>path_length_m&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Total path length&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>k_ave_route&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Length-weighted mean extinction&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>travel_time_s&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Total estimated travel time&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>fed_max_route&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Projected cumulative FED&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>composite_cost&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Additive cost; reported but not ranking under the gate&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>rank_cost&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>The number ordering, the anchor and the same-exit test read&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>segments&lt;/code>&lt;/td>
&lt;td>&lt;code>list[SegmentCost]&lt;/code>&lt;/td>
&lt;td>Per-segment breakdowns&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>queue_time_s&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Estimated queueing time at exit&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>k_max_route&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Worst extinction anywhere on the route. Reported; used only by the all-refused fallback&amp;rsquo;s switch margin&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>tau_route&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Route optical depth &lt;code>k_ave_route * effective_length&lt;/code> (gate). Refuses the route, orders the survivors, and orders the all-refused fallback&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>k_leg_max&lt;/code>&lt;/td>
&lt;td>&lt;code>float&lt;/code>&lt;/td>
&lt;td>Extinction of the route&amp;rsquo;s smokiest leg, each leg taken as its own mean. Decides clean-exit membership&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>clean&lt;/code>&lt;/td>
&lt;td>&lt;code>bool&lt;/code>&lt;/td>
&lt;td>Whether &lt;code>k_leg_max&lt;/code> is within the clean-exit limit. Always &lt;code>False&lt;/code> at the default &lt;code>clean_extinction_threshold = 0.0&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>feasible&lt;/code>&lt;/td>
&lt;td>&lt;code>bool&lt;/code>&lt;/td>
&lt;td>Optical depth and dose both allow the route (gate)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>rejected&lt;/code>&lt;/td>
&lt;td>&lt;code>bool&lt;/code>&lt;/td>
&lt;td>Whether route was rejected&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>rejection_reason&lt;/code>&lt;/td>
&lt;td>&lt;code>str | None&lt;/code>&lt;/td>
&lt;td>Reason for rejection; &lt;code>fallback: &lt;/code> prefix when un-rejected&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;code>rank_cost&lt;/code>, &lt;code>k_max_route&lt;/code>, &lt;code>tau_route&lt;/code>, &lt;code>k_leg_max&lt;/code>, &lt;code>clean&lt;/code> and &lt;code>feasible&lt;/code> are
all written to the route-cost CSV (&lt;code>run.py --output-route-cost-history&lt;/code>), so the
gate&amp;rsquo;s decisions can be audited from its own output. &lt;code>composite_cost&lt;/code> is
deliberately not enough on its own: it does not rank under the gate, and the
quantity that does is &lt;code>tau_route&lt;/code>.&lt;/p>
&lt;h2>References&lt;span class="hx-absolute -hx-mt-20" id="references">&lt;/span>
&lt;a href="#references" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/">route-cost-gate.md&lt;/a>
&amp;ndash; the gate model: the
optical-depth criterion, the ordering, the fallback, the full &lt;code>routing&lt;/code>
key table, and known limitations.&lt;/li>
&lt;li>&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/docs/gate-model-review-notes.md" target="_blank" rel="noopener">gate-model-review-notes.md&lt;/a>
&amp;ndash; provenance
against &lt;code>materials/evac.f90&lt;/code> and the open questions.&lt;/li>
&lt;li>FDS+Evac Technical Reference and User&amp;rsquo;s Guide
&amp;ndash; Korhonen (2021). Smoke-interaction model (Section 3.4).&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/routing/">Routing model&lt;/a>
&amp;ndash; defaults and deviations from the
literature; the published laws are on
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/extinction/">Extinction coefficient&lt;/a>
and
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/">Visibility through smoke&lt;/a>
.&lt;/li>
&lt;li>&lt;a href="https://doi.org/10.1016/j.firesaf.2024.104269" target="_blank" rel="noopener">Boerger et al. (2024)&lt;/a>
&amp;ndash; Beer-Lambert integrated extinction along line of sight (Eq. 8-9),
waypoint-based visibility maps. Fire Safety Journal 150:104269.&lt;/li>
&lt;li>&lt;a href="https://doi.org/10.1016/j.firesaf.2020.103154" target="_blank" rel="noopener">Schroder et al. (2020)&lt;/a>
&amp;ndash;
A map representation of the ASET-RSET concept. Fire Safety Journal.&lt;/li>
&lt;li>Ehtamo, H., Heliövaara, S., Korhonen, T. &amp;amp; Hostikka, S. (2010).
Game theoretic best-response dynamics for evacuees&amp;rsquo; exit selection.
&lt;em>Advances in Complex Systems&lt;/em>, 13(1), 113–134.&lt;/li>
&lt;/ul></description></item><item><title>The gate cost model</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/</guid><description>
&lt;blockquote>
&lt;p>Part of &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/README.md" target="_blank" rel="noopener">pyFDS-Evac&lt;/a>
. Reference for &lt;code>routing.cost_model&lt;/code>.
Provenance, review findings and the open questions live in
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/docs/gate-model-review-notes.md" target="_blank" rel="noopener">gate-model-review-notes.md&lt;/a>
; this page documents
what the shipped code does.&lt;/p>&lt;/blockquote>
&lt;p>Route choice runs under one of two cost models, selected per deck with
&lt;code>routing.cost_model&lt;/code> in the scenario JSON:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>value&lt;/th>
&lt;th>what decides the exit&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>&amp;quot;gate&amp;quot;&lt;/code> (default)&lt;/td>
&lt;td>Route optical depth decides which exits are &lt;em>available&lt;/em> and orders the survivors; travel time breaks ties.&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>&amp;quot;additive&amp;quot;&lt;/code>&lt;/td>
&lt;td>Smoke is a toll per metre walked, folded into one composite cost.&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>Under the gate one quantity does the whole of the smoke reasoning: the route&amp;rsquo;s
optical depth &lt;code>tau = K_ave * L&lt;/code>, the soot column the agent walks through. It
refuses a route, it orders the routes it does not refuse, and it weights every
Dijkstra edge — see &lt;a href="#how-the-gate-decides">How the gate decides&lt;/a>
.&lt;/p>
&lt;p>Both are implemented in &lt;code>pyfds_evac/core/route_graph.py&lt;/code>
(&lt;code>RouteCostConfig&lt;/code>, &lt;code>evaluate_route&lt;/code>, &lt;code>rank_routes&lt;/code>, &lt;code>evaluate_and_reroute&lt;/code>).&lt;/p>
&lt;h2>Quick start&lt;span class="hx-absolute -hx-mt-20" id="quick-start">&lt;/span>
&lt;a href="#quick-start" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Nothing to configure: the gate is the default. To pin the pre-gate behaviour
of an existing deck, set &lt;strong>both&lt;/strong> keys — &lt;code>anticipate&lt;/code> is independent of
&lt;code>cost_model&lt;/code> and defaults to &lt;code>true&lt;/code>:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-json" data-lang="json">&lt;span style="display:flex;">&lt;span>{
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;routing&amp;#34;&lt;/span>: {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;cost_model&amp;#34;&lt;/span>: &lt;span style="color:#e6db74">&amp;#34;additive&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;anticipate&amp;#34;&lt;/span>: &lt;span style="color:#66d9ef">false&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>}&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>To make the gate stricter or laxer, change &lt;code>tau_max&lt;/code> — the optical depth a
route may carry before it is refused:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-json" data-lang="json">&lt;span style="display:flex;">&lt;span>{
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;routing&amp;#34;&lt;/span>: {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;cost_model&amp;#34;&lt;/span>: &lt;span style="color:#e6db74">&amp;#34;gate&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;tau_max&amp;#34;&lt;/span>: &lt;span style="color:#ae81ff">6.0&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;tau_return_margin&amp;#34;&lt;/span>: &lt;span style="color:#ae81ff">0.8&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;current_exit_discount&amp;#34;&lt;/span>: &lt;span style="color:#ae81ff">0.9&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;tau_deadband&amp;#34;&lt;/span>: &lt;span style="color:#ae81ff">0.1&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>}&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;strong>No vismap precondition.&lt;/strong> The gate reads no visibility model. &lt;code>b16e900&lt;/code>
unified the criterion on the route polyline and &lt;code>89d13d4&lt;/code> removed the
&lt;code>_gate_needs_sight&lt;/code> precompute that a gate deck used to trigger, so a
familiarity-1.0 gate deck now runs with no &lt;code>--vis-cache&lt;/code> at all: &lt;code>l_corridor&lt;/code>
takes 5 seconds and reproduces the cached run&amp;rsquo;s 84/16. A visibility model is
still built for decks with discovery agents, which consult it to learn the
graph.&lt;/p>
&lt;h2>How the gate decides&lt;span class="hx-absolute -hx-mt-20" id="how-the-gate-decides">&lt;/span>
&lt;a href="#how-the-gate-decides" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;blockquote>
&lt;p>&lt;strong>This page describes &lt;code>081380b&lt;/code>.&lt;/strong>&lt;/p>&lt;/blockquote>
&lt;p>Per agent, per reevaluation tick:&lt;/p>
&lt;ol>
&lt;li>&lt;strong>Path per exit.&lt;/strong> Dijkstra runs with each edge weighted by its own optical
depth, &lt;code>k_avg * length&lt;/code> (plus a &lt;code>1e-6 * length&lt;/code> floor, below).&lt;/li>
&lt;li>&lt;strong>Availability.&lt;/strong> Each candidate route is tested twice. &lt;strong>Optical depth:&lt;/strong>
it is refused when &lt;code>tau = K_ave * L_eff&lt;/code> exceeds &lt;code>tau_max&lt;/code> (default 6), or
&lt;code>tau_max * tau_return_margin&lt;/code> if it is not the exit the agent already walks
to. &lt;strong>Dose:&lt;/strong> it is refused when its projected FED exceeds
&lt;code>fed_rejection_threshold&lt;/code>. Either refusal alone removes the route.&lt;/li>
&lt;li>&lt;strong>Ordering among survivors.&lt;/strong> Optical depth first, travel time second. The
sort key is &lt;code>(rejected, tier, tau, rank_cost, hops)&lt;/code> with
&lt;code>rank_cost = travel_time_s + w_queue * queue_time_s&lt;/code>. The &lt;code>tau&lt;/code> in the key
is scaled by &lt;code>current_exit_discount&lt;/code> (0.9) for the exit the agent already
heads for. &lt;code>tier&lt;/code> is the clean-exit tier, off by default.&lt;/li>
&lt;li>&lt;strong>Adoption.&lt;/strong> The ordering proposes; the exit-switch anchor disposes.
&lt;code>evaluate_and_reroute&lt;/code> walks the ranked list and takes the first candidate
&lt;code>_anchor_allows&lt;/code> admits, stopping at the agent&amp;rsquo;s own exit — so the head of
the sort is not necessarily the exit the agent walks to. See
&lt;a href="#churn-protection">Churn protection&lt;/a>
for the rule.&lt;/li>
&lt;/ol>
&lt;p>&lt;strong>Three separate &lt;code>tau&lt;/code> comparisons happen per tick&lt;/strong>, and it is worth keeping
them apart: &lt;code>tau_return_margin&lt;/code> decides which routes are &lt;em>candidates&lt;/em>,
&lt;code>current_exit_discount&lt;/code> decides their &lt;em>order&lt;/em>, and &lt;code>tau_deadband&lt;/code> decides
whether the agent &lt;em>acts&lt;/em> on that order. Only the third can change an exit.&lt;/p>
&lt;p>&lt;strong>One quantity refuses a route, orders it, and weights its edges.&lt;/strong> That is the
change at &lt;code>0d9bf79&lt;/code>. Before it, Dijkstra minimised the additive composite, the
survivors were ordered by travel time, and the gate judged them on optical
depth — three currencies, and a gate could refuse an exit on a smoky path while
a longer passable path to the same exit existed and was never offered.&lt;/p>
&lt;p>&lt;strong>Optical depth can order routes where a visibility band could not&lt;/strong>, because
&lt;code>tau = K_ave * L&lt;/code> already contains the distance. Two routes through equally thin
haze order by length; in clear air every &lt;code>tau&lt;/code> is zero and travel time decides
alone; a cleaner route wins only by carrying enough less smoke to pay for its
extra metres. A band compared cleanliness with no reference to how far the agent
had to carry it, which is why it had to be kept out of the main sort: on
&lt;code>l_corridor&lt;/code> it put agents on the 58 m route while both routes read
&lt;code>k_ave = 0.000&lt;/code>. &lt;code>_visibility_band&lt;/code>, &lt;code>_sighting_distance&lt;/code>, &lt;code>band_width_m&lt;/code> and
the saturation constant are all gone at &lt;code>0d9bf79&lt;/code>.&lt;/p>
&lt;p>&lt;strong>The &lt;code>1e-6 * length&lt;/code> floor on the edge weight is not a tuning constant.&lt;/strong> In
clear air every &lt;code>k_avg&lt;/code> is zero, so without it every path ties at weight zero
and Dijkstra returns an arbitrary one. The floor makes the tie break on length.
It is not configurable and has not been measured for its effect at small
nonzero &lt;code>K&lt;/code>.&lt;/p>
&lt;p>In clear air &lt;code>K = 0&lt;/code>, every &lt;code>tau&lt;/code> is zero, nothing is refused, and the gate
reduces to fastest-exit — which in clear air is nearest-exit. The two models
were measured identical on the &lt;code>world_100&lt;/code> (7712 route-cost rows) and
&lt;code>t_junction&lt;/code> (4030 rows) clear-air runs, and the &lt;code>l_corridor&lt;/code> clear-air pair
evacuates 100/0 to the near exit under both with zero switches. &lt;strong>Those runs
predate &lt;code>0d9bf79&lt;/code> entirely&lt;/strong>, and the ordering has changed since. Equivalence
has not been re-measured under &lt;code>tau&lt;/code> ordering, at &lt;code>K = 0&lt;/code> or at &lt;code>K = 1e-4&lt;/code>.&lt;/p>
&lt;h3>Optical depth: what it measures, and what it does not&lt;span class="hx-absolute -hx-mt-20" id="optical-depth-what-it-measures-and-what-it-does-not">&lt;/span>
&lt;a href="#optical-depth-what-it-measures-and-what-it-does-not" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>tau = K_ave * L_eff&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;code>K_ave&lt;/code> is the length-weighted mean extinction over the route&amp;rsquo;s own polyline;
&lt;code>L_eff&lt;/code> is the distance still to walk. The product is the Beer-Lambert integral
of extinction along the walked path — the soot column the agent passes through.
It is an &lt;strong>exposure&lt;/strong> statement.&lt;/p>
&lt;p>&lt;strong>It is not a sighting distance.&lt;/strong> The criterion grew out of one (see
&lt;a href="#where-the-6-comes-from">Where the 6 comes from&lt;/a>
below), but Jin&amp;rsquo;s visibility
law (&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/">Visibility through smoke&lt;/a>
) describes a
straight, unobstructed line to a sign. Integrating &lt;code>K&lt;/code> around two corners
measures how much smoke you walk through, not how far you can see. The two
coincide only on a straight corridor. The names changed at &lt;code>0d9bf79&lt;/code> to say
what the quantity is.&lt;/p>
&lt;p>&lt;strong>The estimator averages &lt;code>K&lt;/code>; it does not take the route&amp;rsquo;s worst point.&lt;/strong> A
maximum over sampled cells is a step function of where the agent stands: one
dense cell entering the sample swings the estimate by an order of magnitude
between ticks, and measured on &lt;code>world100&lt;/code> the same 28.9 m route reported 91 m of
sight, then 8 m, then 91 m again on consecutive seconds, with the ordering
flipping each time. &lt;code>k_max_route&lt;/code> is still computed and reported, and still
decides the all-refused fallback&amp;rsquo;s switch margin, where the question is which
walk is survivable rather than which is cleanest.&lt;/p>
&lt;p>&lt;strong>The line of sight is a diagnostic, not a gate.&lt;/strong> fdsvismap&amp;rsquo;s obstruction-aware
sight line to an exit&amp;rsquo;s own sign is the more faithful measurement of what an
occupant can see, but it is defined only where a sign resolves — so selecting
the criterion per exit let sign geometry decide &lt;em>which exits were tested at
all&lt;/em>. Measured before &lt;code>b16e900&lt;/code>: on &lt;code>l_corridor&lt;/code> the far exit lies around two
corners, never resolved a sight line, was therefore never tested, and the
diversion the gate exists to produce vanished (84/16 became 100/0). On
&lt;code>world100&lt;/code> one exit was tested by sight line 43 times and fell back 2095 times,
so moving a sign two metres would have changed which exits were gated. Mixing
the two was worse still: the same 22 m route read 9.9 m on one tick and 68.3 m
on the next as the sight line resolved. Not seeing a sign is a fact about
wayfinding, not about whether a route can be walked, so it belongs in the
cognitive map (&lt;code>cognitive_map.expand_from_visibility&lt;/code>), not in this gate.&lt;/p>
&lt;p>The rejection reason names the quantity and both factors of it:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>tau 8.41 &amp;gt; 6.00 (K_ave 0.145 x 58.0 m)
tau 5.20 &amp;gt; 4.80 (K_ave 0.388 x 13.4 m)&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The second is a rival exit, held to &lt;code>tau_max * tau_return_margin&lt;/code> = 4.8.&lt;/p>
&lt;p>&lt;a id="where-the-6-comes-from">&lt;/a>&lt;strong>Where the 6 comes from.&lt;/strong> FDS+Evac&amp;rsquo;s tier-4 door test computes
&lt;code>L2_tmp = d * 0.5 / (3.0 / K_ave_Door)&lt;/code> and strikes the door out when
&lt;code>L2_tmp &amp;gt;= 1.0&lt;/code> (&lt;code>evac.f90:16458, :16463&lt;/code>). That expression is &lt;code>K_ave * d / 6&lt;/code>, so
the test is exactly &lt;code>tau &amp;gt; 6&lt;/code> with Jin&amp;rsquo;s &lt;code>c = 3&lt;/code>. The threshold is therefore
citable, but &lt;strong>the quantity it is applied to is not the same quantity&lt;/strong>:
FDS+Evac&amp;rsquo;s &lt;code>K_ave_Door&lt;/code> is a mean along &lt;code>See_door&lt;/code>&amp;rsquo;s straight sight line, and
for a door with no resolved sight line the distance is an L1 norm
(&lt;code>evac.f90:16460&lt;/code>); pyFDS-Evac averages &lt;code>K&lt;/code> along the walked polyline.
Two further scope limits: the test lives in the tier-4 last-resort branch,
reached only once no smoke-free door is available and looping only over doors
that are already known or visible; and in FDS+Evac a struck-out door is struck
out &lt;em>permanently&lt;/em> (&lt;code>Is_Visible_Door(i) = .FALSE.&lt;/code>, &lt;code>:16464-16465&lt;/code>), which
pyFDS-Evac deliberately does not do. So: the gate is &lt;em>inspired by&lt;/em> FDS+Evac&amp;rsquo;s
tier-4 visibility door rule and inherits its threshold with a citation; it does
not implement it. &lt;strong>&lt;code>tau_max&lt;/code> has not been calibrated against a soot-dose or
FED-equivalent limit.&lt;/strong> That is open work.&lt;/p>
&lt;p>Under FDS+Evac&amp;rsquo;s &lt;em>primary&lt;/em> rule the criterion is different again — minimise time
among doors satisfying &lt;code>K_ave_Door &amp;lt; ABS(FED_DOOR_CRIT)&lt;/code> = 0.03 /m
(&lt;code>evac.f90:16265&lt;/code>, &lt;code>:16272&lt;/code>; &lt;code>FED_DOOR_CRIT = -100&lt;/code> becomes &lt;code>3.0/100&lt;/code> at
&lt;code>:5262&lt;/code>). pyFDS-Evac ships that absolute criterion as the opt-in clean-exit tier
below. See &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/model-comparison/#the-smoke-criteria-on-a-door">model-comparison.md&lt;/a>
.&lt;/p>
&lt;h3>The diversion is a departure from FDS+Evac, not a reproduction of it&lt;span class="hx-absolute -hx-mt-20" id="the-diversion-is-a-departure-from-fdsevac-not-a-reproduction-of-it">&lt;/span>
&lt;a href="#the-diversion-is-a-departure-from-fdsevac-not-a-reproduction-of-it" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>The threshold is borrowed; &lt;strong>the place the quantity is used is not&lt;/strong>, and that
difference is what produces the diversion the model exists for.&lt;/p>
&lt;p>In FDS+Evac&amp;rsquo;s first three tiers the rank is &lt;code>T_tmp&lt;/code> — a time in tier 1 when
&lt;code>FAC_DOOR_QUEUE&lt;/code> is active, a plain L2 or L1 distance norm otherwise — and
&lt;code>K_ave_Door&lt;/code> enters only as the boolean admission test
&lt;code>L2_tmp &amp;lt; ABS(FED_DOOR_CRIT)&lt;/code>:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-fortran" data-lang="fortran">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">IF&lt;/span> (T_tmp &lt;span style="color:#f92672">&amp;lt;&lt;/span> L2_min .AND. L2_tmp &lt;span style="color:#f92672">&amp;lt;&lt;/span> ABS(FED_DOOR_CRIT)) &lt;span style="color:#66d9ef">THEN&lt;/span> &lt;span style="color:#75715e">! :16265, :16354, :16401
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e">&lt;/span> L2_min &lt;span style="color:#f92672">=&lt;/span> MAX(&lt;span style="color:#ae81ff">0.0_EB&lt;/span>, T_tmp)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> i_tmp &lt;span style="color:#f92672">=&lt;/span> i
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">END&lt;/span> &lt;span style="color:#66d9ef">IF&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Smoke can move a door between tiers, or out of the admitted set; it cannot
reorder the doors inside a tier, and geometry does not reorder itself. Here
&lt;code>tau&lt;/code> &lt;strong>is&lt;/strong> the ordering, at every tick and for every candidate.&lt;/p>
&lt;p>&lt;strong>One qualification, and it matters.&lt;/strong> Smoke is not absent from FDS+Evac&amp;rsquo;s
ordering everywhere: the tier-4 last-resort branch minimises &lt;code>L2_tmp&lt;/code> directly
(&lt;code>IF (L2_tmp &amp;lt; L2_min)&lt;/code>, &lt;code>:16467&lt;/code>), and under the default &lt;code>FED_DOOR_CRIT &amp;lt; 0&lt;/code>
that &lt;code>L2_tmp&lt;/code> is &lt;code>tau/6&lt;/code>. So the reference does rank on smoke — but only after
tiers 1-3 have all failed to find any admitted door, only over doors already
known or visible, on a bee-line (or L1) distance to the door rather than a
walked route, and with a door struck out there struck out &lt;em>permanently&lt;/em>
(&lt;code>:16463-16465&lt;/code>). Stated exactly: &lt;strong>pyFDS-Evac promotes FDS+Evac&amp;rsquo;s last-resort
ranking criterion to its primary one, and drops the memory that makes it stable
there.&lt;/strong> &amp;ldquo;Smoke never enters FDS+Evac&amp;rsquo;s ordering&amp;rdquo; is too strong and should not
be written; &amp;ldquo;smoke never enters the ordering until every smoke-free tier is
exhausted&amp;rdquo; is what the source supports.&lt;/p>
&lt;p>&lt;strong>The measured consequence, and what it is not.&lt;/strong> On &lt;code>l_corridor&lt;/code> the model
diverts 18 of 100 agents to the longer, cleaner route. The reference criterion
would not: tiers 1-3 rank on distance, both doors clear the &lt;code>K_ave &amp;lt; 0.03 /m&lt;/code>
admission test until the smoke is well developed, and the near exit is nearer
throughout — so essentially every occupant goes near, roughly 100/0. &lt;strong>That
100/0 is a reasoned prediction from the source, not a measured FDS+Evac run.&lt;/strong>
No run of the reference criterion on this deck exists here. The nearest
empirical proxy is the deck&amp;rsquo;s own additive model, which also ranks on a
distance-like composite and does evacuate 100/0 (see &lt;a href="#evidence">Evidence&lt;/a>
) —
a proxy, not the same criterion. The diversion stands or falls on its own
merits, not on fidelity to the reference.&lt;/p>
&lt;p>&lt;strong>A geometric bias rides along with the change of quantity.&lt;/strong> FDS+Evac applies
its threshold to a straight-line &lt;code>d&lt;/code>; pyFDS-Evac applies it to the walked
polyline &lt;code>L&lt;/code>, and &lt;code>L/d&lt;/code> is not the same on every route. On &lt;code>l_corridor&lt;/code> from the
spawn centroid (11.5, 15.0). Both legs are computed corner-to-corner along the
corridor centreline, which is why they come out a metre under the 26 m / 46 m
node-to-node figures the &lt;a href="#evidence">Evidence&lt;/a>
section quotes — those run
spawn-polygon to exit-polygon through the graph&amp;rsquo;s own nodes:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>route&lt;/th>
&lt;th>walked &lt;code>L&lt;/code>&lt;/th>
&lt;th>bee-line &lt;code>d&lt;/code>&lt;/th>
&lt;th>&lt;code>L/d&lt;/code>&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>near (exit A, (0, 1.5))&lt;/td>
&lt;td>25.0 m&lt;/td>
&lt;td>17.7 m&lt;/td>
&lt;td>&lt;strong>1.410&lt;/strong>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>far (exit B, (45, 26.5))&lt;/td>
&lt;td>45.0 m&lt;/td>
&lt;td>35.4 m&lt;/td>
&lt;td>&lt;strong>1.271&lt;/strong>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>At equal &lt;code>K_ave&lt;/code> the polyline form is therefore about 11 % (1.410 / 1.271)
stricter on the near route than the far one, purely from geometry — a
systematic tilt toward the diversion that no smoke measurement put there. The
tilt is against the L2 bee line specifically; against the L1 norm FDS+Evac uses
for non-visible doors (&lt;code>:16460&lt;/code>) it vanishes on this deck, because both
corridors are axis-aligned and the L1 distance equals the polyline exactly. The
sign and size of the bias on other geometries have not been measured.&lt;/p>
&lt;h3>Route choice is an optimality bound, not a perception-limited model&lt;span class="hx-absolute -hx-mt-20" id="route-choice-is-an-optimality-bound-not-a-perception-limited-model">&lt;/span>
&lt;a href="#route-choice-is-an-optimality-bound-not-a-perception-limited-model" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Issue &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/125" target="_blank" rel="noopener">#125&lt;/a>
. Two assumptions
apply to the same agent on the same tick and point in opposite directions:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>The cognitive map assumes the agent does not know the building.&lt;/strong> A
&lt;code>familiarity = 0&lt;/code> agent routes only over &lt;code>cognitive_subgraph&lt;/code> and does not
know an exit exists until it reads a sign — through &lt;code>VisibilityModel&lt;/code>, which
is genuinely perception-limited (obstruction-aware sight lines, sign facing,
extinction along the line).&lt;/li>
&lt;li>&lt;strong>Route choice assumes the agent knows the smoke field.&lt;/strong> To choose among the
exits it does know, it integrates &lt;code>tau = K_ave * L_remaining&lt;/code> over the whole
remaining route, including legs it has never visited and corners it cannot see
past, and with &lt;code>anticipate = True&lt;/code> and &lt;code>foresight_horizon_s = inf&lt;/code> at times
that have not happened yet. The extinction sampler is global; the cognitive
map never touches it.&lt;/li>
&lt;/ul>
&lt;p>These are different kinds of knowledge — topology versus state — so it is not a
formal contradiction. It is still not a coherent position, and it is sharpest
exactly where the discovery tier&amp;rsquo;s modelling is most careful.&lt;/p>
&lt;p>&lt;strong>What this affects.&lt;/strong> &lt;em>Not&lt;/em> map growth: what an agent learns and when runs
through &lt;code>VisibilityModel&lt;/code> alone, so results about map expansion, exploration
order, wander behaviour and the lost-exit failure mode stand. What it undercuts
is any claim that a &lt;strong>discovery agent&amp;rsquo;s route choice&lt;/strong> is perception-limited. It
is not.&lt;/p>
&lt;p>&lt;strong>So name what the model is.&lt;/strong> Route choice here is an &lt;strong>optimality bound&lt;/strong>:
what an evacuee with perfect knowledge of the smoke field would choose &lt;em>over the
part of the building it happens to know&lt;/em>. Not &amp;ldquo;what a perfectly informed
evacuee would choose&amp;rdquo; — the topology restriction is real and still binds. That
bound is a legitimate and useful thing to publish, and the discovery tier
remains a perception-limited model of &lt;em>wayfinding&lt;/em>; but the two are different
claims and only one of them is about smoke.&lt;/p>
&lt;p>Nothing is fixed here. &lt;code>foresight_horizon_s&lt;/code> is the one lever already shipped
that bounds half of it (the temporal half); it defaults to &lt;code>inf&lt;/code>. #125 records
the three options and evaluates none.&lt;/p>
&lt;h3>Two asymmetries favour the exit the agent already walks to&lt;span class="hx-absolute -hx-mt-20" id="two-asymmetries-favour-the-exit-the-agent-already-walks-to">&lt;/span>
&lt;a href="#two-asymmetries-favour-the-exit-the-agent-already-walks-to" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;code>tau_return_margin&lt;/code> (default 0.8) is a deadband on &lt;strong>feasibility&lt;/strong>. The current
route is judged against the bare &lt;code>tau_max&lt;/code>; a rival must come in under
&lt;code>tau_max * 0.8&lt;/code> before it is even a candidate:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>budget = tau_max (current exit)
budget = tau_max * tau_return_margin (any other exit)&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Without it a route whose &lt;code>tau&lt;/code> sits near the budget toggles in and out of the
feasible set every tick and the agent follows it. It replaces the old
&lt;code>sight_return_margin&lt;/code>, which multiplied a sight requirement (1.25 &lt;em>up&lt;/em>) where
this one scales a budget (0.8 &lt;em>down&lt;/em>).&lt;/p>
&lt;p>&lt;code>current_exit_discount&lt;/code> (default 0.9) is a deadband on &lt;strong>ordering&lt;/strong>. Only the
current exit&amp;rsquo;s &lt;code>tau&lt;/code> is discounted, and only in the sort key, so it holds its
place unless a rival is clearly cleaner rather than momentarily cleaner.&lt;/p>
&lt;p>&lt;strong>Its provenance is FDS+Evac&amp;rsquo;s &lt;code>FAC_DOOR_OLD2 = 0.9&lt;/code>&lt;/strong> (&lt;code>evac.f90:1507&lt;/code>), which
is applied as &lt;code>L2_tmp = FAC_DOOR_OLD2 * L2_tmp&lt;/code> to the current door at &lt;code>:16290&lt;/code>
and &lt;code>:16467&lt;/code> — and at &lt;code>:16467&lt;/code> that &lt;code>L2_tmp&lt;/code> is the &lt;code>tau/6&lt;/code> of the tier-4 test,
i.e. the same quantity, discounted in the same place, inside the loop that
minimises it to pick a door. The shipped comment on &lt;code>tau_of&lt;/code> in &lt;code>route_graph.py&lt;/code>
cites that provenance correctly since &lt;code>9508181&lt;/code>; an earlier version cited
&lt;code>FAC_DOOR_WAIT&lt;/code>, which is at &lt;code>:1505&lt;/code> and discounts the current door&amp;rsquo;s &lt;em>travel
time&lt;/em> (&lt;code>T_tmp&lt;/code>), not its smoke. &lt;code>FAC_DOOR_WAIT&lt;/code> is still the correct citation for
&lt;code>exit_switch_anchor&lt;/code> and &lt;code>_PATH_IMPROVEMENT_THRESHOLD&lt;/code>, which are time
comparisons, and the code cites it only there.&lt;/p>
&lt;p>&lt;strong>A reviewer will ask why both.&lt;/strong> They act on different stages — one on the
feasible set, one on the order within it — but they have not been measured
independently, and no run isolates the contribution of either.&lt;/p>
&lt;h3>The clean-exit tier (off by default)&lt;span class="hx-absolute -hx-mt-20" id="the-clean-exit-tier-off-by-default">&lt;/span>
&lt;a href="#the-clean-exit-tier-off-by-default" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;code>clean_extinction_threshold&lt;/code> adds one rank above optical depth. A route whose
smokiest &lt;em>leg&lt;/em> stays at or below the threshold is &lt;code>clean&lt;/code>, and clean routes
outrank smoky ones outright however far they are; among routes of the same tier,
optical depth then time decides. The sort key is
&lt;code>(rejected, tier, tau, rank_cost, hops)&lt;/code>.&lt;/p>
&lt;p>This is FDS+Evac&amp;rsquo;s primary door rule (&lt;code>evac.f90:16265&lt;/code>, &lt;code>:16272&lt;/code>), and its
threshold is not a new constant: &lt;code>FED_DOOR_CRIT = -100&lt;/code> becomes &lt;code>3.0/100&lt;/code> =
0.03 /m at &lt;code>:5262&lt;/code>, which is Jin&amp;rsquo;s &lt;code>S = 3/K&lt;/code> at a 100 m sighting distance. Two
differences from the reference implementation are worth stating:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>FDS+Evac&amp;rsquo;s tier 1 is a hard filter.&lt;/strong> &lt;code>IF (T_tmp &amp;lt; L2_min .AND. L2_tmp &amp;lt; ABS(FED_DOOR_CRIT))&lt;/code> only ever selects a &lt;em>qualifying&lt;/em> door; when no door
qualifies the tier picks nothing and the search falls to the next tier of
doors. Falling through to plain time ranking when our tier is empty is
pyFDS-Evac&amp;rsquo;s choice, not FDS+Evac&amp;rsquo;s.&lt;/li>
&lt;li>&lt;strong>Membership is measured on the smokiest leg, each leg its own mean.&lt;/strong> The
route mean would dilute a smoky stretch with whatever clear corridor follows,
so a long route could qualify by being long — the mirror of the length
penalty the gate exists to remove. The worst &lt;em>sample&lt;/em> is the step function
that made sighting distances jump between ticks. FDS+Evac applies its 0.03 to
&lt;code>K_ave_Door&lt;/code>, a per-door average, for the same reason — though note that
&lt;code>K_ave_Door&lt;/code> is a mean along a bee-line sight line (&lt;code>See_door&lt;/code>,
&lt;code>evac.f90:16149&lt;/code>), not a maximum over legs.&lt;/li>
&lt;/ul>
&lt;p>&lt;code>clean_exit_margin&lt;/code> is hysteresis on membership for the exit the agent already
heads for: its limit is &lt;code>clean_extinction_threshold / clean_exit_margin&lt;/code>.
FDS+Evac supplies the value — &lt;code>FAC_DOOR_OLD = 0.1&lt;/code> (&lt;code>evac.f90:1506&lt;/code>), applied as
&lt;code>L2_tmp = FAC_DOOR_OLD * L2_tmp&lt;/code> for the current door (&lt;code>:16255&lt;/code>), so the door an
agent already walks to stays smoke-free up to ten times the criterion.&lt;/p>
&lt;p>The exit-switch anchor has a matching clause: a rival that is clean while the
current exit is not bypasses the anchor. It was added because the anchor&amp;rsquo;s other
bypass, then keyed on the visibility band, could not fire between a clean route
and one 3.3x smokier — both saturated the band. That reasoning is now stale: the
bypass compares optical depth, which does discriminate there. The clause has not
been re-measured since, and the tier ships off anyway.&lt;/p>
&lt;p>&lt;strong>It ships off (&lt;code>clean_extinction_threshold = 0.0&lt;/code>, which means no route is ever
clean) because measurement refuted it.&lt;/strong> On &lt;code>l_corridor&lt;/code> over five seeds the
far-exit share was 15-22 with the tier against 15-17 without, identical on four
of the five; the prediction that motivated the tier was 25-40. The tier is not
inert — it is non-empty on 52 % of decision ticks and changes the ordering on
41 % — but the effect is churn rather than redirection: agents flip toward the
clean exit and back before reaching the junction. Median RSET rises 17 %
(71.2 s to 83.7 s) and monotonicity goes from 0 returns to an abandoned exit to
34-38 agents per run.&lt;/p>
&lt;p>The proposed mechanism is a threshold too sharp for the signal. Tick-to-tick
movement in a leg mean has median 0.0072 /m and p90 0.134 /m, against a
hysteresis band of 0.0075 /m — narrower than the median jump, 116 crossings per
run. The band is also one-sided: the incumbent is relaxed to 0.0375 /m while a
rival is admitted at the bare 0.03 /m. And because tier membership is binary, a
crossing does not reorder the list, it swaps which objective is in force, so the
target jumps. FDS+Evac can afford that because a door leaves an agent&amp;rsquo;s known
set permanently; here nothing is remembered, by design.&lt;/p>
&lt;p>&lt;strong>Read the refutation as provisional.&lt;/strong> A band of 0.0075 /m is
&lt;code>0.03 / 0.8 - 0.03&lt;/code>, so those runs were made at &lt;code>clean_exit_margin = 0.8&lt;/code> — the
value &lt;code>from_routing_params&lt;/code> still hands out, and the value the same commit
records as invented. At the 0.1 that &lt;code>FAC_DOOR_OLD&lt;/code> supplies, the incumbent&amp;rsquo;s
limit is 0.3 /m and the band is 0.27 /m: twice the p90 of the drift instead of
a third of the median, which is a different regime for exactly the quantity the
mechanism blames. The tier has not been measured at 0.1. Anyone re-opening this
should set the key explicitly and re-run before trusting either the numbers or
the explanation.&lt;/p>
&lt;p>Turn it on for a deck that has a genuinely clean alternative to reach for, and
read the &lt;code>clean&lt;/code> and &lt;code>k_leg_max&lt;/code> columns of the route-cost CSV when you do.&lt;/p>
&lt;h3>Dose vetoes an exit; it does not rank&lt;span class="hx-absolute -hx-mt-20" id="dose-vetoes-an-exit-it-does-not-rank">&lt;/span>
&lt;a href="#dose-vetoes-an-exit-it-does-not-rank" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>FED enters route choice as a veto only. Each route&amp;rsquo;s &lt;code>fed_max_route&lt;/code> is the
dose already taken plus the dose predicted over the walk
(&lt;code>current_fed + sum(fed_growth)&lt;/code>), and a route above &lt;code>fed_rejection_threshold&lt;/code>
(default 1.0, incapacitation) is refused. Refusal is asymmetric in the same way
as optical depth: the current exit is held to the bare threshold so an agent
flees a lethal door at once, while a rival must come in under
&lt;code>fed_rejection_threshold * fed_return_margin&lt;/code> (0.9). A dose refusal is a
&amp;ldquo;must flee&amp;rdquo; rejection, so it bypasses the exit-switch anchor — hysteresis
cannot pin an agent to a door that will kill it.&lt;/p>
&lt;p>Surviving routes are then ordered by optical depth and time. Dose never makes
one exit outrank another; it only removes exits.
&lt;code>tests/test_route_gate.py::TestDoseVetoesAnExit&lt;/code> covers all three parts in clear
air, so the smoke gate cannot be the cause.&lt;/p>
&lt;p>&lt;strong>This is FDS+Evac&amp;rsquo;s other branch, and we run both halves at once.&lt;/strong> In
&lt;code>evac.f90&lt;/code> (&lt;code>Change_Target_Door&lt;/code>, :16439-:16467) the sign of &lt;code>FED_DOOR_CRIT&lt;/code>
selects between a dose criterion and a smoke criterion — they are alternatives,
not layers — and the default of &lt;code>-100.0&lt;/code> (:1459) selects the smoke branch that
this model implements. Which smoke criterion depends on the tier: absolute
&lt;code>K_ave_Door&lt;/code> in tier 1, the &lt;code>0.5 x d&lt;/code> sight ratio in tier 4. Two further
differences are worth knowing:&lt;/p>
&lt;ul>
&lt;li>FDS+Evac&amp;rsquo;s chosen quantity both strikes a door out (&lt;code>L2_tmp &amp;gt;= 1.0&lt;/code> marks it
not visible) &lt;strong>and&lt;/strong> ranks the survivors (&lt;code>L2_tmp &amp;lt; L2_min&lt;/code> picks the door).
Since &lt;code>0d9bf79&lt;/code> pyFDS-Evac&amp;rsquo;s optical depth does the same — it refuses and it
ranks. Dose still only strikes out.&lt;/li>
&lt;li>pyFDS-Evac applies dose and optical depth together rather than choosing one.&lt;/li>
&lt;/ul>
&lt;p>&lt;strong>On the fires we have measured, the dose veto never fires.&lt;/strong> On &lt;code>l_corridor&lt;/code>&amp;rsquo;s
&lt;code>fire_1MW_west&lt;/code> run the largest &lt;code>fed_max_route&lt;/code> over 3498 route-cost rows is
0.0016, against a threshold of 1.0, and &lt;code>world100&lt;/code> is reported the same way. On
these fires the model is &lt;strong>exposure-gated wayfinding, not hazard avoidance&lt;/strong>:
every refusal that changes an exit comes from the optical-depth criterion, and
nothing in the run is near a tenability limit.&lt;/p>
&lt;h3>Refusals are not remembered&lt;span class="hx-absolute -hx-mt-20" id="refusals-are-not-remembered">&lt;/span>
&lt;a href="#refusals-are-not-remembered" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>The criterion is measured against the distance &lt;em>still to walk&lt;/em>, so it relaxes
as the agent closes in: smoke that refuses a door at 40 m accepts it at 2 m.
Every tick re-decides from the current field; there is no permanent exit death.&lt;/p>
&lt;h3>When every route is refused&lt;span class="hx-absolute -hx-mt-20" id="when-every-route-is-refused">&lt;/span>
&lt;a href="#when-every-route-is-refused" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>The agent still has to move. &lt;code>rank_routes&lt;/code> re-sorts the refused routes by
&lt;code>(tau_route, rank_cost)&lt;/code> — least smoke to walk through, then quickest — and
un-rejects the head with a &lt;code>fallback:&lt;/code> prefix on its reason. The agent keeps its
current target unless a rival&amp;rsquo;s worst extinction is better by more than
&lt;code>fallback_switch_margin&lt;/code> (default 0.2), i.e. unless&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>rival.k_max_route &amp;lt;= current.k_max_route * (1 - fallback_switch_margin)&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The fallback sort uses the &lt;strong>undiscounted&lt;/strong> &lt;code>tau_route&lt;/code>;
&lt;code>current_exit_discount&lt;/code> applies to the feasible ordering only, and the
&lt;code>fallback_switch_margin&lt;/code> on &lt;code>k_max_route&lt;/code> is the hysteresis here instead.
Ordering refused routes by &lt;code>k_max&lt;/code> alone once put a 51 m route ahead of a 22 m
one on 2.0 m of sight against 1.8 m — two tenths of a metre of visibility,
neither usable, deciding a 29 m detour. &lt;code>tau&lt;/code> carries the distance with it, so
the least-bad walk is the one with least smoke to walk through.&lt;/p>
&lt;h3>Churn protection&lt;span class="hx-absolute -hx-mt-20" id="churn-protection">&lt;/span>
&lt;a href="#churn-protection" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Three mechanisms hold an agent on its exit.&lt;/p>
&lt;p>&lt;strong>The exit-switch anchor.&lt;/strong> Under &lt;code>&amp;quot;additive&amp;quot;&lt;/code> a different exit is adopted only
when its rank cost beats &lt;code>old_cost * exit_switch_anchor&lt;/code> (default 0.9). Under
the gate, &lt;code>_anchor_allows&lt;/code> decides in this order (&lt;code>25a6f8f&lt;/code>):&lt;/p>
&lt;ol>
&lt;li>The current exit is a &amp;ldquo;must flee&amp;rdquo; rejection — adopt. In practice only a
dose rejection reaches this; see &lt;a href="#known-limitations">limitations&lt;/a>
.&lt;/li>
&lt;li>The rival is clean and the current exit is not — adopt.&lt;/li>
&lt;li>The rival is not &lt;code>feasible&lt;/code> — fall through to the &lt;code>rank_cost&lt;/code> comparison.&lt;/li>
&lt;li>Otherwise a &lt;strong>symmetric deadband&lt;/strong> on &lt;code>tau&lt;/code>, with
&lt;code>margin = tau_max * tau_deadband&lt;/code> (6 x 0.1 = 0.6):&lt;/li>
&lt;/ol>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>delta = current.tau_route - candidate.tau_route
delta &amp;gt; margin -&amp;gt; adopt
delta &amp;lt; -margin -&amp;gt; refuse
otherwise -&amp;gt; candidate.rank_cost &amp;lt; current.rank_cost * exit_switch_anchor&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;strong>Symmetric is the point.&lt;/strong> Before &lt;code>25a6f8f&lt;/code> only the adopt half existed:
leaving an exit had to clear a margin in &lt;code>tau&lt;/code>, while returning to it fell
straight through to the time comparison, which the nearer exit wins
unconditionally. Departure cost a margin and the return was free — the same
shape of failure as the clean tier&amp;rsquo;s, one level up. Hysteresis applied to one
side of a disjunction is not hysteresis.&lt;/p>
&lt;p>&lt;strong>Absolute, not a ratio.&lt;/strong> &lt;code>tau&lt;/code> is zero in clear air, so a ratio test reads
&lt;code>0 &amp;lt; 0&lt;/code>: no agent could switch at all, and &lt;code>w_queue&lt;/code> would count for nothing
exactly where decks calibrate it. An earlier ratio-only form produced 109
returns to abandoned exits on &lt;code>l_corridor&lt;/code>.&lt;/p>
&lt;p>A route the ordering promotes but the anchor refuses no longer hides the rest of
the list: candidates are tried in rank order and the first the anchor would
admit wins, stopping at the agent&amp;rsquo;s own exit.&lt;/p>
&lt;p>&lt;strong>The deadbands.&lt;/strong> &lt;code>tau_return_margin&lt;/code> (0.8) and &lt;code>fed_return_margin&lt;/code> (0.9) make
a rival exit harder to &lt;em>qualify&lt;/em> than the current one; &lt;code>current_exit_discount&lt;/code>
(0.9) makes it harder to &lt;em>outrank&lt;/em> it; &lt;code>tau_deadband&lt;/code> (0.1 of &lt;code>tau_max&lt;/code>) makes
it harder to &lt;em>switch onto&lt;/em>, in both directions. Four constants, three of them
acting on the same quantity at three different stages. A reviewer will
reasonably ask why, and no run isolates any one of them.&lt;/p>
&lt;p>&lt;strong>Monotonicity holds on &lt;code>world100&lt;/code> and does not on &lt;code>l_corridor&lt;/code>.&lt;/strong> The
requirement is that an agent never returns to an exit it has abandoned. At
&lt;code>0d9bf79&lt;/code> and &lt;code>9f55f6e&lt;/code>:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>deck&lt;/th>
&lt;th>before (&lt;code>4ce4ac7&lt;/code>)&lt;/th>
&lt;th>at &lt;code>0d9bf79&lt;/code>&lt;/th>
&lt;th>at &lt;code>9f55f6e&lt;/code>&lt;/th>
&lt;th>at &lt;code>25a6f8f&lt;/code>&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>world100&lt;/code>, far clean exit E3&lt;/td>
&lt;td>12 agents&lt;/td>
&lt;td>&lt;strong>39 agents&lt;/strong>, 9 switches, &lt;strong>0 returns&lt;/strong>&lt;/td>
&lt;td>unchanged&lt;/td>
&lt;td>not re-reported&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>l_corridor&lt;/code>, returns to abandoned exits&lt;/td>
&lt;td>&lt;strong>0&lt;/strong>&lt;/td>
&lt;td>51 across 20 agents&lt;/td>
&lt;td>34 across 14 agents&lt;/td>
&lt;td>34 across 14 agents&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>l_corridor&lt;/code>, switches&lt;/td>
&lt;td>4&lt;/td>
&lt;td>74&lt;/td>
&lt;td>55&lt;/td>
&lt;td>55&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>l_corridor&lt;/code>, far-exit share&lt;/td>
&lt;td>~18&lt;/td>
&lt;td>~18&lt;/td>
&lt;td>~18&lt;/td>
&lt;td>~18&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>The &lt;code>world100&lt;/code> result is what the model was asked for — prefer a clean exit even
when far — and no earlier version of the gate produced it. &lt;strong>&lt;code>l_corridor&lt;/code>
regressed&lt;/strong>, from no returns to 34, and the far-exit share did not move to pay
for it. The figures come from the commit messages of &lt;code>0d9bf79&lt;/code>, &lt;code>9f55f6e&lt;/code> and
&lt;code>25a6f8f&lt;/code>; no CSV for them is in the results folder. &lt;code>25a6f8f&lt;/code> reports only
&lt;code>l_corridor&lt;/code>, so the &lt;code>world100&lt;/code> column for it is unverified rather than
measured.&lt;/p>
&lt;p>&lt;strong>Most of &lt;code>l_corridor&lt;/code>&amp;rsquo;s 34 returns are not oscillation.&lt;/strong> Making the anchor&amp;rsquo;s
&lt;code>tau&lt;/code> deadband symmetric at &lt;code>25a6f8f&lt;/code> changed nothing measurable, and that is
the finding: of the 34 returns, &lt;strong>29 have the returned-to route cleaner by more
than the deadband&lt;/strong> — median 0.95 of optical depth against a margin of 0.6 — so
the agent is following a field that genuinely reversed, not flickering across a
threshold. No further constant can damp those. Whether a memoryless model
&lt;em>should&lt;/em> follow a reversing field is a modelling question, and it is open.&lt;/p>
&lt;p>Enabling the clean-exit tier gives further violations back (34-38 agents per run
on &lt;code>l_corridor&lt;/code>, measured before &lt;code>0d9bf79&lt;/code>), which is why it is off.&lt;/p>
&lt;p>&lt;strong>Three attempts that did not close it, recorded so they are not repeated.&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>&lt;em>Making &lt;code>tau&lt;/code> the anchor&amp;rsquo;s currency&lt;/em> — replacing the &lt;code>rank_cost&lt;/code> ratio test
with a &lt;code>tau&lt;/code> ratio test — took &lt;code>l_corridor&lt;/code> from 51 returns to &lt;strong>90&lt;/strong>. &lt;code>tau&lt;/code>
is zero in clear air, so every ratio test degenerates to &lt;code>0 &amp;lt; 0&lt;/code> and the
anchor stops discriminating. This is also why &lt;code>rank_cost&lt;/code> stays a time: a
&lt;code>tau&lt;/code> anchor would let no agent switch in clear air, and a congestion weight
would count for nothing exactly where decks calibrate one.&lt;/li>
&lt;li>&lt;em>An absolute floor on the one-sided bypass&lt;/em> — the &lt;code>tau_max * 0.1&lt;/code> term — took
109 returns to 51. It helped; it did not close the problem.&lt;/li>
&lt;li>&lt;em>Making that deadband symmetric&lt;/em> (&lt;code>25a6f8f&lt;/code>) — the correct fix for a real
asymmetry, and it moved nothing: 34 returns before and after.&lt;/li>
&lt;/ul>
&lt;h3>Anticipation&lt;span class="hx-absolute -hx-mt-20" id="anticipation">&lt;/span>
&lt;a href="#anticipation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>With &lt;code>anticipate&lt;/code> (default &lt;code>true&lt;/code>), each segment is priced at the time the
agent would &lt;em>arrive&lt;/em> there rather than the time it decides:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>arrival_time = now &amp;#43; min(distance_walked_so_far / base_speed_m_per_s,
foresight_horizon_s)&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The unimpeded &lt;code>base_speed_m_per_s&lt;/code> is used, not the smoke-reduced speed: the
reduction depends on the smoke at the arrival time being computed, and one pass
settles what a second would only refine. &lt;code>foresight_horizon_s&lt;/code> defaults to
infinity, which is perfect foresight of the FDS solution; a finite horizon
models an occupant who can only judge the near future.&lt;/p>
&lt;p>&lt;strong>&lt;code>anticipate&lt;/code> is independent of &lt;code>cost_model&lt;/code>.&lt;/strong> It applies under &lt;code>&amp;quot;additive&amp;quot;&lt;/code>
too, which is why pinning pre-gate behaviour needs both keys.&lt;/p>
&lt;h2>The additive model&lt;span class="hx-absolute -hx-mt-20" id="the-additive-model">&lt;/span>
&lt;a href="#the-additive-model" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>composite = effective_length * (1 &amp;#43; w_smoke * K_ave)
&amp;#43; w_fed * FED_max
&amp;#43; w_queue * base_speed_m_per_s * queue_time (when w_queue &amp;gt; 0)&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>and routes are ordered by &lt;code>(rejected, composite, hops)&lt;/code>.&lt;/p>
&lt;p>Both smoke and length terms scale with route length, so a long clean detour
pays for its own length and can never win however large &lt;code>w_smoke&lt;/code> is — sweeping
it 1 → 20 on &lt;code>assets/world_100&lt;/code> moved 12 of 120 agents. That is the reason the
gate exists.&lt;/p>
&lt;p>&lt;code>composite_cost&lt;/code> is still computed and reported under the gate; it simply does
not rank.&lt;/p>
&lt;h2>Configuration&lt;span class="hx-absolute -hx-mt-20" id="configuration">&lt;/span>
&lt;a href="#configuration" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Every key below is read from the scenario&amp;rsquo;s &lt;code>routing&lt;/code> block by
&lt;code>RouteCostConfig.from_routing_params&lt;/code>. Keys are flat, not nested:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-json" data-lang="json">&lt;span style="display:flex;">&lt;span>{
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;routing&amp;#34;&lt;/span>: {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;cost_model&amp;#34;&lt;/span>: &lt;span style="color:#e6db74">&amp;#34;gate&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#f92672">&amp;#34;tau_max&amp;#34;&lt;/span>: &lt;span style="color:#ae81ff">6.0&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>}&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>JSON key&lt;/th>
&lt;th>Default&lt;/th>
&lt;th>Effect&lt;/th>
&lt;th>Under &lt;code>&amp;quot;gate&amp;quot;&lt;/code>&lt;/th>
&lt;th>Under &lt;code>&amp;quot;additive&amp;quot;&lt;/code>&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>cost_model&lt;/code>&lt;/td>
&lt;td>&lt;code>&amp;quot;gate&amp;quot;&lt;/code>&lt;/td>
&lt;td>Selects the model. Unvalidated: any other string behaves as &lt;code>&amp;quot;additive&amp;quot;&lt;/code>.&lt;/td>
&lt;td>—&lt;/td>
&lt;td>—&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>tau_max&lt;/code>&lt;/td>
&lt;td>&lt;code>6.0&lt;/code>&lt;/td>
&lt;td>Optical depth &lt;code>K_ave * L&lt;/code> a route may carry before it is refused. Also orders the feasible routes.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>inert&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>tau_return_margin&lt;/code>&lt;/td>
&lt;td>&lt;code>0.8&lt;/code>&lt;/td>
&lt;td>Factor a &lt;em>rival&lt;/em> exit&amp;rsquo;s budget is multiplied by, so switching needs a cleaner route than staying.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>inert&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>current_exit_discount&lt;/code>&lt;/td>
&lt;td>&lt;code>0.9&lt;/code>&lt;/td>
&lt;td>Factor the current exit&amp;rsquo;s &lt;code>tau&lt;/code> is scaled by in the sort key. FDS+Evac&amp;rsquo;s &lt;code>FAC_DOOR_OLD2&lt;/code> is 0.9.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>inert&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>tau_deadband&lt;/code>&lt;/td>
&lt;td>&lt;code>0.1&lt;/code>&lt;/td>
&lt;td>Half-width of the exit-switch anchor&amp;rsquo;s symmetric &lt;code>tau&lt;/code> deadband, as a fraction of &lt;code>tau_max&lt;/code> (so 0.6 by default). FDS+Evac applies no hysteresis to this veto — &lt;code>evac.f90:16463&lt;/code> tests the raw value.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>inert&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>clean_extinction_threshold&lt;/code>&lt;/td>
&lt;td>&lt;code>0.0&lt;/code> (off)&lt;/td>
&lt;td>Extinction at or below which a route&amp;rsquo;s smokiest leg makes the exit &lt;code>clean&lt;/code>; clean exits outrank smoky ones. FDS+Evac&amp;rsquo;s value is &lt;code>0.03&lt;/code>.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>inert&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>clean_exit_margin&lt;/code>&lt;/td>
&lt;td>&lt;code>0.1&lt;/code>&lt;/td>
&lt;td>Divides the threshold for the exit the agent already heads for. FDS+Evac&amp;rsquo;s &lt;code>FAC_DOOR_OLD&lt;/code> is 0.1.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>inert&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>anticipate&lt;/code>&lt;/td>
&lt;td>&lt;code>true&lt;/code>&lt;/td>
&lt;td>Price each segment at the agent&amp;rsquo;s arrival time.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>&lt;strong>active&lt;/strong>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>foresight_horizon_s&lt;/code>&lt;/td>
&lt;td>&lt;code>inf&lt;/code>&lt;/td>
&lt;td>Cap on how far ahead anticipation reaches, in seconds.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>&lt;strong>active&lt;/strong>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>fallback_switch_margin&lt;/code>&lt;/td>
&lt;td>&lt;code>0.2&lt;/code>&lt;/td>
&lt;td>Hysteresis when every route is refused.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>inert&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>w_smoke&lt;/code>&lt;/td>
&lt;td>&lt;code>1.0&lt;/code>&lt;/td>
&lt;td>Smoke weight in the additive composite and its Dijkstra edge weights. Since &lt;code>0d9bf79&lt;/code> the gate weights edges by their own &lt;code>tau&lt;/code>, so neither weight reaches route choice under the gate; the composite is still reported.&lt;/td>
&lt;td>&lt;strong>inert&lt;/strong> (reported only)&lt;/td>
&lt;td>active&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>w_fed&lt;/code>&lt;/td>
&lt;td>&lt;code>10.0&lt;/code>&lt;/td>
&lt;td>FED weight. Same.&lt;/td>
&lt;td>&lt;strong>inert&lt;/strong> (reported only)&lt;/td>
&lt;td>active&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>w_queue&lt;/code>&lt;/td>
&lt;td>&lt;code>0.0&lt;/code>&lt;/td>
&lt;td>Congestion weight, off by default.&lt;/td>
&lt;td>active (as &lt;code>w_queue * queue_time_s&lt;/code> on the rank cost)&lt;/td>
&lt;td>active (as distance-equivalent in the composite)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>fed_rejection_threshold&lt;/code>&lt;/td>
&lt;td>&lt;code>1.0&lt;/code>&lt;/td>
&lt;td>Projected FED above which a route is refused. Veto only: dose never ranks.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>active&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>visibility_extinction_threshold&lt;/code>&lt;/td>
&lt;td>&lt;code>0.5&lt;/code>&lt;/td>
&lt;td>&lt;code>K&lt;/code> above which a segment is flagged non-visible; a route whose segments are &lt;em>all&lt;/em> non-visible is refused when some other route has a visible segment.&lt;/td>
&lt;td>&lt;strong>inert&lt;/strong>&lt;/td>
&lt;td>active&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>sampling_step_m&lt;/code>&lt;/td>
&lt;td>&lt;code>2.0&lt;/code>&lt;/td>
&lt;td>Spacing of extinction samples along an edge polyline.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>active&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>base_speed_m_per_s&lt;/code>&lt;/td>
&lt;td>&lt;code>1.3&lt;/code>&lt;/td>
&lt;td>Clear-air walking speed. Sets travel time, anticipation, and the queue conversion. It is not a speed floor; &lt;code>min_speed_factor&lt;/code> is.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>active&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>alpha&lt;/code>&lt;/td>
&lt;td>&lt;code>0.706&lt;/code>&lt;/td>
&lt;td>Router&amp;rsquo;s copy of the linear speed-law coefficient, for travel time only; agents walk with &lt;code>SmokeSpeedConfig&lt;/code> (&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/">smoke-speed model&lt;/a>
).&lt;/td>
&lt;td>active&lt;/td>
&lt;td>active&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>beta&lt;/code>&lt;/td>
&lt;td>&lt;code>-0.057&lt;/code>&lt;/td>
&lt;td>Same, the slope.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>active&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>min_speed_factor&lt;/code>&lt;/td>
&lt;td>&lt;code>0.1&lt;/code>&lt;/td>
&lt;td>Same, the floor on the router&amp;rsquo;s speed factor.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>active&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>default_exit_capacity&lt;/code>&lt;/td>
&lt;td>&lt;code>1.3&lt;/code>&lt;/td>
&lt;td>Fallback exit capacity, agents/s, when the exit sets none.&lt;/td>
&lt;td>active&lt;/td>
&lt;td>active&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;code>clean_exit_margin&lt;/code> had two disagreeing defaults — 0.1 in the dataclass, 0.8
from &lt;code>from_routing_params&lt;/code> — until &lt;code>9508181&lt;/code> made both 0.1, the value
&lt;code>FAC_DOOR_OLD&lt;/code> supplies. The clean-tier measurements below were made at 0.8 and
have not been repeated at 0.1.&lt;/p>
&lt;p>Two &lt;code>RouteCostConfig&lt;/code> fields are &lt;strong>not&lt;/strong> readable from the &lt;code>routing&lt;/code> block and
keep their dataclass defaults in any scenario run: &lt;code>fed_return_margin&lt;/code> (0.9,
the asymmetric FED hysteresis) and &lt;code>impassable_extinction_threshold&lt;/code> (3.0, the
route-average extinction above which a smoke rejection bypasses the anchor).
Setting them requires constructing &lt;code>RouteCostConfig&lt;/code> in Python.&lt;/p>
&lt;p>&lt;code>exit_switch_anchor&lt;/code> (default 0.9) belongs to &lt;code>RerouteConfig&lt;/code>, not to
&lt;code>RouteCostConfig&lt;/code>.&lt;/p>
&lt;h2>Known limitations&lt;span class="hx-absolute -hx-mt-20" id="known-limitations">&lt;/span>
&lt;a href="#known-limitations" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>These are real and documented, not hypothetical. Details and measurements are
in &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/docs/gate-model-review-notes.md" target="_blank" rel="noopener">gate-model-review-notes.md&lt;/a>
. This section is the
single consolidated statement; anyone assessing the model should be able to read
it alone.&lt;/p>
&lt;p>&lt;strong>Two open issues carry the unresolved ones.&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>&lt;strong>&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/124" target="_blank" rel="noopener">#124&lt;/a>
— route
choice oscillates at a genuine optical-depth crossover.&lt;/strong> &lt;code>l_corridor&lt;/code>&amp;rsquo;s 34
returns, 31 of them in &lt;code>t = 40-60 s&lt;/code>, the window in which the two routes&amp;rsquo; &lt;code>tau&lt;/code>
cross over; at &lt;code>t = 40 s&lt;/code> their distributions overlap by 61 %. The crossover
swings through 1.5-3x and sails past any
hysteresis constant that would not also blind the model to real change. What is
missing is &lt;em>commitment&lt;/em> — hysteresis in time or in progress along a leg — not a
bigger threshold. Three attempts are already recorded as failures below.&lt;/li>
&lt;li>&lt;strong>&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/125" target="_blank" rel="noopener">#125&lt;/a>
— route
choice is not perception-limited.&lt;/strong> See
&lt;a href="#route-choice-is-an-optimality-bound-not-a-perception-limited-model">Route choice is an optimality bound&lt;/a>
.
It bounds what the discovery tier can be said to demonstrate about route
choice; it does not touch map growth.&lt;/li>
&lt;/ul>
&lt;p>&lt;strong>And the model is a departure, not a reproduction.&lt;/strong> &lt;code>tau&lt;/code> is the ordering here;
in FDS+Evac smoke ranks only inside the tier-4 last resort, over known-or-visible
doors, on a bee line, with permanent strike-out. See
&lt;a href="#the-diversion-is-a-departure-from-fdsevac-not-a-reproduction-of-it">The diversion is a departure&lt;/a>
.
The 100/0 attributed to the reference criterion on &lt;code>l_corridor&lt;/code> is a reasoned
prediction from &lt;code>evac.f90&lt;/code>, not a measured run of it.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>The &lt;code>L/d&lt;/code> bias tilts the criterion by geometry alone.&lt;/strong> &lt;code>tau&lt;/code> is measured on
the walked polyline where the reference measures on a straight line, and
&lt;code>L/d&lt;/code> is 1.41 on &lt;code>l_corridor&lt;/code>&amp;rsquo;s near route against 1.27 on the far one — so at
equal &lt;code>K_ave&lt;/code> the polyline form is about 11 % stricter on the near route, in
the same direction as the diversion the deck is used to demonstrate. Not
measured on any other geometry.&lt;/li>
&lt;li>&lt;strong>The ordering is in optical depth and the anchor is partly in time.&lt;/strong> The
ordering is &lt;code>tau&lt;/code>; &lt;code>_anchor_allows&lt;/code> falls through to &lt;code>rank_cost&lt;/code>, a travel
time, whenever the two routes&amp;rsquo; &lt;code>tau&lt;/code> are within the deadband. So the two
currencies still meet, and mixing them was the presumed cause of
&lt;code>l_corridor&lt;/code>&amp;rsquo;s 34 returns. Measurement at &lt;code>25a6f8f&lt;/code> does not support that
reading: 29 of the 34 have the returned-to route cleaner by more than the
deadband, so they are the ordering correctly following a field that reversed.
What remains open is the modelling question — whether a memoryless model
should follow a reversing field at all — not a missing constant.&lt;/li>
&lt;li>&lt;strong>Four hysteresis mechanisms act on &lt;code>tau&lt;/code> or on the exit choice.&lt;/strong>
&lt;code>tau_return_margin&lt;/code> on feasibility, &lt;code>current_exit_discount&lt;/code> on the sort,
&lt;code>tau_deadband&lt;/code> on the anchor, &lt;code>exit_switch_anchor&lt;/code> on the time fallthrough.
None has been measured in isolation, and the last two are both keyed to 0.9
and 0.1 x 6 without a joint sweep.&lt;/li>
&lt;li>&lt;strong>The &lt;code>1e-6 * length&lt;/code> edge-weight floor is a hard-coded tiebreaker.&lt;/strong> It
decides path choice in clear air, where every &lt;code>k_avg * length&lt;/code> is zero. Its
effect at small nonzero &lt;code>K&lt;/code> has not been measured.&lt;/li>
&lt;li>&lt;strong>&lt;code>tau_max = 6&lt;/code> is uncalibrated as an exposure budget.&lt;/strong> The threshold is
citable from FDS+Evac&amp;rsquo;s tier-4 rule, but that rule applies it to a straight
sight line, not to a walked route, and nothing here checks 6 against a
soot-dose or FED-equivalent limit.&lt;/li>
&lt;li>&lt;strong>&lt;code>impassable_extinction_threshold&lt;/code> is dead code under the default model.&lt;/strong>
&lt;code>_must_flee_rejection&lt;/code> fires only on a rejection reason starting &lt;code>FED&lt;/code> or
containing &lt;code>&amp;quot;visible&amp;quot;&lt;/code>; the gate&amp;rsquo;s only reason string starts &lt;code>tau&lt;/code>. So no
smoke rejection bypasses the exit-switch anchor, at any density, and the key
still takes a value and does nothing. &lt;code>visibility_extinction_threshold&lt;/code>
(0.5 /m, per segment) is likewise skipped under the gate. The FED bypass
survives, and on the fires measured here FED never reaches its threshold, so
in practice &lt;strong>nothing bypasses the anchor&lt;/strong>.&lt;/li>
&lt;li>&lt;strong>Anticipation samples the field too early.&lt;/strong> Segments are priced at
&lt;code>now + walked_so_far / base_speed_m_per_s&lt;/code>, the &lt;em>unimpeded&lt;/em> speed, while an
agent in smoke walks at as little as &lt;code>min_speed_factor&lt;/code> = 0.1 of it. The
clock therefore runs ahead of the agent systematically, and it runs furthest
ahead exactly where the smoke is thickest. With &lt;code>foresight_horizon_s = inf&lt;/code>
the agent also has perfect foresight of the FDS solution.&lt;/li>
&lt;li>&lt;strong>Six hysteresis constants, none calibrated.&lt;/strong> &lt;code>exit_switch_anchor&lt;/code> (0.9),
&lt;code>fallback_switch_margin&lt;/code> (0.2), &lt;code>fed_return_margin&lt;/code> (0.9),
&lt;code>tau_return_margin&lt;/code> (0.8), &lt;code>tau_deadband&lt;/code> (0.1), and
&lt;code>_PATH_IMPROVEMENT_THRESHOLD&lt;/code> (10 %) are all chosen to stop measured churn,
not fitted to observed behaviour. Two have FDS+Evac values behind them:
&lt;code>clean_exit_margin&lt;/code> = &lt;code>FAC_DOOR_OLD&lt;/code>, and &lt;code>current_exit_discount&lt;/code> =
&lt;code>FAC_DOOR_OLD2&lt;/code>. Neither was fitted here either.&lt;/li>
&lt;li>&lt;strong>Clear-air equivalence has not been re-measured since &lt;code>0d9bf79&lt;/code>.&lt;/strong> The gate
now orders by &lt;code>tau&lt;/code> and weights edges by &lt;code>tau&lt;/code>; the last equivalence runs
predate both. In clear air every &lt;code>tau&lt;/code> is zero and the argument still holds by
construction, but it is an argument, not a measurement.&lt;/li>
&lt;li>&lt;strong>&lt;code>cost_model&lt;/code> is an unvalidated free string.&lt;/strong> A typo silently yields the
additive model.&lt;/li>
&lt;li>&lt;strong>FIC does not participate in routing under either model.&lt;/strong> It drives the
Purser slowdown and incapacitation only. FIC and the optical-depth gate are
driven by the same smoke, so routing on both would double-count.&lt;/li>
&lt;/ul>
&lt;h2>Evidence&lt;span class="hx-absolute -hx-mt-20" id="evidence">&lt;/span>
&lt;a href="#evidence" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;code>assets/l_corridor&lt;/code> is the deck the model is judged on: a near exit reached by
passing the fire, and a clean way round. Since &lt;code>a98f8bb&lt;/code> the spawn sits in the
middle of the vertical leg and the two routes are 26 m and 46 m, a 1.8x ratio;
before that it sat 3 m from the junction and they were 11 m and 58 m, 5.3x —
a spread wide enough that no smoke could justify the detour. Exit shares are
unchanged across the move at 84 / 16, with 4 switches and no agent returning to
an abandoned exit. &lt;strong>The numbers below predate the move&lt;/strong>, so read their route
lengths against the old geometry. Results are in
&lt;code>&amp;lt;sciebo&amp;gt;/fds-evac-data/l_corridor/evac/RESULTS.md&lt;/code> (100 agents, seed 1,
familiarity 1.0):&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>run&lt;/th>
&lt;th>fire&lt;/th>
&lt;th>near / far&lt;/th>
&lt;th>switches&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>gate&lt;/td>
&lt;td>&lt;code>fire_1MW_west&lt;/code>&lt;/td>
&lt;td>&lt;strong>84 / 16&lt;/strong>&lt;/td>
&lt;td>14 &lt;code>smoke_reroute&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>additive&lt;/td>
&lt;td>&lt;code>fire_1MW_west&lt;/code>&lt;/td>
&lt;td>100 / 0&lt;/td>
&lt;td>23 &lt;code>smoke_reroute&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>gate, control&lt;/td>
&lt;td>&lt;code>fire_1MW&lt;/code>&lt;/td>
&lt;td>99 / 1&lt;/td>
&lt;td>—&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>gate, clear air&lt;/td>
&lt;td>none&lt;/td>
&lt;td>100 / 0&lt;/td>
&lt;td>0&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>additive, clear air&lt;/td>
&lt;td>none&lt;/td>
&lt;td>100 / 0&lt;/td>
&lt;td>0&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>The control run puts the fire east of the junction, where the far route smokes
first; the gate then diverts 1 agent instead of 16, so it is not simply
preferring long routes.&lt;/p>
&lt;p>The table is the run made at &lt;code>b3babc0&lt;/code>, before the band left the ordering. The
84/16 split was re-measured after &lt;code>cea33ce&lt;/code> and reported unchanged; that re-run
is not in the folder above, so take the attribution from the commit message
rather than from a CSV.&lt;/p>
&lt;p>At &lt;code>45e146f&lt;/code> the reported state is &lt;code>l_corridor&lt;/code> 84 near / 16 far with 2
switches and no returns to an abandoned exit, and &lt;code>world100&lt;/code> E1 91 / E2 17 /
E3 12 with 5 switches and no returns. &lt;code>a98f8bb&lt;/code> then moved the l_corridor spawn
and re-reported 84 / 16 with 4 switches, still no returns. The &lt;code>world100&lt;/code> far
clean exit is used at all only since &lt;code>b16e900&lt;/code>. These figures come from the
commit messages of &lt;code>b16e900&lt;/code>, &lt;code>45e146f&lt;/code> and &lt;code>a98f8bb&lt;/code>; no CSV for them is in
the results folder.&lt;/p>
&lt;p>&lt;strong>Since &lt;code>0d9bf79&lt;/code> these are history.&lt;/strong> Ranking on optical depth moved
&lt;code>world100&lt;/code>&amp;rsquo;s far clean exit from 12 agents to 39, and moved &lt;code>l_corridor&lt;/code> from no
returns to an abandoned exit to 51, then 34 at &lt;code>9f55f6e&lt;/code>, with the far-exit
share unchanged at about 18. See
&lt;a href="#churn-protection">Churn protection&lt;/a>
for the full table. No archived result set
exists for either; both come from the commit messages.&lt;/p>
&lt;p>&lt;strong>Read the headline with its caveat.&lt;/strong> Counted directly from &lt;code>f_gate_costs.csv&lt;/code>
(3498 rows, the &lt;code>b3babc0&lt;/code> run), the refusals break down as:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>refusal&lt;/th>
&lt;th>near exit&lt;/th>
&lt;th>far exit&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;code>sight (path)&lt;/code>&lt;/td>
&lt;td>200&lt;/td>
&lt;td>1020&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>fallback: sight (path)&lt;/code>&lt;/td>
&lt;td>12&lt;/td>
&lt;td>25&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;code>fallback: sight (los)&lt;/code>&lt;/td>
&lt;td>130&lt;/td>
&lt;td>0&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>So in that run the &lt;code>path&lt;/code> criterion did the work, and it refused the &lt;em>near&lt;/em> exit
200 times as well as the far one — the split is not a one-sided refusal of the
long way round. The &lt;code>los&lt;/code> criterion appears there only under a &lt;code>fallback:&lt;/code>
prefix, i.e. in ticks where every route was already refused. That matters before
anyone calibrates &lt;code>tau_max&lt;/code>. The &lt;code>los&lt;/code> criterion no longer gates at all
(&lt;code>b16e900&lt;/code>), and the reason strings are &lt;code>tau ...&lt;/code> since &lt;code>0d9bf79&lt;/code>, so a current
CSV carries neither label.&lt;/p>
&lt;p>&lt;strong>The route-cost CSV columns changed at &lt;code>0d9bf79&lt;/code>.&lt;/strong> &lt;code>min_visibility_m&lt;/code> and
&lt;code>band&lt;/code> are gone; &lt;code>tau_route&lt;/code> replaces both. Any analysis script reading the old
columns needs updating.&lt;/p>
&lt;p>&lt;code>assets/t_junction&lt;/code> is &lt;strong>not&lt;/strong> a route-choice benchmark. Its 2 MW PVC fire
drives route &lt;code>K&lt;/code> to about 10.7 /m, so every route on it carries an optical depth
far above any plausible budget and all of them are refused. Keep it as a
lethality and speed-collapse case.&lt;/p>
&lt;h2>References&lt;span class="hx-absolute -hx-mt-20" id="references">&lt;/span>
&lt;a href="#references" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>FDS+Evac Technical Reference and User&amp;rsquo;s Guide
— Korhonen (2021).&lt;/li>
&lt;li>&lt;code>materials/evac.f90&lt;/code> — the reference implementation; line-level citations for
the door criteria are in &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/docs/gate-model-review-notes.md" target="_blank" rel="noopener">gate-model-review-notes.md&lt;/a>
.&lt;/li>
&lt;li>&lt;a href="https://doi.org/10.1016/j.firesaf.2024.104269" target="_blank" rel="noopener">Boerger et al. (2024)&lt;/a>
—
waypoint-based visibility, Beer-Lambert integrated extinction (Eq. 8-9).&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/visibility/">Visibility through smoke&lt;/a>
— Jin&amp;rsquo;s law and
its sources.&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/routing/">Routing model&lt;/a>
— defaults and deviations from the
literature.&lt;/li>
&lt;li>&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/assets/l_corridor/README.md" target="_blank" rel="noopener">assets/l_corridor/README.md&lt;/a>
— the deck, its
fire, and the measured smoke contrast.&lt;/li>
&lt;/ul></description></item><item><title>FDS slice sampling</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/fds-sampling/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/fds-sampling/</guid><description>
&lt;blockquote>
&lt;p>Part of &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/README.md" target="_blank" rel="noopener">pyFDS-Evac&lt;/a>
.&lt;/p>&lt;/blockquote>
&lt;p>The &lt;code>SliceFieldSampler&lt;/code> class in &lt;code>pyfds_evac/core/fds_sampling.py&lt;/code> provides
nearest-neighbor spatial and temporal lookup on horizontal FDS slice
files. It&amp;rsquo;s the shared pyFDS-Evac data-access layer used by both the
smoke-speed model (extinction coefficient) and the FED model (gas
concentrations).&lt;/p>
&lt;h2>How it works&lt;span class="hx-absolute -hx-mt-20" id="how-it-works">&lt;/span>
&lt;a href="#how-it-works" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;code>SliceFieldSampler&lt;/code> wraps a single &lt;code>fdsreader&lt;/code> slice object and
exposes a &lt;code>sample(time_s, x, y)&lt;/code> method that returns the scalar value
at the nearest grid cell and timestep.&lt;/p>
&lt;p>Internally it:&lt;/p>
&lt;ol>
&lt;li>Finds the subslice whose bounding box covers the queried &lt;code>(x, y)&lt;/code>
point (one subslice per FDS mesh the slice intersects).&lt;/li>
&lt;li>Resolves the nearest timestep index via binary search
(&lt;code>get_nearest_timestep&lt;/code>).&lt;/li>
&lt;li>Computes the nearest cell indices along the x and y axes.&lt;/li>
&lt;li>Returns &lt;code>subslice.data[t_index, i_index, j_index]&lt;/code>.&lt;/li>
&lt;/ol>
&lt;h3>Performance caches&lt;span class="hx-absolute -hx-mt-20" id="performance-caches">&lt;/span>
&lt;a href="#performance-caches" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Two caches reduce per-call overhead on hot paths (for example, sampling
along a line of sight where all points share the same timestep and
typically the same subslice):&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Last-hit subslice cache&lt;/strong> &amp;ndash; the most recently matched subslice is
checked first before falling back to a linear scan. Consecutive
sample points along a ray almost always hit the same subslice.&lt;/li>
&lt;li>&lt;strong>Timestep cache&lt;/strong> &amp;ndash; when &lt;code>time_s&lt;/code> hasn&amp;rsquo;t changed since the last
call, the cached &lt;code>t_index&lt;/code> is reused, skipping the binary search.&lt;/li>
&lt;/ul>
&lt;h2>Loading a sampler&lt;span class="hx-absolute -hx-mt-20" id="loading-a-sampler">&lt;/span>
&lt;a href="#loading-a-sampler" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Use &lt;code>load_slice_sampler()&lt;/code> to load a single FDS quantity:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.fds_sampling &lt;span style="color:#f92672">import&lt;/span> load_slice_sampler
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>sampler &lt;span style="color:#f92672">=&lt;/span> load_slice_sampler(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">&amp;#34;path/to/fds_case&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">&amp;#34;SOOT EXTINCTION COEFFICIENT&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>value &lt;span style="color:#f92672">=&lt;/span> sampler&lt;span style="color:#f92672">.&lt;/span>sample(time_s&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">30.0&lt;/span>, x&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">5.0&lt;/span>, y&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">3.0&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h3>Selecting a slice height&lt;span class="hx-absolute -hx-mt-20" id="selecting-a-slice-height">&lt;/span>
&lt;a href="#selecting-a-slice-height" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>When an FDS case contains multiple horizontal slices for the same
quantity at different heights, pass &lt;code>slice_height_m&lt;/code> to select the
closest one:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>sampler &lt;span style="color:#f92672">=&lt;/span> load_slice_sampler(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">&amp;#34;path/to/fds_case&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">&amp;#34;SOOT EXTINCTION COEFFICIENT&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> slice_height_m&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">2.0&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>If only one slice matches the quantity, &lt;code>slice_height_m&lt;/code> has no effect.&lt;/p>
&lt;h3>Sharing a &lt;code>Simulation&lt;/code> instance&lt;span class="hx-absolute -hx-mt-20" id="sharing-a-simulation-instance">&lt;/span>
&lt;a href="#sharing-a-simulation-instance" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Parsing an FDS case directory is expensive. When you need both
extinction and FED fields from the same case, load the &lt;code>Simulation&lt;/code>
once and pass it to both factory methods:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> fdsreader &lt;span style="color:#f92672">import&lt;/span> Simulation
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.smoke_speed &lt;span style="color:#f92672">import&lt;/span> ExtinctionField
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.fed &lt;span style="color:#f92672">import&lt;/span> FdsFedField
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>sim &lt;span style="color:#f92672">=&lt;/span> Simulation(&lt;span style="color:#e6db74">&amp;#34;path/to/fds_case&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>extinction &lt;span style="color:#f92672">=&lt;/span> ExtinctionField&lt;span style="color:#f92672">.&lt;/span>from_fds(&lt;span style="color:#e6db74">&amp;#34;path/to/fds_case&amp;#34;&lt;/span>, simulation&lt;span style="color:#f92672">=&lt;/span>sim)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>fed_field &lt;span style="color:#f92672">=&lt;/span> FdsFedField&lt;span style="color:#f92672">.&lt;/span>from_fds(&lt;span style="color:#e6db74">&amp;#34;path/to/fds_case&amp;#34;&lt;/span>, simulation&lt;span style="color:#f92672">=&lt;/span>sim)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>All three factory functions (&lt;code>load_slice_sampler&lt;/code>,
&lt;code>ExtinctionField.from_fds&lt;/code>, &lt;code>FdsFedField.from_fds&lt;/code>) accept an optional
&lt;code>simulation&lt;/code> keyword argument. When omitted, each creates its own
&lt;code>Simulation&lt;/code> instance internally.&lt;/p>
&lt;h2>Integration with models&lt;span class="hx-absolute -hx-mt-20" id="integration-with-models">&lt;/span>
&lt;a href="#integration-with-models" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>Smoke-speed model&lt;span class="hx-absolute -hx-mt-20" id="smoke-speed-model">&lt;/span>
&lt;a href="#smoke-speed-model" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;code>ExtinctionField&lt;/code> wraps a &lt;code>SliceFieldSampler&lt;/code> for the
&lt;code>SOOT EXTINCTION COEFFICIENT&lt;/code> quantity and exposes
&lt;code>sample_extinction(time_s, x, y)&lt;/code>:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.smoke_speed &lt;span style="color:#f92672">import&lt;/span> ExtinctionField, SmokeSpeedModel
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>field &lt;span style="color:#f92672">=&lt;/span> ExtinctionField&lt;span style="color:#f92672">.&lt;/span>from_fds(&lt;span style="color:#e6db74">&amp;#34;path/to/fds_case&amp;#34;&lt;/span>, slice_height_m&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">2.0&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>model &lt;span style="color:#f92672">=&lt;/span> SmokeSpeedModel(field, config)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>speed_factor &lt;span style="color:#f92672">=&lt;/span> model&lt;span style="color:#f92672">.&lt;/span>sample(time_s&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">30.0&lt;/span>, x&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">5.0&lt;/span>, y&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">3.0&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>If a queried point falls outside the FDS domain, &lt;code>sample_extinction&lt;/code>
returns &lt;code>0.0&lt;/code> (clear air) and logs a warning on the first occurrence.&lt;/p>
&lt;h3>FED model&lt;span class="hx-absolute -hx-mt-20" id="fed-model">&lt;/span>
&lt;a href="#fed-model" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;code>FdsFedField&lt;/code> creates one &lt;code>SliceFieldSampler&lt;/code> per gas species (CO,
CO2, O2, and optionally HCN, NO, NO2, HCl, HBr, HF, SO2, acrolein,
formaldehyde):&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.fed &lt;span style="color:#f92672">import&lt;/span> FdsFedField, DefaultFedModel
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>fed_field &lt;span style="color:#f92672">=&lt;/span> FdsFedField&lt;span style="color:#f92672">.&lt;/span>from_fds(&lt;span style="color:#e6db74">&amp;#34;path/to/fds_case&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>model &lt;span style="color:#f92672">=&lt;/span> DefaultFedModel(fed_field)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>inputs &lt;span style="color:#f92672">=&lt;/span> model&lt;span style="color:#f92672">.&lt;/span>sample_inputs(time_s&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">30.0&lt;/span>, x&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">5.0&lt;/span>, y&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">3.0&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h3>Line-of-sight extinction&lt;span class="hx-absolute -hx-mt-20" id="line-of-sight-extinction">&lt;/span>
&lt;a href="#line-of-sight-extinction" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>The &lt;code>integrated_extinction_along_los()&lt;/code> function in
&lt;code>pyfds_evac/core/route_graph.py&lt;/code> computes the Beer-Lambert path-integrated
mean extinction coefficient between two points. It samples at uniform
intervals along the ray and returns the arithmetic mean:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac.core.route_graph &lt;span style="color:#f92672">import&lt;/span> integrated_extinction_along_los
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>k_mean &lt;span style="color:#f92672">=&lt;/span> integrated_extinction_along_los(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> x_from&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">1.0&lt;/span>, y_from&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">2.0&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> x_to&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">10.0&lt;/span>, y_to&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">2.0&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> time_s&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">30.0&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> extinction_sampler&lt;span style="color:#f92672">=&lt;/span>field,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> step_m&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">2.0&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>This is the discrete form of
&lt;a href="https://doi.org/10.1016/j.firesaf.2024.104269" target="_blank" rel="noopener">Boerger et al. (2024)&lt;/a>
,
Eq. 8-9, and is used internally by the route-cost evaluator for
smoke-aware &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/routing/">routing&lt;/a>
.&lt;/p>
&lt;h2>Next steps&lt;span class="hx-absolute -hx-mt-20" id="next-steps">&lt;/span>
&lt;a href="#next-steps" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/fds-case-requirements/">FDS case requirements&lt;/a>
&amp;ndash; the &lt;code>&amp;amp;SLCF&lt;/code> lines a
case must declare, and the failure modes when they&amp;rsquo;re missing.&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/smoke-speed-model/">Smoke-speed model&lt;/a>
&amp;ndash; how extinction drives
agent speed reduction.&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/routing/">Smoke-aware routing&lt;/a>
&amp;ndash; how extinction and FED drive
dynamic route selection.&lt;/li>
&lt;/ul></description></item><item><title>Model comparison: FDS+Evac vs pyFDS-Evac</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/model-comparison/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/model-comparison/</guid><description>
&lt;blockquote>
&lt;p>This document compares the evacuation models in FDS+Evac (v2.6.0,
Korhonen 2021) and pyFDS-Evac as implemented in this repository.
Claims are referenced to the FDS+Evac Technical Reference and User&amp;rsquo;s
Guide [1] and to the pyFDS-Evac source code. Where the two systems
differ, the differences are stated precisely; where they agree, that
is noted too.&lt;/p>&lt;/blockquote>
&lt;hr>
&lt;h2>1. Movement model&lt;span class="hx-absolute -hx-mt-20" id="1-movement-model">&lt;/span>
&lt;a href="#1-movement-model" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Aspect&lt;/th>
&lt;th>FDS+Evac&lt;/th>
&lt;th>pyFDS-Evac&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>Locomotion model&lt;/strong>&lt;/td>
&lt;td>Social Force Model (Helbing et al. [6], three-circle body shape [1] Table 1), continuous 2-D equation of motion solved with a modified velocity-Verlet integrator ([1] §3.1–3.2, §3.6)&lt;/td>
&lt;td>JuPedSim collision-free speed model (operational model configured externally); no social forces&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Body shape&lt;/strong>&lt;/td>
&lt;td>Three overlapping circles (torso Rd, shoulder Rs, head Rt) with rotational degree of freedom ([1] Table 1, Fig. 1)&lt;/td>
&lt;td>Point agent (circle of configurable radius in JuPedSim)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Counterflow&lt;/strong>&lt;/td>
&lt;td>Dedicated counterflow collision-avoidance algorithm ([1] §3.3)&lt;/td>
&lt;td>Handled by JuPedSim&amp;rsquo;s operational model; no separate counterflow algorithm&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Spatial discretisation&lt;/strong>&lt;/td>
&lt;td>Rectilinear evacuation mesh (separate from the FDS fire mesh); geometry is fitted to the underlying grid; minimum ~0.25 m cell size recommended ([1] §1.2)&lt;/td>
&lt;td>Continuous walkable polygon (Shapely geometry); no grid&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;strong>References:&lt;/strong> [1] §3.1–3.2 (agent model), §3.3 (counterflow), §3.6 (numerical method).&lt;/p>
&lt;hr>
&lt;h2>2. Exit / route selection&lt;span class="hx-absolute -hx-mt-20" id="2-exit--route-selection">&lt;/span>
&lt;a href="#2-exit--route-selection" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This is the area of largest conceptual difference.&lt;/p>
&lt;h3>FDS+Evac&lt;span class="hx-absolute -hx-mt-20" id="fdsevac">&lt;/span>
&lt;a href="#fdsevac" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Exit selection is formulated as an &lt;strong>N-player game&lt;/strong> where each agent
minimises its own &lt;strong>estimated evacuation time&lt;/strong> by choosing among
available exits [8]. The model is based on the concept of
&lt;em>best-response dynamics&lt;/em>: each agent periodically updates its exit
choice by selecting the exit that minimises its estimated evacuation
time, given the current choices of all other agents.&lt;/p>
&lt;h4>Cost function&lt;span class="hx-absolute -hx-mt-20" id="cost-function">&lt;/span>
&lt;a href="#cost-function" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;p>The estimated evacuation time of agent &lt;em>i&lt;/em> through exit &lt;em>e_k&lt;/em> is
([8] Eq. 6):&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>T_i(e_k, s_{-i}; r) = beta_k * lambda_i(e_k, s_{-i}; r) &amp;#43; tau_i(e_k; r_i)&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>where:&lt;/p>
&lt;ul>
&lt;li>&lt;code>beta_k&lt;/code> is a capacity parameter for exit &lt;em>k&lt;/em> (seconds per agent)&lt;/li>
&lt;li>&lt;code>lambda_i&lt;/code> is the number of other agents heading to exit &lt;em>e_k&lt;/em>
who are closer to it than agent &lt;em>i&lt;/em> ([8] Eq. 7)&lt;/li>
&lt;li>&lt;code>tau_i = d(e_k; r_i) / v_i^0&lt;/code> is the walking time ([8] Eq. 8); in
this section &lt;code>tau&lt;/code> is Ehtamo&amp;rsquo;s notation for a time, unrelated to the
optical depth &lt;code>tau&lt;/code> used for pyFDS-Evac below&lt;/li>
&lt;/ul>
&lt;p>The term &lt;code>beta_k * lambda_i&lt;/code> estimates the queueing delay; &lt;code>tau_i&lt;/code>
estimates the walking time. Thus &lt;code>T_i&lt;/code> = queueing time + walking
time.&lt;/p>
&lt;p>In the FDS+Evac implementation ([1] §3.6 p35–36), this is computed
as:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>T = alpha * t_walk &amp;#43; (1 - alpha) * t_queue&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>where &lt;code>t_walk = distance / v0&lt;/code>, &lt;code>t_queue = N_queue / (FAC_DOOR_QUEUE * Width)&lt;/code>, and &lt;code>alpha&lt;/code> is &lt;code>FAC_DOOR_ALPHA&lt;/code>.
Distance is L2 (Euclidean) for visible exits and L1 (Manhattan) for
non-visible exits. The currently chosen exit is favoured by 10%
(anchoring parameter) to prevent oscillation.&lt;/p>
&lt;h4>Nash equilibrium and convergence&lt;span class="hx-absolute -hx-mt-20" id="nash-equilibrium-and-convergence">&lt;/span>
&lt;a href="#nash-equilibrium-and-convergence" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;p>Ehtamo et al. [8] prove that this game has a &lt;strong>Nash equilibrium in
pure strategies&lt;/strong> (Theorem 3.1) when all agents have the same walking
speed. The NE is a fixed point of the system of all agents'
best-response functions. The existence proof is constructive: agents
can be fixed to their equilibrium strategies one by one, in
ascending order of their evacuation times.&lt;/p>
&lt;p>Three decentralised algorithms are analysed for finding the NE:&lt;/p>
&lt;ol>
&lt;li>&lt;strong>Parallel Update Algorithm (PUA)&lt;/strong>: all agents update
simultaneously; converges in at most N iterations ([8] Theorem 4.1)&lt;/li>
&lt;li>&lt;strong>Round Robin Algorithm (RRA)&lt;/strong>: agents update one at a time in
sequence; converges in at most N² iterations ([8] Theorem 4.2).
&lt;strong>FDS+Evac uses RRA&lt;/strong> ([8] §6 p127).&lt;/li>
&lt;li>&lt;strong>Random Polling Algorithm (RPA)&lt;/strong>: agents update stochastically.&lt;/li>
&lt;/ol>
&lt;p>In practice, RRA converges in ~3–4 iteration rounds regardless of
parameter values, while PUA can oscillate and require 10–18 rounds
([8] §6, Figs. 1–4). The initial assignment at simulation start
iterates until door-target counts stabilise (evac.f90:6699).&lt;/p>
&lt;p>Numerical experiments in [8] show that using the exit selection
game (vs. nearest-exit) reduces total evacuation time by ~29%
(Fig. 6).&lt;/p>
&lt;h4>Preference ordering&lt;span class="hx-absolute -hx-mt-20" id="preference-ordering">&lt;/span>
&lt;a href="#preference-ordering" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;p>Exits are additionally filtered by a &lt;strong>preference order&lt;/strong> (Table 3
in [1], Table 1 in [8]) determined by three Boolean criteria:
visibility (&lt;code>vis&lt;/code>), familiarity (&lt;code>fam&lt;/code>), and disturbing conditions
(&lt;code>con&lt;/code>). The best-response is constrained to the highest-priority
non-empty group ([8] Eq. 31):&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>s_i = BR_i(s_{-i}; r) = arg min T_i(s&amp;#39;_i, s_{-i}; r)
s.t. s&amp;#39;_i in E_i(z_bar)&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>where &lt;code>E_i(z_bar)&lt;/code> is the set of exits in the best available
preference group for agent &lt;em>i&lt;/em>.&lt;/p>
&lt;p>Four agent types modulate which exits enter the feasible set:
conservative, active, herding, follower ([1] §3.5.1–3.5.4).
Herding and follower agents incorporate social information from
neighbours to expand their feasible exit set.&lt;/p>
&lt;h4>Hawk-dove game extension&lt;span class="hx-absolute -hx-mt-20" id="hawk-dove-game-extension">&lt;/span>
&lt;a href="#hawk-dove-game-extension" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;p>A separate &lt;strong>hawk-dove game&lt;/strong> (Heliövaara et al. [9]) is available
as an optional overlay (&lt;code>C_HAWK &amp;gt;= 0&lt;/code>, default off: &lt;code>C_HAWK = -2.0&lt;/code>,
evac.f90:1523). When enabled, agents near an exit play a
hawk-dove game against nearby neighbours to decide whether to push
aggressively (hawk) or yield (dove). This modulates &lt;em>behaviour at
congestion points&lt;/em>, not the exit-selection cost function itself.&lt;/p>
&lt;h4>The smoke criteria on a door&lt;span class="hx-absolute -hx-mt-20" id="the-smoke-criteria-on-a-door">&lt;/span>
&lt;a href="#the-smoke-criteria-on-a-door" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;p>FDS+Evac&amp;rsquo;s &lt;em>primary&lt;/em> smoke test on a door is &lt;strong>absolute&lt;/strong>: a door
counts as smoke-free while &lt;code>K_ave &amp;lt; ABS(FED_DOOR_CRIT)&lt;/code>. The input is
a visibility in metres (&lt;code>FED_DOOR_CRIT = -100.0&lt;/code>, evac.f90:1459) which
is converted to an extinction coefficient by Jin&amp;rsquo;s relation,
&lt;code>FED_DOOR_CRIT = 3.0 / FED_DOOR_CRIT&lt;/code> (evac.f90:5262) — so the default
is 0.03 /m. &lt;code>K_ave_Door&lt;/code> is assigned from &lt;code>See_door&lt;/code> at evac.f90:16149,
and the tier-1 test that reads it is at evac.f90:16265 and :16272. Among
the doors that pass, the agent minimises the time &lt;code>T&lt;/code> above.&lt;/p>
&lt;p>&lt;strong>That test is a hard filter.&lt;/strong> &lt;code>IF (T_tmp &amp;lt; L2_min .AND. L2_tmp &amp;lt; ABS(FED_DOOR_CRIT))&lt;/code> sets the chosen door &lt;code>i_tmp&lt;/code> only for a door that
qualifies; when none qualifies the tier selects nothing and the search
moves to the next tier of doors, not to a time-only ranking. pyFDS-Evac&amp;rsquo;s
clean-exit tier falls through to the plain optical-depth ranking when it
is empty, which is our construction and not FDS+Evac&amp;rsquo;s. The
distance-relative rule — &amp;ldquo;check that visibility &amp;gt; 0.5 * distance to the
door&amp;rdquo; — is FDS+Evac&amp;rsquo;s &lt;strong>tier-4 last resort&lt;/strong> (evac.f90:16456-16463), reached
only when no smoke-free door exists.
That rule reads &lt;code>K_ave&lt;/code> along the straight, occlusion-blocked bee line
(&lt;code>See_door&lt;/code>, evac.f90:15343), so &lt;code>S &amp;gt; 0.5 * d&lt;/code> is a statement about
optical depth along one real sight line.&lt;/p>
&lt;p>Written out, the tier-4 test is
&lt;code>L2_tmp = d * 0.5 / (3.0 / K_ave_Door)&lt;/code> struck out at &lt;code>L2_tmp &amp;gt;= 1.0&lt;/code>
(evac.f90:16458, :16463) — that is &lt;code>K_ave * d / 6 &amp;gt;= 1&lt;/code>, i.e. an optical
depth above 6, which is where pyFDS-Evac&amp;rsquo;s &lt;code>tau_max = 6&lt;/code> comes from.
Two scope limits on the citation: the loop runs only over doors already
known or visible, and a door struck out here is struck out
&lt;strong>permanently&lt;/strong> (&lt;code>Is_Visible_Door(i) = .FALSE.&lt;/code>, :16464-16465).
pyFDS-Evac inherits the threshold, applies it to the walked polyline
rather than the bee line, and remembers nothing.&lt;/p>
&lt;p>Inside that tier, &lt;strong>dose and sight are alternatives, not layers&lt;/strong>: the
sign of &lt;code>FED_DOOR_CRIT&lt;/code> picks the branch (evac.f90:16439-16467).
Positive, and the door is scored by the dose the agent would arrive
with (&lt;code>IntDose + FED_max_Door * distance / Speed&lt;/code>); negative — the
default &lt;code>-100.0&lt;/code> — and it is scored by the visibility rule above.
Either way the resulting &lt;code>L2_tmp&lt;/code> does &lt;strong>two&lt;/strong> jobs: &lt;code>L2_tmp &amp;gt;= 1.0&lt;/code>
strikes the door out (&amp;ldquo;too much smoke&amp;rdquo;), and &lt;code>L2_tmp &amp;lt; L2_min&lt;/code> picks
the winner among those left. The criterion vetoes &lt;em>and&lt;/em> ranks, with
&lt;code>FAC_DOOR_OLD2 = 0.9&lt;/code> favouring the door already targeted.&lt;/p>
&lt;p>pyFDS-Evac splits those jobs differently. Dose vetoes only: a route above
&lt;code>fed_rejection_threshold&lt;/code> is removed and dose never makes one exit outrank
another. Optical depth does both, as in tier 4 — it refuses a route above
&lt;code>tau_max&lt;/code> &lt;em>and&lt;/em> orders the survivors, with travel time breaking ties. It also
applies dose and smoke at once rather than choosing one by a sign.&lt;/p>
&lt;h4>Where the quantity is used: the decisive difference&lt;span class="hx-absolute -hx-mt-20" id="where-the-quantity-is-used-the-decisive-difference">&lt;/span>
&lt;a href="#where-the-quantity-is-used-the-decisive-difference" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h4>&lt;p>The threshold &lt;code>tau &amp;gt; 6&lt;/code> is borrowed from tier 4 with a citation. &lt;strong>The place
the quantity is used is not&lt;/strong>, and that is what changes the behaviour.&lt;/p>
&lt;p>In FDS+Evac&amp;rsquo;s first three tiers the rank is &lt;code>T_tmp&lt;/code> — a time in tier 1 when
&lt;code>FAC_DOOR_QUEUE&lt;/code> is active, a plain L2 or L1 distance norm otherwise — while
&lt;code>K_ave_Door&lt;/code> appears only as a boolean admission test:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-fortran" data-lang="fortran">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">IF&lt;/span> (T_tmp &lt;span style="color:#f92672">&amp;lt;&lt;/span> L2_min .AND. L2_tmp &lt;span style="color:#f92672">&amp;lt;&lt;/span> ABS(FED_DOOR_CRIT)) &lt;span style="color:#66d9ef">THEN&lt;/span> &lt;span style="color:#960050;background-color:#1e0010">!&lt;/span> :&lt;span style="color:#ae81ff">16265&lt;/span>, :&lt;span style="color:#ae81ff">16354&lt;/span>, :&lt;span style="color:#ae81ff">16401&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>A change in the smoke field can move a door between tiers or out of the admitted
set, but it cannot reorder the doors inside a tier — and geometry does not
reorder itself. Smoke enters the ordering only in the &lt;strong>tier-4 last resort&lt;/strong>
(&lt;code>IF (L2_tmp &amp;lt; L2_min)&lt;/code>, &lt;code>:16467&lt;/code>), reached once no smoke-free door is available,
looping only over doors already known or visible, scoring a bee-line or L1
distance, and striking a refused door out permanently (&lt;code>:16463-16465&lt;/code>).&lt;/p>
&lt;p>pyFDS-Evac promotes that last-resort ranking criterion to its primary one, over
all candidates, at every tick, on the walked polyline, and drops the permanent
strike-out that makes it stable in the reference. Measured consequence on
&lt;code>assets/l_corridor&lt;/code>: this model diverts &lt;strong>18 of 100&lt;/strong> agents to the longer,
cleaner route, where the reference criterion — distance ranking, both doors
clearing the 0.03 /m admission test until the smoke is well developed — would
send essentially everyone to the near exit, roughly 100/0. &lt;strong>That 100/0 is a
prediction reasoned from &lt;code>evac.f90&lt;/code>, not a measured run of FDS+Evac&lt;/strong>; the
deck&amp;rsquo;s own additive model is the nearest empirical proxy and does evacuate
100/0. The diversion is a deliberate departure, and stands or falls on its own
merits. See
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/#the-diversion-is-a-departure-from-fdsevac-not-a-reproduction-of-it">route-cost-gate.md&lt;/a>
for the accompanying &lt;code>L/d&lt;/code> geometric bias (1.41 near against 1.27 far on that
deck).&lt;/p>
&lt;h3>pyFDS-Evac&lt;span class="hx-absolute -hx-mt-20" id="pyfds-evac">&lt;/span>
&lt;a href="#pyfds-evac" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>Route selection uses a &lt;strong>stage graph&lt;/strong> (directed weighted graph of
distributions, checkpoints, and exits) with &lt;strong>Dijkstra shortest-path&lt;/strong>
queries (&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/routing/">docs/routing.md&lt;/a>
, &lt;code>route_graph.py&lt;/code>).&lt;/p>
&lt;p>At each reevaluation tick, per-edge costs are computed from current
smoke and FED fields. The weight depends on the cost model
(&lt;code>route_graph.py:1299-1310&lt;/code>):&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>gate edge_cost = k_avg * length_m &amp;#43; 1e-6 * length_m
additive edge_cost = length_m * (1 &amp;#43; w_smoke * k_avg) &amp;#43; w_fed * fed_growth&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Under the gate the edge weight is the segment&amp;rsquo;s optical depth, with a
length floor that keeps a clear-air graph from collapsing to all-zero
weights; under &lt;code>&amp;quot;additive&amp;quot;&lt;/code> it is the composite decomposed per segment.
Dijkstra then finds one cheapest path per reachable exit using these
dynamic weights.&lt;/p>
&lt;p>The routes it returns are then judged by one of two models
(&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/">docs/route-cost-gate.md&lt;/a>
):&lt;/p>
&lt;ul>
&lt;li>&lt;strong>&lt;code>&amp;quot;gate&amp;quot;&lt;/code> (default).&lt;/strong> Route optical depth &lt;code>tau = K_ave * L&lt;/code>
(dimensionless; &lt;code>L&lt;/code> is the distance still to walk) decides
availability &lt;em>and&lt;/em> the ordering. Here &lt;code>tau&lt;/code> is always optical
depth, not the relaxation time τ of FDS+Evac&amp;rsquo;s movement model. A route is refused when &lt;code>tau&lt;/code>
exceeds &lt;code>tau_max&lt;/code> (6), or &lt;code>tau_max * tau_return_margin&lt;/code> (4.8) for an
exit the agent is not already walking to; survivors are ordered by
&lt;code>tau&lt;/code>, with travel time (plus &lt;code>w_queue&lt;/code> x queue time) breaking ties.
&lt;code>K_ave&lt;/code> is the length-weighted mean over the route&amp;rsquo;s own polyline, so
&lt;code>tau&lt;/code> is the soot column the agent walks through — an exposure
statement, not a sighting distance. Dijkstra weights each edge by its
own &lt;code>tau&lt;/code>, so path choice and exit choice are one objective. Sign
legibility is not consulted in route choice; it decides only which
exits enter the agent&amp;rsquo;s cognitive map. An opt-in tier above &lt;code>tau&lt;/code>
reproduces FDS+Evac&amp;rsquo;s absolute criterion
(&lt;code>clean_extinction_threshold&lt;/code>, default 0, off).&lt;/li>
&lt;li>&lt;strong>&lt;code>&amp;quot;additive&amp;quot;&lt;/code>.&lt;/strong> The composite
&lt;code>path_length * (1 + w_smoke * K_ave) + w_fed * FED_max&lt;/code>.&lt;/li>
&lt;/ul>
&lt;p>Under both, routes are rejected if FED exceeds a threshold, and a
fallback un-rejection ensures agents always have a path. The
all-segments-non-visible rejection runs under &lt;code>&amp;quot;additive&amp;quot;&lt;/code> only.&lt;/p>
&lt;p>&lt;strong>The borrowing is partial and should be read as such.&lt;/strong> pyFDS-Evac
takes FDS+Evac&amp;rsquo;s &lt;em>last-resort&lt;/em> criterion as the primary criterion of
its &lt;code>&amp;quot;gate&amp;quot;&lt;/code> cost model. The
absolute &lt;code>K_ave &amp;lt; 0.03 /m&lt;/code> test exists as an opt-in tier that ships
off, because measured on two decks it produced churn rather than
redirection — see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/#the-clean-exit-tier-off-by-default">route-cost-gate.md&lt;/a>
.
The distance-relative
form is the point of the choice — the same haze is usable 5 m from an
exit and not at 40 m — but it is not what FDS+Evac reaches for first.&lt;/p>
&lt;p>There is &lt;strong>no&lt;/strong> preference ordering, no agent types
(conservative/active/herding/follower), and no game-theoretic
component. Familiarity is modelled, but as a per-agent cognitive map
that restricts the graph before routing, not as a per-exit preference
group. Per-exit familiarity is not supported
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/136" target="_blank" rel="noopener">#136&lt;/a>
).&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Aspect&lt;/th>
&lt;th>FDS+Evac&lt;/th>
&lt;th>pyFDS-Evac&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>Algorithm&lt;/strong>&lt;/td>
&lt;td>N-player best-response game (NE in pure strategies) with preference-order filter [8]&lt;/td>
&lt;td>Dijkstra shortest-path with dynamic edge weights, then a per-route check under the selected cost model&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Cost function&lt;/strong>&lt;/td>
&lt;td>&lt;code>T_i = beta_k * lambda_i + tau_i&lt;/code> (queueing + walking time) [8] Eq. 6&lt;/td>
&lt;td>gate: route optical depth &lt;code>K_ave * L&lt;/code>, with travel time (+ &lt;code>w_queue&lt;/code> x queue time) as tie-break; additive: &lt;code>length * (1 + w_smoke * K) + w_fed * FED&lt;/code>&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Smoke test on a door&lt;/strong>&lt;/td>
&lt;td>Absolute &lt;code>K_ave &amp;lt; 0.03 /m&lt;/code> (&lt;code>FED_DOOR_CRIT&lt;/code>, evac.f90:1459, :5262, :16149); the &lt;code>0.5 x d&lt;/code> visibility rule is the tier-4 last resort (:16463), where &lt;code>L2_tmp = d * 0.5 / (3/K_ave) &amp;gt;= 1&lt;/code> is exactly &lt;code>K_ave * d &amp;gt; 6&lt;/code>&lt;/td>
&lt;td>Optical depth &lt;code>K_ave * L &amp;lt;= 6&lt;/code>, the same threshold, but applied to the walked polyline rather than a straight sight line; x 0.8 budget for a rival exit; no absolute &lt;code>K&lt;/code> door criterion. It vetoes &lt;em>and&lt;/em> ranks&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Congestion&lt;/strong>&lt;/td>
&lt;td>Modelled: queueing time depends on count of closer agents heading to same exit&lt;/td>
&lt;td>Optional (&lt;code>w_queue&lt;/code>), off by default; a global tally of agents targeting the exit&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Familiarity&lt;/strong>&lt;/td>
&lt;td>Per-agent per-exit familiarity (user-configurable, constrains feasible exit set)&lt;/td>
&lt;td>Per-agent cognitive map: an agent can only route over stages it knows or has discovered. The restriction is on &lt;em>topology&lt;/em> only — smoke is sampled globally, so a discovery agent&amp;rsquo;s route choice is not perception-limited (&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/125" target="_blank" rel="noopener">#125&lt;/a>
). Per-exit familiarity is not supported (&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/136" target="_blank" rel="noopener">#136&lt;/a>
)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Social behaviour&lt;/strong>&lt;/td>
&lt;td>Herding and follower agent types observe neighbours&lt;/td>
&lt;td>Not modelled&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Smoke in cost&lt;/strong>&lt;/td>
&lt;td>Admission only in tiers 1–3 (&lt;code>K_ave_Door &amp;lt; 0.03 /m&lt;/code>, the rank stays &lt;code>T_tmp&lt;/code>); smoke ranks doors only in the tier-4 last resort (&lt;code>:16467&lt;/code>), over known-or-visible doors, with permanent strike-out&lt;/td>
&lt;td>gate: availability &lt;em>and&lt;/em> ordering, both on route optical depth, for every candidate at every tick and with no memory; additive: continuous weighted term&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>FED in cost&lt;/strong>&lt;/td>
&lt;td>Not in cost function by default (&lt;code>FED_DOOR_CRIT &amp;lt; 0&lt;/code>); only used for incapacitation at FED &amp;gt;= 1.0&lt;/td>
&lt;td>Veto threshold under both models; a ranking term (&lt;code>w_fed * FED_max&lt;/code>) under additive only&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Distance metric&lt;/strong>&lt;/td>
&lt;td>L2 for visible exits, L1 (Manhattan) for non-visible exits; direct agent-to-exit&lt;/td>
&lt;td>Polyline arc length along corridor geometry (via JuPedSim RoutingEngine); routes through intermediate stages&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Anticipation&lt;/strong>&lt;/td>
&lt;td>&lt;code>FED_max_Door * dist / Speed&lt;/code>: extrapolation from presently observable conditions&lt;/td>
&lt;td>&lt;code>anticipate&lt;/code> prices each segment at the agent&amp;rsquo;s arrival time from the FDS solution itself — an upper bound on foresight, not FDS+Evac&amp;rsquo;s model&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Equilibrium&lt;/strong>&lt;/td>
&lt;td>Proven NE existence; RRA converges in ~3–4 rounds [8]&lt;/td>
&lt;td>No equilibrium concept; each agent independently picks the cheapest Dijkstra path&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Anchoring&lt;/strong>&lt;/td>
&lt;td>Current door favoured by 10 %: &lt;code>FAC_DOOR_WAIT&lt;/code> = 0.9 on the time criterion (:1505, applied :16264), &lt;code>FAC_DOOR_OLD2&lt;/code> = 0.9 on the smoke criterion (:1507, applied :16290, :16467)&lt;/td>
&lt;td>&lt;code>exit_switch_anchor&lt;/code> = 0.9 on travel time, mirroring &lt;code>FAC_DOOR_WAIT&lt;/code>; &lt;code>current_exit_discount&lt;/code> = 0.9 on the current exit&amp;rsquo;s optical depth in the sort, mirroring &lt;code>FAC_DOOR_OLD2&lt;/code>; bypasses for a lethal current exit and (gate) a feasible rival clearer by more than the symmetric deadband &lt;code>tau_deadband * tau_max&lt;/code> (0.6), which also refuses a rival dirtier by that much; plus deadbands of 0.9 on dose and 0.8 on the optical-depth budget&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Rerouting frequency&lt;/strong>&lt;/td>
&lt;td>~1 Hz (every second on average, &lt;code>TAU_CHANGE_DOOR = 1.0&lt;/code>, evac.f90:1466)&lt;/td>
&lt;td>Configurable interval, staggered per agent&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;hr>
&lt;h2>3. Smoke–speed interaction&lt;span class="hx-absolute -hx-mt-20" id="3-smokespeed-interaction">&lt;/span>
&lt;a href="#3-smokespeed-interaction" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Both systems use the same underlying correlation from the Frantzich
&amp;amp; Nilsson experiments (Lund 2003, Report 3126 [3]).&lt;/p>
&lt;h3>Speed reduction formula&lt;span class="hx-absolute -hx-mt-20" id="speed-reduction-formula">&lt;/span>
&lt;a href="#speed-reduction-formula" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>speed_factor(K) = 1 &amp;#43; (beta / alpha) * K&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>with the Frantzich–Nilsson coefficients; the published values are on
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/fundamentals/walking-speed/">Walking speed in smoke&lt;/a>
and the code defaults
on the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/#parameters">smoke-speed model&lt;/a>
page.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Aspect&lt;/th>
&lt;th>FDS+Evac&lt;/th>
&lt;th>pyFDS-Evac&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>Speed formula&lt;/strong>&lt;/td>
&lt;td>&lt;code>c(Ks) = 1 + beta * Ks / alpha&lt;/code> ([1] §3.4 Eq. 11)&lt;/td>
&lt;td>Same formula (&lt;code>smoke_speed.py:227&lt;/code>)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Default alpha/beta&lt;/strong>&lt;/td>
&lt;td>Frantzich–Nilsson values (evac.f90:1479–1480)&lt;/td>
&lt;td>Same values (&lt;code>smoke_speed.py:91–92&lt;/code>); see the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/smoke-speed/#parameters">smoke-speed model&lt;/a>
&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Minimum speed&lt;/strong>&lt;/td>
&lt;td>Configurable &lt;code>SMOKE_MIN_SPEED_FACTOR&lt;/code>; additional visibility-based cutoff (evac.f90:8183–8189)&lt;/td>
&lt;td>Configurable &lt;code>min_speed_factor&lt;/code> (default 0.1)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Smoke input&lt;/strong>&lt;/td>
&lt;td>Soot density from FDS mesh converted to extinction via &lt;code>K = MASS_EXTINCTION_COEFF * SOOT_DENS * 1e-6&lt;/code> (evac.f90:8160–8161)&lt;/td>
&lt;td>Extinction coefficient K read directly from FDS &lt;code>SOOT EXTINCTION COEFFICIENT&lt;/code> slice via fdsreader&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Sampling geometry&lt;/strong>&lt;/td>
&lt;td>Local value at agent position on the evacuation mesh&lt;/td>
&lt;td>Local value at agent position: nearest cell of the extinction slice at a fixed height&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;strong>Key difference:&lt;/strong> Both systems apply the speed reduction using the
&lt;em>local&lt;/em> smoke at the agent&amp;rsquo;s position. FDS+Evac converts the soot
density in the agent&amp;rsquo;s grid cell at runtime; pyFDS-Evac reads the
extinction coefficient from the nearest cell of an FDS slice at a fixed
height. The polyline-integrated mean of Boerger et al. (2024) is used
in pyFDS-Evac for route cost only, not for walking speed.&lt;/p>
&lt;hr>
&lt;h2>4. Toxicity / FED model&lt;span class="hx-absolute -hx-mt-20" id="4-toxicity--fed-model">&lt;/span>
&lt;a href="#4-toxicity--fed-model" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>FDS+Evac&lt;span class="hx-absolute -hx-mt-20" id="fdsevac-1">&lt;/span>
&lt;a href="#fdsevac-1" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>FED is computed using Purser&amp;rsquo;s Fractional Effective Dose concept [4].
The default calculation uses &lt;strong>CO, CO2, and O2&lt;/strong> gas phase
concentrations ([1] §1.2 p11, §2.7 p19). The effects of additional
gases (NO, NO2, CN, HCl, HBr, HF, SO2, C3H4O, CH2O) are included
&lt;strong>only if the user provides the corresponding FDS species&lt;/strong> ([1] §1.2
p11). By default, HCN and HCl effects are &lt;strong>not&lt;/strong> modelled; only the
CO2 hyperventilation factor is included ([1] §2.7 p19).&lt;/p>
&lt;p>The FED function itself is in FDS&amp;rsquo;s &lt;code>PHYSICAL_FUNCTIONS&lt;/code> module (the
&lt;code>FED&lt;/code> function imported at evac.f90:26), not in evac.f90 directly.&lt;/p>
&lt;p>FED activity level is configurable: 1 (at rest), 2 (light work,
default), 3 (heavy work) (evac.f90:1465).&lt;/p>
&lt;p>Incapacitation occurs at FED &amp;gt;= 1.0 ([1] §3.4 p31).&lt;/p>
&lt;h3>pyFDS-Evac&lt;span class="hx-absolute -hx-mt-20" id="pyfds-evac-1">&lt;/span>
&lt;a href="#pyfds-evac-1" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>FED is computed with the Purser equations as written out in the
FDS+Evac guide [1] (&lt;code>fed.py&lt;/code>), from up to 12 gas species: CO, CO2,
O2, HCN, NO, NO2, HCl, HBr, HF, SO2, acrolein and formaldehyde.
This is not the ISO 13571 [5] form: ISO 13571 keeps irritants in a
separate fractional effective concentration (FEC) and does not add
them into the FED, whereas here an irritant lethal-dose term is summed
into the FED total. Irritants also slow agents through Purser&amp;rsquo;s
fractional irritant concentration (FIC). ISO 13571&amp;rsquo;s FEC is a related
but different quantity, with its own denominators. The full model is:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>FED_tot = (FED_CO &amp;#43; FED_CN &amp;#43; FED_NOx &amp;#43; FLD_irr) * HV_CO2 &amp;#43; FED_O2&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>where:&lt;/p>
&lt;ul>
&lt;li>&lt;code>FED_CO&lt;/code>: CO narcosis (Eq. 13 from guide)&lt;/li>
&lt;li>&lt;code>FED_CN&lt;/code>: HCN − NO2 (protective effect of NO2 on HCN toxicity)&lt;/li>
&lt;li>&lt;code>FED_NOx&lt;/code>: NO + NO2 (Ct product = 1500 ppm·min)&lt;/li>
&lt;li>&lt;code>FLD_irr&lt;/code>: irritant gases (HCl, HBr, HF, SO2, NO2, acrolein, formaldehyde)&lt;/li>
&lt;li>&lt;code>HV_CO2&lt;/code>: CO2 hyperventilation factor&lt;/li>
&lt;li>&lt;code>FED_O2&lt;/code>: O2 vitiation&lt;/li>
&lt;/ul>
&lt;p>Required FDS slices: CO, CO2, O2.
Optional slices: HCN, NO, NO2, HCl, HBr, HF, SO2, acrolein,
formaldehyde.&lt;/p>
&lt;p>When only CO/CO2/O2 are available, the model falls back to the
three-gas subset: every optional species defaults to zero concentration in
&lt;code>DefaultFedInputs&lt;/code> (&lt;code>fed.py&lt;/code>), so an absent slice contributes nothing.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Aspect&lt;/th>
&lt;th>FDS+Evac&lt;/th>
&lt;th>pyFDS-Evac&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>FED standard&lt;/strong>&lt;/td>
&lt;td>Purser&amp;rsquo;s FED concept [4]&lt;/td>
&lt;td>Purser equations as in the FDS+Evac guide, with irritants summed into FED (not the ISO 13571 split into FED and FEC)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Default gases&lt;/strong>&lt;/td>
&lt;td>CO, CO2, O2&lt;/td>
&lt;td>CO, CO2, O2 (same three-gas minimum)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Optional gases&lt;/strong>&lt;/td>
&lt;td>NO, NO2, CN, HCl, HBr, HF, SO2, C3H4O, CH2O (user must provide species)&lt;/td>
&lt;td>HCN, NO, NO2, HCl, HBr, HF, SO2, acrolein, formaldehyde (auto-detected from FDS slices)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>HCN/HCl by default&lt;/strong>&lt;/td>
&lt;td>Not modelled unless user provides species ([1] §2.7 p19)&lt;/td>
&lt;td>Not modelled unless FDS slices are present&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Incapacitation&lt;/strong>&lt;/td>
&lt;td>FED &amp;gt;= 1.0, agent stops (v0 = 0) ([1] §3.4 p31)&lt;/td>
&lt;td>Agent stops (desired speed 0) and remains as a static obstacle. Per-agent threshold, log-normal with median 1.0 by default, or 1.0 for every agent in deterministic mode. The convective heat FED is a separate running total; crossing either threshold incapacitates&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Activity level&lt;/strong>&lt;/td>
&lt;td>Configurable (rest/light/heavy)&lt;/td>
&lt;td>Not supported (&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/135" target="_blank" rel="noopener">#135&lt;/a>
); the CO term uses the light-work coefficient (&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#coded-form">FED model&lt;/a>
)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>FED in routing&lt;/strong>&lt;/td>
&lt;td>Not used in exit selection cost by default (&lt;code>FED_DOOR_CRIT &amp;lt; 0&lt;/code>); only used for incapacitation&lt;/td>
&lt;td>A veto under both cost models; additionally a ranking term (&lt;code>w_fed * FED_max&lt;/code>) under &lt;code>&amp;quot;additive&amp;quot;&lt;/code> only, not under the default gate&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Temperature/radiation&lt;/strong>&lt;/td>
&lt;td>Not implemented for agent effects ([1] §1.2 p11)&lt;/td>
&lt;td>Convective heat FED (SFPE Handbook Eq. 63.44) from an FDS &lt;code>TEMPERATURE&lt;/code> slice, tracked separately from the gas FED; it does not affect route choice or speed. Radiant heat is not modelled&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;hr>
&lt;h2>5. FDS data integration&lt;span class="hx-absolute -hx-mt-20" id="5-fds-data-integration">&lt;/span>
&lt;a href="#5-fds-data-integration" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Aspect&lt;/th>
&lt;th>FDS+Evac&lt;/th>
&lt;th>pyFDS-Evac&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>Coupling&lt;/strong>&lt;/td>
&lt;td>Tightly coupled: evacuation module is compiled into FDS and runs as subroutines within the same executable ([1] §2.1)&lt;/td>
&lt;td>Loosely coupled: reads pre-computed FDS output files via fdsreader; runs as a separate post-processing step&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Smoke data&lt;/strong>&lt;/td>
&lt;td>Direct access to soot density on the FDS computational grid at runtime&lt;/td>
&lt;td>Reads FDS slice files (SOOT EXTINCTION COEFFICIENT, gas species volume fractions)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Visibility check&lt;/strong>&lt;/td>
&lt;td>Bee-line visibility from agent to exit; checks if smoke along the line exceeds a user-defined threshold ([1] §3.5 p32, §3.6 p36)&lt;/td>
&lt;td>Sign legibility on a precomputed visibility grid (smoke-aware fdsvismap with &lt;code>--fds-dir&lt;/code> and &lt;code>--vis-cache&lt;/code>, clear air otherwise); it decides which exits enter the agent&amp;rsquo;s cognitive map, not whether a route is admitted. Route cost separately uses the Beer-Lambert path-integrated mean of K along edge polylines (Boerger et al. 2024 [2])&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Temporal resolution&lt;/strong>&lt;/td>
&lt;td>Same timestep as FDS fire simulation&lt;/td>
&lt;td>Reads FDS output at whatever temporal resolution is available in the slice files&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;hr>
&lt;h2>6. Exit flow / throughput&lt;span class="hx-absolute -hx-mt-20" id="6-exit-flow--throughput">&lt;/span>
&lt;a href="#6-exit-flow--throughput" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Aspect&lt;/th>
&lt;th>FDS+Evac&lt;/th>
&lt;th>pyFDS-Evac&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>Flow model&lt;/strong>&lt;/td>
&lt;td>Emergent from social-force dynamics; door width × specific flow (default 1.3 1/m/s) used for queueing time estimation ([1] §3.6 p36)&lt;/td>
&lt;td>JuPedSim collision-free model produces emergent flow; optional per-checkpoint throughput throttling (&lt;code>enable_throughput_throttling&lt;/code>, &lt;code>max_throughput&lt;/code> in scenario config)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Agent update order&lt;/strong>&lt;/td>
&lt;td>Agents are updated in sequence within each evacuation mesh; order can affect results ([1] §3.6)&lt;/td>
&lt;td>JuPedSim handles agent updates internally&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;hr>
&lt;h2>7. Geometry representation&lt;span class="hx-absolute -hx-mt-20" id="7-geometry-representation">&lt;/span>
&lt;a href="#7-geometry-representation" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Aspect&lt;/th>
&lt;th>FDS+Evac&lt;/th>
&lt;th>pyFDS-Evac&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>Domain&lt;/strong>&lt;/td>
&lt;td>2-D rectilinear evacuation meshes; obstacles conform to grid ([1] §1.2)&lt;/td>
&lt;td>Continuous walkable polygon (Shapely); arbitrary geometry&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Multi-floor&lt;/strong>&lt;/td>
&lt;td>Separate evacuation meshes per floor connected by DOOR/ENTR/CORR/STRS namelists ([1] §1.2, §8.11–8.15)&lt;/td>
&lt;td>Single floor only; multi-floor buildings are not supported&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Stairs&lt;/strong>&lt;/td>
&lt;td>Dedicated staircase models (CORR, EVSS, STRS) with speed reduction factors ([1] §1.2 p13)&lt;/td>
&lt;td>Not implemented&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Doors&lt;/strong>&lt;/td>
&lt;td>Explicit DOOR and EXIT namelists with width, flow fields, opening/closing times ([1] §8.10–8.12)&lt;/td>
&lt;td>Stages (distributions, checkpoints, exits) in the stage graph&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;hr>
&lt;h2>8. Advantages and disadvantages&lt;span class="hx-absolute -hx-mt-20" id="8-advantages-and-disadvantages">&lt;/span>
&lt;a href="#8-advantages-and-disadvantages" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;h3>FDS+Evac&lt;span class="hx-absolute -hx-mt-20" id="fdsevac-2">&lt;/span>
&lt;a href="#fdsevac-2" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>Advantages:&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>
&lt;p>&lt;strong>Congestion-aware routing with theoretical guarantees.&lt;/strong> The
game-theoretic exit-selection model accounts for queueing at exits.
The NE existence proof [8] guarantees a consistent solution, and
the RRA converges in a few rounds. This means agents
self-organise to balance load across exits — shown to reduce total
evacuation time by ~29% versus nearest-exit [8] Fig. 6.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Rich behavioural model.&lt;/strong> Four agent types
(conservative/active/herding/follower) with per-agent exit
familiarity and visibility model a heterogeneous population. The
preference-order system captures the well-documented tendency to
prefer familiar exits over unfamiliar ones, even when the familiar
route is longer [8] §5.2, [1] §3.5.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Tight FDS coupling.&lt;/strong> Running inside the FDS executable gives
access to all fire-simulation fields at native resolution and
timestep. No I/O overhead or temporal interpolation.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Social Force Model with counterflow.&lt;/strong> The three-circle body
shape and social-force locomotion produce realistic crowd dynamics
including pushing, lane formation, and counterflow effects ([1]
§3.2–3.3).&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Documented testing.&lt;/strong> The guide reports component tests based on
the IMO test cases ([1] §4.2), sensitivity analyses ([1] Ch. 5), and
comparisons with test data and with other evacuation models ([1]
Ch. 6).&lt;/p>
&lt;/li>
&lt;/ul>
&lt;p>&lt;strong>Disadvantages:&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>
&lt;p>&lt;strong>Smoke does not enter the cost function continuously.&lt;/strong> Fire
conditions affect exit selection only through the binary
preference-order filter (disturbing conditions yes/no) and
visibility checks. A heavily smoke-filled route and a lightly
smoke-filled route receive the same cost if both are in the same
preference group. The cost function T_i has no continuous
extinction or FED term.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>No path-integrated smoke assessment.&lt;/strong> Visibility is checked
along a bee line from agent to exit ([1] §3.6 p36). Smoke
conditions along the actual walking path (which may follow
corridors around obstacles) are not sampled.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>FED does not influence routing by default.&lt;/strong> FED is accumulated
per agent and triggers incapacitation at FED &amp;gt;= 1.0, but with the
default &lt;code>FED_DOOR_CRIT &amp;lt; 0&lt;/code> projected FED along
candidate routes is not used to steer agents away from toxic paths
before incapacitation occurs.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Rectilinear mesh constraint.&lt;/strong> Geometry is fitted to a
rectangular grid; obstacles snap to cell boundaries. Minimum
corridor width ~0.7 m. Fine geometric details (angled walls,
curved corridors) require very fine meshes ([1] §1.2, §2.7).&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Distance metric approximation.&lt;/strong> L1 (Manhattan) distance is
used for non-visible exits as an approximation of walking distance
([1] §3.5.1 p33). This can over- or under-estimate actual
corridor-following paths.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Single-threaded evacuation.&lt;/strong> The evacuation calculation runs
as a single thread even when FDS uses MPI parallelism ([1] §1.2
p12). This limits scalability for large agent populations.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>FDS version lock-in.&lt;/strong> The evacuation module is compiled into
FDS. Updating the movement model or routing algorithm requires
modifying and recompiling the FDS Fortran source.&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h3>pyFDS-Evac&lt;span class="hx-absolute -hx-mt-20" id="pyfds-evac-2">&lt;/span>
&lt;a href="#pyfds-evac-2" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>Advantages:&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>
&lt;p>&lt;strong>Two explicit route-choice models, switchable per deck.&lt;/strong>
&lt;code>&amp;quot;additive&amp;quot;&lt;/code> keeps smoke and projected FED as continuous, weighted
terms in the per-edge cost Dijkstra minimises: a toll per metre plus
&lt;code>w_fed * FED&lt;/code>. &lt;code>&amp;quot;gate&amp;quot;&lt;/code> (default) weights each edge by its own
optical depth, refuses a route whose optical depth exceeds &lt;code>tau_max&lt;/code>
or whose projected FED exceeds the veto threshold, and orders the
survivors by optical depth, with travel time breaking ties. The gate
exists because in the additive form both terms scale with route
length, so a long clean detour can never win.
Being able to run the same deck under both is itself the
comparison instrument — see
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/">docs/route-cost-gate.md&lt;/a>
.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Path-integrated extinction sampling for route cost.&lt;/strong> For route
cost, smoke is sampled along corridor-following polylines (JuPedSim
RoutingEngine waypoints) using the Beer-Lambert path-integrated mean
[2]. This captures spatially varying smoke along the actual walking
path, not just along a straight line.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Continuous geometry.&lt;/strong> Walkable areas are arbitrary polygons
(Shapely). No grid snapping, no minimum corridor width imposed by
cell size.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Decoupled from FDS.&lt;/strong> Reads FDS output via fdsreader. Can be
used with any FDS version, any fire simulator that produces
compatible output, or even with synthetic hazard fields. The
movement model (JuPedSim) and routing logic (Python) can be
updated independently.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>FED-aware route rejection.&lt;/strong> Routes with projected FED above a
threshold are rejected before the agent commits to them. This is
proactive (avoid lethal routes) rather than reactive (incapacitate
after exposure).&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Modular and extensible.&lt;/strong> Python codebase with clear separation
between routing (&lt;code>route_graph.py&lt;/code>), FED (&lt;code>fed.py&lt;/code>), smoke-speed
(&lt;code>smoke_speed.py&lt;/code>), and scenario orchestration (&lt;code>scenario.py&lt;/code>).&lt;/p>
&lt;/li>
&lt;/ul>
&lt;p>&lt;strong>Disadvantages:&lt;/strong>&lt;/p>
&lt;ul>
&lt;li>
&lt;p>&lt;strong>Congestion modelling is opt-in and scale-dependent.&lt;/strong> A queueing
term exists (&lt;code>w_queue&lt;/code>) but is &lt;strong>off by default&lt;/strong>: it is driven by a
global tally of every agent targeting an exit, so no constant is
right at more than one crowd size. With it off, agents
independently pick the cheapest path without considering how many
others are heading to the same exit — exactly the overcrowding the
game-theoretic model in FDS+Evac was designed to solve. See
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/routing/#why-it-is-opt-in-and-what-0024-means">docs/routing.md&lt;/a>
.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>No equilibrium concept.&lt;/strong> Without agent interaction in the cost
function, there is no mechanism for agents to self-organise across
exits. If 100 agents face two exits and one has slightly less
smoke, all 100 may choose the same exit.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Familiarity but no social behaviour.&lt;/strong> Agents can be given a
per-agent cognitive map, so an unfamiliar agent routes only over
stages it has discovered. There is still no model for herding,
following, or a preference for the familiar route among known ones.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>The &lt;code>&amp;quot;gate&amp;quot;&lt;/code> cost model measures exposure, not sight.&lt;/strong> Its
criterion is always the mean K over the route polyline times the
distance still to walk; no line of sight to the exit is tested.
Integrated around corners this is the soot column the agent walks
through, not how far it can see, and &lt;code>tau_max&lt;/code> is not calibrated
against a soot-dose or FED-equivalent limit.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Loose FDS coupling.&lt;/strong> Reading pre-computed FDS output means the
evacuation cannot influence the fire (e.g., agents opening doors),
and temporal resolution depends on slice file output frequency.
There is no real-time feedback loop.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Single-floor only.&lt;/strong> Multi-floor buildings and stairs are not
supported.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>No collision-based locomotion model.&lt;/strong> JuPedSim&amp;rsquo;s collision-free
speed model does not produce social forces, body compression, or
pushing. Counterflow effects depend on the operational model&amp;rsquo;s
collision avoidance rather than explicit force-based interactions.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Limited validation.&lt;/strong> The routing model is new and has not yet
been validated against experimental evacuation data or benchmarked
against other evacuation models.&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h3>Summary&lt;span class="hx-absolute -hx-mt-20" id="summary">&lt;/span>
&lt;a href="#summary" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>The two systems make fundamentally different trade-offs:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Trade-off&lt;/th>
&lt;th>FDS+Evac&lt;/th>
&lt;th>pyFDS-Evac&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;strong>Congestion vs. hazard awareness&lt;/strong>&lt;/td>
&lt;td>Strong congestion model (game-theoretic queueing); weak continuous hazard influence on routing&lt;/td>
&lt;td>Congestion opt-in and uncalibrated across crowd sizes; strong hazard influence on routing&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Behavioural realism&lt;/strong>&lt;/td>
&lt;td>Rich (familiarity, herding, hawk-dove)&lt;/td>
&lt;td>Familiarity via cognitive maps; no herding or social types&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Geometric fidelity&lt;/strong>&lt;/td>
&lt;td>Grid-constrained; L1/L2 distance approximations&lt;/td>
&lt;td>Continuous geometry; polyline corridor paths&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Coupling&lt;/strong>&lt;/td>
&lt;td>Tight (inside FDS)&lt;/td>
&lt;td>Loose (post-processing)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Extensibility&lt;/strong>&lt;/td>
&lt;td>Fortran, tightly integrated&lt;/td>
&lt;td>Python, modular&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;strong>Maturity&lt;/strong>&lt;/td>
&lt;td>Validated, published, widely used&lt;/td>
&lt;td>New, not yet validated&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>Neither model is strictly superior. An ideal system would combine
FDS+Evac&amp;rsquo;s congestion-aware game-theoretic routing and behavioural
agent types with pyFDS-Evac&amp;rsquo;s smoke-aware route cost (optical depth
under &lt;code>&amp;quot;gate&amp;quot;&lt;/code>, a smoke/FED-weighted toll under &lt;code>&amp;quot;additive&amp;quot;&lt;/code>) and
path-integrated hazard sampling.&lt;/p>
&lt;hr>
&lt;h2>References&lt;span class="hx-absolute -hx-mt-20" id="references">&lt;/span>
&lt;a href="#references" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ol>
&lt;li>
&lt;p>Korhonen, T. (2021). &lt;em>Fire Dynamics Simulator with Evacuation:
FDS+Evac. Technical Reference and User&amp;rsquo;s Guide.&lt;/em> FDS 6.7.6, Evac
2.6.0-draft. VTT Technical Research Centre of Finland.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Boerger, M., Mayer, L., Mühlberger, A. &amp;amp; Pauli, P. (2024).
Waypoint-based visibility and evacuation modeling. &lt;em>Fire Safety
Journal&lt;/em>, 150, 104269.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Frantzich, H. &amp;amp; Nilsson, D. (2003). &lt;em>Utrymning genom tät rök:
beteende och förflyttning.&lt;/em> Department of Fire Safety Engineering,
Lund University, Report 3126.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Purser, D. A. (2008). Assessment of hazards to occupants from
smoke, toxic gases, and heat. In &lt;em>SFPE Handbook of Fire Protection
Engineering&lt;/em> (4th ed.), Chapter 2-6.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>ISO 13571:2012. &lt;em>Life-threatening components of fire — Guidelines
for the estimation of time to compromised tenability in fires.&lt;/em>&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Helbing, D. &amp;amp; Molnár, P. (1995). Social force model for
pedestrian dynamics. &lt;em>Physical Review E&lt;/em>, 51(5), 4282.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Korhonen, T. &amp;amp; Hostikka, S. (2009). Fire Dynamics Simulator with
Evacuation: FDS+Evac. Technical Reference and User&amp;rsquo;s Guide.
VTT Working Papers 119.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Ehtamo, H., Heliövaara, S., Korhonen, T. &amp;amp; Hostikka, S. (2010).
Game theoretic best-response dynamics for evacuees&amp;rsquo; exit selection.
&lt;em>Advances in Complex Systems&lt;/em>, 13(1), 113–134.
DOI: 10.1142/S021952591000244X.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Heliövaara, S., Ehtamo, H., Helbing, D. &amp;amp; Korhonen, T. (2013).
Patient and impatient pedestrians in a spatial game for egress
congestion. &lt;em>Physical Review E&lt;/em>, 87(1), 012802.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Ronchi, E., Fridolf, K., Frantzich, H., Nilsson, D., Walter, A.
L. &amp;amp; Modig, H. (2013). A tunnel evacuation experiment on movement
speed and exit choice in smoke. &lt;em>Fire Safety Journal&lt;/em>, 97, 126–136.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>Schroder, B., Arnold, L., Seyfried, A. (2020). A map
representation of the ASET-RSET concept. &lt;em>Fire Safety Journal&lt;/em>,
115, 103154.&lt;/p>
&lt;/li>
&lt;/ol></description></item><item><title>How do I get the egress time and its spread from an ensemble of seeds?</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/howto-rset-ensemble/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/howto-rset-ensemble/</guid><description>
&lt;p>&lt;strong>This how-to reads tracked FDS output. You do not need to run FDS.&lt;/strong> Part 1
needs no FDS output at all.&lt;/p>
&lt;p>Run the scenario once per seed. Read each agent&amp;rsquo;s exit time from the trajectory
file, not from &lt;code>result.evacuation_time&lt;/code>. Then report the last exit time over the
seeds as a mean, a standard deviation (SD), a range and a high percentile, not
as a single value.&lt;/p>
&lt;h2>What the simulation gives you&lt;span class="hx-absolute -hx-mt-20" id="what-the-simulation-gives-you">&lt;/span>
&lt;a href="#what-the-simulation-gives-you" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>ISO/TR 16738 splits the required safe escape time as&lt;/p>
&lt;p>&lt;em>RSET&lt;/em> = &lt;em>t&lt;/em>&lt;sub>det&lt;/sub> + &lt;em>t&lt;/em>&lt;sub>a&lt;/sub> + &lt;em>t&lt;/em>&lt;sub>pre&lt;/sub> + &lt;em>t&lt;/em>&lt;sub>trav&lt;/sub>&lt;/p>
&lt;p>where &lt;em>t&lt;/em>&lt;sub>det&lt;/sub> is the detection time, &lt;em>t&lt;/em>&lt;sub>a&lt;/sub> the alarm
time, &lt;em>t&lt;/em>&lt;sub>pre&lt;/sub> the pre-movement time and &lt;em>t&lt;/em>&lt;sub>trav&lt;/sub> the travel
time, all in seconds.&lt;/p>
&lt;p>A pyFDS-Evac run starts at &lt;em>t&lt;/em> = 0 and gives at most
&lt;em>t&lt;/em>&lt;sub>pre&lt;/sub> + &lt;em>t&lt;/em>&lt;sub>trav&lt;/sub>. It includes &lt;em>t&lt;/em>&lt;sub>pre&lt;/sub> only if
the scenario models pre-movement (&lt;code>use_premovement&lt;/code> in a spawn area). You add
&lt;em>t&lt;/em>&lt;sub>det&lt;/sub> and &lt;em>t&lt;/em>&lt;sub>a&lt;/sub> yourself. The scenario in Part 1 models no
pre-movement, so its exit times are travel times only.&lt;/p>
&lt;h2>Why not &lt;code>result.evacuation_time&lt;/code>?&lt;span class="hx-absolute -hx-mt-20" id="why-not-resultevacuation_time">&lt;/span>
&lt;a href="#why-not-resultevacuation_time" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;code>result.evacuation_time&lt;/code> is the time at which the simulation loop stopped.
The loop stops when no agent is left, or at &lt;code>max_simulation_time&lt;/code>. An
incapacitated agent stays in the simulation, so one incapacitation makes
&lt;code>evacuation_time&lt;/code> equal to &lt;code>max_simulation_time&lt;/code>
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/141" target="_blank" rel="noopener">#141&lt;/a>
).&lt;/li>
&lt;li>&lt;code>result.success&lt;/code> is &lt;code>True&lt;/code> when the run reaches &lt;code>max_simulation_time&lt;/code>, even
with agents still inside
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/139" target="_blank" rel="noopener">#139&lt;/a>
).&lt;/li>
&lt;/ul>
&lt;p>Part 2 below shows both.&lt;/p>
&lt;h2>Runnable example&lt;span class="hx-absolute -hx-mt-20" id="runnable-example">&lt;/span>
&lt;a href="#runnable-example" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The full script is &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/examples/rset_ensemble.py" target="_blank" rel="noopener">&lt;code>examples/rset_ensemble.py&lt;/code>&lt;/a>
.
Run it from the repository root (runtime about 10 s):&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run python examples/rset_ensemble.py&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">import&lt;/span> json
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">import&lt;/span> pathlib
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">import&lt;/span> sqlite3
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">import&lt;/span> numpy &lt;span style="color:#66d9ef">as&lt;/span> np
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> matplotlib.figure &lt;span style="color:#f92672">import&lt;/span> Figure
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#f92672">from&lt;/span> pyfds_evac &lt;span style="color:#f92672">import&lt;/span> (
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> DefaultFedConfig,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> DefaultFedModel,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> FdsFedField,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> TenabilityConfig,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> load_scenario,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> run_scenario,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>SEEDS &lt;span style="color:#f92672">=&lt;/span> [&lt;span style="color:#ae81ff">1&lt;/span>, &lt;span style="color:#ae81ff">2&lt;/span>, &lt;span style="color:#ae81ff">3&lt;/span>, &lt;span style="color:#ae81ff">4&lt;/span>, &lt;span style="color:#ae81ff">5&lt;/span>]
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>FIGURE &lt;span style="color:#f92672">=&lt;/span> pathlib&lt;span style="color:#f92672">.&lt;/span>Path(&lt;span style="color:#e6db74">&amp;#34;site/static/images/howto/egress_exit_curve.png&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>COLOURS &lt;span style="color:#f92672">=&lt;/span> [&lt;span style="color:#e6db74">&amp;#34;#0072B2&amp;#34;&lt;/span>, &lt;span style="color:#e6db74">&amp;#34;#E69F00&amp;#34;&lt;/span>, &lt;span style="color:#e6db74">&amp;#34;#009E73&amp;#34;&lt;/span>, &lt;span style="color:#e6db74">&amp;#34;#CC79A7&amp;#34;&lt;/span>, &lt;span style="color:#e6db74">&amp;#34;#56B4E9&amp;#34;&lt;/span>] &lt;span style="color:#75715e"># Okabe-Ito&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>LINESTYLES &lt;span style="color:#f92672">=&lt;/span> [&lt;span style="color:#e6db74">&amp;#34;-&amp;#34;&lt;/span>, &lt;span style="color:#e6db74">&amp;#34;--&amp;#34;&lt;/span>, &lt;span style="color:#e6db74">&amp;#34;-.&amp;#34;&lt;/span>, &lt;span style="color:#e6db74">&amp;#34;:&amp;#34;&lt;/span>, (&lt;span style="color:#ae81ff">0&lt;/span>, (&lt;span style="color:#ae81ff">5&lt;/span>, &lt;span style="color:#ae81ff">1&lt;/span>, &lt;span style="color:#ae81ff">1&lt;/span>, &lt;span style="color:#ae81ff">1&lt;/span>, &lt;span style="color:#ae81ff">1&lt;/span>, &lt;span style="color:#ae81ff">1&lt;/span>))]&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h3>Read exit times from the trajectory&lt;span class="hx-absolute -hx-mt-20" id="read-exit-times-from-the-trajectory">&lt;/span>
&lt;a href="#read-exit-times-from-the-trajectory" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;code>run_scenario&lt;/code> writes the trajectory to a SQLite file (&lt;code>result.sqlite_file&lt;/code>)
at 10 frames per second. An agent&amp;rsquo;s exit time is the time of the last frame
it appears in, so it is accurate to 0.1 s. Agents still inside at the end
appear up to the last frame and are dropped. The incapacitated agents are
read from &lt;code>result.fed_history&lt;/code>, which exists only when a FED model ran.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">def&lt;/span> &lt;span style="color:#a6e22e">exit_times&lt;/span>(result):
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">&amp;#34;&amp;#34;&amp;#34;Exit time [s] of every agent that left, from the trajectory file.&amp;#34;&amp;#34;&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> con &lt;span style="color:#f92672">=&lt;/span> sqlite3&lt;span style="color:#f92672">.&lt;/span>connect(result&lt;span style="color:#f92672">.&lt;/span>sqlite_file)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#66d9ef">try&lt;/span>:
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> fps &lt;span style="color:#f92672">=&lt;/span> float(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> con&lt;span style="color:#f92672">.&lt;/span>execute(&lt;span style="color:#e6db74">&amp;#34;SELECT value FROM metadata WHERE key = &amp;#39;fps&amp;#39;&amp;#34;&lt;/span>)&lt;span style="color:#f92672">.&lt;/span>fetchone()[&lt;span style="color:#ae81ff">0&lt;/span>]
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> )
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> last &lt;span style="color:#f92672">=&lt;/span> con&lt;span style="color:#f92672">.&lt;/span>execute(&lt;span style="color:#e6db74">&amp;#34;SELECT MAX(frame) FROM trajectory_data GROUP BY id&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> times &lt;span style="color:#f92672">=&lt;/span> sorted(frame &lt;span style="color:#f92672">/&lt;/span> fps &lt;span style="color:#66d9ef">for&lt;/span> (frame,) &lt;span style="color:#f92672">in&lt;/span> last)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#66d9ef">finally&lt;/span>:
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> con&lt;span style="color:#f92672">.&lt;/span>close()
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#75715e"># Agents still inside at the end are the ones seen in the last frames.&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#66d9ef">return&lt;/span> times[: len(times) &lt;span style="color:#f92672">-&lt;/span> result&lt;span style="color:#f92672">.&lt;/span>agents_remaining]
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">def&lt;/span> &lt;span style="color:#a6e22e">incapacitated&lt;/span>(result):
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">&amp;#34;&amp;#34;&amp;#34;Number of agents incapacitated during the run.&amp;#34;&amp;#34;&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> rows &lt;span style="color:#f92672">=&lt;/span> result&lt;span style="color:#f92672">.&lt;/span>fed_history &lt;span style="color:#f92672">or&lt;/span> []
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#66d9ef">return&lt;/span> len({row[&lt;span style="color:#e6db74">&amp;#34;agent_id&amp;#34;&lt;/span>] &lt;span style="color:#66d9ef">for&lt;/span> row &lt;span style="color:#f92672">in&lt;/span> rows &lt;span style="color:#66d9ef">if&lt;/span> row[&lt;span style="color:#e6db74">&amp;#34;incapacitated&amp;#34;&lt;/span>]})&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h3>Part 1: one run per seed&lt;span class="hx-absolute -hx-mt-20" id="part-1-one-run-per-seed">&lt;/span>
&lt;a href="#part-1-one-run-per-seed" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>The scenario is &lt;code>assets/fic_vs_fed_speed&lt;/code>: 30 agents at one end of a
4 m × 50 m corridor with one exit, in clear air. Each run writes a manifest
next to its trajectory, and the loop reads the seed back from it.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>scenario &lt;span style="color:#f92672">=&lt;/span> load_scenario(&lt;span style="color:#e6db74">&amp;#34;assets/fic_vs_fed_speed&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">for&lt;/span> name, dist &lt;span style="color:#f92672">in&lt;/span> scenario&lt;span style="color:#f92672">.&lt;/span>distributions&lt;span style="color:#f92672">.&lt;/span>items():
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> params &lt;span style="color:#f92672">=&lt;/span> dist[&lt;span style="color:#e6db74">&amp;#34;parameters&amp;#34;&lt;/span>]
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> print(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>&lt;span style="color:#e6db74">{&lt;/span>name&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>params[&lt;span style="color:#e6db74">&amp;#39;number&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> agents, &amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;use_premovement=&lt;/span>&lt;span style="color:#e6db74">{&lt;/span>params&lt;span style="color:#f92672">.&lt;/span>get(&lt;span style="color:#e6db74">&amp;#39;use_premovement&amp;#39;&lt;/span>, &lt;span style="color:#66d9ef">False&lt;/span>)&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> )
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>curves &lt;span style="color:#f92672">=&lt;/span> {}
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">for&lt;/span> seed &lt;span style="color:#f92672">in&lt;/span> SEEDS:
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> result &lt;span style="color:#f92672">=&lt;/span> run_scenario(scenario, seed&lt;span style="color:#f92672">=&lt;/span>seed)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> manifest &lt;span style="color:#f92672">=&lt;/span> json&lt;span style="color:#f92672">.&lt;/span>loads(pathlib&lt;span style="color:#f92672">.&lt;/span>Path(result&lt;span style="color:#f92672">.&lt;/span>manifest_file)&lt;span style="color:#f92672">.&lt;/span>read_text())
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> curves[manifest[&lt;span style="color:#e6db74">&amp;#34;seed&amp;#34;&lt;/span>]] &lt;span style="color:#f92672">=&lt;/span> exit_times(result)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> print(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;seed &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>manifest[&lt;span style="color:#e6db74">&amp;#39;seed&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">: last exit &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>curves[seed][&lt;span style="color:#f92672">-&lt;/span>&lt;span style="color:#ae81ff">1&lt;/span>]&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.1f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s, &amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;evacuated &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>len(curves[seed])&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">, incapacitated &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>incapacitated(result)&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">, &amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;remaining &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>result&lt;span style="color:#f92672">.&lt;/span>agents_remaining&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> )
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> result&lt;span style="color:#f92672">.&lt;/span>cleanup()
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;versions: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>manifest[&lt;span style="color:#e6db74">&amp;#39;versions&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;git: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>manifest[&lt;span style="color:#e6db74">&amp;#39;git_commit&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">, dirty: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>manifest[&lt;span style="color:#e6db74">&amp;#39;git_dirty&amp;#39;&lt;/span>]&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Expected output, without the &lt;code>Evacuated 30/30 … done&lt;/code> progress line that
&lt;code>run_scenario&lt;/code> prints for each run:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>jps-distributions_0: 30 agents, use_premovement=False
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>seed 1: last exit 44.4 s, evacuated 30, incapacitated 0, remaining 0
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>seed 2: last exit 43.0 s, evacuated 30, incapacitated 0, remaining 0
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>seed 3: last exit 43.6 s, evacuated 30, incapacitated 0, remaining 0
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>seed 4: last exit 44.3 s, evacuated 30, incapacitated 0, remaining 0
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>seed 5: last exit 42.2 s, evacuated 30, incapacitated 0, remaining 0
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>versions: {&amp;#39;pyfds-evac&amp;#39;: &amp;#39;0.1.0&amp;#39;, &amp;#39;jupedsim&amp;#39;: &amp;#39;1.4.2&amp;#39;, &amp;#39;fdsreader&amp;#39;: &amp;#39;1.11.7&amp;#39;, &amp;#39;fdsvismap&amp;#39;: &amp;#39;0.2.1&amp;#39;}
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>git: 3f47897b351d9f8c403782611282c0bc2a2bdcb0, dirty: True&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The versions and the git state depend on your checkout. &lt;code>dirty: True&lt;/code> means the
working tree had uncommitted changes when the run started. The seed changes
where the agents start in their spawn area, so each seed gives a different
time. There is no fire in Part 1, so nobody is incapacitated.&lt;/p>
&lt;h3>Summarise the last exit over the seeds&lt;span class="hx-absolute -hx-mt-20" id="summarise-the-last-exit-over-the-seeds">&lt;/span>
&lt;a href="#summarise-the-last-exit-over-the-seeds" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>last &lt;span style="color:#f92672">=&lt;/span> np&lt;span style="color:#f92672">.&lt;/span>array([times[&lt;span style="color:#f92672">-&lt;/span>&lt;span style="color:#ae81ff">1&lt;/span>] &lt;span style="color:#66d9ef">for&lt;/span> times &lt;span style="color:#f92672">in&lt;/span> curves&lt;span style="color:#f92672">.&lt;/span>values()])
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;n = &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>len(last)&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> seeds&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;mean = &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>last&lt;span style="color:#f92672">.&lt;/span>mean()&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.1f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s, SD = &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>last&lt;span style="color:#f92672">.&lt;/span>std(ddof&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">1&lt;/span>)&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.1f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;min-max = &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>last&lt;span style="color:#f92672">.&lt;/span>min()&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.1f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">-&lt;/span>&lt;span style="color:#e6db74">{&lt;/span>last&lt;span style="color:#f92672">.&lt;/span>max()&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.1f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;95th percentile = &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>np&lt;span style="color:#f92672">.&lt;/span>percentile(last, &lt;span style="color:#ae81ff">95&lt;/span>)&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.1f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s&amp;#34;&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>n = 5 seeds
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>mean = 43.5 s, SD = 0.9 s
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>min-max = 42.2-44.4 s
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>95th percentile = 44.4 s&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;strong>Five seeds are too few for a study.&lt;/strong> They keep this example fast. The SD and
the percentiles need many more runs to settle. The 95th percentile uses NumPy&amp;rsquo;s
default linear interpolation; with five values it lies between the two largest.
Increase &lt;code>SEEDS&lt;/code>, and check that the summary no longer changes much when you
add runs.&lt;/p>
&lt;h3>Plot the exit curves&lt;span class="hx-absolute -hx-mt-20" id="plot-the-exit-curves">&lt;/span>
&lt;a href="#plot-the-exit-curves" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>fig &lt;span style="color:#f92672">=&lt;/span> Figure(figsize&lt;span style="color:#f92672">=&lt;/span>(&lt;span style="color:#ae81ff">6&lt;/span>, &lt;span style="color:#ae81ff">4&lt;/span>))
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>ax &lt;span style="color:#f92672">=&lt;/span> fig&lt;span style="color:#f92672">.&lt;/span>subplots()
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">for&lt;/span> (seed, times), colour, style &lt;span style="color:#f92672">in&lt;/span> zip(curves&lt;span style="color:#f92672">.&lt;/span>items(), COLOURS, LINESTYLES):
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> counts &lt;span style="color:#f92672">=&lt;/span> np&lt;span style="color:#f92672">.&lt;/span>arange(&lt;span style="color:#ae81ff">1&lt;/span>, len(times) &lt;span style="color:#f92672">+&lt;/span> &lt;span style="color:#ae81ff">1&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> ax&lt;span style="color:#f92672">.&lt;/span>step(times, counts, where&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;post&amp;#34;&lt;/span>, color&lt;span style="color:#f92672">=&lt;/span>colour, ls&lt;span style="color:#f92672">=&lt;/span>style, label&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;seed &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>seed&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>ax&lt;span style="color:#f92672">.&lt;/span>axvspan(last&lt;span style="color:#f92672">.&lt;/span>min(), last&lt;span style="color:#f92672">.&lt;/span>max(), color&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;lightgrey&amp;#34;&lt;/span>, alpha&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">0.5&lt;/span>, lw&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">0&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>ax&lt;span style="color:#f92672">.&lt;/span>text(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> last&lt;span style="color:#f92672">.&lt;/span>min() &lt;span style="color:#f92672">-&lt;/span> &lt;span style="color:#ae81ff">0.3&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#ae81ff">2&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;last exit &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>last&lt;span style="color:#f92672">.&lt;/span>min()&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.1f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">-&lt;/span>&lt;span style="color:#e6db74">{&lt;/span>last&lt;span style="color:#f92672">.&lt;/span>max()&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.1f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s&lt;/span>&lt;span style="color:#ae81ff">\n&lt;/span>&lt;span style="color:#e6db74">over &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>len(last)&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> seeds&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> color&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;dimgrey&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> ha&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;right&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>ax&lt;span style="color:#f92672">.&lt;/span>grid(color&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;lightgrey&amp;#34;&lt;/span>, linewidth&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">0.8&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>ax&lt;span style="color:#f92672">.&lt;/span>set_axisbelow(&lt;span style="color:#66d9ef">True&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">for&lt;/span> spine &lt;span style="color:#f92672">in&lt;/span> ax&lt;span style="color:#f92672">.&lt;/span>spines&lt;span style="color:#f92672">.&lt;/span>values():
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> spine&lt;span style="color:#f92672">.&lt;/span>set_visible(&lt;span style="color:#66d9ef">False&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>ax&lt;span style="color:#f92672">.&lt;/span>tick_params(length&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">0&lt;/span>, labelcolor&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;dimgrey&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>ax&lt;span style="color:#f92672">.&lt;/span>set_xlabel(&lt;span style="color:#e6db74">&amp;#34;time since start of simulation [s]&amp;#34;&lt;/span>, color&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;dimgrey&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>ax&lt;span style="color:#f92672">.&lt;/span>set_ylabel(&lt;span style="color:#e6db74">&amp;#34;agents evacuated [-]&amp;#34;&lt;/span>, color&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;dimgrey&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>ax&lt;span style="color:#f92672">.&lt;/span>legend(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> frameon&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#66d9ef">True&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> facecolor&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;white&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> framealpha&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">0.8&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> edgecolor&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;lightgrey&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> labelcolor&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;dimgrey&amp;#34;&lt;/span>,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>FIGURE&lt;span style="color:#f92672">.&lt;/span>parent&lt;span style="color:#f92672">.&lt;/span>mkdir(parents&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#66d9ef">True&lt;/span>, exist_ok&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#66d9ef">True&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>fig&lt;span style="color:#f92672">.&lt;/span>savefig(FIGURE, dpi&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">150&lt;/span>, bbox_inches&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;tight&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(&lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;figure: &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>FIGURE&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>)&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;img src="https://pedestriandynamics.org/pyFDS-Evac/images/howto/egress_exit_curve.png" alt="Five step curves of agents evacuated against time, one per seed in its own colour and line style; each rises from 1 near 33–35 s to 30 between 42 and 45 s, and a grey band spans the range of last exit times" loading="lazy" />&lt;/p>
&lt;p>&lt;em>Cumulative number of agents evacuated [-] against time since the start of the
simulation [s], &lt;code>assets/fic_vs_fed_speed&lt;/code>, N = 30 agents, clear air, no
pre-movement. One line per seed, each with its own colour and line style,
n = 5 seeds. The grey band spans the last exit time over the seeds, from the
earliest to the latest. Exit times are read from the
trajectory at 0.1 s resolution. Regenerated by &lt;code>examples/rset_ensemble.py&lt;/code>.&lt;/em>&lt;/p>
&lt;h3>Part 2: an incapacitated agent and &lt;code>evacuation_time&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="part-2-an-incapacitated-agent-and-evacuation_time">&lt;/span>
&lt;a href="#part-2-an-incapacitated-agent-and-evacuation_time" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>No tracked FDS case gives incapacitations and exits in the same run within a
few seconds of runtime. Part 2 therefore uses the ISO 20414 Test 19 room of the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/walkthrough/">Real-FDS walkthrough&lt;/a>
: one occupant held still in 0.10 % CO,
2.0 % CO2 and 15.0 % O2, read from the FDS slices in
&lt;code>assets/iso_table22_coupled/fds/a&lt;/code>.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>room &lt;span style="color:#f92672">=&lt;/span> load_scenario(&lt;span style="color:#e6db74">&amp;#34;assets/iso_table22_coupled/config_a.json&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>gas_dir &lt;span style="color:#f92672">=&lt;/span> &lt;span style="color:#e6db74">&amp;#34;assets/iso_table22_coupled/fds/a&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>fed &lt;span style="color:#f92672">=&lt;/span> DefaultFedModel(FdsFedField&lt;span style="color:#f92672">.&lt;/span>from_fds(gas_dir), DefaultFedConfig())
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>tenability &lt;span style="color:#f92672">=&lt;/span> TenabilityConfig(incapacitation_mode&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#e6db74">&amp;#34;deterministic&amp;#34;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>fire &lt;span style="color:#f92672">=&lt;/span> run_scenario(room, seed&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">420&lt;/span>, fed_model&lt;span style="color:#f92672">=&lt;/span>fed, tenability_config&lt;span style="color:#f92672">=&lt;/span>tenability)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;evacuation_time &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>fire&lt;span style="color:#f92672">.&lt;/span>evacuation_time&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.0f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s &amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;(max_simulation_time &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>room&lt;span style="color:#f92672">.&lt;/span>max_simulation_time&lt;span style="color:#e6db74">:&lt;/span>&lt;span style="color:#e6db74">.0f&lt;/span>&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74"> s), &amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;success &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>fire&lt;span style="color:#f92672">.&lt;/span>success&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>print(
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;exit times &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>exit_times(fire)&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">, incapacitated &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>incapacitated(fire)&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">, &amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#e6db74">f&lt;/span>&lt;span style="color:#e6db74">&amp;#34;remaining &lt;/span>&lt;span style="color:#e6db74">{&lt;/span>fire&lt;span style="color:#f92672">.&lt;/span>agents_remaining&lt;span style="color:#e6db74">}&lt;/span>&lt;span style="color:#e6db74">&amp;#34;&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>fire&lt;span style="color:#f92672">.&lt;/span>cleanup()&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-text" data-lang="text">&lt;span style="display:flex;">&lt;span>evacuation_time 1150 s (max_simulation_time 1150 s), success True
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>exit times [], incapacitated 1, remaining 1&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>The occupant is incapacitated at 982 s and never leaves. Nevertheless
&lt;code>evacuation_time&lt;/code> reports the time limit and &lt;code>success&lt;/code> is &lt;code>True&lt;/code>. The exit
times, the incapacitated count and the remaining count tell you what happened.&lt;/p>
&lt;h2>Incapacitation mode&lt;span class="hx-absolute -hx-mt-20" id="incapacitation-mode">&lt;/span>
&lt;a href="#incapacitation-mode" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;code>TenabilityConfig()&lt;/code> defaults to &lt;code>incapacitation_mode=&amp;quot;probabilistic&amp;quot;&lt;/code>. Each
agent then draws its own threshold, &lt;code>fed_threshold * exp(susceptibility_sigma * Z)&lt;/code>
with &lt;em>Z&lt;/em> ~ N(0, 1): a log-normal with median &lt;code>fed_threshold&lt;/code>. The defaults
are on the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#tenability-irritant-slowdown-and-incapacitation">FED model&lt;/a>
page. For results comparable with FDS+Evac, where
every agent is incapacitated at FED = 1, use
&lt;code>TenabilityConfig(incapacitation_mode=&amp;quot;deterministic&amp;quot;)&lt;/code>, or
&lt;code>--incapacitation-mode deterministic&lt;/code> on the command line. Part 2 uses the
deterministic mode.&lt;/p>
&lt;h2>Related options&lt;span class="hx-absolute -hx-mt-20" id="related-options">&lt;/span>
&lt;a href="#related-options" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>To add smoke or FED from FDS output to an ensemble, pass the same models to
&lt;code>run_scenario&lt;/code> inside the loop, as in the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/walkthrough/">Real-FDS walkthrough&lt;/a>
.&lt;/li>
&lt;li>Pre-movement is set per spawn area in the scenario JSON, with
&lt;code>use_premovement&lt;/code>, &lt;code>premovement_distribution&lt;/code> (&lt;code>gamma&lt;/code>, &lt;code>lognormal&lt;/code>,
&lt;code>weibull&lt;/code> or &lt;code>uniform&lt;/code>), &lt;code>premovement_param_a&lt;/code>, &lt;code>premovement_param_b&lt;/code> and
&lt;code>premovement_seed&lt;/code>. If &lt;code>premovement_seed&lt;/code> is &lt;code>null&lt;/code>, it is derived from the
run seed, so the pre-movement times change with the seed. If it is set, they
stay the same across seeds.&lt;/li>
&lt;li>Each manifest also records &lt;code>uv_lock_sha256&lt;/code>, &lt;code>scenario_path&lt;/code>, &lt;code>fds_dir&lt;/code>,
&lt;code>fds_version&lt;/code> and &lt;code>created_utc&lt;/code>. &lt;code>cleanup()&lt;/code> deletes it with the trajectory,
so copy both first if you want to keep them. With &lt;code>run.py --output-sqlite PATH&lt;/code>, the manifest is copied next to &lt;code>PATH&lt;/code> as &lt;code>&amp;lt;stem&amp;gt;.manifest.json&lt;/code>.&lt;/li>
&lt;/ul>
&lt;h2>See also&lt;span class="hx-absolute -hx-mt-20" id="see-also">&lt;/span>
&lt;a href="#see-also" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/quickstart/">Quickstart&lt;/a>
&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/walkthrough/">Real-FDS walkthrough&lt;/a>
&lt;/li>
&lt;li>&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/limitations/">Limitations&lt;/a>
&lt;/li>
&lt;/ul>
&lt;p>pyFDS-Evac is research software, provided without warranty.&lt;/p></description></item><item><title>Homogeneous CO FED Validation</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/testing-homogeneous/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/testing-homogeneous/</guid><description>
&lt;h2>Purpose&lt;span class="hx-absolute -hx-mt-20" id="purpose">&lt;/span>
&lt;a href="#purpose" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This test case validates the FED (Fractional Effective Dose) accumulation and
probabilistic incapacitation logic in the pyFDS-Evac pipeline against
hand-calculated predictions, using a simplified scenario with a spatially
&lt;strong>homogeneous&lt;/strong> (uniform) CO concentration field.&lt;/p>
&lt;p>By removing spatial gradients as a variable, any deviation between simulated
and hand-calculated FED/incapacitation results can be attributed to the
model/pipeline logic itself, rather than to local concentration variation —
isolating the FED accumulation and incapacitation-probability code from the
gas-transport physics.&lt;/p>
&lt;p>Three CO concentration levels are tested (2000, 4000, and 8000 ppm) to check
that the pipeline&amp;rsquo;s FED/incapacitation results scale correctly with dose,
rather than validating against just a single data point.&lt;/p>
&lt;p>Testing was also conducted using probabilistic tenability to make ensure that encapacitation
occurs at a log-normal distribution.&lt;/p>
&lt;blockquote>
&lt;p>&lt;strong>Note:&lt;/strong> &lt;code>fed_hand_calc.py&lt;/code> and the two onset-distribution graphs
referenced below are not currently committed to the repo — the formulae
and pass/fail numbers here were computed and verified with them at the
time, but a reader can&amp;rsquo;t regenerate the reference curve or view the plots
from this checkout. The &lt;code>TODO&lt;/code> in &lt;a href="#how-to-run">How to Run&lt;/a>
and the
&lt;a href="#remaining-before-this-test-case-is-complete">Remaining&lt;/a>
section below
are original and still open.&lt;/p>&lt;/blockquote>
&lt;h2>Test Setup&lt;span class="hx-absolute -hx-mt-20" id="test-setup">&lt;/span>
&lt;a href="#test-setup" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;strong>Geometry:&lt;/strong> Sealed room (no vents/openings), so CO cannot escape and the
concentration field stays spatially uniform once mixed.&lt;/li>
&lt;li>&lt;strong>Source:&lt;/strong> Constant CO generation rate, homogeneous throughout the room.
Three variants of this test are run, at 2000 ppm, 4000 ppm, and 8000 ppm
CO, each producing a known, time-resolved CO concentration that can be
predicted analytically.&lt;/li>
&lt;li>&lt;strong>Domain decomposition:&lt;/strong> 4 MPI sub-meshes — confirms FED values are
consistent across mesh boundaries (i.e. splitting the domain doesn&amp;rsquo;t
introduce discontinuities in the gas data JuPedSim consumes).&lt;/li>
&lt;li>&lt;strong>Agents:&lt;/strong> 100 JuPedSim agents, non-evacuating travelling in a rectangular circuit, exposed
uniformly to the CO field — isolates FED/incapacitation tracking from
movement and exit-routing dynamics.&lt;/li>
&lt;/ul>
&lt;h2>What&amp;rsquo;s Being Validated&lt;span class="hx-absolute -hx-mt-20" id="whats-being-validated">&lt;/span>
&lt;a href="#whats-being-validated" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ol>
&lt;li>&lt;strong>FED accumulation&lt;/strong> — per-agent FED(t) computed from FDS CO output
matches the hand-calculated FED curve for a known, constant CO
concentration, across all three concentration levels.&lt;/li>
&lt;li>&lt;strong>Probabilistic incapacitation&lt;/strong> — agents are incapacitated according to
the correct FED-derived probability model, not deterministically at a
fixed FED threshold.&lt;/li>
&lt;li>&lt;strong>Pipeline integration&lt;/strong> — FDS gas slice output → &lt;code>fdsreader&lt;/code> → JuPedSim
correctly carries FED data across the 4-mesh decomposition with no
per-mesh discrepancies. (Not &lt;code>fdsvismap&lt;/code>: that&amp;rsquo;s a separate component
used only by the sign-visibility model that gates rerouting/&lt;code>discovery&lt;/code>
familiarity — this test has a single exit, non-evacuating agents, and no
&lt;code>--vis-cache&lt;/code>/&lt;code>--enable-rerouting&lt;/code>, so it never touches fdsvismap.)&lt;/li>
&lt;/ol>
&lt;h2>Method&lt;span class="hx-absolute -hx-mt-20" id="method">&lt;/span>
&lt;a href="#method" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ol>
&lt;li>Hand-calculate the expected FED(t) curve for each of the three CO
concentrations (2000, 4000, 8000 ppm) using the FDS-native Purser
FED/CO incapacitation model (see &lt;code>fed_hand_calc.py&lt;/code>).&lt;/li>
&lt;li>Run the full FDS → fdsreader → JuPedSim pipeline on each of the three
scenarios.&lt;/li>
&lt;li>Compare simulated per-agent FED accumulation curves and incapacitation
timing/probability distribution against the hand-calculated reference,
for each concentration.&lt;/li>
&lt;li>Confirm agreement within tolerance across all 100 agents and all 4
sub-meshes, at all three concentration levels.&lt;/li>
&lt;li>Run the homogenous test at different seeds with the probabilistic incapacitation to ensure they&amp;rsquo;re following the log-normal distribution.&lt;/li>
&lt;/ol>
&lt;h2>Hand-Calculated FED Reference&lt;span class="hx-absolute -hx-mt-20" id="hand-calculated-fed-reference">&lt;/span>
&lt;a href="#hand-calculated-fed-reference" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Computed with &lt;code>fed_hand_calc.py&lt;/code>, using the exact FDS Purser equations
(NIST SP 1019, Section 22.10.18, eqs 22.42–22.49) — the same equations FDS
itself uses to produce its &lt;code>FED&lt;/code> device output. Background CO2 = 500 ppm
(0.05%), O2 = 20.9% (ambient, no hypoxia contribution) held constant for
all three cases.&lt;/p>
&lt;h3>Formulae used&lt;span class="hx-absolute -hx-mt-20" id="formulae-used">&lt;/span>
&lt;a href="#formulae-used" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>These are the terms the hand calculation evaluates; the coded forms are on the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#coded-form">FED model&lt;/a>
page.&lt;/p>
&lt;p>&lt;strong>FED from CO&lt;/strong> (eq. 22.43), C_CO in ppm, t in minutes:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>FED_CO = 2.764e-5 x (C_CO)^1.036 x t&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;strong>FED from O2 hypoxia&lt;/strong> (eq. 22.48), C_O2 in volume percent, t in minutes:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>FED_O2 = t / exp[8.13 - 0.54 x (20.9 - C_O2)]&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;strong>Hyperventilation factor from CO2&lt;/strong> (eq. 22.49), C_CO2 in percent:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>HV_CO2 = exp(0.1903 x C_CO2 &amp;#43; 2.0004) / 7.1&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;strong>Total FED&lt;/strong> (eq. 22.42) — FED_CN, FED_NOx, and FLD_irr are omitted here
since this scenario is CO-only:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>FED_tot = FED_CO x HV_CO2 &amp;#43; FED_O2&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;code>fed_hand_calc.py&lt;/code> accumulates this per-timestep (matching how FDS itself
does it internally) rather than as a single closed-form multiplication —
this matters because &lt;code>HV_CO2&lt;/code> is not 1.0 even at 0% CO2 (it evaluates to
~1.04), so it can&amp;rsquo;t be pulled out and applied once at the end.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>CO Concentration&lt;/th>
&lt;th>FED = 0.3 (onset)&lt;/th>
&lt;th>FED = 1.0 (incapacitation)&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>2000 ppm&lt;/td>
&lt;td>234.7 s&lt;/td>
&lt;td>782.4 s&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>4000 ppm&lt;/td>
&lt;td>114.7 s&lt;/td>
&lt;td>382.3 s&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>8000 ppm&lt;/td>
&lt;td>56.0 s&lt;/td>
&lt;td>186.6 s&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;h2>Directory Contents&lt;span class="hx-absolute -hx-mt-20" id="directory-contents">&lt;/span>
&lt;a href="#directory-contents" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Each case is a self-contained scenario directory. The deck, the JuPedSim
config and the walkable area live together; FDS run output lands beside them
and is gitignored.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>assets/
├── fed_incap_co_2000ppm/
│ ├── fed_incap_co_2000ppm.fds — FDS deck, 2000 ppm CO (CHID demo_homogeneous_CO_2000ppm)
│ ├── config.json — JuPedSim scenario (agents, journey, routing)
│ ├── geometry.wkt — Walkable area (Shapely WKT)
│ └── &amp;lt;untracked run output&amp;gt; — .smv .out .pickle .sf/.sf.bnd (4 meshes x 17 quantities),
│ _devc.csv _hrr.csv _cpu.csv _steps.csv _git.txt
├── fed_incap_co_4000ppm/ — same layout, 4000 ppm
└── fed_incap_co_8000ppm/ — same layout, 8000 ppm&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Three earlier iterations from this debugging lineage are no longer tracked:
&lt;code>fed_incap_co_v1&lt;/code> and &lt;code>fed_incap_co_v2&lt;/code> (both 4000 ppm), and
&lt;code>fed_incap_co_smol&lt;/code>, which never had a deck of its own. The 2000/4000/8000 ppm
ladder supersedes all three.&lt;/p>
&lt;p>pyFDS-Evac run artefacts (written wherever you point the &lt;code>--output-*&lt;/code> flags):&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>&amp;lt;scenario&amp;gt;.sqlite — Agent trajectory database
&amp;lt;scenario&amp;gt;_fed_history.csv — Per-agent FED accumulation over time
&amp;lt;scenario&amp;gt;_smoke_history.csv — Per-agent smoke exposure over time
&amp;lt;scenario&amp;gt;_route_history.csv — Agent route decisions over time
&amp;lt;scenario&amp;gt;_route_cost_history.csv — Route cost values over time&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Analysis plots (incapacitation-onset histograms and cumulative onset curves
per concentration) are produced from the FED history CSV by the plotting
scripts in &lt;code>scripts/&lt;/code>; see &lt;code>docs/usage.md&lt;/code>.&lt;/p>
&lt;h2>How to Run&lt;span class="hx-absolute -hx-mt-20" id="how-to-run">&lt;/span>
&lt;a href="#how-to-run" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;blockquote>
&lt;p>TODO — fill in exact commands once finalized.&lt;/p>&lt;/blockquote>
&lt;h2>Results / Pass Criteria&lt;span class="hx-absolute -hx-mt-20" id="results--pass-criteria">&lt;/span>
&lt;a href="#results--pass-criteria" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;strong>Status: All cases of 2000, 4000, and 8000 ppm are passing.&lt;/strong>&lt;/p>
&lt;p>For the 8000 ppm case, all 100 agents now show &lt;code>incapacitated = True&lt;/code> at
~186–187 s, matching the hand-calculated FED = 1.0 time of &lt;strong>186.6 s&lt;/strong>
within about 1 s. This is the first run where the sampler bug (CO/CO2/O2
all reading 0, per earlier debugging) is no longer visibly affecting the
result.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Case&lt;/th>
&lt;th>FED = 1.0 (Hand Calculation)&lt;/th>
&lt;th>FED = 1.0 (Simulation)&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>2000 ppm&lt;/td>
&lt;td>782.4 s&lt;/td>
&lt;td>786 s&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>4000 ppm&lt;/td>
&lt;td>382.3 s&lt;/td>
&lt;td>384 s&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>8000 ppm&lt;/td>
&lt;td>186.6 s&lt;/td>
&lt;td>187 s&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>All of these result in a percent difference &amp;lt;0.50% which is accepted.&lt;/p>
&lt;p>
&lt;img src="../graphs/homogenous_co_incapacitation_onset_8000ppm.png" alt="Incapacitation onset time distribution, 8000ppm" loading="lazy" />&lt;/p>
&lt;p>
&lt;img src="../graphs/homogenous_onset_cumulative_8000ppm.png" alt="Cumulative incapacitation over time, 8000ppm" loading="lazy" />&lt;/p>
&lt;p>Note on terminology: the pipeline&amp;rsquo;s &amp;ldquo;onset time&amp;rdquo; (plotted above) is the
timestamp at which &lt;code>incapacitated&lt;/code> first flips &lt;code>True&lt;/code> — this corresponds
to the hand-calc&amp;rsquo;s &lt;strong>FED = 1.0&lt;/strong> threshold (186.6 s), not the FED = 0.3
&amp;ldquo;onset&amp;rdquo; threshold used elsewhere in this doc&amp;rsquo;s reference table (56.0 s for
8000 ppm). Same word, two different thresholds — worth not conflating the
two when comparing numbers.&lt;/p>
&lt;h3>Remaining before this test case is complete&lt;span class="hx-absolute -hx-mt-20" id="remaining-before-this-test-case-is-complete">&lt;/span>
&lt;a href="#remaining-before-this-test-case-is-complete" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;ul>
&lt;li>Decide and document a formal tolerance (±5%, absolute FED difference,
etc.) rather than eyeballing agreement from the plots.&lt;/li>
&lt;li>Confirm whether the single-instant incapacitation spike is expected
behavior for the probabilistic model in a homogeneous scenario.&lt;/li>
&lt;/ul></description></item><item><title>Homogeneous Heat FED Validation</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/testing-heat/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/testing-heat/</guid><description>
&lt;h2>Purpose&lt;span class="hx-absolute -hx-mt-20" id="purpose">&lt;/span>
&lt;a href="#purpose" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This test case validates the heat FED (Fractional Effective Dose from
convective heat, SFPE Handbook Ch. 63 Eq. 63.44) accumulation logic in the pyFDS-Evac
pipeline against hand-calculated predictions, using a simplified scenario
with a spatially &lt;strong>homogeneous&lt;/strong> (uniform) gas-phase temperature field. It
is the direct sibling of &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/testing-homogeneous/">&lt;code>docs/testing-homogeneous.md&lt;/code>&lt;/a>
(the toxic-CO validation), following the same method and the same directory
conventions.&lt;/p>
&lt;p>By removing spatial gradients as a variable, any deviation between simulated
and hand-calculated heat FED results can be attributed to the model/pipeline
logic itself, rather than to local temperature variation — isolating the
heat FED accumulation code from the heat-transport physics.&lt;/p>
&lt;p>Three temperature levels are tested (100, 150, 200 °C) to check that the
pipeline&amp;rsquo;s heat FED results scale correctly with the T^3.4 power law, rather
than validating against just a single data point.&lt;/p>
&lt;p>&lt;strong>Scope note:&lt;/strong> this validates the heat FED &lt;em>dose&lt;/em> track (incapacitation)
only. Heat does not currently factor into route cost or rejection — that
routing question is deliberately deferred pending further design discussion,
so there is nothing routing-related to validate here.&lt;/p>
&lt;p>Heat also has no effect on walking speed below the threshold. Extinction
(Frantzich–Nilsson) and irritants (FIC) both degrade speed continuously; heat
does nothing at all until the dose is reached and then sets the target speed to
zero. That is faithful to the standard, which gives a tolerance time rather
than a performance decrement — but it means an agent walks unimpeded through a
200 °C layer for the 45 s the dose takes to accumulate, so do not read a heat
FED trace as a speed trace.&lt;/p>
&lt;h2>Test Setup&lt;span class="hx-absolute -hx-mt-20" id="test-setup">&lt;/span>
&lt;a href="#test-setup" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>
&lt;p>&lt;strong>Geometry:&lt;/strong> Sealed room (no vents/openings), so temperature cannot
redistribute, with &lt;strong>adiabatic boundaries on all six faces&lt;/strong>. The boundary
condition is the one place the CO ladder&amp;rsquo;s layout cannot be copied: FDS&amp;rsquo;s
default is an &lt;code>INERT&lt;/code> wall at &lt;code>TMP_FRONT = TMPA&lt;/code>, which would hold ~2160 m²
of enclosure surface at ambient against the prescribed gas and drain the
room. Species do not diffuse into inert walls; temperature does.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Source:&lt;/strong> No combustion at all — unlike the CO ladder, this deck tracks
no species and needs no &lt;code>&amp;amp;REAC&lt;/code>/yield setup. A single &lt;code>&amp;amp;INIT&lt;/code> prescribes a
constant initial temperature across the whole domain. With no source term,
no decay and no heat flux through any surface, the field stays at that
temperature for the whole run, which is what makes the closed-form rate the
right reference to compare against.&lt;/p>
&lt;p>Verify this before trusting a result: the &lt;code>TEMP_center*&lt;/code> and &lt;code>TEMP_corner*&lt;/code>
devices should all read the prescribed value at &lt;code>T_END&lt;/code>, not merely at
t = 0. A falling trace means the boundaries are not holding and the
comparison below is measuring the deck rather than the model.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Domain decomposition:&lt;/strong> 4 MPI sub-meshes — confirms heat FED values are
consistent across mesh boundaries (i.e. splitting the domain doesn&amp;rsquo;t
introduce discontinuities in the gas data JuPedSim consumes), same as the
CO ladder.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>&lt;strong>Agents:&lt;/strong> 100 JuPedSim agents, non-evacuating travelling in a rectangular
circuit, exposed uniformly to the temperature field — isolates heat FED
tracking from movement and exit-routing dynamics. Reuses the CO ladder&amp;rsquo;s
&lt;code>config.json&lt;/code>/&lt;code>geometry.wkt&lt;/code> verbatim (the scenario config is physics-track
agnostic).&lt;/p>
&lt;/li>
&lt;/ul>
&lt;h2>What&amp;rsquo;s Being Validated&lt;span class="hx-absolute -hx-mt-20" id="whats-being-validated">&lt;/span>
&lt;a href="#whats-being-validated" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ol>
&lt;li>&lt;strong>Heat FED accumulation&lt;/strong> — per-agent FED_HEAT(t) computed from the FDS
TEMPERATURE slice output matches the hand-calculated closed form for a
known, constant gas temperature, across all three levels.&lt;/li>
&lt;li>&lt;strong>Pipeline integration&lt;/strong> — FDS TEMPERATURE slice output → &lt;code>fdsreader&lt;/code> →
JuPedSim correctly carries heat FED data across the 4-mesh decomposition
with no per-mesh discrepancies.&lt;/li>
&lt;li>&lt;strong>Slice-height correctness&lt;/strong> — &lt;code>FdsHeatField.from_fds()&lt;/code> goes through
&lt;code>load_slice_sampler()&lt;/code> (unlike the gas &lt;code>FdsFedField&lt;/code>, a known pre-existing
blind spot — see &lt;code>pyfds_evac/core/fed.py&lt;/code>), so this is also, incidentally,
the first asset to exercise the height-mismatch warning path for a newly
added slice type.&lt;/li>
&lt;/ol>
&lt;h2>Method&lt;span class="hx-absolute -hx-mt-20" id="method">&lt;/span>
&lt;a href="#method" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ol>
&lt;li>Hand-calculate the expected FED_HEAT(t) curve for each of the three
temperatures (100, 150, 200 °C) using SFPE Handbook Eq. 63.44 (see
&lt;code>scripts/fed_heat_hand_calc.py&lt;/code>).&lt;/li>
&lt;li>Run the full FDS → fdsreader → JuPedSim pipeline on each of the three
scenarios.&lt;/li>
&lt;li>Compare simulated per-agent heat FED accumulation curves against the
hand-calculated reference, for each temperature.&lt;/li>
&lt;li>Confirm agreement within tolerance across all 100 agents and all 4
sub-meshes, at all three temperature levels.&lt;/li>
&lt;/ol>
&lt;h2>Hand-Calculated Heat FED Reference&lt;span class="hx-absolute -hx-mt-20" id="hand-calculated-heat-fed-reference">&lt;/span>
&lt;a href="#hand-calculated-heat-fed-reference" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Computed with &lt;code>scripts/fed_heat_hand_calc.py&lt;/code>, using SFPE Handbook Eq. 63.44
directly — &lt;em>SFPE Handbook of Fire Protection Engineering&lt;/em>, 5th ed., Ch. 63
&amp;ldquo;Assessment of Hazards to Occupants from Smoke, Toxic Gases, and Heat&amp;rdquo;
(Purser &amp;amp; McAllister), p. 2382, quoted rather than derived. &lt;strong>Not&lt;/strong> ISO TS
13571 (the formula was previously miscited as that standard) and &lt;strong>not&lt;/strong> in
the FDS+Evac guide (that document has no heat term at all).&lt;/p>
&lt;h3>Formula used&lt;span class="hx-absolute -hx-mt-20" id="formula-used">&lt;/span>
&lt;a href="#formula-used" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h3>&lt;p>&lt;strong>FED from convective heat&lt;/strong> (SFPE Handbook Eq. 63.44), T in °C, Δt in minutes;
the coded form is on the &lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#convective-heat">FED model&lt;/a>
page:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>FED_HEAT = sum_{t1}^{t2} [ T^3.4 / 5e7 ] * dt&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Under this test&amp;rsquo;s constant-T exposure the accumulator reduces to an exact
closed form, &lt;code>FED_HEAT(t) = (T^3.4 / 5e7) * t_min&lt;/code>, and that is what
&lt;code>scripts/fed_heat_hand_calc.py&lt;/code> evaluates — one multiplication per temperature,
not a per-timestep sum. The sum is what the &lt;em>engine&lt;/em> does, over the FDS dump&amp;rsquo;s
own timestamps; the point of the closed form is to be an independent check on
it, so the reference deliberately does not reproduce the engine&amp;rsquo;s loop.&lt;/p>
&lt;p>Driving the reference off a real dump would only be necessary for a
time-varying field. These decks hold temperature constant precisely so that it
is not.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Temperature&lt;/th>
&lt;th>FED = 0.3 (onset)&lt;/th>
&lt;th>FED = 1.0 (incapacitation)&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>100 °C&lt;/td>
&lt;td>142.6 s&lt;/td>
&lt;td>475.5 s&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>150 °C&lt;/td>
&lt;td>35.9 s&lt;/td>
&lt;td>119.8 s&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>200 °C&lt;/td>
&lt;td>13.5 s&lt;/td>
&lt;td>45.0 s&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>(Accumulator and closed-form agree to all printed digits — expected, since
there is no HV-style multiplier or ramp to introduce discretisation error
the way the CO ladder&amp;rsquo;s CO2 hyperventilation term does.)&lt;/p>
&lt;h2>Directory Contents&lt;span class="hx-absolute -hx-mt-20" id="directory-contents">&lt;/span>
&lt;a href="#directory-contents" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Each case is a self-contained scenario directory, following the CO ladder&amp;rsquo;s
layout exactly:&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>assets/
├── fed_incap_heat_100c/
│ ├── fed_incap_heat_100c.fds — FDS deck, 100 C (CHID demo_homogeneous_heat_100C)
│ ├── config.json — JuPedSim scenario (reused from fed_incap_co_2000ppm)
│ ├── geometry.wkt — Walkable area (reused from fed_incap_co_2000ppm)
│ └── &amp;lt;untracked run output&amp;gt; — .smv .out .sf/.sf.bnd (4 meshes), _devc.csv _cpu.csv _steps.csv
├── fed_incap_heat_150c/ — same layout, 150 C
└── fed_incap_heat_200c/ — same layout, 200 C&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;code>config.json&lt;/code>/&lt;code>geometry.wkt&lt;/code> are byte-identical copies of
&lt;code>fed_incap_co_2000ppm&lt;/code>&amp;rsquo;s — the scenario config (agents, journey, routing
weights) has no dependency on which hazard track is under test.&lt;/p>
&lt;p>pyFDS-Evac run artefacts (written wherever you point the &lt;code>--output-*&lt;/code> flags):&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>&amp;lt;scenario&amp;gt;.sqlite — Agent trajectory database
&amp;lt;scenario&amp;gt;_fed_history.csv — Per-agent FED &amp;#43; heat FED accumulation over time&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h2>How to Run&lt;span class="hx-absolute -hx-mt-20" id="how-to-run">&lt;/span>
&lt;a href="#how-to-run" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># Requires an FDS install with a working MPI runtime (e.g. Intel MPI) on PATH.&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>mpiexec -n &lt;span style="color:#ae81ff">4&lt;/span> fds assets/fed_incap_heat_100c/fed_incap_heat_100c.fds
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>mpiexec -n &lt;span style="color:#ae81ff">4&lt;/span> fds assets/fed_incap_heat_150c/fed_incap_heat_150c.fds
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>mpiexec -n &lt;span style="color:#ae81ff">4&lt;/span> fds assets/fed_incap_heat_200c/fed_incap_heat_200c.fds
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>uv run python run.py &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --scenario assets/fed_incap_heat_100c &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --fds-dir assets/fed_incap_heat_100c &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --heat-fed-threshold 1.0 &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-fed-history /tmp/heat_100c_fed_history.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-sqlite /tmp/heat_100c.sqlite
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e"># repeat --fds-dir/--scenario for the 150c and 200c decks&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>No &lt;code>--fed-threshold&lt;/code>/CO-related flags are needed — these decks track no gas
species, so the toxic FED path stays inactive (&lt;code>fed_model=None&lt;/code>) and only
&lt;code>heat_fed_model&lt;/code> is built.&lt;/p>
&lt;h2>Results / Pass Criteria&lt;span class="hx-absolute -hx-mt-20" id="results--pass-criteria">&lt;/span>
&lt;a href="#results--pass-criteria" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;strong>Status: not yet run.&lt;/strong> FDS could not be executed to produce real output in
the environment this test case was built in — the installed FDS binary
requires the Intel MPI runtime (&lt;code>impi.dll&lt;/code>), which is not present, and
installing it is a system-level change outside the scope of adding this
verification asset. The decks, hand-calc reference, and pipeline wiring are
all in place and unit/behavioural-tested (see &lt;code>tests/verification/test_s6_heat_fed.py&lt;/code>
and &lt;code>test_heat_fed_verif.py&lt;/code>, which validate the same formula and OR-incapacitation
logic against synthetic fields, no FDS required) — what remains is running
the three decks above in an environment with FDS+MPI available and filling
in this table:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Case&lt;/th>
&lt;th>FED = 1.0 (Hand Calculation)&lt;/th>
&lt;th>FED = 1.0 (Simulation)&lt;/th>
&lt;th>% difference&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>100 °C&lt;/td>
&lt;td>475.5 s&lt;/td>
&lt;td>—&lt;/td>
&lt;td>—&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>150 °C&lt;/td>
&lt;td>119.8 s&lt;/td>
&lt;td>—&lt;/td>
&lt;td>—&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>200 °C&lt;/td>
&lt;td>45.0 s&lt;/td>
&lt;td>—&lt;/td>
&lt;td>—&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>Target tolerance: &amp;lt;0.5%, matching the CO ladder&amp;rsquo;s precedent in
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/testing-homogeneous/">&lt;code>docs/testing-homogeneous.md&lt;/code>&lt;/a>
.&lt;/p>
&lt;p>Remaining before this test case is complete:&lt;/p>
&lt;ul>
&lt;li>Run all three decks through FDS and fill in the table above.&lt;/li>
&lt;li>Confirm the &lt;code>FdsHeatField.from_fds()&lt;/code> height-matching warning path fires
correctly if a mismatched &lt;code>--smoke-slice-height&lt;/code> is passed (it reuses the
same flag as smoke/gas FED).&lt;/li>
&lt;/ul></description></item><item><title>Familiarity Routing Test (Full vs. Discovery)</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/testing-familiarity/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/testing-familiarity/</guid><description>
&lt;h2>Purpose&lt;span class="hx-absolute -hx-mt-20" id="purpose">&lt;/span>
&lt;a href="#purpose" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>This test case validates that the &lt;code>full&lt;/code> and &lt;code>discovery&lt;/code> agent familiarity
tiers produce &lt;strong>different evacuation behavior&lt;/strong>, not just a config
flag with no observable effect.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>&lt;code>full&lt;/code>&lt;/strong> agents know the entire building layout from the moment they
spawn (trained staff) and take the true shortest route to the exit.&lt;/li>
&lt;li>&lt;strong>&lt;code>discovery&lt;/code>&lt;/strong> agents start knowing almost nothing (a first-time visitor)
and can only route through checkpoints they&amp;rsquo;ve either started at or
physically passed through. When no known route to an exit exists yet, they
head toward the nearest checkpoint they know about but haven&amp;rsquo;t explored,
discover what&amp;rsquo;s beyond it on arrival, and repeat until an exit is found.&lt;/li>
&lt;/ul>
&lt;p>The two configs (&lt;code>Full&lt;/code>/&lt;code>Discovery&lt;/code>) are identical except for one field
(&lt;code>familiarity&lt;/code> on the spawn distribution) and share the same hand-drawn maze
geometry and the same paired real-fire FDS deck, so any difference in
outcome is attributable to the familiarity mechanism itself.&lt;/p>
&lt;p>This test also exercises, indirectly, three engine-level fixes/features that
were required before familiarity could have &lt;em>any&lt;/em> observable effect at all
(see &lt;a href="#background-why-this-test-exists">Background&lt;/a>
below).&lt;/p>
&lt;blockquote>
&lt;p>&lt;strong>Deck rework (2026-08-10, PR #99).&lt;/strong> The decks no longer contain the
scripted journey and shortcut-only transitions described below: journeys
and transitions are empty, so the stage graph auto-wires (as in
&lt;code>station_fahy&lt;/code>), the second exit was dropped, all four checkpoints carry
authored direction-facing signs, and CP3 was moved 0.4 m off the
partition wall so an FDS-resolution visibility grid can resolve its
sightline. The scripted-tour/shortcut mechanics and the results quoted
below document the &lt;em>original&lt;/em> experiment — kept because the engine fixes
it motivated are part of the routing system&amp;rsquo;s history. Discovery on the
reworked decks is genuine frontier exploration, not a retrace of a tour.&lt;/p>&lt;/blockquote>
&lt;h2>Test Setup&lt;span class="hx-absolute -hx-mt-20" id="test-setup">&lt;/span>
&lt;a href="#test-setup" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;strong>Geometry:&lt;/strong> hand-drawn maze-like floor plan, 20 m × 18 m, 0.1 m walls,
1.2 m doors throughout, generated parametrically by each config folder&amp;rsquo;s
&lt;code>build_geometry.py&lt;/code> (&lt;code>assets/familiarity_test_full/&lt;/code>,
&lt;code>assets/familiarity_test_discovery/&lt;/code>). One spawn room (bottom-right), one
exit alcove (top-left).&lt;/li>
&lt;li>&lt;strong>Model:&lt;/strong> &lt;code>SocialForceModel&lt;/code>, seed 420, 20 agents (&lt;code>by_number&lt;/code> spawn, all
at once — not flow-spawned).&lt;/li>
&lt;li>&lt;strong>Fire deck:&lt;/strong> &lt;code>assets/familiarity_test_full/familiarity_test.fds&lt;/code>.
Real combustion via &lt;code>&amp;amp;REAC&lt;/code> (not a prescribed &lt;code>&amp;amp;INIT&lt;/code>); the FDS walls
mirror the walkable geometry exactly so smoke propagates through the same
doorways agents use. &lt;em>Not required for the pure-routing result below&lt;/em> —
see &lt;a href="#scope--caveats">Scope&lt;/a>
.&lt;/li>
&lt;li>&lt;strong>Checkpoint graph:&lt;/strong> the maze has one &lt;strong>scripted tour&lt;/strong> —
&lt;code>spawn → CP0 → CP1 → CP2 → CP3 → exit&lt;/code> — walking the doorway at the west
wall (CP0), through a floating-wall gap (CP1), a pocket door (CP2), and the
door into the exit alcove (CP3). This is the only route either tier is
&lt;em>assigned&lt;/em> at spawn, and it&amp;rsquo;s what a config with rerouting disabled always
produces regardless of &lt;code>familiarity&lt;/code>.&lt;/li>
&lt;li>&lt;strong>Shortcut edges:&lt;/strong> the spawn room and the exit-alcove doorway (CP3) sit in
the same open room with no wall between them — a real, walkable shortcut
the scripted tour ignores. Two extra graph edges (&lt;code>CP0→CP3&lt;/code>,
&lt;code>spawn→CP3&lt;/code>, real walking distance ≈12.3 m / ≈11.4 m via JuPedSim&amp;rsquo;s own
routing engine) are declared in each config&amp;rsquo;s top-level &lt;code>transitions&lt;/code>,
tagged with a &lt;code>journey_id&lt;/code> that isn&amp;rsquo;t part of the scripted tour — so they
exist only for the rerouting/cognitive-map system to discover, and never
affect a plain (non-rerouted) run.&lt;/li>
&lt;/ul>
&lt;h2>What&amp;rsquo;s Being Validated&lt;span class="hx-absolute -hx-mt-20" id="whats-being-validated">&lt;/span>
&lt;a href="#whats-being-validated" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ol>
&lt;li>&lt;strong>&lt;code>full&lt;/code> finds and uses the shortcut&lt;/strong> — with its complete graph
knowledge, a &lt;code>full&lt;/code> agent&amp;rsquo;s very first route evaluation (forced to happen
at spawn, before it starts walking the scripted tour) finds
&lt;code>spawn→CP3→exit&lt;/code> cheaper than the scripted route and reroutes onto it
immediately (&lt;code>reason=&amp;quot;better_path&amp;quot;&lt;/code>).&lt;/li>
&lt;li>&lt;strong>&lt;code>discovery&lt;/code> explores instead of knowing&lt;/strong> — a &lt;code>discovery&lt;/code> agent starts
knowing only its spawn&amp;rsquo;s one declared neighbor (CP0). With no exit
reachable in its own knowledge yet, it heads to the nearest known-but-
unvisited checkpoint (frontier exploration, &lt;code>reason=&amp;quot;explore&amp;quot;&lt;/code>),
physically arrives, learns what&amp;rsquo;s beyond it, and repeats. For this
maze&amp;rsquo;s specific distances, the nearest unexplored checkpoint at every
step happens to be the next stop on the original scripted tour (e.g. from
CP0, the CP1 detour at ~6.5 m is closer than the CP3 shortcut at
~12.3 m) — so &lt;code>discovery&lt;/code> retraces the full scripted tour, never
discovering the shortcut before it reaches the exit anyway.&lt;/li>
&lt;li>&lt;strong>The two tiers produce measurably different outcomes&lt;/strong> — not just
different internal state, but different total evacuation time.&lt;/li>
&lt;/ol>
&lt;h2>Background: why this test exists&lt;span class="hx-absolute -hx-mt-20" id="background-why-this-test-exists">&lt;/span>
&lt;a href="#background-why-this-test-exists" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>Two independent bugs meant &lt;code>familiarity&lt;/code> had &lt;em>no effect whatsoever&lt;/em> before
this work, on any scenario:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>&lt;code>journeys_v2&lt;/code> schema mismatch.&lt;/strong> The web editor saves drawn routes as
&lt;code>journeys_v2&lt;/code>; the simulation loader only ever read the legacy &lt;code>journeys&lt;/code>
field. Editor-drawn routes were silently dropped and the scenario fell
back to auto-routing. Fixed by migrating &lt;code>journeys_v2&lt;/code> → legacy
&lt;code>journeys&lt;/code>/&lt;code>transitions&lt;/code> in &lt;code>load_scenario&lt;/code>.&lt;/li>
&lt;li>&lt;strong>&lt;code>familiarity&lt;/code> dropped before reaching agents.&lt;/strong> &lt;code>_process_distributions&lt;/code>
rebuilt each distribution&amp;rsquo;s parameters from a fixed allowlist that didn&amp;rsquo;t
include &lt;code>familiarity&lt;/code>, so every agent was silently treated as &lt;code>full&lt;/code>
regardless of what the config said. Fixed by adding it to the allowlist
and propagating it onto each spawned agent&amp;rsquo;s path state.&lt;/li>
&lt;/ul>
&lt;p>And even with both fixed, the routing engine had no way to &lt;em>act&lt;/em> on a
&lt;code>full&lt;/code> agent&amp;rsquo;s superior knowledge in a single-exit scenario: rerouting only
ever compared &lt;em>which exit&lt;/em> to head for, never &lt;em>which path&lt;/em> to a fixed exit.
Two additions closed that gap:&lt;/p>
&lt;ul>
&lt;li>&lt;code>nearest_frontier_target&lt;/code> (frontier exploration) for the &amp;ldquo;no route to an
exit known yet&amp;rdquo; case.&lt;/li>
&lt;li>Same-exit path-cost comparison in &lt;code>evaluate_and_reroute&lt;/code>
(&lt;code>reason=&amp;quot;better_path&amp;quot;&lt;/code>) so an agent can switch onto a cheaper path to the
exit it&amp;rsquo;s already assigned to, not just a different exit.&lt;/li>
&lt;/ul>
&lt;h2>Method&lt;span class="hx-absolute -hx-mt-20" id="method">&lt;/span>
&lt;a href="#method" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ol>
&lt;li>Load both configs (&lt;code>assets/familiarity_test_full&lt;/code>,
&lt;code>assets/familiarity_test_discovery&lt;/code>) — identical except &lt;code>familiarity&lt;/code>.&lt;/li>
&lt;li>Run each with rerouting enabled (default on) and route-history collection
turned on.&lt;/li>
&lt;li>Compare total evacuation time and inspect &lt;code>route_history&lt;/code> for which
reroute reasons fired for each tier.&lt;/li>
&lt;/ol>
&lt;h2>Directory Contents&lt;span class="hx-absolute -hx-mt-20" id="directory-contents">&lt;/span>
&lt;a href="#directory-contents" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;pre>&lt;code>assets/familiarity_test_full/
├── config.json — JuPedSim scenario: exit, four signed checkpoints,
│ distribution (familiarity: &amp;#34;full&amp;#34;); journeys and
│ transitions empty, so the stage graph auto-wires
├── geometry.wkt — Walkable area (Shapely WKT), matches the FDS deck
├── build_geometry.py — Parametric geometry generator (re-run to reshape)
├── layout_preview.png — Rendered floor plan (green=spawn, red=exit)
└── familiarity_test.fds — FDS input, real &amp;amp;REAC combustion; walls mirror
geometry.wkt so smoke propagates through the
same doorways agents use. Not run/committed
here — see Scope for what does and doesn&amp;#39;t
need it.
assets/familiarity_test_discovery/
└── (same five files; config.json differs only in familiarity: &amp;#34;discovery&amp;#34;)
tests/test_familiarity_routing.py — Unit tests for shortest_path_to,
nearest_frontier_target, and the
better_path/explore reroute branches&lt;/code>&lt;/pre>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;h2>How to Run&lt;span class="hx-absolute -hx-mt-20" id="how-to-run">&lt;/span>
&lt;a href="#how-to-run" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>CLI (rerouting is on by default; shown explicitly for clarity):&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-bash" data-lang="bash">&lt;span style="display:flex;">&lt;span>uv run run.py --scenario assets/familiarity_test_full &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --enable-rerouting --reroute-interval &lt;span style="color:#ae81ff">1&lt;/span> &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-route-history results/familiarity_full_routes.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --seed &lt;span style="color:#ae81ff">420&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>uv run run.py --scenario assets/familiarity_test_discovery &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --enable-rerouting --reroute-interval &lt;span style="color:#ae81ff">1&lt;/span> &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --output-route-history results/familiarity_discovery_routes.csv &lt;span style="color:#ae81ff">\
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#ae81ff">&lt;/span> --seed &lt;span style="color:#ae81ff">420&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>Or from the webapp: pick the scenario in the sidebar, leave &lt;code>Enable rerouting&lt;/code> checked (default), and run.&lt;/p>
&lt;p>No &lt;code>--fds-dir&lt;/code>/&lt;code>--vis-cache&lt;/code> needed for the result below — the divergence is
purely distance-based (see &lt;a href="#scope--caveats">Scope&lt;/a>
).&lt;/p>
&lt;h2>Results / Pass Criteria&lt;span class="hx-absolute -hx-mt-20" id="results--pass-criteria">&lt;/span>
&lt;a href="#results--pass-criteria" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;strong>Status: passing.&lt;/strong> Both tiers evacuate all agents; &lt;code>full&lt;/code> takes the
shortcut and finishes markedly faster than &lt;code>discovery&lt;/code>, which retraces the
scripted maze tour.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Tier&lt;/th>
&lt;th>Evacuated&lt;/th>
&lt;th>Evacuation time&lt;/th>
&lt;th>Route switches&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>full&lt;/td>
&lt;td>20/20&lt;/td>
&lt;td>&lt;strong>35.1 s&lt;/strong>&lt;/td>
&lt;td>20 × &lt;code>better_path&lt;/code> (at spawn, t≈0)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>discovery&lt;/td>
&lt;td>20/20&lt;/td>
&lt;td>&lt;strong>75.1 s&lt;/strong>&lt;/td>
&lt;td>0 (nearest-unexplored always matched the scripted tour)&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>&lt;code>discovery&lt;/code> showing zero explicit switches is expected, not a bug: its
frontier choice happens to coincide with the scripted route at every step
for this maze&amp;rsquo;s specific checkpoint distances (see
&lt;a href="#whats-being-validated">What&amp;rsquo;s Being Validated&lt;/a>
, point 2) — it&amp;rsquo;s still
routing purely off its own explored knowledge, it just never gets lucky
enough to find the shortcut before finishing.&lt;/p>
&lt;p>Full test suite (&lt;code>tests/test_familiarity_routing.py&lt;/code> plus the existing
&lt;code>tests/test_route_graph.py&lt;/code> / &lt;code>tests/verification/test_s4_tjunction_reroute.py&lt;/code>)
passes with no regressions.&lt;/p>
&lt;h2>Scope / Caveats&lt;span class="hx-absolute -hx-mt-20" id="scope--caveats">&lt;/span>
&lt;a href="#scope--caveats" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;ul>
&lt;li>&lt;strong>The scenario is authored to exhibit this behavior&lt;/strong>, not automatically
discovering it on arbitrary geometry. The shortcut edges are hand-added to
the config; a checkpoint graph with only one possible route (or a
single-hop map with nothing to explore) will make &lt;code>full&lt;/code> and &lt;code>discovery&lt;/code>
behave identically regardless of the underlying mechanism.&lt;/li>
&lt;li>&lt;strong>This result doesn&amp;rsquo;t need the fire deck.&lt;/strong> &lt;code>familiarity_test.fds&lt;/code> exists
and is geometrically paired with this maze for a follow-up smoke/toxicity-
driven variant (discovery agents losing sight of exits through smoke via
&lt;code>--vis-cache&lt;/code>).&lt;/li>
&lt;li>&lt;strong>The tiers limit topology, not perception of smoke.&lt;/strong> A &lt;code>discovery&lt;/code> agent is
ignorant of the &lt;em>building&lt;/em> — it routes over &lt;code>cognitive_subgraph&lt;/code> and learns
nodes through &lt;code>VisibilityModel&lt;/code>, which is genuinely perception-limited. It is
not ignorant of the &lt;em>smoke field&lt;/em>: to choose among the exits it does know it
integrates &lt;code>tau = K_ave * L_remaining&lt;/code> over the whole remaining route,
including legs it has never visited, and (with &lt;code>anticipate = True&lt;/code> and
&lt;code>foresight_horizon_s = inf&lt;/code>) at times that have not happened. The extinction
sampler is global and the cognitive map never touches it. So the results on
this page — which are purely distance-based and run in clear air — are
unaffected, and so is every result about map growth, exploration order and
wander behaviour. What must &lt;strong>not&lt;/strong> be claimed from this test, or from any
discovery run, is that a discovery agent&amp;rsquo;s &lt;em>route choice&lt;/em> is
perception-limited. It is an optimality bound over the agent&amp;rsquo;s known
subgraph. See
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/125" target="_blank" rel="noopener">#125&lt;/a>
and
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/route-cost-gate/#route-choice-is-an-optimality-bound-not-a-perception-limited-model">route-cost-gate.md&lt;/a>
.&lt;/li>
&lt;li>On a tie (two frontier candidates equidistant from a discovery agent), the
choice is deterministic (lexicographically-first checkpoint ID), not
randomized per agent — every discovery agent facing an identical tie makes
the identical choice.&lt;/li>
&lt;/ul>
&lt;h2>Observing the map from outside: &lt;code>collect_cognitive_map_history&lt;/code>&lt;span class="hx-absolute -hx-mt-20" id="observing-the-map-from-outside-collect_cognitive_map_history">&lt;/span>
&lt;a href="#observing-the-map-from-outside-collect_cognitive_map_history" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>The cognitive map is built and grown inside &lt;code>run_scenario&lt;/code>, so until recently
the only way to inspect it was to re-implement the expansion against a graph
built the same way — which tests the re-implementation, not the engine&amp;rsquo;s call
sites, and those call sites are where the bugs above actually lived.&lt;/p>
&lt;p>&lt;code>run_scenario(..., collect_cognitive_map_history=True)&lt;/code> returns
&lt;code>ScenarioResult.cognitive_map_history&lt;/code>: one row each time an agent&amp;rsquo;s map
changes, with &lt;code>time_s&lt;/code>, &lt;code>agent_id&lt;/code>, &lt;code>familiarity&lt;/code>, &lt;code>known_nodes&lt;/code> and
&lt;code>known_edges&lt;/code>. Maps only ever grow, so recording changes rather than every
timestep keeps a crowd-scale run cheap while losing nothing.&lt;/p>
&lt;div class="hextra-code-block hx-relative hx-mt-6 first:hx-mt-0 hx-group/code">
&lt;div>&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-python" data-lang="python">&lt;span style="display:flex;">&lt;span>result &lt;span style="color:#f92672">=&lt;/span> run_scenario(scenario, seed&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#ae81ff">420&lt;/span>, vis_model&lt;span style="color:#f92672">=&lt;/span>vis,
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> collect_cognitive_map_history&lt;span style="color:#f92672">=&lt;/span>&lt;span style="color:#66d9ef">True&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">for&lt;/span> event &lt;span style="color:#f92672">in&lt;/span> result&lt;span style="color:#f92672">.&lt;/span>cognitive_map_history:
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> print(event[&lt;span style="color:#e6db74">&amp;#34;time_s&amp;#34;&lt;/span>], len(event[&lt;span style="color:#e6db74">&amp;#34;known_nodes&amp;#34;&lt;/span>]))&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;/div>&lt;div class="hextra-code-copy-btn-container hx-opacity-0 hx-transition group-hover/code:hx-opacity-100 hx-flex hx-gap-1 hx-absolute hx-m-[11px] hx-right-0 hx-top-0">
&lt;button
class="hextra-code-copy-btn hx-group/copybtn hx-transition-all active:hx-opacity-50 hx-bg-primary-700/5 hx-border hx-border-black/5 hx-text-gray-600 hover:hx-text-gray-900 hx-rounded-md hx-p-1.5 dark:hx-bg-primary-300/10 dark:hx-border-white/10 dark:hx-text-gray-400 dark:hover:hx-text-gray-50"
title="Copy code"
>
&lt;div class="copy-icon group-[.copied]/copybtn:hx-hidden hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;div class="success-icon hx-hidden group-[.copied]/copybtn:hx-block hx-pointer-events-none hx-h-4 hx-w-4">&lt;/div>
&lt;/button>
&lt;/div>
&lt;/div>
&lt;p>&lt;code>scripts/animate_cognitive_map.py&lt;/code> turns that history into a movie of one agent
walking with its known nodes highlighted, and authors inward-facing sign angles
for a deck that has none.&lt;/p>
&lt;p>&lt;strong>Perception needs a visibility model.&lt;/strong> With &lt;code>vis_model=None&lt;/code>,
&lt;code>cognitive_map._expand_visible&lt;/code> returns immediately, so a discovery agent&amp;rsquo;s map
grows only when it physically arrives somewhere (&lt;code>expand_on_arrival&lt;/code>). On a deck
where the agent walks straight from spawn to an exit, that means it never grows
at all — see the &amp;ldquo;blind agent&amp;rdquo; issue in the tracker. A clear-air stand-in
(line of sight against the walkable polygon plus the sign&amp;rsquo;s readable half-plane)
is enough to exercise the wiring without FDS output; the test suite and the
animation script each carry one.&lt;/p>
&lt;h2>Clear-air visibility without an FDS run&lt;span class="hx-absolute -hx-mt-20" id="clear-air-visibility-without-an-fds-run">&lt;/span>
&lt;a href="#clear-air-visibility-without-an-fds-run" class="subheading-anchor" aria-label="Permalink for this section">&lt;/a>&lt;/h2>&lt;p>&lt;code>VisibilityModel.clear_air(walkable, sign_descriptors, cell_size_m=...)&lt;/code> builds a
visibility model from geometry alone: fdsvismap&amp;rsquo;s own ray casting, view angle and
&lt;code>max_vis&lt;/code> handling, over a uniform zero extinction field. It exists because
&lt;code>vis_model=None&lt;/code> silently disables perception — a discovery agent&amp;rsquo;s map then
never grows — and because five separate approximations of fdsvismap had
accumulated in this repo, none of them applying the view angle.&lt;/p>
&lt;p>&lt;strong>Resolution is a real parameter.&lt;/strong> A cell blocks sight when its centre lies
outside the walkable polygon, so a wall thinner than one cell disappears and
sight passes through it — the same property an FDS mesh has. At the 0.5 m
default the ~0.4 m walls of &lt;code>assets/blind_spawn_discovery&lt;/code> vanish entirely and
occlusion tests silently stop testing anything; 0.25 m resolves them. Cost grows
as the inverse square. Pick it below the thinnest wall that must block.&lt;/p>
&lt;p>This requires the fdsvismap branch adding &lt;code>set_grid&lt;/code> / &lt;code>set_uniform_extco&lt;/code>
(FireDynamics/fdsvismap#41); &lt;code>pyproject.toml&lt;/code> pins it until that is released.&lt;/p></description></item><item><title>Scenario assets</title><link>https://pedestriandynamics.org/pyFDS-Evac/docs/assets/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://pedestriandynamics.org/pyFDS-Evac/docs/assets/</guid><description>
&lt;p>Scenario definitions are stored in &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/assets/" target="_blank" rel="noopener">&lt;code>assets/&lt;/code>&lt;/a>
.
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/assets/README.md" target="_blank" rel="noopener">&lt;code>assets/README.md&lt;/code>&lt;/a>
indexes the folders and the file
conventions; what each one proves, and where that proof is checked, is below.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>ISO-table21&lt;/strong>: &lt;strong>ISO 20414:2020 Table 21&lt;/strong> (Test 18, &lt;em>reduced visibility vs
walking speed&lt;/em>) — corridor 2 m x 100 m, one occupant at 1,25 m/s, constant
extinction. See &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/assets/ISO-table21/README.md" target="_blank" rel="noopener">the asset README&lt;/a>
for the
clause-by-clause comparison, including the two places we deviate. Proves the smoke speed reduction law is applied correctly end to end:
&lt;code>test_iso_table21_constant_extinction_matches_expected_time_ratio&lt;/code> runs the
scenario clear and then under a &lt;code>ConstantExtinctionField&lt;/code> at five extinction
coefficients (0.5, 1.0, 3.0, 7.5, 10.0 /m), asserting the ratio of evacuation
times matches &lt;code>1 / speed_factor_from_extinction(k)&lt;/code> within 8% and that every
recorded &lt;code>speed_factor&lt;/code> equals the expected one exactly. Doubles as the
standard small fixture in &lt;code>test_progress_callback.py&lt;/code>, &lt;code>test_webapp.py&lt;/code> and
&lt;code>test_fed.py&lt;/code>, which use it for its size rather than its ISO provenance.&lt;/li>
&lt;li>&lt;strong>ISO-table22&lt;/strong>: &lt;strong>ISO 20414:2020 Table 22&lt;/strong> (Test 19, &lt;em>occupant
incapacitation by fire/smoke&lt;/em>) — room 10 m x 10 m x 3 m, one occupant held
still by ISO&amp;rsquo;s prescribed pre-evacuation time above 10 000 000 s. See
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/assets/ISO-table22/README.md" target="_blank" rel="noopener">the asset README&lt;/a>
. The gas field is stubbed, so
it verifies the accumulator, not the FDS coupling; the coupled four-case
version is &lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/assets/iso_table22_coupled/README.md" target="_blank" rel="noopener">&lt;code>iso_table22_coupled&lt;/code>&lt;/a>
. One
agent with &lt;code>v0&lt;/code> forced to 0 in a fixed gas concentration; config and geometry only, no deck. Proves the
runtime FED accumulator agrees with the closed form:
&lt;code>test_iso_table22_stationary_runtime_matches_analytic_threshold_time&lt;/code> takes
the analytic FED=1.0 time from &lt;code>time_to_fed_threshold_s()&lt;/code> and asserts the
observed crossing lands within one timestep of it, that &lt;code>fed_max &amp;gt;= 1.0&lt;/code>, and
that the agent does not evacuate. Holding the gas inputs constant is
deliberate; this tests the accumulator, not the gas sampling. Also backs the
FED history throttling test.&lt;/li>
&lt;li>&lt;strong>t_junction&lt;/strong>: T-corridor FDS scenario with cable fire, two exits (A open, B smoke-accumulating),
200 visitors spawning in the branch; used for visibility-aware routing and cognitive
map verification. Includes &lt;code>config_full.json&lt;/code> and &lt;code>config_discovery.json&lt;/code>
for familiarity-tier comparison. The rerouting mechanism itself is verified by
scenario &lt;strong>S4&lt;/strong> in
&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/blob/main/tests/verification/test_s4_tjunction_reroute.py" target="_blank" rel="noopener">&lt;code>tests/verification/test_s4_tjunction_reroute.py&lt;/code>&lt;/a>
(control arm and null-field control both record zero switches; smoke forces
every agent B→A and never the reverse; reroute latency stays within the
configured interval; switch count is reproducible under a fixed seed). Note
that S4 builds its own T-corridor via &lt;code>harness.t_junction_scenario()&lt;/code> with a
synthetic smoke field rather than loading this asset, so the mechanism is
covered but the deck and config here are not exercised by the suite. This
config uses flow spawning deliberately: under by-number placement an agent&amp;rsquo;s
route-eval source node is its assigned exit, which makes rerouting degenerate
(&lt;a href="https://github.com/PedestrianDynamics/pyFDS-Evac/issues/21" target="_blank" rel="noopener">issue #21&lt;/a>
).&lt;/li>
&lt;li>&lt;strong>fed_incap_co_2000ppm&lt;/strong> / &lt;strong>fed_incap_co_4000ppm&lt;/strong> / &lt;strong>fed_incap_co_8000ppm&lt;/strong>:
FED accumulation and probabilistic-incapacitation verification against a
hand-calculated reference (&lt;code>fed_hand_calc.py&lt;/code>), at three constant CO
concentrations (2000/4000/8000 ppm) in a sealed, spatially uniform room —
removing gas-transport physics as a variable isolates the FED/incapacitation
&lt;em>pipeline&lt;/em> logic. 100 non-evacuating agents circling a rectangular path, FDS
domain split across 4 MPI meshes to confirm gas data is consistent at mesh
boundaries. All three concentrations currently match the hand-calc&amp;rsquo;s FED=1.0
crossing time to &amp;lt;0.5% (2000 ppm: 782.4 s hand-calc vs 786 s simulated;
4000 ppm: 382.3 s vs 384 s; 8000 ppm: 186.6 s vs 187 s). Verified by running
the cases and comparing, not by a test in &lt;code>tests/&lt;/code>. (See the FDS input
pitfalls on the
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/models/fed/#fds-input-pitfalls" target="_blank" rel="noopener">FED page&lt;/a>
for the conflicting-&lt;code>&amp;amp;INIT&lt;/code> pitfall this suite surfaced.) Full writeup:
&lt;code>docs/testing-homogeneous.md&lt;/code>.&lt;/li>
&lt;li>&lt;strong>Cognitive Map Memory&lt;/strong>: 4x32 m corridor with a side alcove, 20 &lt;code>discovery&lt;/code>
agents. The side exit&amp;rsquo;s sign faces west and is legible only from
&lt;code>y ∈ [12.5, 27.5]&lt;/code> on the centreline — a window that falls out of
&lt;code>view_angle * max_vis &amp;gt;= distance&lt;/code> rather than being tuned, and that
&lt;code>build_geometry.py&lt;/code> recomputes and asserts. Proves the cognitive map does the
one thing a visibility query cannot: &lt;strong>remember&lt;/strong>. The side exit is unknown at
spawn, enters the map on crossing &lt;code>y=12.5&lt;/code>, and is &lt;em>still&lt;/em> there at &lt;code>y=30&lt;/code>
where the sign is long unreadable. Persistence is the load-bearing claim —
delete the expansion rules and acquisition still appears to work for any agent
starting inside the window. A third test closes the loop to routing: a
remembered-but-illegible exit must still be routable. &lt;code>scripts/generate_cognitive_map_states.py&lt;/code> renders the
three states (unknown / legible now / remembered), and the amber band is the
memory made visible. Checked by &lt;code>tests/test_cognitive_map_memory.py&lt;/code>.&lt;/li>
&lt;li>&lt;strong>FIC vs FED Speed&lt;/strong>: 4x50 m sealed corridor, 30 agents, one exit. The gas is
&lt;em>prescribed&lt;/em> by a single &lt;code>&amp;amp;INIT&lt;/code> (CO at 2000 ppm, acrolein at 10 ppm) rather
than burned, so concentration is constant in space and time and the only
variable across runs is which tenability rules are enabled — set from the
command line (&lt;code>--disable-tenability&lt;/code>, &lt;code>--fic-alpha 0&lt;/code>, or the default).
Separates the two rules by timescale: &lt;strong>FED is a cumulative dose with a
threshold and does nothing below it&lt;/strong> (0.079 /min here, so 13 minutes to reach
FED = 1, against a ~33 s egress), while &lt;strong>FIC responds instantaneously&lt;/strong>
(&lt;code>FIC = 0.5&lt;/code>, speed factor 0.65, so ~51 s). The prediction is stated in the
asset README before running and is falsifiable: if FED materially slows an
agent over a minute of exposure, the model or the reasoning is wrong. A
control test records that acrolein is &lt;em>not&lt;/em> only an irritant — it also sits in
FED&amp;rsquo;s Fractional Lethal Dose sum, so removing it takes 100% of the speed
penalty but only 3% of the dose rate, and that asymmetry is what makes the two
rules separable. Also pins the O2 hypoxia term against the published closed
form (Fire Safety Journal, surrogate-gases paper, Eq. 9) rather than against
our own docs. Checked by &lt;code>tests/test_fic_vs_fed_speed.py&lt;/code>.&lt;/li>
&lt;li>&lt;strong>Exit Visibility Alpha&lt;/strong>: 4x30 m corridor, 40 &lt;code>discovery&lt;/code> agents, two
exits. The two configs differ in exactly one value — the viewing bearing
(&lt;code>alpha&lt;/code>) of the near exit&amp;rsquo;s sign — so any difference in exit choice is
attributable to sign orientation and nothing else. Proves that legibility
decides cognitive-map membership, and membership decides the exit: at
&lt;code>alpha=0&lt;/code> both exits enter the map and agents take the nearer one; at
&lt;code>alpha=180&lt;/code> the near exit never enters the map and agents walk 10 m further
to the only exit they know about, even though distance favours the near one
by more than 2:1. The near exit is &lt;em>absent&lt;/em>, not rejected — a stronger claim,
since a rejected route still appears in the ranking and the all-rejected
fallback can reinstate it. Checked by
&lt;code>tests/test_exit_visibility_alpha.py&lt;/code>, which needs no FDS output: it
reimplements fdsvismap&amp;rsquo;s clear-air rule (&lt;code>view_angle * max_vis &amp;gt;= distance&lt;/code>)
so the test exercises the routing decision rather than the third-party
solver. A companion test pins that a &lt;code>full&lt;/code>-familiarity agent ignores the
bearing entirely — signs are wayfinding information and bind only where
knowledge is incomplete. The folder README documents the &lt;strong>30 m visibility
ceiling&lt;/strong> that makes a sign illegible at any bearing, and how much tighter it
becomes once smoke is present (&lt;code>c / K̄&lt;/code>, so 6 m at &lt;code>c=3&lt;/code>, &lt;code>K̄=0.5&lt;/code>).&lt;/li>
&lt;li>&lt;strong>Familiarity Test Full&lt;/strong> / &lt;strong>Familiarity Test Discovery&lt;/strong>: &lt;code>SocialForceModel&lt;/code>
scenario on a hand-drawn maze-like floor plan (20x18 m, 0.1 m walls, 1.2 m
doors throughout, generated parametrically by each folder&amp;rsquo;s
&lt;code>build_geometry.py&lt;/code>), differing only in the spawn distribution&amp;rsquo;s
&lt;code>familiarity&lt;/code> value. A matching fire deck lives at
&lt;code>assets/familiarity_test_full/familiarity_test.fds&lt;/code> (real
combustion via &lt;code>&amp;amp;REAC&lt;/code>, not a prescribed &lt;code>&amp;amp;INIT&lt;/code>; walls mirror the
walkable geometry exactly so smoke propagates through the same
doorways agents use). These two configs use the legacy &lt;code>journeys&lt;/code>/
&lt;code>transitions&lt;/code> shape directly (the web editor&amp;rsquo;s &lt;code>journeys_v2&lt;/code> format is
auto-migrated to this shape by &lt;code>load_scenario&lt;/code>, but was hand-converted
here to add the extra edge below). The maze&amp;rsquo;s start room is one open
box that connects directly to the checkpoint outside the exit door
(&lt;code>jps-checkpoints_0 → jps-checkpoints_3&lt;/code>), completely bypassing the
scripted checkpoint tour through the rest of the maze — a real ~39%
shorter route (32 m vs 52 m) that&amp;rsquo;s declared as an extra graph edge
(tagged &lt;code>journey_id: &amp;quot;shortcut&amp;quot;&lt;/code> in &lt;code>transitions&lt;/code>, invisible to the
static spawn-time journey) for the rerouting/cognitive-map system to
find. Run with &lt;code>--enable-rerouting&lt;/code> (no &lt;code>--fds-dir&lt;/code>/vis-cache needed —
divergence here is pure-distance, not smoke-driven) to see it: &lt;code>full&lt;/code>
agents know the whole graph immediately and reroute onto the shortcut
within the first reevaluation tick; &lt;code>discovery&lt;/code> agents start knowing
only the spawn&amp;rsquo;s declared neighbor and explore the nearest
known-but-unvisited doorway at each step — which for this maze&amp;rsquo;s
geometry happens to coincide with the original scripted tour the whole
way, so they end up taking the long route without ever finding the
shortcut. Verified: &lt;code>full&lt;/code> evacuates in 35.1 s vs &lt;code>discovery&lt;/code>&amp;rsquo;s 75.1 s
(both 20/20 evacuated; see the results table in
&lt;a href="https://pedestriandynamics.org/pyFDS-Evac/docs/testing-familiarity/">&lt;code>docs/testing-familiarity.md&lt;/code>&lt;/a>
).&lt;/li>
&lt;/ul></description></item></channel></rss>